---
phase: 29.1-habits-to-do-features
verified: 2026-05-12T06:30:00Z
status: human_needed
score: 21/21 must-haves verified
overrides_applied: 0
human_verification:
  - test: "End-to-end chat to-do roundtrip on a real device"
    expected: "User A (in a group chat) taps SendBar +, picks 'To-Do List', sees the two-step picker (Single item / List of items), enters title + assignee, taps Send. User B (the assignee, on another device) sees a ChatTodoBubble appear in the thread. User B taps the checkbox; ChatTodoBubble shows '✓ Done <relativeTime>' and a SystemMessageRow `✓ {B.name} completed {title}` appears centered + italic in BOTH users' threads via Realtime."
    why_human: "End-to-end roundtrip requires two authenticated devices + live Realtime fan-out + the actual complete_chat_todo RPC firing the system message. The unit tests assert each leg in isolation (picker UI, bubble render, RPC call, system branch dispatch) but the full roundtrip can only be confirmed on hardware. Plan 07 SUMMARY explicitly defers this to a hardware verification gate."
  - test: "Group habit per-user Realtime fan-out check-in"
    expected: "User A creates a group habit with User B as invitee. User B accepts the invite. Both users open Squad → Activity. User A toggles done on the HabitsTile inline checkbox; User B's HabitsTile should refresh to show 'X/Y done today' incremented within a few seconds (single per-user Realtime channel, refetch trigger)."
    why_human: "Realtime postgres_changes fan-out requires a live Supabase project + two distinct authenticated sessions. The unit test asserts a single per-user channel exists but cannot prove cross-device delivery. Plan 03 documents the channel filter is currently `user_id=eq.${userId}` (caller-only) — co-member updates surface via the next manual refetch (screen focus / pull-to-refresh), not via Realtime push. Verify whether the resulting latency is acceptable on real devices."
  - test: "Home Habits widget inline check rows (D-19 'today's habits with inline check')"
    expected: "User opens Home. The HomeHabitsTile shows up to 2 of today's habits as inline rows with a 44×44 done-check glyph that toggles on tap (with Light haptic + optimistic flip + revert on error)."
    why_human: "Plan 08 SUMMARY explicitly documents that inline check rows on HomeHabitsTile were DEFERRED to a future polish pass (lines 109-112 in HomeHabitsTile.tsx: 'Inline check rows ... deliberately deferred to a follow-up polish pass'). The card body navigates to /squad/habits where the full HabitRow inline-toggle UX exists. D-19's literal language reads 'compact Habits widget (today's habits with inline check)' — the in-tile inline check has not shipped, only navigate-to-detail. The hero + eyebrow + navigate work as specified; user/UX should confirm whether the navigate-only behavior is acceptable for v1 release."
  - test: "Visual smoke — Squad Activity 3×2 Bento grid + Pitfall 7 Home neon-green separation"
    expected: "Squad → Activity tab shows a 3-row 6-tile grid (IOU+Habits cyan/violet, Birthdays+ToDos pink/violet, Streak+Goals). Home tab Habits + To-Dos widgets use the neon-green Phase 29 aesthetic — NOT cyan/violet. Both surfaces visible side by side should feel distinct."
    why_human: "Grep confirms HomeHabitsTile + HomeTodosTile import zero Bento accents and use only colors.interactive.accent / rgba(185,255,59,...). But Pitfall 7 is a visual rule — the human must visually confirm the two surfaces don't bleed (no cyan/violet on Home; the Squad/Activity tiles do use cyan/violet)."
  - test: "Cadence rules for 'about-to-break' habit Spotlight"
    expected: "Caller has a daily habit with last check-in = yesterday and not done today. Spotlight tile shows 'One more check-in to keep it going' for that habit. Switching cadence to 'n_per_week' with weekly_target=3 and only 1 done so far this week on Saturday should ALSO trigger habit-urgent."
    why_human: "isHabitAboutToBreak's three-cadence rule is unit-tested with fake dates, but visual confirmation that the Spotlight tile selects the right habit (highest priority) under realistic data shows the deterministic rule matches user expectations."
  - test: "Idempotent group habit check-in (network failure scenario)"
    expected: "Open Squad → Activity. Toggle a habit row's done checkbox. Simulate network loss mid-RPC. UI should revert to un-checked (snapshot+revert pattern). Re-toggle should re-fire RPC; once the network recovers the row should land in the correct state with no duplicate check-ins (composite PK on habit_checkins prevents this server-side, but UI revert correctness needs hardware verification)."
    why_human: "Optimistic snapshot+revert is unit-tested with mocked RPC error, but real network-loss scenarios (slow 3G, dropped packets) require hardware testing under actual conditions. Plan 03 documents a latent setState-snapshot bug in useChatRoom.addReaction that this plan avoided by switching to ref-mirrored state — confirming the new pattern works in practice is a hardware gate item."
---

# Phase 29.1: Habits & To-Do Features Verification Report

**Phase Goal:** Two new social features (Habits + To-Dos) integrated end-to-end across schema → Bento tiles in Squad → Activity → full-screen list/detail routes → chat attachment with two-step picker → system message roundtrip → home widgets. Wires up the existing chat "To-Do List" attachment stub and adds idempotent group-habit check-ins with per-user Realtime fan-out.

**Verified:** 2026-05-12T06:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                       | Status     | Evidence                                                                                                                                                                                                                                                       |
| --- | --------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Migration 0024 is LIVE on Supabase Local AND Remote                                                                          | VERIFIED   | `supabase migration list --linked` confirms 0024 in all three columns (Local \| Remote \| Time)                                                                                                                                                                  |
| 2   | All 6 tables exist (habits, habit_members, habit_checkins, todos, chat_todo_lists, chat_todo_items)                          | VERIFIED   | `grep -c "CREATE TABLE public\."` = 6; all six explicit table names found                                                                                                                                                                                       |
| 3   | 2 SECURITY DEFINER helpers (is_habit_member, is_chat_todo_list_owner)                                                        | VERIFIED   | Both `CREATE OR REPLACE FUNCTION public.is_habit_member` and `is_chat_todo_list_owner` present; helper filters `accepted_at IS NOT NULL` (T-29.1-12)                                                                                                            |
| 4   | 8 RPCs (create_habit, toggle_habit_today_checkin, create_todo, create_chat_todo_list, complete_chat_todo, get_habits_overview, get_my_todos, get_chat_todos) | VERIFIED   | All 8 grep-matched; SUMMARY's Rule 1 deviation noted that `get_my_todos` + `get_chat_todos` are 2 separate RPCs (cleaner row shapes than a unified discriminator)                                                                                                |
| 5   | messages.message_type CHECK constraint accepts 'system' and 'todo'                                                          | VERIFIED   | `CHECK (message_type IN ('text','image','poll','deleted','system','todo'))` — preserves 'deleted' from migration 0019 (Plan 01 Rule 1 deviation)                                                                                                                |
| 6   | RLS policies enforce membership-gated access on all 6 tables                                                                | VERIFIED   | `ENABLE ROW LEVEL SECURITY` count = 6; `messages` INSERT policy whitelists ('text','image','poll') — system+todo are RPC-only (T-29.1-03); 10 `GRANT EXECUTE` entries                                                                                            |
| 7   | MessageType TS union includes 'system' and 'todo'                                                                            | VERIFIED   | `src/types/chat.ts:1` reads `'text' \| 'image' \| 'poll' \| 'deleted' \| 'system' \| 'todo'`                                                                                                                                                                  |
| 8   | src/types/habits.ts exports Habit, HabitMember, HabitCheckin, HabitCadence, HabitOverviewRow                                | VERIFIED   | 5 named exports confirmed; `HabitOverviewRow` contains `last_checkin_date_local` + `current_week_completions` for Plan 03 OQ4                                                                                                                                  |
| 9   | src/types/todos.ts exports Todo, TodoPriority, ChatTodoList, ChatTodoItem, MyTodoRow, ChatTodoRow                            | VERIFIED   | 6 named exports confirmed; types mirror live RPC return shapes (Plan 02 Rule 1 deviations on MyTodoRow + ChatTodoRow)                                                                                                                                          |
| 10  | All 5 hooks created/modified (useHabits, useHabitDetail, useTodos, useChatTodos, useSpotlight)                              | VERIFIED   | All 5 files exist; useHabits calls `get_habits_overview` + `toggle_habit_today_checkin`; useTodos calls `get_my_todos` + `get_chat_todos`; useChatTodos calls `create_chat_todo_list` + `complete_chat_todo`; useSpotlight extends SpotlightKind with 'habit'+'todo' |
| 11  | useHabits subscribes to SINGLE per-user Realtime channel `user-${userId}-checkins`                                          | VERIFIED   | grep confirms channel name; filter narrowed to `user_id=eq.${userId}` (Plan 03 Rule 3 deviation — HabitOverviewRow doesn't expose member_ids for broader IN-filter). Test asserts no `habit-` prefixed channels exist                                            |
| 12  | useSpotlight.selectSpotlight has 6-kind priority cascade (birthday > iou > habit > todo > streak > fallback) with isHabitAboutToBreak helper                   | VERIFIED   | SpotlightKind union extended; `isHabitAboutToBreak` private helper implements OQ4 three-cadence rule (Daily/Weekly/N×); 5 useSpotlight tests pass                                                                                                                |
| 13  | Bento tiles HabitsTile + TodosTile + BentoGrid extended to ROWS=4 + 6 feature tiles                                          | VERIFIED   | `BentoGrid.tsx` `const ROWS = 4` confirmed; render tree shows HabitsTile + TodosTile + GoalsTile (D-15 placeholder preserved); tileAccents.habits='#22D3EE' + tileAccents.todos='#A78BFA'                                                                       |
| 14  | SpotlightTile ICON_BY_ACCENT + accentColor switch extended with habit + todo cases                                          | VERIFIED   | `habit: 'checkmark-done-outline'`, `todo: 'list-outline'`, `case 'habit'` and `case 'todo'` branches present                                                                                                                                                    |
| 15  | 3 habit routes (/squad/habits/index, [id], create) + 5 habit components                                                      | VERIFIED   | All 3 route files exist; 5 components (HabitRow, HabitCadencePicker, HabitMemberStrip, HabitCheckinHistory, HabitInvitationRow); 3 Stack.Screen entries in `_layout.tsx`                                                                                          |
| 16  | 3 todo routes (/squad/todos/index, [id], create) + 3 todo components                                                         | VERIFIED   | All 3 route files exist; 3 components (TodoRow, ChatTodoListRow, TodoQuickAdd); 3 Stack.Screen entries in `_layout.tsx`                                                                                                                                          |
| 17  | Chat picker, bubble, system row + MessageBubble dispatch + ChatRoomScreen wireup                                            | VERIFIED   | ChatTodoPickerSheet + ChatTodoBubble + SystemMessageRow exist; MessageBubble lines 481-484 bail on isSystem/isTodo (Pitfall 9); render branches at 617-633 dispatch isSystem→isTodo→isPoll (Pitfall 10); ChatRoomScreen wires `setShowChatTodoPickerSheet`, `useChatTodos`, `useGroupMembers` |
| 18  | "Coming Soon" alert is gone from ChatRoomScreen                                                                              | VERIFIED   | `grep -n "Coming Soon" src/screens/chat/ChatRoomScreen.tsx` returns zero matches. The only project-wide occurrence is a documentation header comment in ChatTodoPickerSheet.tsx describing what it replaced.                                                    |
| 19  | Home widgets HomeHabitsTile + HomeTodosTile + HomeHabitsTodosRow + HomeTilePrimitives factored                              | VERIFIED   | All 4 component files exist; HomeScreen.tsx imports useHabits + useTodos and mounts `<HomeHabitsTodosRow habits={habits} todos={todos} />` below `<YourZoneSection>`                                                                                            |
| 20  | Pitfall 7 enforced — neon-green only on home widgets (NOT cyan/violet)                                                       | VERIFIED   | `grep -E "BentoCard\|tileAccents\|tileSurface\|TILE_ACCENTS"` returns ZERO matches across HomeHabitsTile + HomeTodosTile + HomeHabitsTodosRow; widgets use `colors.interactive.accent` + `rgba(185,255,59,...)` palette                                            |
| 21  | ROADMAP Phase 30 D-21 supersession note added                                                                                | VERIFIED   | ROADMAP.md:132 top-level bullet updated; ROADMAP.md:261-268 Phase 30 section: Requirements reduced to `SQUAD-06`, dedicated `**Note**:` paragraph names SQUAD-05/07/08 superseded by 29.1; success criteria reduced to 1 (Friends list polish only)                |

**Score:** 21/21 truths verified

### Required Artifacts

| Artifact                                                                  | Expected                                          | Status     | Details                                                          |
| ------------------------------------------------------------------------- | ------------------------------------------------- | ---------- | ---------------------------------------------------------------- |
| `supabase/migrations/0024_habits_todos_v1_8.sql`                           | 6 tables + 2 helpers + 8 RPCs + RLS              | VERIFIED   | 866 LOC, live on remote project zqmaauaopyolutfoizgq               |
| `src/types/habits.ts`                                                     | 5 named exports                                   | VERIFIED   | 54 LOC; HabitCadence, Habit, HabitMember, HabitCheckin, HabitOverviewRow |
| `src/types/todos.ts`                                                      | 6 named exports                                   | VERIFIED   | 83 LOC; TodoPriority, Todo, ChatTodoList, ChatTodoItem, MyTodoRow, ChatTodoRow |
| `src/types/chat.ts` MessageType union                                     | extended with 'system' + 'todo'                   | VERIFIED   | Line 1 contains all 6 literals                                    |
| `src/lib/dateLocal.ts`                                                    | exports todayLocal()                              | VERIFIED   | 22 LOC; client-tz YYYY-MM-DD via en-CA locale                     |
| `src/hooks/useHabits.ts`                                                  | useHabits + toggleToday + Realtime channel        | VERIFIED   | 146 LOC; calls get_habits_overview + toggle_habit_today_checkin   |
| `src/hooks/useHabitDetail.ts`                                             | useHabitDetail(habitId)                           | VERIFIED   | 108 LOC; parallel fetch habit/members/checkins                    |
| `src/hooks/useTodos.ts`                                                   | useTodos returning mine + fromChats sections      | VERIFIED   | 133 LOC; calls get_my_todos + get_chat_todos                      |
| `src/hooks/useChatTodos.ts`                                               | sendChatTodo + completeChatTodo                   | VERIFIED   | 82 LOC; calls create_chat_todo_list + complete_chat_todo          |
| `src/hooks/useSpotlight.ts`                                               | extended SpotlightKind with habit + todo          | VERIFIED   | 213 LOC; isHabitAboutToBreak helper inside                        |
| `src/components/squad/bento/HabitsTile.tsx`                               | Habits Bento tile                                 | VERIFIED   | 109 LOC; cyan-400 accent, X/Y done today hero                     |
| `src/components/squad/bento/TodosTile.tsx`                                | To-Dos Bento tile                                 | VERIFIED   | 114 LOC; violet-400 accent flips destructive on overdue           |
| `src/components/squad/bento/BentoGrid.tsx`                                | ROWS=4 + 6 tiles                                  | VERIFIED   | Animated stagger across 4 rows; D-15 GoalsTile preserved           |
| `src/components/squad/bento/tileAccents.ts`                               | habits + todos hex values                         | VERIFIED   | habits='#22D3EE', todos='#A78BFA'                                  |
| `src/components/squad/bento/SpotlightTile.tsx`                            | ICON_BY_ACCENT + accentColor extended             | VERIFIED   | habit/todo entries added                                          |
| `src/app/squad/habits/index.tsx`, `[id].tsx`, `create.tsx`                | 3 habit routes                                    | VERIFIED   | All exist; uses useHabits + useHabitDetail + create_habit RPC      |
| `src/app/squad/todos/index.tsx`, `[id].tsx`, `create.tsx`                 | 3 todo routes                                     | VERIFIED   | All exist; SectionList with Mine + From chats (D-04)               |
| `src/components/habits/*` (5 files)                                       | HabitRow, HabitCadencePicker, HabitMemberStrip, HabitCheckinHistory, HabitInvitationRow | VERIFIED | All 5 exist                                                       |
| `src/components/todos/*` (3 files)                                        | TodoRow, ChatTodoListRow, TodoQuickAdd            | VERIFIED   | All 3 exist                                                       |
| `src/components/chat/ChatTodoPickerSheet.tsx`                             | 2-step picker D-09                                | VERIFIED   | 709 LOC; step state + Single/List flavors                         |
| `src/components/chat/ChatTodoBubble.tsx`                                  | chat to-do bubble D-11/D-13                       | VERIFIED   | 321 LOC; assignee-gated checkbox                                  |
| `src/components/chat/SystemMessageRow.tsx`                                | centered + italic system row                      | VERIFIED   | 62 LOC; minimal centered render                                    |
| `src/components/chat/MessageBubble.tsx` (modified)                        | isSystem/isTodo dispatch + long-press bails       | VERIFIED   | Lines 481-484 bails, 617-633 dispatch                              |
| `src/screens/chat/ChatRoomScreen.tsx` (modified)                          | picker mount + 'todo' action wire                 | VERIFIED   | useChatTodos + useGroupMembers; ChatTodoPickerSheet rendered       |
| `src/hooks/useGroupMembers.ts`                                            | new shared hook                                   | VERIFIED   | 76 LOC; group_channel_members → profiles join                     |
| `src/components/home/HomeHabitsTile.tsx`                                  | neon-green Habits widget                          | VERIFIED   | 104 LOC; no Bento imports                                          |
| `src/components/home/HomeTodosTile.tsx`                                   | neon-green To-Dos widget                          | VERIFIED   | 143 LOC; 4-branch heroColor cascade + Add chip                    |
| `src/components/home/HomeHabitsTodosRow.tsx`                              | row wrapper                                       | VERIFIED   | 42 LOC                                                            |
| `src/components/home/HomeTilePrimitives.tsx`                              | TileShell + EyebrowPill factored                  | VERIFIED   | 165 LOC; useSpringPress co-located                                 |
| `src/components/home/YourZoneSection.tsx` (modified)                      | inline primitives removed                         | VERIFIED   | -135 LOC; imports primitives                                       |
| `src/screens/home/HomeScreen.tsx` (modified)                              | HomeHabitsTodosRow mounted                        | VERIFIED   | Line 273 mount below YourZoneSection                              |
| `.planning/ROADMAP.md` Phase 30 entry                                     | D-21 supersession note                            | VERIFIED   | Lines 132, 261-268 updated                                         |

### Key Link Verification

| From                                                | To                                                          | Via                                                  | Status   | Details                                                                                                  |
| --------------------------------------------------- | ----------------------------------------------------------- | ---------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------- |
| `complete_chat_todo` RPC                            | `messages (message_type='system')`                          | INSERT in same txn after UPDATE                      | WIRED    | Migration 0024 lines 618-643 — single message + optional list-finished message in same atomic block      |
| `habit_checkins` SELECT policy                      | `is_habit_member` helper                                    | SECURITY DEFINER avoids RLS recursion (Pitfall 2)    | WIRED    | Migration 0024 lines 258-274                                                                              |
| `create_chat_todo_list` RPC                         | `group_channel_members` table                               | EXISTS check (D-10)                                  | WIRED    | Migration 0024 lines 494-510; T-29.1-01 mitigated                                                         |
| `useHabits.toggleToday`                             | `toggle_habit_today_checkin` RPC                            | Ref-mirrored optimistic + revert                     | WIRED    | useHabits.ts; tests assert snapshot+revert on RPC error                                                   |
| `useHabits` subscribeRealtime                       | `user-${userId}-checkins` channel                           | SINGLE channel per user                              | WIRED    | useHabits.ts; test asserts no `habit-` per-habit channels                                                  |
| `useChatTodos.completeChatTodo`                     | `complete_chat_todo` RPC                                    | Optimistic + chat Realtime fan-out                   | WIRED    | useChatTodos.ts                                                                                           |
| `BentoGrid`                                         | `HabitsTile` + `TodosTile`                                  | Row composition in 4th row + 2nd row                 | WIRED    | Rendered alongside StreakTile + GoalsTile (D-15 preserved)                                                |
| `Squad screen Activity tab`                         | `useHabits + useTodos`                                      | hook invocation + BentoGrid props                    | WIRED    | `src/app/(tabs)/squad.tsx`: imports + invokes both hooks, threads into `<BentoGrid />`                    |
| `Route /squad/habits`                               | `useHabits` hook                                            | FlatList + toggleToday on row tap                    | WIRED    | `src/app/squad/habits/index.tsx`                                                                          |
| `Route /squad/habits/create`                        | `create_habit` RPC                                          | Form submit                                          | WIRED    | `src/app/squad/habits/create.tsx`                                                                          |
| `Route /squad/todos`                                | `useTodos` hook                                             | SectionList Mine + From chats                        | WIRED    | `src/app/squad/todos/index.tsx`                                                                          |
| `ChatTodoListRow`                                   | `useTodos.completeChatTodo` / `complete_chat_todo`          | Child item checkbox tap                              | WIRED    | `src/app/squad/todos/index.tsx` lazy-fetch + handler                                                       |
| `ChatRoomScreen.handleAttachmentAction('todo')`     | `ChatTodoPickerSheet visible=true`                          | Replaces Alert.alert('Coming Soon')                  | WIRED    | Line 171: `setShowChatTodoPickerSheet(true)` — Coming Soon string verified ABSENT                          |
| `ChatTodoPickerSheet onSend`                        | `useChatTodos.sendChatTodo` → `create_chat_todo_list`       | Single or list flavor                                | WIRED    | ChatRoomScreen wires `sendChatTodo` from useChatTodos                                                     |
| `ChatTodoBubble checkbox tap`                       | `useChatTodos.completeChatTodo` → `complete_chat_todo`      | Assignee-only; produces system message               | WIRED    | T-29.1-32 double-gate (UI Pressable + server-side RLS check)                                              |
| `MessageBubble isSystem branch`                     | `SystemMessageRow`                                          | Before isPoll dispatch (Pitfall 10)                  | WIRED    | Lines 617-619                                                                                              |
| `MessageBubble isTodo branch`                       | `ChatTodoBubble`                                            | Before isPoll dispatch (Pitfall 10)                  | WIRED    | Lines 620-633                                                                                              |
| `MessageBubble.handleLongPress`                     | bails on isSystem + isTodo (Pitfall 9)                      | Early return guards                                  | WIRED    | Lines 481-482                                                                                              |
| `HomeScreen`                                        | `HomeHabitsTodosRow`                                        | Mounted below `<YourZoneSection>`                    | WIRED    | Lines 25-30 imports, 63-64 hook calls, 273 mount                                                          |
| `HomeHabitsTile` + `HomeTodosTile`                  | `TileShell + EyebrowPill`                                   | Shared primitives (NOT BentoCard) — Pitfall 7        | WIRED    | grep absence of BentoCard/tileAccents/tileSurface/TILE_ACCENTS in all home widgets                        |
| `HomeHabitsTile.onPress`                            | `/squad/habits`                                             | router.push                                          | WIRED    | Line 92                                                                                                    |
| `HomeTodosTile.onPress`                             | `/squad/todos`                                              | router.push                                          | WIRED    | TodosTile body Press handler                                                                              |
| `HomeTodosTile Add chip onPress`                    | `/squad/todos/create`                                       | router.push                                          | WIRED    | Add chip handler                                                                                          |

### Data-Flow Trace (Level 4)

| Artifact                       | Data Variable          | Source                              | Produces Real Data | Status     |
| ------------------------------ | ---------------------- | ----------------------------------- | ------------------ | ---------- |
| HabitsTile                     | `habits.habits`        | `useHabits()` → `get_habits_overview` RPC | Yes (live DB)      | FLOWING    |
| TodosTile                      | `todos.mine`/`fromChats` | `useTodos()` → `get_my_todos` + `get_chat_todos` RPCs | Yes (live DB) | FLOWING    |
| HomeHabitsTile                 | same as HabitsTile     | same                                | Yes                | FLOWING    |
| HomeTodosTile                  | same as TodosTile      | same                                | Yes                | FLOWING    |
| Habits list route (/squad/habits) | `useHabits` + invitation query | live DB + RLS | Yes              | FLOWING    |
| Habit detail (/squad/habits/[id])  | `useHabitDetail`    | parallel queries to habits + habit_members + habit_checkins | Yes | FLOWING |
| Todos list route (/squad/todos) | `useTodos` mine + fromChats | live RPCs                          | Yes                | FLOWING    |
| ChatTodoBubble                 | `list` + `items`       | ChatRoomScreen lazy-fetch by message_id | Yes (RLS-gated) | FLOWING    |
| SpotlightTile                  | `useSpotlight` cascade | extended with habit/todo branches    | Yes (deterministic helper) | FLOWING |

All wired artifacts have a live RPC or RLS-gated table query producing real data. No hollow/disconnected props detected.

### Behavioral Spot-Checks

| Behavior                                                | Command                                              | Result                                           | Status |
| ------------------------------------------------------- | ---------------------------------------------------- | ------------------------------------------------ | ------ |
| Migration 0024 present in Local AND Remote              | `npx supabase migration list --linked`               | `0024 \| 0024 \| 0024` (all three columns)     | PASS   |
| Migration 0024 contains all 6 tables                    | `grep -c "CREATE TABLE public\\."`                   | 6                                                 | PASS   |
| Migration 0024 has 8 RPCs declared                      | grep `CREATE OR REPLACE FUNCTION public\\.(...)`      | 8 matches                                         | PASS   |
| Migration 0024 has RLS enabled on all 6 new tables      | `grep -c "ENABLE ROW LEVEL SECURITY"`                | 6                                                 | PASS   |
| Migration 0024 GRANTs EXECUTE on all functions          | `grep -c "GRANT EXECUTE ON FUNCTION"`                | 10 (2 helpers + 8 RPCs)                          | PASS   |
| messages.message_type CHECK admits 'system' and 'todo'  | grep CHECK literal                                   | `'text','image','poll','deleted','system','todo'` | PASS   |
| Coming Soon alert absent from ChatRoomScreen.tsx        | `grep -n "Coming Soon" src/screens/chat/ChatRoomScreen.tsx` | (empty)                                          | PASS   |
| Pitfall 7: no Bento imports in home widgets             | `grep -E "BentoCard\|tileAccents\|tileSurface\|TILE_ACCENTS" src/components/home/Home*.tsx` | (empty)                                          | PASS   |
| Full jest test suite GREEN                              | `npx jest --no-coverage`                             | 20 suites, 72 tests, all passed                   | PASS   |

### Decisions Coverage (D-01..D-21)

| Decision | Description                                                                                       | Status      | Evidence                                                                                                                                |
| -------- | ------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| D-01     | Habit feature called "Habits"                                                                     | SATISFIED   | HabitsTile + screen header use "Habits" (Plan 04 + Plan 05)                                                                              |
| D-02     | Solo/Group chip discriminator                                                                     | SATISFIED   | HabitRow.tsx renders Solo/Group chip                                                                                                     |
| D-03     | To-Do feature called "To-Dos"                                                                     | SATISFIED   | TodosTile + screen header use "To-Dos"                                                                                                   |
| D-04     | Mine / From chats two-section split                                                               | SATISFIED   | /squad/todos/index.tsx SectionList with both sections                                                                                    |
| D-05     | Daily/Weekly/N× per week cadence                                                                  | SATISFIED   | HabitCadencePicker.tsx + migration CHECK constraint                                                                                       |
| D-06     | Per-member check-in rule                                                                          | SATISFIED   | habit_checkins composite PK (habit_id, user_id, date_local); toggle RPC uses auth.uid()                                                  |
| D-07     | Check-in inline on Activity tile + detail screen                                                  | SATISFIED   | HabitsTile uses habits.toggleToday; /squad/habits/[id] has Mark done toggle                                                              |
| D-08     | "X/Y done today" hero on HabitsTile                                                               | SATISFIED   | HabitsTile renders {doneCount}/{totalCount}                                                                                              |
| D-09     | Two-step picker (Single / List)                                                                   | SATISFIED   | ChatTodoPickerSheet step:1\|2 with Single item / List of items                                                                          |
| D-10     | Single assignee per chat to-do                                                                    | SATISFIED   | create_chat_todo_list re-verifies assignee in group_channel_members (T-29.1-01)                                                          |
| D-11     | Completion roundtrip → bubble + system message                                                    | SATISFIED   | complete_chat_todo posts system message; ChatTodoBubble + SystemMessageRow render                                                       |
| D-12     | Personal "Mine" shape (title + due + notes + priority)                                            | SATISFIED   | todos table has all 4 fields; /squad/todos/create form covers all 4                                                                      |
| D-13     | Chat lists assigned to one person, expandable progress                                            | SATISFIED   | chat_todo_lists has single assignee_id; ChatTodoListRow + ChatTodoBubble list flavor expand                                              |
| D-14     | Squad IA stays 3 tabs; Habits + To-Dos in Activity Bento                                          | SATISFIED   | BentoGrid ROWS=4 hosts 6 tiles in Activity                                                                                                |
| D-15     | Squad Challenges placeholder stays                                                                | SATISFIED   | GoalsTile preserved in Row 3 (Streak + Goals)                                                                                            |
| D-16     | Habits + To-Dos eligible Spotlight                                                                | SATISFIED   | useSpotlight extended with 'habit' and 'todo' priority branches                                                                          |
| D-17     | Hero metrics — Habits X/Y done + To-Dos N due                                                     | SATISFIED   | HabitsTile shows X/Y; TodosTile shows N due                                                                                              |
| D-18     | Routes nested under /squad/habits/* and /squad/todos/*                                            | SATISFIED   | All 6 routes (3 habits + 3 todos) registered in _layout.tsx                                                                              |
| D-19     | Home Habits + To-Dos widgets                                                                      | PARTIAL     | Widgets exist + navigate to detail; **"today's habits with inline check"** sub-clause DEFERRED per Plan 08 SUMMARY (see human verification 3) |
| D-20     | Profile tab gets NO Habits/To-Dos surface                                                         | SATISFIED   | No files under src/app/profile or src/screens/profile touched by this phase                                                              |
| D-21     | Phase 30 scope reduction note                                                                     | SATISFIED   | ROADMAP.md:132, 261-268 updated with supersession note                                                                                  |

### Requirements Coverage

Phase 29.1 uses inline decisions D-01..D-21 instead of REQ-IDs. All decisions are covered by Plan 01..08 and verified above.

### Anti-Patterns Found

| File                                              | Line     | Pattern                                                  | Severity | Impact                                                                                                                                                                                                          |
| ------------------------------------------------- | -------- | -------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/components/home/HomeHabitsTile.tsx`          | 104-112  | In-file comment documenting deferred inline check rows   | Info     | Plan 08 SUMMARY documents this as a deliberate deferral (verb "supports" interpretation). Functional path still works — tile navigates to /squad/habits where full inline UX exists. See human-verification item 3. |
| Multiple files (useHabits, useHabitDetail, useTodos, useChatTodos, /squad/habits/*, /squad/todos/*, ChatRoomScreen) | various  | `(supabase as any)` / `(supabase as never as ...)` casts | Info     | Anticipated pattern documented in Plan 03/05/06/07 SUMMARYs: database.ts predates migration 0024. Same pattern already exists in useChatRoom.ts:612 (create_poll). Will clear when database.ts is regenerated.    |
| `src/components/chat/ChatTodoPickerSheet.tsx`     | 3        | Comment text contains "Coming Soon"                       | Info     | Only documentation; the actual user-facing Alert.alert is gone. Comment explains what the picker replaced.                                                                                                       |
| `src/hooks/useHabits.ts`                          | n/a      | Realtime filter is `user_id=eq.${userId}` (caller-only)  | Info     | Plan 03 Rule 3 deviation documented: HabitOverviewRow doesn't expose member_ids. Co-member updates surface via manual refetch. See human-verification item 2.                                                    |

No blocker-class anti-patterns. All warnings are documented Plan-level decisions with traceable rationale.

### Human Verification Required

See YAML frontmatter `human_verification:` block above for the 6 items requiring real-device testing. Summary:

1. **End-to-end chat to-do roundtrip** — picker → bubble → assignee check → system message via Realtime (2-device test)
2. **Group habit per-user Realtime fan-out** — verify Plan 03's narrowed `user_id=eq.${userId}` filter latency is acceptable
3. **Home Habits widget inline check rows** — DEFERRED per Plan 08; confirm whether navigate-only behavior is acceptable for v1
4. **Visual smoke — Squad Activity 3×2 vs Home neon-green separation** — Pitfall 7 compliance on hardware
5. **Spotlight habit-urgent cadence rules** — three-cadence deterministic helper in real data
6. **Idempotent group habit check-in (network failure)** — snapshot+revert pattern under real network conditions

### Gaps Summary

No blocker gaps. All 21 must-have truths verified at code level; all 32 required artifacts exist; all 23 key links wired; data-flow trace confirms live RPC/RLS-gated queries everywhere; full test suite (72 tests / 20 suites) GREEN.

The only partial-implementation note is **D-19's "inline check rows on home Habits widget"** which Plan 08 explicitly deferred. The widget renders eyebrow + hero + caption and navigates to /squad/habits — the literal D-19 phrase "today's habits with inline check" is partially honored (architecture supports it; UI deferred). This is a documented scope decision in the SUMMARY, not a verification failure. Routed to human verification (item 3) for product/UX call.

Other items routed to human verification are inherent-Realtime/hardware concerns that no automated check can prove:
- Live Supabase Realtime fan-out timing
- Two-device chat picker → bubble → system message roundtrip
- Visual Pitfall 7 separation on real screens
- Optimistic snapshot+revert under real network conditions

All these are expected human-gate items per project convention (`project_hardware_gate_deferral` memory — manual smoke tests consolidated at v1.3 Hardware Verification Gate phase, feature phases don't block on them).

---

_Verified: 2026-05-12T06:30:00Z_
_Verifier: Claude (gsd-verifier)_
