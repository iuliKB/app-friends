---
phase: 29.1
slug: habits-to-do-features
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-12
---

# Phase 29.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Derived from RESEARCH.md `## Validation Architecture`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest 29.x via `jest-expo` preset + `@testing-library/react-native` |
| **Config file** | `jest.config.js` at repo root |
| **Quick run command** | `npm test -- --testPathPattern={pattern}` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~30–60 seconds (full unit suite, depends on prior baseline) |

---

## Sampling Rate

- **After every task commit:** Run `npm test -- --testPathPattern={touched-file}`
- **After every plan wave:** Run `npm test`
- **Before `/gsd-verify-work`:** Full suite must be green AND manual integration smoke (D-05 invalid cadence rejection, D-10 cross-user assignment denial, D-11 atomic system-msg + completed_at insert) green
- **Max feedback latency:** 60 seconds

---

## Per-Decision Verification Map

> CONTEXT.md uses decisions D-01..D-21 instead of REQ-IDs. Map each behavior to a test artifact.

| D-ID | Behavior | Test Type | Automated Command | File Exists | Status |
|------|----------|-----------|-------------------|-------------|--------|
| D-01..D-04 | Feature names "Habits", "To-Dos", Solo/Group labels, Mine/From chats sections render correctly | unit (RTL render assertion) | `npm test -- --testPathPattern=HabitsTile.test`, `TodosTile.test`, `todos/index.test` | ❌ Wave 0 | ⬜ pending |
| D-05 | `create_habit` RPC rejects invalid weekly_target / cadence combos | integration (Supabase pgTAP or manual SQL) | Manual smoke — documented in HUMAN-UAT.md | ❌ Wave 0 (manual) | ⬜ pending |
| D-06 | "X/Y done today" computes correctly from per-member checkins | **server-side** (SQL in `get_habits_overview` RPC, Plan 01) | `assert_habits_overview_row_shape` schema check (Plan 01 acceptance criteria); HabitsTile RTL test renders `${doneToday}/${total}` from RPC output (Plan 04 Task 2). Integration smoke for D-05/D-11 in HUMAN-UAT.md. | n/a (server-side) | ⬜ pending |
| D-07 | Inline check-in toggle round-trips through optimistic state | unit (hook test, mock RPC) | `npm test -- --testPathPattern=useHabits` | ❌ Wave 0 | ⬜ pending |
| D-08 | Habits tile hero text is "X/Y done today" | unit (RTL text assertion) | `npm test -- --testPathPattern=HabitsTile.test` | ❌ Wave 0 | ⬜ pending |
| D-09 | Two-step picker shows step 1 then step 2 on next | unit (RTL fireEvent) | `npm test -- --testPathPattern=ChatTodoPickerSheet.test` | ❌ Wave 0 | ⬜ pending |
| D-10 | `assign_chat_todo` RPC validates assignee is a member of the chat | integration | Manual smoke — documented in HUMAN-UAT.md | ❌ Wave 0 (manual) | ⬜ pending |
| D-11 | Completing chat to-do inserts `'system'` message AND updates `chat_todo_items.completed_at` atomically | integration | Manual smoke — documented in HUMAN-UAT.md | ❌ Wave 0 (manual) | ⬜ pending |
| D-12 | Personal "Mine" form accepts title + due + notes + priority | unit (RTL form) | `npm test -- --testPathPattern=TodoCreate.test` | ❌ Wave 0 | ⬜ pending |
| D-13 | Chat to-do list expands on tap, shows N/M progress | unit (RTL) | `npm test -- --testPathPattern=ChatTodoListRow.test` | ❌ Wave 0 | ⬜ pending |
| D-14, D-15 | Bento grid renders 6 tiles + spotlight; Challenges placeholder still present | unit (RTL count) | `npm test -- --testPathPattern=BentoGrid.test` | partial (no existing test) | ⬜ pending |
| D-16 | `selectSpotlight` returns habit-urgent / todo-urgent items per priority order | unit (pure function) | `npm test -- --testPathPattern=useSpotlight.test` | ❌ Wave 0 | ⬜ pending |
| D-17 | Habits/Todos tiles render correct hero text under varying state | unit (RTL) | `npm test -- --testPathPattern=HabitsTile.test`, `TodosTile.test` | ❌ Wave 0 | ⬜ pending |
| D-18 | Routes resolve under expo-router | smoke (manual) | Manual navigate in Expo Go | ❌ | ⬜ pending |
| D-19 | Home Habits/Todos widgets render and navigate to `/squad?tab=activity` | unit (RTL) | `npm test -- --testPathPattern=HomeHabitsWidget.test`, `HomeTodosWidget.test` | ❌ Wave 0 | ⬜ pending |
| D-20 | Profile tab has no Habits/Todos surface | regression (visual diff) | Manual scroll Profile | n/a | ⬜ pending |
| D-21 | Phase 30 ROADMAP entry updated | doc check | `grep -n "Phase 30" .planning/ROADMAP.md` | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/hooks/__tests__/useSpotlight.test.ts` — covers D-16 (extend existing `useSpotlight` test if any; otherwise create)
- [ ] `src/hooks/__tests__/useHabits.test.ts` — covers D-07 toggle hook
- [ ] `src/hooks/__tests__/useTodos.test.ts` — covers D-04, D-12 sectioning + create
- [ ] `src/hooks/__tests__/useChatTodos.test.ts` — covers D-10, D-11 chat round-trip
- [ ] `src/components/squad/bento/__tests__/HabitsTile.test.tsx` — covers D-08, D-17
- [ ] `src/components/squad/bento/__tests__/TodosTile.test.tsx` — covers D-17
- [ ] `src/components/squad/bento/__tests__/BentoGrid.test.tsx` — covers D-14, D-15 (NEW — currently no test)
- [ ] `src/components/chat/__tests__/ChatTodoPickerSheet.test.tsx` — covers D-09
- [ ] `src/components/chat/__tests__/ChatTodoBubble.test.tsx` — covers D-11 visual states
- [ ] `src/components/chat/__tests__/MessageBubble.system.test.tsx` — covers system-message render branch
- [ ] `src/components/todos/__tests__/ChatTodoListRow.test.tsx` — covers D-13 expand/progress
- [ ] `src/components/home/__tests__/HomeHabitsWidget.test.tsx`, `HomeTodosWidget.test.tsx` — covers D-19
- [x] D-06 ("X/Y done today" pure aggregation) — **server-side in `get_habits_overview` SQL (Plan 01)**; verified by Plan 01 acceptance criteria + Plan 04 HabitsTile RTL render test. No client-side pure helper required. (B3 — checker pass 1)

---

## Manual-Only Verifications

| Behavior | Decision | Why Manual | Test Instructions |
|----------|----------|------------|-------------------|
| Invalid weekly_target rejection | D-05 | Hits Postgres CHECK constraint; project has no pgTAP setup | Create habit with `cadence='n_per_week'` and `weekly_target=0` or `8`; RPC must raise exception |
| Cross-user chat-to-do assignment denied | D-10 | Cross-user RLS scenario requires two real auth sessions | Sign in as user A; attempt `assign_chat_todo` with assignee = user X (not in chat); RPC must raise exception |
| Atomic system message + completed_at insert | D-11 | Requires inspecting two rows after one RPC call in a real DB | Run `complete_chat_todo`; query both `chat_todo_items.completed_at IS NOT NULL` AND `messages WHERE message_type='system'` exist for that chat |
| Route resolution under expo-router | D-18 | Deep-link behavior verified in running app | Manual navigate to `/squad/habits`, `/squad/todos/[id]` in Expo Go |
| Realtime fan-out under free-tier budget | Pitfall 4 | Cross-device timing | Sign in 3 devices same friend group; one user check-ins; verify others see "X/Y done today" update via per-user channel (single subscription, no per-habit channels) |
| Off-by-one date in negative-UTC tz | Pitfall 5 | Requires changing device tz | Set device to UTC-7; mark habit done at 11pm local; verify tile reads "1/1 done today" (not "0/1") |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
