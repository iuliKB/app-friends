---
phase: 04
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "Migration 0011 has been applied to the live Supabase project (pushed via `supabase db push`)."
    - "The live DB has a function `public.get_squad_streak(uuid, text)` callable by `authenticated` role."
    - "The function is a function (prokind='f'), not a materialized view or table."
    - "Supabase-generated types (src/types/supabase.ts if used, or equivalent) know about the new RPC OR a hand-patch exists."
  artifacts: []
  key_links:
    - from: "Live Supabase project"
      to: "public.get_squad_streak(uuid, text)"
      via: "supabase db push"
      pattern: "get_squad_streak"
---

<objective>
**[BLOCKING]** Apply migration `0011_squad_streak_v1_3.sql` to the live Supabase project via `supabase db push`. Attest that the function is live, is a regular function (not a materialized view), has the correct signature, and grants `execute` to `authenticated`. Without this push, Plan 04 (`useStreakData` + StreakCard) cannot call the RPC — `supabase.rpc('get_squad_streak', ...)` would return a 404 and always render the zero-state fallback, producing a silent false-positive verification state per the schema_push_requirement.

Purpose: STREAK-07 — confirm the function is computed (not materialized) AND reachable from the client.
Output: Attestation lines pasted into `04-02-SUMMARY.md` plus a verified push transcript.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-morning-prompt-squad-goals-streak/04-CONTEXT.md
@.planning/phases/04-morning-prompt-squad-goals-streak/04-RESEARCH.md
@supabase/migrations/0011_squad_streak_v1_3.sql

<interfaces>
<!-- Supabase CLI push command (from Phase 2 + 3 precedent) -->

Push command:
  supabase db push

Non-TTY workaround: set SUPABASE_ACCESS_TOKEN in the environment first.

Verification queries (run via `supabase db execute` or psql):
  select proname, prokind from pg_proc where proname = 'get_squad_streak';
    -- expect prokind = 'f'
  select has_function_privilege('authenticated', 'public.get_squad_streak(uuid, text)', 'execute');
    -- expect 't'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply migration 0011 via supabase db push and attest</name>
  <files></files>
  <read_first>
    - supabase/migrations/0011_squad_streak_v1_3.sql  (verify file exists before pushing)
    - .planning/phases/02-status-liveness-ttl/02-02-PLAN.md  (precedent for [BLOCKING] push + attestation pattern)
    - .planning/phases/03-friend-went-free-loop/03-02-PLAN.md  (precedent for 7-SELECT attestation + env var workaround)
  </read_first>
  <action>
1. Verify the migration file exists: `ls -la supabase/migrations/0011_squad_streak_v1_3.sql`. If missing, stop and flag Plan 01 as incomplete.
2. Run `supabase db push` from the repo root. If it reports a non-TTY error, set `SUPABASE_ACCESS_TOKEN=<token>` (user provides) and retry. Capture full stdout/stderr to the summary.
3. Run these four attestation SELECTs via `supabase db execute` OR via a one-off psql session (whichever the project has used in Phases 2-3) and paste literal output into `04-02-SUMMARY.md`:
   - `select proname, prokind, prosecdef from pg_proc where proname = 'get_squad_streak';`
     (expect 1 row, prokind='f', prosecdef='t')
   - `select has_function_privilege('authenticated', 'public.get_squad_streak(uuid, text)', 'execute');`
     (expect 't')
   - `select pg_get_function_arguments(p.oid) from pg_proc p where proname = 'get_squad_streak';`
     (expect `viewer_id uuid, tz text`)
   - `select pg_get_function_result(p.oid) from pg_proc p where proname = 'get_squad_streak';`
     (expect `TABLE(current_weeks integer, best_weeks integer)`)
4. Run one sanity RPC call as the user's own auth context (or via `select * from public.get_squad_streak(auth.uid(), 'UTC');` executed as the authenticated role in Studio) and paste the returned row. Zero-result is fine (e.g., `(0, 0)`) — only a non-error return is required.
5. If any attestation fails, STOP: do not mark the plan complete. Flag the mismatch to the user; do NOT attempt to hand-patch the live DB.
6. Re-generate or hand-patch `src/types/database.ts` (or wherever Supabase types live in this repo — check Phase 3 Plan 03-02 precedent) so that `supabase.rpc('get_squad_streak', ...)` is type-safe. If the project uses a generator command, run it and commit the diff. If the project hand-patches (per Phase 3 D-03), append the `get_squad_streak` RPC entry manually following the existing file pattern.

Hard rules:
- Do NOT edit `supabase/migrations/0011_squad_streak_v1_3.sql` from within this plan. If the push reveals a SQL bug, stop and open a revision to Plan 01.
- Do NOT skip the attestation SELECTs — they are the Nyquist proof that the DB state matches the migration file.
- Do NOT run `supabase db reset` or any destructive command.
  </action>
  <verify>
    <automated>test -f supabase/migrations/0011_squad_streak_v1_3.sql &amp;&amp; (command -v supabase >/dev/null 2>&amp;1 || { echo "supabase CLI missing"; exit 1; })</automated>
  </verify>
  <acceptance_criteria>
    - The file `.planning/phases/04-morning-prompt-squad-goals-streak/04-02-SUMMARY.md` exists and contains:
      - a literal `supabase db push` transcript line showing `Applying migration 0011_squad_streak_v1_3.sql` (or the CLI's equivalent success line)
      - the four attestation SELECT outputs with these exact matches:
        - `prokind = f` AND `prosecdef = t`
        - `has_function_privilege = t` (or `true`)
        - function arguments line containing `viewer_id uuid` AND `tz text`
        - function result line containing `current_weeks` AND `best_weeks`
      - the sanity RPC call returning either a row or an empty result (NOT an error).
    - If Supabase types are generated: `git diff --stat src/types/database.ts` (or equivalent) shows `get_squad_streak` added.
    - If Supabase types are hand-patched (Phase 3 precedent): `grep -q "get_squad_streak" src/types/database.ts` (or the types file path the project uses).
    - `supabase/migrations/0011_squad_streak_v1_3.sql` is unchanged (`git diff supabase/migrations/0011_squad_streak_v1_3.sql` empty after the push).
    - No `supabase db reset` command appears in the summary or shell history for this plan.
  </acceptance_criteria>
  <done>Migration 0011 is live in the Supabase project, all four attestation SELECTs pass with expected values, the sanity RPC returns without error, and Supabase types include the new RPC signature.</done>
</task>

</tasks>

<verification>
- Attestation block pasted into `04-02-SUMMARY.md`.
- `supabase/migrations/0011_squad_streak_v1_3.sql` untouched after push.
- Types file references `get_squad_streak`.
</verification>

<success_criteria>
Plan 04 (Squad streak UI) can safely call `supabase.rpc('get_squad_streak', { viewer_id, tz })` with full type safety, the function runs as `SECURITY DEFINER` with authz guard, and `authenticated` role has EXECUTE. All four attestation SELECTs are recorded verbatim in the summary.
</success_criteria>

<output>
After completion, create `.planning/phases/04-morning-prompt-squad-goals-streak/04-02-SUMMARY.md` containing the push transcript + four attestation SELECT outputs + sanity RPC output.
</output>
