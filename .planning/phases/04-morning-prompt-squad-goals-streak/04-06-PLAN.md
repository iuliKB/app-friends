---
phase: 04
plan: 06
type: execute
wave: 4
depends_on:
  - "04-03"
  - "04-04"
  - "04-05"
files_modified:
  - .planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md
autonomous: false
requirements:
  - STREAK-08
must_haves:
  truths:
    - "Every user-facing string introduced by Phase 4 is listed in `.planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md` alongside its source file and line-ish location."
    - "The file includes a 'Non-engineer approval' field with either (a) pasted approval text from a non-engineer reviewer OR (b) reviewer name + ISO date."
    - "The approval field is populated BEFORE Phase 4 VERIFICATION runs — this plan is a blocking gate per D-20."
    - "Any strings the reviewer wants changed have been updated in-file AND re-listed with the final wording."
    - "Copy language is positive-only: no countdown, no 'about to lose it', no loss-aversion framing (STREAK-08)."
  artifacts:
    - path: ".planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md"
      provides: "Consolidated copy inventory + non-engineer approval checkpoint"
      contains: "Non-engineer approval"
      min_lines: 40
  key_links: []
---

<objective>
**[BLOCKING]** Run the STREAK-08 copy review gate. Enumerate every user-facing string introduced by Phase 4 (notification title, StreakCard labels, Profile section labels, permission-denied hint), have a non-engineer review them against the positive-only constraint, and paste their approval into `04-COPY-REVIEW.md`. This plan is the terminal gate before VERIFICATION; without a filled approval field, Phase 4 cannot be marked complete per D-20.

Purpose: STREAK-08 non-engineer review mandate.
Output: `.planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md` with populated approval field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-morning-prompt-squad-goals-streak/04-CONTEXT.md
@src/lib/morningPrompt.ts
@src/components/squad/StreakCard.tsx
@src/app/(tabs)/profile.tsx
@src/app/_layout.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author 04-COPY-REVIEW.md with consolidated string inventory</name>
  <files>.planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md</files>
  <read_first>
    - src/lib/morningPrompt.ts  (title string "☀️ What's your status today?")
    - src/components/squad/StreakCard.tsx  (all StreakCard strings: "week streak", "Best: {N} weeks", "Start your first week — make a plan with friends.", accessibility labels)
    - src/app/(tabs)/profile.tsx  (Morning prompt row labels: "MORNING PROMPT" section header, "Morning prompt", "Time", inline permission-denied hint "Enable notifications in iOS Settings to receive morning prompts.")
    - src/app/_layout.tsx  (any user-visible strings in the morning_prompt branch — there should be none because the tap handler does not render UI, but double-check)
    - .planning/phases/04-morning-prompt-squad-goals-streak/04-CONTEXT.md  (D-19, D-20 are the full spec for this gate)
  </read_first>
  <behavior>
    - The file lists every user-facing string introduced by Phase 4 in a single table: column 1 = source file path, column 2 = approximate line number or element, column 3 = exact string, column 4 = rationale / context (who sees it, when).
    - The file has a "STREAK-08 positive-only guardrails" checklist the reviewer can tick: no countdown, no 'about to lose it', no hourglass, no grace-counter surfacing, no loss-aversion framing.
    - The file has a "Non-engineer approval" section with space for reviewer name, date, and free-form notes.
    - Before this plan is marked complete, the reviewer's approval text or name+date MUST be pasted in.
  </behavior>
  <action>
Create `.planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md` with exactly the structure below. Populate every string by grepping the source files listed in read_first — DO NOT guess; every string must be verifiable with a grep against the live code.

```markdown
# Phase 4 — Copy Review Gate

**Status:** pending-review
**Blocking:** YES — Phase 4 VERIFICATION cannot run until the "Non-engineer approval" field below is filled.
**Requirement:** STREAK-08
**Decision ref:** D-19, D-20

***

## Why this review exists

STREAK-08 mandates: "Copy is positive-only — celebratory framing, no countdown / hourglass / 'you're about to lose it' UI; copy reviewed by a non-engineer before ship."

D-20 locks the enforcement: a dedicated blocking plan task listing every user-facing string. D-15 additionally forbids surfacing grace-week state. This file is that enforcement.

***

## String inventory

Every user-facing string introduced by Phase 4. Strings already shipped in earlier phases (e.g., Phase 1 `PrePromptModal` body) are NOT re-reviewed here.

| # | File | Element | String | Seen when |
|---|------|---------|--------|-----------|
| 1 | `src/lib/morningPrompt.ts` | Notification title | `☀️ What's your status today?` | Daily local push fires at user's configured time |
| 2 | `src/lib/morningPrompt.ts` | Notification body | *(empty — title only)* | Same as #1 |
| 3 | `src/components/squad/StreakCard.tsx` | Main label | `week streak` | Goals tab, every render |
| 4 | `src/components/squad/StreakCard.tsx` | Subline (non-zero state) | `Best: ${bestWeeks} weeks` | Goals tab, when current streak > 0 |
| 5 | `src/components/squad/StreakCard.tsx` | Subline (zero state) | `Start your first week — make a plan with friends.` | Goals tab, when current streak === 0 |
| 6 | `src/components/squad/StreakCard.tsx` | Accessibility label (non-zero) | `${currentWeeks} week streak. Best ${bestWeeks} weeks` | Screen reader |
| 7 | `src/components/squad/StreakCard.tsx` | Accessibility label (zero) | `Start your first week streak` | Screen reader |
| 8 | `src/components/squad/StreakCard.tsx` | Emoji accessibility | `on fire` | Screen reader reads 🔥 |
| 9 | `src/app/(tabs)/profile.tsx` | Section header | `MORNING PROMPT` | Profile screen |
| 10 | `src/app/(tabs)/profile.tsx` | Switch row label | `Morning prompt` | Profile screen |
| 11 | `src/app/(tabs)/profile.tsx` | Time row label | `Time` | Profile screen |
| 12 | `src/app/(tabs)/profile.tsx` | Permission-denied hint | `Enable notifications in iOS Settings to receive morning prompts.` | After user denies the permission prompt |

*If any string above does not exist verbatim in the referenced source file, the implementing plan has drifted — flag to planner and re-run Plan 03/04/05 before proceeding with review.*

***

## STREAK-08 positive-only guardrails

The reviewer should tick each box after reading every string in the inventory:

- [ ] No countdown timers ("5 days left", "4 hours remaining", "this week is almost over")
- [ ] No "about to lose it" / "don't break your streak" framing
- [ ] No hourglass, flame-dying, or similar urgency imagery language
- [ ] No grace-week counter exposed to the user (D-15 — grace is silent)
- [ ] No loss-aversion phrasing ("you'll lose your {N}-week streak")
- [ ] Zero-state copy is inviting, not scolding
- [ ] Notification title reads as a friendly nudge, not a nag
- [ ] Profile labels are neutral and descriptive (no exclamation-driven hype, no guilt)

***

## Review decisions

For any string the reviewer wants changed, record the final wording below. The engineer implementing the change updates the source file AND re-runs this review with the new string pasted into the inventory above.

| # | Original string | Revised string | Reason |
|---|-----------------|----------------|--------|
|   |                 |                |        |

*(Leave this table empty if no changes are requested — "all strings approved as-is" is a valid outcome.)*

***

## Non-engineer approval

**Reviewer:** _(pasted name OR pasted approval message from the reviewer — free form)_

**Date (ISO 8601):** _(YYYY-MM-DD)_

**Notes:** _(optional — any caveats, follow-up questions, or phrasing the reviewer felt strongly about)_

**Approval statement:** I have read every string in the inventory above, ticked the positive-only guardrails, and I approve these strings for v1.3 ship.

***

## Known copy debt (for v1.4, not blocking v1.3)

- The Phase 1 `PrePromptModal` body is friend-free framed and is reused here for the morning-prompt onboarding path (D-38). The mismatch is acknowledged; a `body` prop refactor is tracked for v1.4. Flag in this review ONLY IF the reviewer feels it is a v1.3 ship blocker.

***

*Copy review instantiated: {date via `date +%Y-%m-%d`}*
*Gate: BLOCKING — Phase 4 VERIFICATION depends on a populated "Non-engineer approval" field.*
```

After creating the file, run `grep` against each source file to confirm every inventoried string exists in the code AS WRITTEN. If any string in the table cannot be found via `grep -F "..."`, STOP and flag a drift — do NOT paste a fake approval or proceed to review with a mismatched inventory.

Hard rules:
- Do NOT fill in the "Non-engineer approval" field yourself — that is the human checkpoint.
- Do NOT edit any source file from this task; if a string needs to change, that is the checkpoint resolution.
- The inventory MUST cover all 12 rows above; add more rows if new strings are discovered.
- If a string in the live code differs from the inventory, fix the INVENTORY to match the code (code is the source of truth at this point) and flag the drift.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md &amp;&amp; grep -q "Non-engineer approval" .planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md &amp;&amp; grep -q "BLOCKING" .planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md &amp;&amp; grep -q "week streak" .planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md &amp;&amp; grep -q "MORNING PROMPT" .planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md &amp;&amp; grep -q "positive-only" .planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md` exists.
    - `wc -l` >= 40.
    - `grep -c "Non-engineer approval" .planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md` >= 1.
    - `grep -c "BLOCKING" .planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md` >= 1.
    - `grep -c "positive-only" .planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md` >= 1.
    - The inventory table lists each of these strings at least once (each verified by `grep -F` against the MD file):
      - `What's your status today?`
      - `week streak`
      - `Best: ${bestWeeks} weeks`
      - `Start your first week — make a plan with friends.`
      - `Morning prompt`
      - `Enable notifications in iOS Settings to receive morning prompts.`
    - Every inventoried string is also present in its claimed source file (verified by `grep -F` against each source path in the inventory).
    - `grep -c "countdown\|about to lose\|hourglass" .planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md` >= 3 (the guardrail checklist mentions them — to make sure the reviewer sees the ban list).
  </acceptance_criteria>
  <done>The inventory file exists, is accurate against the live code, and is ready for a non-engineer to fill in the approval field. This task itself does NOT fill in the approval — that is Task 2's checkpoint.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Non-engineer copy review and approval checkpoint</name>
  <files>.planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md</files>
  <read_first>
    - .planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md  (the inventory authored by Task 1)
    - src/components/squad/StreakCard.tsx  (so the reviewer sees the strings in their rendered context on a dev build if they prefer)
    - src/app/(tabs)/profile.tsx  (Morning prompt section)
    - src/lib/morningPrompt.ts  (notification title string)
  </read_first>
  <action>
This task is a blocking human checkpoint per D-20 and STREAK-08. STREAK-08 mandates non-engineer review of every user-facing string against the positive-only constraint BEFORE the feature ships. This cannot be automated — it is the one genuinely human-only task in Phase 4.

Steps:

1. Open `.planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md`.
2. Share the "String inventory" table with a non-engineer reviewer. Options:
   - Paste the table into a DM / note app and ask them to read each string.
   - Or walk them through the StreakCard and the Profile "Morning prompt" section on a running dev build.
3. Ask them to confirm every guardrail in the "STREAK-08 positive-only guardrails" checklist (no countdown, no "about to lose it" framing, no hourglass/flame-dying imagery, no grace-counter exposure, no loss-aversion phrasing, zero-state inviting not scolding, title reads as friendly nudge not nag, Profile labels neutral).
4. If the reviewer wants any string changed:
   - Record the revised wording in the "Review decisions" table in 04-COPY-REVIEW.md.
   - Edit the matching source file to use the new wording (morningPrompt.ts, StreakCard.tsx, or profile.tsx).
   - Update the "String inventory" table in 04-COPY-REVIEW.md to show the final string.
   - Re-run the acceptance_criteria grep checks from Plan 04 Task 2 and Plan 05 Task 1 to ensure the code still matches, and re-run `npx expo lint` + `npx tsc --noEmit` on the modified files.
5. Fill in the bottom of `04-COPY-REVIEW.md`:
   - "Reviewer:" with the reviewer's name OR a pasted approval message from them.
   - "Date (ISO 8601):" with YYYY-MM-DD.
   - "Notes:" with any caveats (optional).
6. Commit the filled-in `04-COPY-REVIEW.md` and any source file edits from step 4.
7. Reply in the session with one of the three resume signals below.

Resume-signal (reply with one):
- `approved` — the reviewer signed off with no changes; the file now has their name + date filled in.
- `approved with changes: <summary>` — reviewer approved after the listed source changes were applied.
- `rejected: <reason>` — reviewer found a STREAK-08 violation that cannot be resolved by wording; escalate to a planner revision.

Hard rules:
- Do NOT fake an approval. If no non-engineer is available, pause the phase and tell the user. STREAK-08 is a documented ship gate.
- Do NOT run VERIFICATION for Phase 4 until this file has a populated approval block.
- Do NOT modify strings yourself without reviewer direction — you are facilitating, not deciding.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md &amp;&amp; grep -q "Reviewer:" .planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md &amp;&amp; ! grep -qE "Reviewer:\s*_\(" .planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md` has the "Reviewer:" line followed by either a non-empty name or a pasted approval message (not the placeholder `_(pasted name OR pasted approval message ...)_`).
    - The "Date (ISO 8601):" line contains a YYYY-MM-DD value that is not the placeholder `_(YYYY-MM-DD)_`.
    - `grep -c "_(pasted name OR pasted approval" .planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md` == 0 (placeholder replaced).
    - If the reviewer requested changes: every revised string is present in both the source file (verified by `grep -F` against the matching file in `src/`) AND updated in the inventory table.
    - `npx tsc --noEmit` still exits 0 after any source edits.
    - `npx expo lint` still exits 0 for any edited files.
    - The resume-signal was pasted into the session: one of `approved`, `approved with changes: ...`, or `rejected: ...`.
  </acceptance_criteria>
  <done>A non-engineer has reviewed every Phase 4 user-facing string, the approval block is populated (reviewer + ISO date), any requested wording changes are applied to both code and inventory, and Phase 4 VERIFICATION is unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewer ↔ inventory | The reviewer trusts that the inventory matches live code; Task 1 guarantees this by grep-verifying every inventoried string before presenting to the reviewer. |
| Approval field ↔ VERIFICATION gate | An empty approval field blocks VERIFICATION; a filled field unblocks it. No other signal carries this semantic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-20 | Repudiation (copy shipped without review) | `04-COPY-REVIEW.md` approval field | mitigate | This plan is `autonomous: false` with a `checkpoint:human-action` gate. VERIFICATION script should grep for a non-empty approval line before marking Phase 4 complete. The checkpoint's `resume-signal` explicitly requires one of three acknowledgement strings. |
| T-04-21 | Information Disclosure (accidental negative framing in notification copy) | Notification title + Profile labels | mitigate | STREAK-08 guardrail checklist explicitly lists countdown / loss-aversion / hourglass / grace-counter as banned; reviewer must tick each before approving. |
</threat_model>

<verification>
- `.planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md` exists with a populated "Non-engineer approval" block (reviewer name/message + ISO date) after the checkpoint resolves.
- Every inventoried string is verifiable via `grep -F` in its claimed source file.
- If the reviewer requested changes, both the source file and the inventory reflect the final wording.
</verification>

<success_criteria>
STREAK-08 is satisfied: every user-facing string has been reviewed by a non-engineer against the positive-only constraint, the approval is recorded in a blocking checkpoint file, and Phase 4 VERIFICATION can proceed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-morning-prompt-squad-goals-streak/04-06-SUMMARY.md` containing the reviewer's approval message (or name + date) plus a list of any string changes applied.
</output>
