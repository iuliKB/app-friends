---
phase: 03-card-stack-view
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - src/screens/home/HomeScreen.tsx
autonomous: false
requirements:
  - CARD-01
  - CARD-02
  - CARD-03
  - CARD-04
  - CARD-05
  - HOME-05
must_haves:
  truths:
    - "Switching to Cards view shows the FriendSwipeCard deck (not the placeholder)"
    - "Switching to Radar view shows the radar (not cards)"
    - "cardsPlaceholder View and its text are completely removed from the file"
    - "The two-section friend layout (Free grid / Everyone Else) is gone — Phase 2 already removed it; this plan confirms the placeholder is gone too"
    - "Cards view crossfade animation from Phase 2 continues to work around CardStackView"
  artifacts:
    - path: "src/screens/home/HomeScreen.tsx"
      provides: "HomeScreen with CardStackView wired into cards branch"
      contains: "CardStackView"
  key_links:
    - from: "src/screens/home/HomeScreen.tsx"
      to: "src/components/home/CardStackView.tsx"
      via: "import + JSX usage inside Animated.View"
      pattern: "CardStackView"
---

<objective>
Wire `CardStackView` into `HomeScreen` by replacing the `cardsPlaceholder` View (lines 127-130) with `<CardStackView friends={friends} />`.

This is the final integration task. The crossfade `Animated.View` wrapper from Phase 2 stays completely unchanged — `CardStackView` just renders inside it. The `cardsPlaceholder` styles in `StyleSheet.create` are also removed.

This plan also satisfies HOME-05: the two-section layout is confirmed gone (Phase 2 removed it; this plan removes the last placeholder artifact replacing it).

Purpose: Make the Cards toggle branch functional. After this plan, the full phase is complete.
Output: Updated `src/screens/home/HomeScreen.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-card-stack-view/03-CONTEXT.md
@.planning/phases/03-card-stack-view/03-RESEARCH.md

<interfaces>
<!-- CardStackView — from Plan 03 output (src/components/home/CardStackView.tsx) -->
```typescript
export interface CardStackViewProps {
  friends: FriendWithStatus[];
}
export function CardStackView(props: CardStackViewProps): JSX.Element | null;
```

<!-- HomeScreen — current cards placeholder (lines 127-130) -->
```typescript
// CURRENT state of the Animated.View cards branch (lines 123-132):
<Animated.View
  style={[styles.absoluteFill, { opacity: cardsOpacity }]}
  pointerEvents={view === 'cards' ? 'auto' : 'none'}
>
  <View style={styles.cardsPlaceholder}>
    <Text style={styles.placeholderHeading}>Cards View</Text>
    <Text style={styles.placeholderBody}>Coming in the next update.</Text>
  </View>
</Animated.View>

// REPLACE cardsPlaceholder View with:
<CardStackView friends={friends} />
```

<!-- RESEARCH.md integration note -->
```typescript
// HomeScreen.tsx line 127-130 integration point:
// Replace:
//   <View style={styles.cardsPlaceholder}>
//     <Text style={styles.placeholderHeading}>Cards View</Text>
//     <Text style={styles.placeholderBody}>Coming in the next update.</Text>
//   </View>
// With:
//   <CardStackView friends={friends} />
// The surrounding Animated.View (crossfade) stays unchanged.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace placeholder with CardStackView</name>
  <files>src/screens/home/HomeScreen.tsx</files>
  <read_first>
    - src/screens/home/HomeScreen.tsx — read the ENTIRE file before making any change, to understand current imports, the exact cardsPlaceholder block, and which StyleSheet entries to remove
    - src/components/home/CardStackView.tsx — confirm the exported name and props before importing
  </read_first>
  <action>
Make the following changes to `src/screens/home/HomeScreen.tsx`:

**1. Add import for CardStackView** (alongside existing home component imports):
```typescript
import { CardStackView } from '@/components/home/CardStackView';
```

**2. Replace the cardsPlaceholder block** (inside the `Animated.View` with `cardsOpacity`):
Find:
```typescript
<View style={styles.cardsPlaceholder}>
  <Text style={styles.placeholderHeading}>Cards View</Text>
  <Text style={styles.placeholderBody}>Coming in the next update.</Text>
</View>
```
Replace with:
```typescript
<CardStackView friends={friends} />
```
The `Animated.View` wrapper with `styles.absoluteFill` and `pointerEvents` is NOT changed — only the inner content changes.

**3. Remove dead styles** from `StyleSheet.create({...})`:
Remove these three style entries (they are no longer referenced):
- `cardsPlaceholder: { ... }` — the old placeholder container
- `placeholderHeading: { ... }` — the "Cards View" text style
- `placeholderBody: { ... }` — the "Coming in the next update." text style

**4. Remove dead Text import if it becomes unused** — check after removing the placeholder: `Text` is still used by `styles.errorText` rendering (`{error !== null && <Text ...>}`). Keep the `Text` import.

Do NOT change:
- The `Animated.View` wrappers or crossfade logic
- The `RadarView` branch
- The `RadarViewToggle`
- The `FAB` or `StatusPickerSheet`
- The `ScreenHeader` or `OwnStatusCard`
- Any imports that are still used
  </action>
  <verify>
    <automated>npx tsc --noEmit --project /Users/iulian/Develop/campfire/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/screens/home/HomeScreen.tsx` contains `import { CardStackView } from '@/components/home/CardStackView'`
    - `src/screens/home/HomeScreen.tsx` contains `<CardStackView friends={friends} />`
    - `src/screens/home/HomeScreen.tsx` does NOT contain `cardsPlaceholder` (neither JSX usage nor StyleSheet entry)
    - `src/screens/home/HomeScreen.tsx` does NOT contain `placeholderHeading` (StyleSheet entry gone)
    - `src/screens/home/HomeScreen.tsx` does NOT contain `placeholderBody` (StyleSheet entry gone)
    - `src/screens/home/HomeScreen.tsx` does NOT contain `"Cards View"` text string (placeholder heading gone)
    - `src/screens/home/HomeScreen.tsx` does NOT contain `"Coming in the next update"` text string (placeholder body gone)
    - `src/screens/home/HomeScreen.tsx` still contains `RadarView` (radar branch untouched)
    - `src/screens/home/HomeScreen.tsx` still contains `RadarViewToggle` (toggle untouched)
    - `npx tsc --noEmit` exits with zero errors
  </acceptance_criteria>
  <done>Cards view branch renders FriendSwipeCard deck (not placeholder). HOME-05 is satisfied — no two-section layout, no placeholder. Phase 3 is feature-complete pending human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify card stack end-to-end in Expo Go</name>
  <what-built>
    Complete Phase 3 card stack: FriendSwipeCard gesture physics, CardStackView deck management, HomeScreen wiring. Toggle between Radar and Cards views. ALIVE/FADING friends in deck.
  </what-built>
  <how-to-verify>
    1. Start Expo Go (`npx expo start`) and open the app on your device
    2. Navigate to the Home tab
    3. Confirm the Radar/Cards toggle is visible at the top of the friend area
    4. Tap "Cards" — the view should crossfade to show a stack of friend cards
    5. Verify each card shows: friend avatar (left, 64px), name, mood · context tag, last-active time (e.g. "now", "2h ago")
    6. Verify the counter above the deck shows "N more free" or "Just you right now"
    7. Swipe a card left or right — it should fly off screen with rotation. The next friend's card should appear
    8. Tap the "Skip" button (X circle) on a card — same fly-off animation should occur
    9. Tap the "Nudge" button (chat circle, orange) — should navigate to DM screen with that friend
    10. After skipping to the last card, confirm the deck loops back to the first card
    11. Swipe a card to skip it, then swipe DOWN on the next card — the previous card should return
    12. Tap "Radar" — should crossfade back to the radar bubble layout
    13. Confirm no DEAD friends appear in the card deck
  </how-to-verify>
  <resume-signal>Type "approved" if all 13 steps pass. Describe any issues found for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| friends prop → CardStackView | FriendWithStatus[] from authenticated useHomeScreen hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Information Disclosure | friends prop | accept | friends data is fetched from RLS-protected Supabase view. HomeScreen already had access to this data for the Radar view — passing it to CardStackView introduces no new exposure. |
</threat_model>

<verification>
1. `grep "CardStackView" src/screens/home/HomeScreen.tsx` — import and usage both present
2. `grep "cardsPlaceholder\|placeholderHeading\|placeholderBody" src/screens/home/HomeScreen.tsx` — returns nothing (dead code removed)
3. `npx tsc --noEmit` — zero errors
4. Human checkpoint: swipe gestures work in Expo Go, toggle crossfade works, DM navigation works
</verification>

<success_criteria>
- HomeScreen cards branch renders `<CardStackView friends={friends} />` (not placeholder)
- Placeholder JSX and styles completely removed
- TypeScript zero errors
- Human verification: swipe, skip, nudge, loop, undo, toggle all work as designed
</success_criteria>

<output>
After completion, create `.planning/phases/03-card-stack-view/03-04-SUMMARY.md`
</output>
