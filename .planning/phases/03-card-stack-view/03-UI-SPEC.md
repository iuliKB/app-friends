---
phase: 3
slug: card-stack-view
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-11
---

# Phase 3 — UI Design Contract: Card Stack View

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — custom design token system |
| Preset | not applicable |
| Component library | React Native core + Expo (LinearGradient, Haptics) |
| Icon library | @expo/vector-icons (Ionicons) — matches existing codebase pattern |
| Font | System default (no custom font declared in project) |

Source: codebase scan of `src/theme/` — no shadcn, no Tailwind. All tokens live in `src/theme/{colors,typography,spacing,radii}.ts`.

---

## Spacing Scale

Declared values — sourced from `src/theme/spacing.ts`:

| Token | Value | Usage in this phase |
|-------|-------|---------------------|
| xs | 4px | Avatar overlap offsets, tight internal gaps |
| sm | 8px | Icon-label gap in action buttons, inline gaps |
| md | 12px | Card internal section gaps |
| lg | 16px | Card padding (horizontal + vertical), screen edge padding |
| xl | 24px | Counter-to-deck vertical gap, bottom action area padding |
| xxl | 32px | Screen-level vertical breathing room above deck |

Exceptions:
- Card touch targets for Nudge and Skip buttons: minimum 44×44px per touch-target-size rule (ui-ux-pro-max skill)
- Stack offset for depth cards: 8px vertical offset and 4px horizontal shrink per depth level (Claude discretion — see Stack Depth section)

---

## Typography

Sourced from `src/theme/typography.ts`. This phase uses 4 of the 6 declared sizes and both declared weights.

| Role | Token | Size | Weight | Line Height | Usage |
|------|-------|------|--------|-------------|-------|
| Counter label | `FONT_SIZE.md` | 14px | `FONT_WEIGHT.regular` (400) | 1.4 | "2 more free" counter above deck |
| Card name | `FONT_SIZE.lg` | 16px | `FONT_WEIGHT.semibold` (600) | 1.3 | Friend display name on card |
| Card mood/context | `FONT_SIZE.md` | 14px | `FONT_WEIGHT.regular` (400) | 1.4 | Mood · context tag line |
| Card last-active | `FONT_SIZE.sm` | 13px | `FONT_WEIGHT.regular` (400) | 1.4 | "2h ago", "just now" secondary text |
| Action button label | `FONT_SIZE.sm` | 13px | `FONT_WEIGHT.regular` (400) | 1.0 | "Skip" and "Nudge" labels under icons |

Notes:
- No heading-level text in this component — it slots inside the HomeScreen scroll context
- Counter uses `COLORS.text.secondary` to remain subordinate to the deck

---

## Color

Sourced from `src/theme/colors.ts`. 60/30/10 split applied to card-stack context.

| Role | Token | Hex | Usage |
|------|-------|-----|-------|
| Dominant (60%) | `COLORS.surface.base` | `#1a1a1a` | Screen background visible around card edges, depth card fill |
| Secondary (30%) | `COLORS.surface.card` | `#2a2a2a` | Card base background color |
| Accent (10%) | `COLORS.interactive.accent` | `#f97316` | Nudge button background only |
| Status-free | `COLORS.status.free` | `#22c55e` | Gradient overlay on Free friend cards; pulse ring color |
| Status-maybe | `COLORS.status.maybe` | `#eab308` | Gradient overlay on Maybe friend cards |
| Status-busy | `COLORS.status.busy` | `#ef4444` | Gradient overlay on Busy friend cards |
| Destructive | `COLORS.interactive.destructive` | `#ef4444` | Not used in this phase (no destructive actions) |

Accent (`#f97316`) reserved for: **Nudge button background only.**
Skip button uses `COLORS.surface.overlay` (`#ffffff14`) — a ghost/outline style to visually subordinate it to Nudge.

Status gradient specification (sourced from RadarBubble pattern, D-04 in CONTEXT.md):
- Free card: `rgba(34,197,94,0.18)` top-left → `transparent` bottom-right
- Maybe card: `rgba(234,179,8,0.15)` top-left → `transparent` bottom-right
- Busy card: `rgba(239,68,68,0.15)` top-left → `transparent` bottom-right

FADING friend cards: render at 60% opacity (`opacity: 0.6`) — same rule as `HomeFriendCard.fadingCard` and `RADAR-03`.

---

## Component Inventory

Components to build for this phase:

### CardStackView (container)
- Renders the deck of ALIVE+FADING friends
- Manages deck state: current index, history for undo, loop behavior
- Provides counter label above deck: `"{N} more free"` or `"Just you for now"`
- Width: 80% of screen width (D-01 — breathing room around edges)
- Source: slots into `HomeScreen.tsx` line 127-130, replacing `cardsPlaceholder`

### FriendSwipeCard (single card)
- Avatar (64px `AvatarCircle`) left-aligned — D-02
- Name (`FONT_SIZE.lg`, semibold) right of avatar
- Mood · context tag (`FONT_SIZE.md`) right of avatar, below name
- Last-active time (`FONT_SIZE.sm`, secondary color) below mood line — D-03
- Status gradient covering full card background — D-04
- Border radius: `RADII.xl` (16px) — D-05 ("more playful than RADII.lg")
- Card elevation: `shadowColor: COLORS.shadow` with shadowRadius 12, shadowOpacity 0.4, elevation 8

### StackDepthEffect (depth cards behind front)
- 2 depth cards visible behind front card — D-06
- Depth card 1 (index+1): scale 0.95, translateY +8px, opacity 0.6
- Depth card 2 (index+2): scale 0.90, translateY +16px, opacity 0.35
- Background: `COLORS.surface.card` (no gradient on depth cards)

### ActionButtonRow (below deck)
- Two buttons: Skip (left) and Nudge (right)
- Layout: icon centered, small text label underneath — D-15
- Skip button: 56px circle, `COLORS.surface.overlay` background, `✕` icon (`Ionicons: close`), `COLORS.text.secondary` color
- Nudge button: 56px circle, `COLORS.interactive.accent` background, chat icon (`Ionicons: chatbubble`), `COLORS.text.primary` color
- Minimum touch target: 44×44px (both buttons exceed at 56px)
- Haptic feedback: `ImpactFeedbackStyle.Light` on both button presses — existing pattern

---

## Interaction Contract

### Swipe Gesture
- Horizontal swipe only — left or right both = Skip (D-07)
- Gesture handler: `react-native-gesture-handler` `Gesture.Pan` (D-08)
- Swipe threshold: 100px horizontal translation before commit
- Card rotation during drag: `rotation = translateX / 12` degrees (max ±15°)
- Exit animation: fly off screen in swipe direction with final rotation of ±20° — D-09
  - Duration: 250ms, `Easing.out(Easing.ease)`
  - useNativeDriver: true (transform + opacity only)
- isInteraction: false on all `Animated.timing` loops (STATE.md locked decision)

### Swipe Down to Undo
- Pan gesture downward (threshold: 80px vertical, velocity > 500) — D-10
- Undo animation: card returns from off-screen above, sliding down with slight deceleration
  - Duration: 300ms, `Easing.out(Easing.back(1.5))`

### Deck Loop
- Auto-loops: when index reaches end, reset to index 0 — D-11
- No "end of deck" empty state needed — the deck always has cards (ALIVE+FADING friends)

### Nudge Button
- Tap → async DM navigation via `supabase.rpc('get_or_create_dm_channel')` — D-14
- Loading state: disable button + reduce opacity to 0.5 during async call
- Error state: `Alert.alert('Error', "Couldn't open chat. Try again.")` — mirrors `HomeFriendCard` pattern

---

## Copywriting Contract

| Element | Copy | Source |
|---------|------|--------|
| Counter (friends present) | `"{N} more free"` | REQUIREMENTS CARD-05 |
| Counter (1 friend) | `"1 more free"` | REQUIREMENTS CARD-05 — singular form |
| Counter (user is only active) | `"Just you right now"` | Default — empty-but-looping state |
| Skip button label | `"Skip"` | CONTEXT D-15 |
| Nudge button label | `"Nudge"` | CONTEXT D-15 / REQUIREMENTS CARD-02 |
| Nudge error | `"Couldn't open chat. Try again."` | Matches HomeFriendCard error pattern |
| Accessibility label (card) | `"{display_name}, {mood} · {context}. Active {time}. Swipe to skip or tap Nudge to message."` | Claude discretion |
| Accessibility label (Skip) | `"Skip {display_name}"` | Claude discretion |
| Accessibility label (Nudge) | `"Nudge {display_name} — open DM"` | Claude discretion |

No destructive actions in this phase. No confirmation dialogs required.

Empty state: not applicable — deck auto-loops (D-11). If 0 ALIVE/FADING friends exist (edge case), CardStackView renders nothing and the toggle's Cards branch shows an empty container. The HomeScreen-level empty state (all friends DEAD) is out of scope for Phase 3.

---

## States & Variants

| State | Visual Rule |
|-------|-------------|
| ALIVE friend card | Full opacity, status gradient, PulseRing on avatar (optional — defer to Phase 3 plan discretion) |
| FADING friend card | 60% opacity, status gradient at half intensity, no pulse ring |
| Card being dragged | Rotation proportional to drag distance, slight scale-down to 0.97 |
| Card exiting (skip) | Fly off in drag direction, 250ms, fade to 0 opacity |
| Card returning (undo) | Slide in from above, 300ms, spring easing |
| Nudge button loading | opacity 0.5, disabled (no press feedback) |
| Depth card 1 | scale 0.95, translateY +8px, opacity 0.6, no gradient |
| Depth card 2 | scale 0.90, translateY +16px, opacity 0.35, no gradient |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no shadcn in project |
| Third-party | none | not applicable |

No new npm dependencies for swipe gesture — React Native Gesture Handler + Reanimated are already installed (STATE.md: "Uses React Native Gesture Handler + Reanimated for fluid gesture handling" D-08). The STATE.md pending todo "Verify rn-swiper-list compatibility with Expo Go" is noted — if `rn-swiper-list` is chosen at planning time, its vetting must be re-evaluated before committing.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
