---
phase: 11-birthday-feature
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - supabase/migrations/0017_birthday_social_v1_4.sql
autonomous: false
requirements:
  - D-01
  - D-03
  - D-07
  - D-11
  - D-15
  - D-17

must_haves:
  truths:
    - "`supabase db push` completes without errors"
    - "Migration history shows 0017 applied to project zqmaauaopyolutfoizgq"
    - "profiles.birthday_year column exists in live DB"
    - "wish_list_items table exists in live DB"
    - "wish_list_claims table exists in live DB"
    - "group_channels and group_channel_members tables exist in live DB"
    - "messages.group_channel_id column exists in live DB"
  artifacts:
    - path: "supabase/migrations/0017_birthday_social_v1_4.sql"
      provides: "Applied migration (no changes to file itself, DB state changes)"
      contains: "CREATE TABLE public.wish_list_items"
  key_links:
    - from: "supabase db push CLI"
      to: "Supabase project zqmaauaopyolutfoizgq"
      via: "SUPABASE_ACCESS_TOKEN environment variable"
      pattern: "supabase db push"
---

<objective>
Run `supabase db push` to apply migration 0017 to the live Supabase database. This is the [BLOCKING] gate — no client code can be written against new columns or tables until this push succeeds.

Purpose: Make the new schema live so Wave 3 client code can target real columns.
Output: Live database updated with all Phase 11 schema objects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-birthday-feature/11-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Push migration 0017 to Supabase</name>
  <read_first>
    - supabase/migrations/0017_birthday_social_v1_4.sql (confirm file exists and is complete before pushing)
  </read_first>
  <what-built>Migration 0017 SQL file written in Plan 01.</what-built>
  <how-to-verify>
Run the following command (requires SUPABASE_ACCESS_TOKEN in environment):
```
supabase db push
```
Expected output includes: `Applying migration 0017_birthday_social_v1_4.sql... done`

If the push fails due to a SQL error, fix the migration file and re-run.
If the push requires interactive confirmation for destructive changes, respond "y" when prompted.

After push completes, verify in Supabase Dashboard (https://supabase.com/dashboard/project/zqmaauaopyolutfoizgq/database/tables):
- profiles table has `birthday_year` column
- `wish_list_items` table exists
- `wish_list_claims` table exists
- `group_channels` table exists
- `group_channel_members` table exists
- `messages` table has `group_channel_id` column
  </how-to-verify>
  <resume-signal>Type "pushed" when supabase db push completes successfully, or describe the error if it fails.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → Supabase project | Push runs migration with superuser; SQL syntax errors or constraint violations fail here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-P02-01 | Tampering | Migration push | mitigate | Migration is idempotent-safe: all CREATE TABLE/ALTER TABLE operations fail gracefully on retry; SECURITY DEFINER functions use CREATE OR REPLACE |
| T-11-P02-02 | Denial of Service | birthday_year CHECK constraint | accept | CHECK (birthday_year BETWEEN 1900 AND 2100) is a minor DoS surface; valid user input always within range |
</threat_model>

<verification>
After push:
- `supabase db push` exits 0
- Migration history shows `0017_birthday_social_v1_4` as applied
</verification>

<success_criteria>
`supabase db push` exits 0. All Phase 11 schema objects exist in the live Supabase project. Client code written in Wave 3 plans will target real columns.
</success_criteria>

<output>
After completion, create `.planning/phases/11-birthday-feature/11-02-SUMMARY.md`
</output>
