---
phase: 11-birthday-feature
plan: 08
type: execute
wave: 5
depends_on:
  - 11-06
  - 11-07
files_modified: []
autonomous: false
requirements:
  - D-01
  - D-02
  - D-04
  - D-05
  - D-06
  - D-08
  - D-09
  - D-10
  - D-12
  - D-13
  - D-15
  - D-16
  - D-18

must_haves:
  truths:
    - "Profile edit shows three-column birthday picker (Month | Day | Year)"
    - "Profile edit shows My Wish List section with working add/delete"
    - "Birthday list rows show 'turning N' age label and are tappable"
    - "Friend Birthday Page shows wish list items with claim toggles"
    - "Friend Birthday Page shows friend picker checkboxes"
    - "Plan Birthday button creates a group chat and navigates to the chat room"
    - "Birthday friend does NOT appear in the group member picker"
  artifacts:
    - path: "src/app/profile/edit.tsx"
      provides: "birthday_year field + My Wish List section"
      contains: "birthdayYear"
    - path: "src/app/squad/birthday/[id].tsx"
      provides: "Friend Birthday Page — wish list + friend picker + group creation"
      contains: "create_birthday_group"
  key_links:
    - from: "Friend Birthday Page"
      to: "/chat/room"
      via: "router.push with group_channel_id"
      pattern: "group_channel_id"
---

<objective>
Human visual verification checkpoint. Confirm all Phase 11 birthday feature behaviors are working correctly before the phase is marked complete.

Purpose: Gate against shipping broken UX — verify the full user journey from profile setup through birthday page to group chat creation.
Output: Human approval or bug report for gap closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-birthday-feature/11-06-SUMMARY.md
@.planning/phases/11-birthday-feature/11-07-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify birthday year + wish list in profile edit</name>
  <what-built>Profile edit extended with birthday year column in picker and My Wish List section (Plan 11-06).</what-built>
  <how-to-verify>
1. Open the app and navigate to Profile tab → Edit Profile
2. Verify the Birthday field shows THREE columns: Month | Day | Year (not just two)
3. Tap Year — verify a scrollable list of years appears (e.g., 2025, 2024, 2023...)
4. Select a month, day, and year
5. Tap Save Changes — no alert about "Birthday incomplete" should appear
6. Reopen Edit Profile — verify the saved year is pre-selected in the Year column
7. Scroll down to the "My Wish List" section
8. Tap in the "Item title" field — type a title (e.g., "A good book")
9. Optionally fill in a link and notes
10. Tap "Add Item" — verify the item appears in the list above
11. Tap "Remove" on the item — verify it disappears from the list
12. Verify saving with Month+Day set but Year empty shows the alert: "Please add your birth year to save your birthday."
  </how-to-verify>
  <resume-signal>Type "profile-ok" to confirm both birthday year and wish list work, or describe issues found.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify birthday list "turning N" labels and tap navigation</name>
  <what-built>birthdays.tsx updated to show "turning N" age labels and tap to Friend Birthday Page (Plan 11-07).</what-built>
  <how-to-verify>
1. Navigate to Squad tab → tap "Birthdays" (or access the birthdays screen)
2. Verify birthday rows now show a combined label like "Jan 15 · turning 28 · In 3 days" for friends who have a year set
3. Friends without a year set should show "Jan 15 · In 3 days" (no age label)
4. Tap any friend's birthday row — verify it navigates to a "Birthday" screen for that friend
5. Verify the new screen shows:
   - A "Wish List" section with the friend's items (or an empty state)
   - A "Plan Birthday With" section listing the friend's other friends
   - A "Plan Birthday" button
  </how-to-verify>
  <resume-signal>Type "birthday-list-ok" to confirm tap and labels work, or describe issues found.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify claim toggle and birthday group chat creation</name>
  <what-built>WishListItem claim toggle in Friend Birthday Page; create_birthday_group RPC + navigation (Plans 11-05, 11-07).</what-built>
  <how-to-verify>
1. On a friend's Friend Birthday Page, tap "Claim" on a wish list item (if any exist)
2. Verify the button changes to "Unclaim" — tap again to unclaim
3. In the "Plan Birthday With" section, tap to select at least one friend (checkbox turns orange)
4. Tap "Plan Birthday" — verify a loading state appears briefly
5. Verify navigation to a chat room screen titled "[Friend]'s birthday"
6. Type and send a test message — verify it appears in the chat
7. Return to the Friend Birthday Page — tap "Plan Birthday" again with the same selection
8. Verify you arrive at a NEW group chat (a second group is created — this is acceptable behavior for the scope)

Note: Verifying that the birthday person cannot see claims requires two accounts. If you have two test accounts:
   - Log in as Friend A; claim an item on Friend B's wish list
   - Log in as Friend B; navigate to profile edit → My Wish List — verify no "Claimed" indicator appears on your own items
  </how-to-verify>
  <resume-signal>Type "group-chat-ok" to confirm group creation and chat navigation work, or describe issues found. Type "claim-visibility-deferred" if you cannot test with two accounts — the RLS enforcement is covered by the migration.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human verification | Final gate before phase complete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-P08-01 | Information Disclosure | claim visibility (owner sees own claims) | transfer | Human verification with two accounts recommended; RLS migration 0017 is the authoritative control; UI-only verification cannot confirm server-side behavior |
</threat_model>

<verification>
Human approves all three checkpoints.
</verification>

<success_criteria>
All three human verify checkpoints approved. Phase 11 birthday feature is complete: birthday year saves and round-trips, wish list CRUD works in profile, birthday list shows ages and navigates, Friend Birthday Page shows wish list + friend picker, birthday group chat creates and opens.
</success_criteria>

<output>
After completion, create `.planning/phases/11-birthday-feature/11-08-SUMMARY.md`
</output>
