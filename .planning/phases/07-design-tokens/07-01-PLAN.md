---
phase: 07-design-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/theme/colors.ts
  - src/theme/spacing.ts
  - src/theme/typography.ts
  - src/theme/radii.ts
  - src/theme/shadows.ts
  - src/theme/index.ts
autonomous: true
requirements: [TOKN-01, TOKN-02, TOKN-03, TOKN-04, TOKN-05]

must_haves:
  truths:
    - "Importing COLORS from '@/theme' provides all existing color values plus new semantic groups and COLORS.info"
    - "Importing SPACING from '@/theme' provides named constants for values 4, 8, 12, 16, 24, 32"
    - "Importing FONT_SIZE and FONT_WEIGHT from '@/theme' provides named size and weight constants"
    - "Importing RADII from '@/theme' provides named border radius constants"
    - "Importing SHADOWS from '@/theme' provides spreadable shadow preset objects with elevation"
    - "TypeScript autocomplete offers token names when typing SPACING. or FONT_SIZE. in any file"
  artifacts:
    - path: "src/theme/colors.ts"
      provides: "Semantic color tokens with nested groups"
      contains: "COLORS"
      exports: ["COLORS"]
    - path: "src/theme/spacing.ts"
      provides: "Spacing scale constants"
      contains: "SPACING"
      exports: ["SPACING"]
    - path: "src/theme/typography.ts"
      provides: "Font size and weight constants"
      contains: "FONT_SIZE"
      exports: ["FONT_SIZE", "FONT_WEIGHT"]
    - path: "src/theme/radii.ts"
      provides: "Border radius constants"
      contains: "RADII"
      exports: ["RADII"]
    - path: "src/theme/shadows.ts"
      provides: "Shadow preset objects"
      contains: "SHADOWS"
      exports: ["SHADOWS"]
    - path: "src/theme/index.ts"
      provides: "Barrel export for all tokens"
      exports: ["COLORS", "SPACING", "FONT_SIZE", "FONT_WEIGHT", "RADII", "SHADOWS"]
  key_links:
    - from: "src/theme/index.ts"
      to: "all token files"
      via: "re-export"
      pattern: "export .* from"
    - from: "src/theme/colors.ts"
      to: "src/constants/colors.ts"
      via: "replaces (old file kept as re-export during migration)"
      pattern: "COLORS"
---

<objective>
Create all design token files in src/theme/ with color, spacing, typography, border radius, and shadow constants derived from the existing codebase audit.

Purpose: Establish the single source of truth for all visual constants so Phase 8 (shared components) and Phase 9 (screen sweep) can reference named tokens instead of raw values.
Output: 6 files in src/theme/ — 5 token files + 1 barrel export
</objective>

<execution_context>
@/Users/iulian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iulian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-design-tokens/07-CONTEXT.md
@.planning/UI-REVIEW.md
@.planning/research/PITFALLS.md

Key source file:
@src/constants/colors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all token files in src/theme/</name>
  <files>src/theme/colors.ts, src/theme/spacing.ts, src/theme/typography.ts, src/theme/radii.ts, src/theme/shadows.ts</files>
  <read_first>
    - src/constants/colors.ts (existing COLORS definition — source of truth for current values)
    - .planning/UI-REVIEW.md (hardcoded color inventory, typography audit, spacing audit, borderRadius audit)
    - .planning/phases/07-design-tokens/07-CONTEXT.md (locked decisions on naming, structure, values)
  </read_first>
  <action>
Create `src/theme/` directory and the following 5 files. All use `as const` pattern.

**src/theme/colors.ts** — Restructure existing COLORS into semantic nested groups. Preserve every existing value. Add new tokens per audit.

```typescript
export const COLORS = {
  // Text
  text: {
    primary: '#f5f5f5',
    secondary: '#9ca3af',
  },

  // Surfaces
  surface: {
    base: '#1a1a1a',       // app background (was: dominant)
    card: '#2a2a2a',       // cards, inputs, tab bar (was: secondary)
    overlay: '#ffffff14',  // (was: surfaceOverlay)
  },

  // Interactive
  interactive: {
    accent: '#f97316',     // campfire orange — primary CTA
    destructive: '#ef4444', // inline errors, destructive actions
  },

  // Feedback / indicators
  feedback: {
    info: '#3b82f6',       // notification/indicator blue (was undeclared #3b82f6 in ChatListRow)
    error: '#ef4444',      // same as destructive — semantic alias
  },

  // Status
  status: {
    free: '#22c55e',
    busy: '#ef4444',
    maybe: '#eab308',
  },

  // Borders
  border: '#3f3f46',

  // Shadows
  shadow: '#000000',

  // Offline banner
  offline: {
    bg: '#92400e',
    text: '#fef3c7',
  },

  // Splash
  splash: {
    gradientStart: '#ff6b35',
    gradientEnd: '#dc2626',
    text: '#fef3c7',
  },
} as const;
```

IMPORTANT: The exact group names (text, surface, interactive, feedback) are from CONTEXT.md. The user decided `#3b82f6` becomes `COLORS.feedback.info` (a generic info color, not "unreadDot"). Keep `feedback.error` as a semantic alias pointing to the same value as `interactive.destructive`.

**src/theme/spacing.ts** — Size-based naming per user decision:

```typescript
export const SPACING = {
  xs: 4,    // avatar overlap offset, tight gaps
  sm: 8,    // separator height, inline gaps, icon-label gap
  md: 12,   // card section gaps, separator marginLeft
  lg: 16,   // standard screen padding, card padding
  xl: 24,   // large section spacing, button-from-form gap, FAB bottom offset
  xxl: 32,  // screen header top, auth header padding
} as const;
```

All 6 values (4, 8, 12, 16, 24, 32) come from the codebase audit. Value 12 MUST be included (see PITFALLS.md — Pitfall 4).

**src/theme/typography.ts** — Separate size and weight tokens per user decision (NOT combined presets):

```typescript
export const FONT_SIZE = {
  xs: 11,   // tab bar labels only
  sm: 13,   // timestamps, captions
  md: 14,   // form labels, secondary info
  lg: 16,   // body text, card titles, button labels
  xl: 20,   // section headings
  xxl: 24,  // screen titles
} as const;

export const FONT_WEIGHT = {
  regular: '400' as const,
  semibold: '600' as const,
} as const;
```

NOTE: Only 2 font weights exist app-wide ('400' and '600'). The FONT_SIZE scale covers the core 6 sizes found in the audit (11, 13, 14, 16, 20, 24). Decorative sizes (28, 32, 48, 64) used only in splash/emoji are intentionally excluded — those are one-offs, not design tokens.

**src/theme/radii.ts** — Extract from borderRadius audit:

```typescript
export const RADII = {
  xs: 4,    // small indicators (unread dot, action sheet handle)
  sm: 6,    // segmented control thumb
  md: 8,    // buttons, inputs, small cards, tags
  lg: 12,   // cards, containers, modals, form fields
  xl: 16,   // large containers (QR display, scan overlay)
  pill: 18, // message bubbles
  full: 9999, // fully circular (avatars, FABs — use with equal width/height)
} as const;
```

Derived from the 57 borderRadius instances in the codebase. The values 2, 5, 10 are outliers to be normalized during Phase 9 screen sweep — they are NOT given dedicated tokens.

**src/theme/shadows.ts** — Only one shadow pattern exists in the codebase (FAB shadow), so define one preset:

```typescript
import { Platform } from 'react-native';

export const SHADOWS = {
  fab: {
    shadowColor: '#000',
    shadowOpacity: 0.3,
    shadowRadius: 4,
    shadowOffset: { width: 0, height: 2 },
    elevation: 4,
  },
  card: {
    shadowColor: '#000',
    shadowOpacity: 0.15,
    shadowRadius: 3,
    shadowOffset: { width: 0, height: 1 },
    elevation: 2,
  },
  none: {
    shadowColor: 'transparent',
    shadowOpacity: 0,
    shadowRadius: 0,
    shadowOffset: { width: 0, height: 0 },
    elevation: 0,
  },
} as const;
```

The `fab` preset is extracted exactly from HomeScreen.tsx:196-200, PlansListScreen.tsx:251-255, FriendsList.tsx:139-143 (all identical). The `card` preset is a lighter variant for elevated card surfaces. The `none` preset is a reset utility.
  </action>
  <verify>
    <automated>cd /Users/iulian/Develop/campfire && ls src/theme/colors.ts src/theme/spacing.ts src/theme/typography.ts src/theme/radii.ts src/theme/shadows.ts && npx tsc --noEmit --strict 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/theme/colors.ts contains `export const COLORS =` and `as const`
    - src/theme/colors.ts contains `feedback:` with `info: '#3b82f6'`
    - src/theme/colors.ts contains `text:`, `surface:`, `interactive:`, `status:`, `border:`, `shadow:`, `offline:`, `splash:`
    - src/theme/spacing.ts contains `export const SPACING =` with values xs: 4, sm: 8, md: 12, lg: 16, xl: 24, xxl: 32
    - src/theme/typography.ts contains `export const FONT_SIZE =` and `export const FONT_WEIGHT =`
    - src/theme/typography.ts contains `xs: 11` and `sm: 13` and `md: 14` and `lg: 16` and `xl: 20` and `xxl: 24`
    - src/theme/typography.ts contains `regular: '400'` and `semibold: '600'`
    - src/theme/radii.ts contains `export const RADII =` with xs, sm, md, lg, xl, pill, full
    - src/theme/shadows.ts contains `export const SHADOWS =` with `fab:` and `card:` presets
    - src/theme/shadows.ts `fab` preset has `shadowOpacity: 0.3`, `shadowRadius: 4`, `elevation: 4`
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>All 5 token files exist in src/theme/, export named constants with `as const`, and TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create barrel export and verify import path works</name>
  <files>src/theme/index.ts</files>
  <read_first>
    - src/theme/colors.ts (just created — verify export names)
    - src/theme/spacing.ts (just created — verify export names)
    - src/theme/typography.ts (just created — verify export names)
    - src/theme/radii.ts (just created — verify export names)
    - src/theme/shadows.ts (just created — verify export names)
    - tsconfig.json (confirm @/* path alias covers src/theme/)
  </read_first>
  <action>
Create `src/theme/index.ts` as a barrel re-export:

```typescript
export { COLORS } from './colors';
export { SPACING } from './spacing';
export { FONT_SIZE, FONT_WEIGHT } from './typography';
export { RADII } from './radii';
export { SHADOWS } from './shadows';
```

The `@/*` path alias in tsconfig.json maps to `./src/*`, so `@/theme` resolves to `src/theme/index.ts` automatically — no tsconfig changes needed.

After creating the file, verify that a test import compiles. Create a temporary file `src/theme/_verify.ts`:

```typescript
import { COLORS, SPACING, FONT_SIZE, FONT_WEIGHT, RADII, SHADOWS } from '@/theme';

// Type-level verification — these must not be `any`
const _c: string = COLORS.text.primary;
const _s: number = SPACING.lg;
const _f: number = FONT_SIZE.lg;
const _w: string = FONT_WEIGHT.semibold;
const _r: number = RADII.lg;
const _sh: number = SHADOWS.fab.elevation;
```

Run `npx tsc --noEmit` to confirm. Then delete `src/theme/_verify.ts`.
  </action>
  <verify>
    <automated>cd /Users/iulian/Develop/campfire && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/theme/index.ts contains `export { COLORS } from './colors'`
    - src/theme/index.ts contains `export { SPACING } from './spacing'`
    - src/theme/index.ts contains `export { FONT_SIZE, FONT_WEIGHT } from './typography'`
    - src/theme/index.ts contains `export { RADII } from './radii'`
    - src/theme/index.ts contains `export { SHADOWS } from './shadows'`
    - `npx tsc --noEmit` exits with code 0
    - src/theme/_verify.ts does NOT exist (deleted after verification)
  </acceptance_criteria>
  <done>Barrel export at src/theme/index.ts re-exports all 6 token objects. Import path `@/theme` resolves correctly and TypeScript provides autocomplete on all token properties.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (all token files type-check)
- `ls src/theme/` shows: colors.ts, spacing.ts, typography.ts, radii.ts, shadows.ts, index.ts
- `grep -c "as const" src/theme/colors.ts src/theme/spacing.ts src/theme/typography.ts src/theme/radii.ts src/theme/shadows.ts` shows at least 1 per file
- `grep "info:" src/theme/colors.ts` returns the line with `'#3b82f6'`
</verification>

<success_criteria>
All 5 token domains (color, spacing, typography, radii, shadows) are defined as TypeScript `as const` objects in src/theme/. A single barrel import from `@/theme` provides COLORS, SPACING, FONT_SIZE, FONT_WEIGHT, RADII, and SHADOWS with full autocomplete. The undeclared `#3b82f6` is captured as `COLORS.feedback.info`. TypeScript strict mode passes.
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-tokens/07-01-SUMMARY.md`
</output>
