---
phase: 06-notifications-polish
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - src/app/(tabs)/profile.tsx
  - src/screens/home/HomeScreen.tsx
  - src/screens/plans/PlansListScreen.tsx
  - src/screens/chat/ChatListScreen.tsx
  - src/screens/friends/FriendsList.tsx
  - src/screens/friends/FriendRequests.tsx
  - src/screens/plans/PlanDashboardScreen.tsx
  - src/screens/chat/ChatRoomScreen.tsx
  - src/screens/friends/AddFriend.tsx
  - src/components/plans/RSVPButtons.tsx
autonomous: true
requirements: [UIPOL-01, UIPOL-04]

must_haves:
  truths:
    - "Profile tab shows large avatar, display name, pencil edit icon, and notification toggle"
    - "All list screens use shared EmptyState component instead of inline empty states"
    - "All async screens use shared LoadingIndicator instead of inline ActivityIndicators"
    - "Status colours are referenced from COLORS.status everywhere (no hardcoded hex)"
    - "FriendActionSheet 'View Profile' navigates to /friends/[id] instead of showing placeholder alert"
  artifacts:
    - path: "src/app/(tabs)/profile.tsx"
      provides: "Redesigned profile tab with avatar header, notification toggle"
      contains: "notifications_enabled"
    - path: "src/components/plans/RSVPButtons.tsx"
      provides: "RSVP buttons using COLORS.status instead of hardcoded hex"
      contains: "COLORS.status"
  key_links:
    - from: "src/app/(tabs)/profile.tsx"
      to: "src/app/profile/edit.tsx"
      via: "router.push('/profile/edit')"
      pattern: "profile/edit"
    - from: "src/screens/friends/FriendsList.tsx"
      to: "src/app/friends/[id].tsx"
      via: "router.push in onViewProfile"
      pattern: "friends/"
    - from: "src/screens/home/HomeScreen.tsx"
      to: "src/components/common/EmptyState.tsx"
      via: "import EmptyState"
      pattern: "EmptyState"
---

<objective>
Profile tab redesign with avatar header and notification toggle, plus full UI consistency audit across all screens.

Purpose: Makes the profile tab feel polished with a personal avatar header and notification controls. Replaces all inline empty states and loading indicators with shared components. Ensures status colours are consistent. Wires "View Profile" to the new friend profile screen. This is the final polish pass that makes the app production-ready.
Output: Redesigned profile tab, 8+ screens updated with shared components, status colour audit complete, FriendActionSheet wired.
</objective>

<execution_context>
@/Users/iulian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iulian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-notifications-polish/06-CONTEXT.md
@.planning/phases/06-notifications-polish/06-UI-SPEC.md
@.planning/phases/06-notifications-polish/06-01-SUMMARY.md
@.planning/phases/06-notifications-polish/06-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 shared components -->

From src/components/common/EmptyState.tsx:
```typescript
export function EmptyState(props: {
  icon: string;
  iconType?: 'emoji' | 'ionicons';
  heading: string;
  body: string;
  ctaLabel?: string;
  onCta?: () => void;
}): JSX.Element;
```

From src/components/common/LoadingIndicator.tsx:
```typescript
export function LoadingIndicator(props: {
  color?: string;
  size?: 'small' | 'large';
  style?: ViewStyle;
}): JSX.Element;
```

<!-- From Plan 01 push hook -->

From src/hooks/usePushNotifications.ts:
```typescript
export async function registerForPushNotifications(userId: string): Promise<void>;
export async function getNotificationsEnabled(): Promise<boolean>;
export async function setNotificationsEnabled(enabled: boolean): Promise<void>;
```

<!-- Existing code being modified -->

From src/app/(tabs)/profile.tsx:
```typescript
// Current header: <Text style={styles.heading}>Profile</Text> + <Text style={styles.email}>{session?.user.email}</Text>
// Sections: YOUR STATUS, FRIENDS (My Friends, Friend Requests, My QR Code), Logout
// Needs: avatar header replacement, pencil edit icon, notification toggle section
```

From src/constants/colors.ts:
```typescript
export const COLORS = {
  status: { free: '#22c55e', busy: '#ef4444', maybe: '#eab308' },
  // ...all other colors
} as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile tab redesign — avatar header, edit link, notification toggle</name>
  <files>
    src/app/(tabs)/profile.tsx
  </files>
  <read_first>
    src/app/(tabs)/profile.tsx,
    src/hooks/usePushNotifications.ts,
    src/components/common/AvatarCircle.tsx,
    src/constants/colors.ts,
    src/lib/supabase.ts,
    .planning/phases/06-notifications-polish/06-UI-SPEC.md
  </read_first>
  <action>
Redesign the profile tab header and add notification toggle. Keep all existing sections (YOUR STATUS, FRIENDS, logout) intact.

**Replace header** (lines ~60-61 in current file):
Remove:
```
<Text style={styles.heading}>Profile</Text>
<Text style={styles.email}>{session?.user.email}</Text>
```

Replace with avatar header section:
- Fetch profile data on mount: `supabase.from('profiles').select('display_name, avatar_url').eq('id', session.user.id).single()` — store in local state `profile: { display_name: string, avatar_url: string | null } | null`
- Refetch on focus using `useFocusEffect` (already imported) to pick up edits from Edit Profile screen
- Centred container with `alignItems: 'center', paddingBottom: 24`:
  - `<View style={{ position: 'relative' }}>` wrapping:
    - `<AvatarCircle size={80} imageUri={profile?.avatar_url} displayName={profile?.display_name || 'U'} onPress={() => router.push('/profile/edit' as never)} />`
    - Pencil overlay: `<View style={{ position: 'absolute', bottom: 0, right: 0, width: 24, height: 24, borderRadius: 12, backgroundColor: COLORS.secondary, alignItems: 'center', justifyContent: 'center' }}><Ionicons name="pencil-outline" size={16} color={COLORS.accent} /></View>`
  - Display name: `<Text style={{ fontSize: 20, fontWeight: '600', color: COLORS.textPrimary, marginTop: 12, textAlign: 'center' }}>{profile?.display_name || ''}</Text>`
  - Entire avatar + name area tappable via `<TouchableOpacity onPress={() => router.push('/profile/edit' as never)}>`

**Add NOTIFICATIONS section** after the FRIENDS section (after My QR Code row, before logout):
- Section header: `<Text style={styles.sectionHeader}>NOTIFICATIONS</Text>`
- Row with bell icon + switch:
```tsx
<View style={styles.row}>
  <Ionicons name="notifications-outline" size={20} color={COLORS.textSecondary} style={styles.rowIcon} />
  <Text style={styles.rowLabel}>Plan invites</Text>
  <Switch
    value={notificationsEnabled}
    onValueChange={handleToggleNotifications}
    trackColor={{ false: COLORS.border, true: COLORS.accent + '40' }}
    thumbColor={notificationsEnabled ? COLORS.accent : COLORS.border}
  />
</View>
```
- State: `notificationsEnabled` boolean, initialized from `getNotificationsEnabled()` on mount
- `handleToggleNotifications(value: boolean)`:
  - `await setNotificationsEnabled(value)` — updates AsyncStorage
  - `setNotificationsEnabled_state(value)`
  - If turning ON: call `registerForPushNotifications(session.user.id)` — this will prompt for permission if needed
- Import `Switch` from 'react-native'
- Import `{ registerForPushNotifications, getNotificationsEnabled, setNotificationsEnabled }` from `@/hooks/usePushNotifications`

**Add imports:**
- `Switch` added to react-native import
- `registerForPushNotifications, getNotificationsEnabled, setNotificationsEnabled` from `@/hooks/usePushNotifications`

**Remove old styles:** `heading`, `email` styles no longer needed. Add new styles for avatar header section.
  </action>
  <verify>
    <automated>cd /Users/iulian/Develop/campfire && npx expo lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(tabs)/profile.tsx does NOT contain `styles.email` or `session?.user.email`
    - src/app/(tabs)/profile.tsx contains `AvatarCircle` with `size={80}`
    - src/app/(tabs)/profile.tsx contains `pencil-outline`
    - src/app/(tabs)/profile.tsx contains `profile/edit`
    - src/app/(tabs)/profile.tsx contains `NOTIFICATIONS`
    - src/app/(tabs)/profile.tsx contains `Plan invites`
    - src/app/(tabs)/profile.tsx contains `Switch`
    - src/app/(tabs)/profile.tsx contains `notifications-outline`
    - src/app/(tabs)/profile.tsx contains `registerForPushNotifications`
    - src/app/(tabs)/profile.tsx contains `getNotificationsEnabled`
    - src/app/(tabs)/profile.tsx contains `setNotificationsEnabled`
    - src/app/(tabs)/profile.tsx contains `profiles` (fetches profile data)
  </acceptance_criteria>
  <done>Profile tab shows 80px avatar with pencil overlay, display name below, tappable to edit. NOTIFICATIONS section with "Plan invites" toggle using Switch + AsyncStorage. Old email-only header removed.</done>
</task>

<task type="auto">
  <name>Task 2: UI audit — empty states, loading indicators, status colours, view profile wiring</name>
  <files>
    src/screens/home/HomeScreen.tsx,
    src/screens/plans/PlansListScreen.tsx,
    src/screens/chat/ChatListScreen.tsx,
    src/screens/friends/FriendsList.tsx,
    src/screens/friends/FriendRequests.tsx,
    src/screens/plans/PlanDashboardScreen.tsx,
    src/screens/chat/ChatRoomScreen.tsx,
    src/screens/friends/AddFriend.tsx,
    src/components/plans/RSVPButtons.tsx
  </files>
  <read_first>
    src/screens/home/HomeScreen.tsx,
    src/screens/plans/PlansListScreen.tsx,
    src/screens/chat/ChatListScreen.tsx,
    src/screens/friends/FriendsList.tsx,
    src/screens/friends/FriendRequests.tsx,
    src/screens/plans/PlanDashboardScreen.tsx,
    src/screens/chat/ChatRoomScreen.tsx,
    src/screens/friends/AddFriend.tsx,
    src/components/plans/RSVPButtons.tsx,
    src/components/common/EmptyState.tsx,
    src/components/common/LoadingIndicator.tsx,
    src/constants/colors.ts,
    .planning/phases/06-notifications-polish/06-UI-SPEC.md
  </read_first>
  <action>
Systematic audit across 9 files. For each file, replace inline empty states with `<EmptyState>`, replace inline `<ActivityIndicator>` full-screen loaders with `<LoadingIndicator>`, and fix any hardcoded status colours.

**1. src/screens/home/HomeScreen.tsx:**
- Replace inline empty state (lines ~93-105, the `<View style={styles.emptyState}>` block) with:
  `<EmptyState icon="🔥" heading="Nobody's free right now" body="When friends set their status to Free, they'll show up here." />`
  Note: the CURRENT empty state is "No friends yet" (when friends.length === 0). Per UI-SPEC, use "Nobody's free right now" copy. Keep the condition `friends.length === 0 && !statusLoading`. No CTA button — the FAB is already visible.
- Remove `emptyState`, `emptyEmoji`, `emptyHeading`, `emptyBody` styles
- Add `import { EmptyState } from '@/components/common/EmptyState';`

**2. src/screens/plans/PlansListScreen.tsx:**
- Replace inline empty state with:
  `<EmptyState icon="📅" heading="No plans yet" body="Tap the + button to create a quick plan and invite your free friends." />`
- Remove inline empty state styles
- Add EmptyState import

**3. src/screens/chat/ChatListScreen.tsx:**
- Replace inline empty state with:
  `<EmptyState icon="💬" heading="No conversations yet" body="Start a DM from a friend's card, or create a plan to get a group chat going." />`
- Replace any full-screen `<ActivityIndicator>` with `<LoadingIndicator />`
- Remove inline empty state styles
- Add EmptyState and LoadingIndicator imports

**4. src/screens/friends/FriendsList.tsx:**
- Replace `renderEmpty()` function with:
  `<EmptyState icon="👥" heading="No friends yet" body="Add friends by username or share your QR code — tap the + button to get started." ctaLabel="Add Friend" onCta={() => router.push('/friends/add')} />`
- Wire `onViewProfile` in FriendActionSheet: change `handleViewProfile` from `Alert.alert('Coming soon', 'Coming in Phase 6')` to `router.push(\`/friends/${selectedFriend.friend_id}\` as never); handleCloseSheet();`
- Remove `renderEmpty` function and inline empty state styles (`emptyContainer`, `emptyHeading`, `emptyBody`)
- Add EmptyState import

**5. src/screens/friends/FriendRequests.tsx:**
- Replace `renderEmpty()` function with:
  `<EmptyState icon="person-add-outline" iconType="ionicons" heading="No pending requests" body="Friend requests you receive will show up here." />`
- Remove `renderEmpty` function and inline empty state styles
- Add EmptyState import

**6. src/screens/plans/PlanDashboardScreen.tsx:**
- Replace any full-screen `<ActivityIndicator>` (initial loading state) with `<LoadingIndicator />`
- Add LoadingIndicator import

**7. src/screens/chat/ChatRoomScreen.tsx:**
- Replace any full-screen `<ActivityIndicator>` with `<LoadingIndicator />`
- Add LoadingIndicator import (if applicable — the chat room empty state "Start the conversation!" is NOT a list empty state, leave it as-is)

**8. src/screens/friends/AddFriend.tsx:**
- Replace any full-screen `<ActivityIndicator>` with `<LoadingIndicator />`
- Add LoadingIndicator import if applicable

**9. src/components/plans/RSVPButtons.tsx — STATUS COLOUR FIX:**
- Replace hardcoded hex values in RSVP_OPTIONS array:
  - `activeColor: '#22c55e'` -> `activeColor: COLORS.status.free`
  - `activeColor: '#eab308'` -> `activeColor: COLORS.status.maybe`
  - `activeColor: '#ef4444'` -> `activeColor: COLORS.status.busy`
- Add `import { COLORS } from '@/constants/colors';` if not already imported

For every file: remove unused style declarations after replacing inline components. Keep `import { ActivityIndicator } from 'react-native'` if it's still used for inline button spinners — only remove the import if ALL ActivityIndicator usages in that file are replaced.
  </action>
  <verify>
    <automated>cd /Users/iulian/Develop/campfire && npx expo lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/screens/home/HomeScreen.tsx contains `EmptyState` import
    - src/screens/home/HomeScreen.tsx contains `Nobody's free right now`
    - src/screens/home/HomeScreen.tsx does NOT contain `emptyEmoji:` style definition
    - src/screens/plans/PlansListScreen.tsx contains `EmptyState` import
    - src/screens/plans/PlansListScreen.tsx contains `No plans yet`
    - src/screens/chat/ChatListScreen.tsx contains `EmptyState` import
    - src/screens/chat/ChatListScreen.tsx contains `No conversations yet`
    - src/screens/friends/FriendsList.tsx contains `EmptyState` import
    - src/screens/friends/FriendsList.tsx contains `No friends yet`
    - src/screens/friends/FriendsList.tsx does NOT contain `Coming in Phase 6`
    - src/screens/friends/FriendsList.tsx contains `/friends/` (view profile navigation)
    - src/screens/friends/FriendRequests.tsx contains `EmptyState` import
    - src/screens/friends/FriendRequests.tsx contains `No pending requests`
    - src/components/plans/RSVPButtons.tsx contains `COLORS.status.free`
    - src/components/plans/RSVPButtons.tsx contains `COLORS.status.maybe`
    - src/components/plans/RSVPButtons.tsx contains `COLORS.status.busy`
    - src/components/plans/RSVPButtons.tsx does NOT contain `'#22c55e'`
    - src/components/plans/RSVPButtons.tsx does NOT contain `'#eab308'`
  </acceptance_criteria>
  <done>All 5 list screens use shared EmptyState with correct copy from UI-SPEC. All full-screen loaders use shared LoadingIndicator. RSVPButtons references COLORS.status instead of hardcoded hex. FriendsList "View Profile" navigates to /friends/[id]. No inline empty state styles remain in updated files.</done>
</task>

</tasks>

<verification>
- Profile tab shows avatar header with pencil icon and notification toggle
- All 5 list screens (Home, Plans, Chat, Friends, Requests) use `<EmptyState>` component
- Full-screen loaders in PlanDashboard, ChatRoom, AddFriend use `<LoadingIndicator>`
- RSVPButtons.tsx uses `COLORS.status.free/maybe/busy` (grep confirms no hardcoded hex)
- FriendsList `handleViewProfile` navigates to `/friends/[id]` (no more "Coming in Phase 6")
- `npx expo lint` passes
</verification>

<success_criteria>
Profile tab has avatar-centric header with edit navigation and notification toggle. Every list screen shows an informative EmptyState when empty. Every async screen shows LoadingIndicator during loading. Status colours are referenced from COLORS.status consistently (no hardcoded hex strings). "View Profile" in FriendActionSheet navigates to the other-user profile screen.
</success_criteria>

<output>
After completion, create `.planning/phases/06-notifications-polish/06-03-SUMMARY.md`
</output>
