---
phase: 09-iou-list-summary
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - src/app/(tabs)/squad.tsx
autonomous: false
requirements:
  - IOU-03
  - IOU-05

must_haves:
  truths:
    - "IOUCard appears in Goals tab between StreakCard and BirthdayCard"
    - "Tapping IOUCard navigates to /squad/expenses balance index"
    - "IOUCard shows correct aggregate net balance (green if owed-to-you, red if owe-by-you)"
    - "IOUCard shows All settled up! when no unsettled expenses exist"
    - "Human confirms Goals tab renders correctly with three cards in correct order"
  artifacts:
    - path: "src/app/(tabs)/squad.tsx"
      provides: "Goals tab with IOUCard wired between StreakCard and BirthdayCard"
      contains: "IOUCard"
  key_links:
    - from: "src/app/(tabs)/squad.tsx"
      to: "src/components/squad/IOUCard.tsx"
      via: "import + JSX render between StreakCard and BirthdayCard"
      pattern: "IOUCard"
    - from: "src/app/(tabs)/squad.tsx"
      to: "src/hooks/useIOUSummary.ts"
      via: "hook call in component body"
      pattern: "useIOUSummary"
---

<objective>
Wire IOUCard into the Goals tab in squad.tsx and verify the full IOU feature visually in Expo Go.

Purpose: D-14 requires IOUCard placed between StreakCard and BirthdayCard in the Goals tab. This plan delivers that integration and gates on human visual confirmation that all three cards render correctly, navigation works, and the IOU balance screens are functional end-to-end.

Output:
- `squad.tsx` updated: `useIOUSummary` hook added, `IOUCard` rendered between `StreakCard` and `BirthdayCard`
- Human sign-off that the complete IOU feature (card + balance index + history screen) works in Expo Go
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-iou-list-summary/09-CONTEXT.md
@.planning/phases/09-iou-list-summary/09-UI-SPEC.md
@.planning/phases/09-iou-list-summary/09-02-SUMMARY.md

<interfaces>
<!-- Key contracts needed. No codebase exploration required. -->

From src/app/(tabs)/squad.tsx (current state — add IOUCard between StreakCard and BirthdayCard):
```typescript
// Current imports to keep:
import { StreakCard } from '@/components/squad/StreakCard';
import { useStreakData } from '@/hooks/useStreakData';
import { BirthdayCard } from '@/components/squad/BirthdayCard';
import { useUpcomingBirthdays } from '@/hooks/useUpcomingBirthdays';

// ADD these two imports:
import { IOUCard } from '@/components/squad/IOUCard';
import { useIOUSummary } from '@/hooks/useIOUSummary';

// Current hook calls in component body:
const streak = useStreakData();
const birthdays = useUpcomingBirthdays();

// ADD this hook call:
const iouSummary = useIOUSummary();

// Current Goals tab ScrollView render order:
<StreakCard streak={streak} />
<BirthdayCard birthdays={birthdays} />

// NEW render order (D-14: StreakCard → IOUCard → BirthdayCard):
<StreakCard streak={streak} />
<IOUCard summary={iouSummary} />
<BirthdayCard birthdays={birthdays} />
```

From src/hooks/useIOUSummary.ts (Plan 01 output):
```typescript
export interface IOUSummaryData {
  rows: IOUSummaryRow[];
  netCents: number;
  unsettledCount: number;
  loading: boolean;
  error: string | null;
  refetch: () => Promise<void>;
}
export function useIOUSummary(): IOUSummaryData
```

From src/components/squad/IOUCard.tsx (Plan 02 output):
```typescript
interface IOUCardProps {
  summary: IOUSummaryData; // from useIOUSummary
}
export function IOUCard({ summary }: IOUCardProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire IOUCard into squad.tsx Goals tab (D-14)</name>
  <read_first>
    - src/app/(tabs)/squad.tsx (full file — understand current imports, hook calls, and Goals tab render order before modifying)
    - src/components/squad/IOUCard.tsx (confirm IOUCard export name and props interface)
    - src/hooks/useIOUSummary.ts (confirm hook export name and return type)
  </read_first>
  <files>src/app/(tabs)/squad.tsx</files>
  <action>
Make three changes to `src/app/(tabs)/squad.tsx`:

**Change 1 — Add two imports** (after the existing BirthdayCard + useUpcomingBirthdays imports):
```typescript
import { IOUCard } from '@/components/squad/IOUCard';
import { useIOUSummary } from '@/hooks/useIOUSummary';
```

**Change 2 — Add hook call** (in the component body, after `const birthdays = useUpcomingBirthdays();`):
```typescript
const iouSummary = useIOUSummary();
```

**Change 3 — Insert IOUCard in Goals tab ScrollView** (between `<StreakCard streak={streak} />` and `<BirthdayCard birthdays={birthdays} />`):
```typescript
<StreakCard streak={streak} />
<IOUCard summary={iouSummary} />
<BirthdayCard birthdays={birthdays} />
```

Do NOT change any other part of the file — no style changes, no structural changes, no import reordering. This is a minimal additive change.

Per D-14: the order StreakCard → IOUCard → BirthdayCard is mandatory (IOUs feel more action-oriented than birthdays).
  </action>
  <verify>
    <automated>grep -n "import.*IOUCard" /Users/iulian/Develop/campfire/src/app/(tabs)/squad.tsx && grep -n "import.*useIOUSummary" /Users/iulian/Develop/campfire/src/app/(tabs)/squad.tsx && grep -n "iouSummary" /Users/iulian/Develop/campfire/src/app/(tabs)/squad.tsx && grep -n "IOUCard" /Users/iulian/Develop/campfire/src/app/(tabs)/squad.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(tabs)/squad.tsx` contains `import { IOUCard } from '@/components/squad/IOUCard'`
    - `src/app/(tabs)/squad.tsx` contains `import { useIOUSummary } from '@/hooks/useIOUSummary'`
    - `src/app/(tabs)/squad.tsx` contains `const iouSummary = useIOUSummary()`
    - `src/app/(tabs)/squad.tsx` contains `<IOUCard summary={iouSummary} />`
    - In the JSX, `<IOUCard summary={iouSummary} />` appears after `<StreakCard` and before `<BirthdayCard` (order enforces D-14)
    - `npx tsc --noEmit` from project root passes with zero new errors
  </acceptance_criteria>
  <done>squad.tsx imports IOUCard and useIOUSummary; Goals tab renders StreakCard → IOUCard → BirthdayCard in that order; TypeScript compiles cleanly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human visual verification of IOU feature</name>
  <what-built>
Complete IOU List & Summary feature:
- Plan 01: useIOUSummary hook + useExpensesWithFriend hook + BalanceRow + ExpenseHistoryRow components + get_iou_summary type in database.ts
- Plan 02: IOUCard component + Stack route registration + balance index screen (/squad/expenses) + per-friend history screen (/squad/expenses/friend/[id])
- Plan 03 Task 1: IOUCard wired into Goals tab (StreakCard → IOUCard → BirthdayCard)
  </what-built>
  <how-to-verify>
Open Expo Go and navigate to the Squad tab.

**Goals tab — IOUCard:**
1. Tap "Goals" tab — confirm IOUCard appears between StreakCard and BirthdayCard
2. If you have no expenses: IOUCard shows "All settled up!" in secondary text color
3. If you have expenses: IOUCard shows "You're owed $X" in green OR "You owe $X" in red, plus "{N} unsettled" sub-label
4. Tap the IOUCard — confirm it navigates to the Balances screen (/squad/expenses)

**Balance index screen (/squad/expenses):**
5. Screen title shows "Balances"
6. If you have unsettled expenses: each friend row shows avatar + name + signed amount ("+$X → you" in green or "-$X ← you" in red) + "N unsettled" sub-label
7. If no unsettled balances: EmptyState shows "All settled up!" heading
8. Pull down — spinner appears, rows refresh

**Per-friend history screen:**
9. Tap a friend row — navigates to "/squad/expenses/friend/[id]"
10. Screen title shows "Expenses with {name}"
11. Net balance strip at top shows "Net balance" label + signed amount in correct color
12. All shared expenses listed (newest first): each shows title + amount + "Paid by {name} · {date}"
13. Settled expenses appear dimmed (reduced opacity)
14. Tapping a history row navigates to the existing expense detail screen (Phase 8)
15. Pull down — expenses refresh
  </how-to-verify>
  <action>Human visually verifies all 15 steps listed in how-to-verify using Expo Go. No automated steps — this gate confirms UI correctness of the complete IOU feature.</action>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint; verification is manual per how-to-verify steps</automated>
  </verify>
  <done>Human types "approved" confirming all 15 verification steps pass in Expo Go</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Goals tab → IOUCard | Hook called on tab mount; authenticated via useAuthStore session guard in useIOUSummary |
| IOUCard → /squad/expenses | Navigation only — no data mutation on tap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-P03-01 | Information Disclosure | useIOUSummary in squad.tsx Goals tab | mitigate | Hook guards on `userId` from `useAuthStore` — returns empty rows with `loading: false` when unauthenticated. Users who are not logged in cannot reach the Squad tab (auth gate on tab navigator). |
| T-09-P03-02 | Denial of Service | useIOUSummary called on Goals tab mount | accept | Single RPC call on mount with `loading` guard preventing concurrent re-fetch. Low risk: Goals tab is only mounted when user navigates to it, not on every app foreground. |
</threat_model>

<verification>
After Task 1:

1. `grep -n "IOUCard" src/app/(tabs)/squad.tsx` — must show import AND JSX usage
2. `grep -n "useIOUSummary" src/app/(tabs)/squad.tsx` — must show import AND hook call
3. `npx tsc --noEmit` — zero new TypeScript errors

After checkpoint approval:

4. All 6 phase success criteria confirmed visually in Expo Go (from ROADMAP.md Phase 9 Success Criteria)
</verification>

<success_criteria>
- `squad.tsx` imports and renders `IOUCard` between `StreakCard` and `BirthdayCard` (D-14)
- Human confirms IOUCard displays correct aggregate balance (green/red/settled) in Goals tab
- Human confirms tapping IOUCard navigates to balance index screen
- Human confirms balance index shows per-friend signed amounts with correct color coding
- Human confirms per-friend history screen shows net balance strip, expense list, settled dimming, and navigation to detail
- Human confirms pull-to-refresh works on both new screens
- TypeScript compiles cleanly (`npx tsc --noEmit` zero errors)
</success_criteria>

<output>
After completion, create `.planning/phases/09-iou-list-summary/09-03-SUMMARY.md`
</output>
