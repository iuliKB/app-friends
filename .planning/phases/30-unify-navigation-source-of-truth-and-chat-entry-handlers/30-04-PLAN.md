---
phase: 30-unify-navigation-source-of-truth-and-chat-entry-handlers
plan: 04
type: execute
wave: 2
depends_on: [01, 03]
files_modified:
  - src/components/common/CustomTabBar.tsx
  - src/screens/chat/ChatRoomScreen.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "CustomTabBar reads its visibility from `useNavigationStore` rather than inspecting nested navigator state"
    - "ChatRoomScreen sets `currentSurface='chat'` on focus and restores `'tabs'` on blur via useFocusEffect"
    - "When `currentSurface === 'chat'`, CustomTabBar returns null (bar hidden)"
    - "From ANY entry path (8 verification-anchor smoke tests), bar hides on entry and restores on exit"
    - "useCallback is imported as a bare named import from 'react' (alphabetised in the existing destructure) and called as bare `useCallback(...)` — never as `React.useCallback(...)`"
  artifacts:
    - path: "src/components/common/CustomTabBar.tsx"
      provides: "Updated visibility logic reading from useNavigationStore"
      contains: "useNavigationStore((s) => s.currentSurface)"
    - path: "src/screens/chat/ChatRoomScreen.tsx"
      provides: "useFocusEffect that writes 'chat' surface on focus, 'tabs' on blur"
      contains: "setSurface('chat')"
  key_links:
    - from: "src/components/common/CustomTabBar.tsx"
      to: "src/stores/useNavigationStore.ts"
      via: "useNavigationStore((s) => s.currentSurface)"
      pattern: "useNavigationStore\\(\\(s\\)"
    - from: "src/screens/chat/ChatRoomScreen.tsx"
      to: "src/stores/useNavigationStore.ts"
      via: "useFocusEffect setSurface('chat') / setSurface('tabs')"
      pattern: "setSurface\\('chat'\\)"
---

<objective>
Refactor `src/components/common/CustomTabBar.tsx` to consume `useNavigationStore` for visibility decisions, and add a `useFocusEffect` writer in `src/screens/chat/ChatRoomScreen.tsx` that pushes `'chat'` on focus and `'tabs'` on blur. This is the producer-consumer wire-up that closes the Phase 30 root-cause bug.

Purpose: Replace the broken nested-navigator-state inspection at `CustomTabBar.tsx:123-129` with a read from the navigation store. The store is written by the screen via `useFocusEffect`, so visibility now keys off "what's on screen" — independent of how the user got there.

Output: One ~7-line replacement in `CustomTabBar.tsx`, and one `useFocusEffect` block added to `ChatRoomScreen.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/30-unify-navigation-source-of-truth-and-chat-entry-handlers/CONTEXT.md
@.planning/phases/30-unify-navigation-source-of-truth-and-chat-entry-handlers/30-PATTERNS.md
@src/components/common/CustomTabBar.tsx
@src/screens/chat/ChatRoomScreen.tsx
@src/stores/useNavigationStore.ts
@src/screens/friends/FriendRequests.tsx

<interfaces>
<!-- Current CustomTabBar visibility logic (src/components/common/CustomTabBar.tsx:123-129) — to be replaced -->

```ts
const focusedRoute = state.routes[state.index];
const nestedState = focusedRoute?.state;
if (nestedState) {
  const nestedIndex = nestedState.index ?? 0;
  const nestedRoute = nestedState.routes[nestedIndex];
  if (nestedRoute?.name === 'room') return null;
}
```

<!-- New consumer pattern (PATTERNS.md lines 253-265) -->

```ts
const surface = useNavigationStore((s) => s.currentSurface);
if (surface === 'chat') return null;
```

<!-- useFocusEffect writer pattern (analog: FriendRequests.tsx:24-30) -->

```tsx
useFocusEffect(
  useCallback(() => {
    fetchPendingRequests();
  }, [])
);
```

<!-- Store hook signature (from Plan 01) -->

From src/stores/useNavigationStore.ts:
```ts
export type NavigationSurface = 'tabs' | 'chat' | 'plan' | 'modal' | 'auth';
export const useNavigationStore: () => {
  currentSurface: NavigationSurface;
  setSurface: (next: NavigationSurface) => void;
  reset: () => void;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace nested-navigator-state inspection in CustomTabBar.tsx with useNavigationStore selector</name>
  <files>src/components/common/CustomTabBar.tsx</files>
  <read_first>
    - src/components/common/CustomTabBar.tsx (entire file — 204 lines — to understand the line 123-129 block's role)
    - src/stores/useNavigationStore.ts (the store created by Plan 01)
    - src/screens/chat/ChatListScreen.tsx (lines 25-26 — selector access pattern: `const session = useAuthStore((s) => s.session);`)
    - .planning/phases/30-unify-navigation-source-of-truth-and-chat-entry-handlers/30-PATTERNS.md (lines 233-278 — current logic, replacement pattern, selector vs destructure rule)
  </read_first>
  <behavior>
    - Test 1: When `useNavigationStore.getState().currentSurface === 'chat'`, the CustomTabBar render returns `null`
    - Test 2: When `currentSurface === 'tabs'`, the CustomTabBar renders the tab pill (5 tabs)
    - Test 3: When `currentSurface === 'plan'` or `'modal'` or `'auth'`, the bar is hidden (any surface other than `'tabs'`)
    - Test 4: The bar no longer references `nestedRoute` or `focusedRoute.state` for visibility decisions
  </behavior>
  <action>
    In `src/components/common/CustomTabBar.tsx`:

    Step 1: Add an import for the navigation store. Place it in the existing `@/stores`-adjacent import group. Insert AFTER the line `import { useInvitationCount } from '@/hooks/useInvitationCount';` (line 13) — that is the last `@/hooks` import before the const-block. Add this exact line:

    ```ts
    import { useNavigationStore } from '@/stores/useNavigationStore';
    ```

    Step 2: REPLACE the 7-line visibility block at lines 123-129 (verbatim text below) — REMOVE these lines:

    ```ts
      const focusedRoute = state.routes[state.index];
      const nestedState = focusedRoute?.state;
      if (nestedState) {
        const nestedIndex = nestedState.index ?? 0;
        const nestedRoute = nestedState.routes[nestedIndex];
        if (nestedRoute?.name === 'room') return null;
      }
    ```

    Substitute with this exact 2-line block:

    ```ts
      const surface = useNavigationStore((s) => s.currentSurface);
      if (surface !== 'tabs') return null;
    ```

    The hook call MUST be placed at the SAME location (immediately after the `useMemo` block that returns `styles`, i.e. immediately before the `const badges` declaration that currently lives at line 131). Position is critical because React hooks must run in the same order on every render.

    CRITICAL constraints:
    - Use selector form `useNavigationStore((s) => s.currentSurface)` — NEVER destructure `const { currentSurface } = useNavigationStore()` (PATTERNS.md §"Zustand store selector access" enforces this — destructure causes re-render on every store change).
    - The visibility condition is `surface !== 'tabs'` (return null for ANY non-tabs surface), NOT `surface === 'chat'` — this future-proofs new surfaces (`plan`, `modal`, `auth`) that will also want the bar hidden when added by later phases.
    - DO NOT delete the `state`/`navigation` props from the function signature — they are still used by the tab-press handlers below (`state.routes.map(...)`, `state.index === index`, `navigation.emit`, `navigation.navigate`).
    - DO NOT modify any other line in the file — the styles, the tab rendering, the badge logic all stay.
    - The `useNavigationStore` import path is `@/stores/useNavigationStore` (matches the `useAuthStore` import style elsewhere in the codebase).
    - Order of hooks at the top of `CustomTabBar` after edit: `useTheme()`, `useSafeAreaInsets()`, `usePendingRequestsCount()`, `useInvitationCount()`, `useMemo(...)` (styles), then NEW `useNavigationStore(...)`. The new hook is the FIRST one after the styles useMemo — this preserves rules-of-hooks ordering.
  </action>
  <verify>
    <automated>grep -c "useNavigationStore" src/components/common/CustomTabBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "import { useNavigationStore } from '@/stores/useNavigationStore';" src/components/common/CustomTabBar.tsx` returns `1`
    - `grep -cF "const surface = useNavigationStore((s) => s.currentSurface);" src/components/common/CustomTabBar.tsx` returns `1`
    - `grep -cF "if (surface !== 'tabs') return null;" src/components/common/CustomTabBar.tsx` returns `1`
    - `grep -cE "nestedRoute|nestedState|nestedIndex" src/components/common/CustomTabBar.tsx` returns `0` (old logic removed)
    - `grep -cE "focusedRoute\\?\\.\\bstate\\b" src/components/common/CustomTabBar.tsx` returns `0` (old `focusedRoute?.state` access removed)
    - `grep -cF "state.routes.map" src/components/common/CustomTabBar.tsx` returns `1` (tab rendering loop preserved)
    - `grep -cF "state.index === index" src/components/common/CustomTabBar.tsx` returns `1` (focus check preserved)
    - `grep -cE "useNavigationStore\\(\\(\\)" src/components/common/CustomTabBar.tsx` returns `0` (no destructure form)
    - `npx tsc --noEmit 2>&1 | grep -c "CustomTabBar.tsx"` returns `0` (no new TS errors)
  </acceptance_criteria>
  <done>CustomTabBar reads visibility from `useNavigationStore`. The old `nestedRoute?.name === 'room'` check is gone. Bar is hidden whenever surface is not `'tabs'`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add useFocusEffect writer in ChatRoomScreen — surface='chat' on focus, 'tabs' on blur</name>
  <files>src/screens/chat/ChatRoomScreen.tsx</files>
  <read_first>
    - src/screens/chat/ChatRoomScreen.tsx (top ~80 lines — to find the existing import block and the component function start)
    - src/screens/friends/FriendRequests.tsx (lines 1-30 — analog for `useFocusEffect` from `expo-router` plus a `useCallback` body)
    - src/stores/useNavigationStore.ts (the store created by Plan 01)
    - .planning/phases/30-unify-navigation-source-of-truth-and-chat-entry-handlers/30-PATTERNS.md (lines 357-371 — `useFocusEffect` pattern; lines 267-278 — the writer site is the screen file, not the route file)
  </read_first>
  <behavior>
    - Test 1: When `ChatRoomScreen` mounts and is focused, `useNavigationStore.getState().currentSurface` is `'chat'`
    - Test 2: When `ChatRoomScreen` blurs (user navigates back), `currentSurface` becomes `'tabs'`
    - Test 3: The effect uses `useFocusEffect` (not `useEffect`), so the cleanup runs on blur — not on unmount only
    - Test 4: The hook is called at the top level of the function component, not inside a conditional or loop
  </behavior>
  <action>
    In `src/screens/chat/ChatRoomScreen.tsx`:

    Step 1: Update the `expo-router` import on line 21 from:

    ```ts
    import { useNavigation, useRouter } from 'expo-router';
    ```

    to:

    ```ts
    import { useFocusEffect, useNavigation, useRouter } from 'expo-router';
    ```

    (Add `useFocusEffect` to the existing named import list, alphabetically first.)

    Step 2: Add a new import line for the navigation store. Place it in the `@/stores`-adjacent group, AFTER the existing `import { useAuthStore } from '@/stores/useAuthStore';` line (currently line 27):

    ```ts
    import { useNavigationStore } from '@/stores/useNavigationStore';
    ```

    Step 3: Inside the `ChatRoomScreen` function component body, near the top (alongside the other `useXxx` hook calls — locate where `useAuthStore` is read), add this block. It MUST be placed BEFORE any conditional return or `if` branch:

    ```ts
      const setSurface = useNavigationStore((s) => s.setSurface);

      useFocusEffect(
        useCallback(() => {
          setSurface('chat');
          return () => setSurface('tabs');
        }, [setSurface])
      );
    ```

    CRITICAL constraints:
    - The selector retrieves only `setSurface` (a stable reference because zustand's set is stable across renders). DO NOT also grab `currentSurface` from the same selector — that would cause every store update to re-render the chat screen.
    - The dependency array is `[setSurface]` — zustand's setters are stable, so this is a const reference; including it satisfies the exhaustive-deps lint without causing re-runs.
    - **LOCKED IMPORT STYLE (DO NOT IMPROVISE):** `ChatRoomScreen.tsx` line 1 currently reads (verified 2026-05-13):
      ```ts
      import React, { useEffect, useMemo, useRef, useState } from 'react';
      ```
      Edit that exact line to ADD `useCallback` to the named-import list alphabetically. The line MUST become:
      ```ts
      import React, { useCallback, useEffect, useMemo, useRef, useState } from 'react';
      ```
      Then call it as **bare `useCallback(...)`** — NEVER as `React.useCallback(...)`. This is the canonical form for this file and there is no alternative acceptable in this plan. The previous "either is acceptable" wording is REMOVED — `React.useCallback` is forbidden because it would break the consistent named-import convention this file already uses for every other React hook (useEffect, useMemo, useRef, useState).
    - The cleanup `return () => setSurface('tabs');` is the CRITICAL piece — it restores the bar when the user exits the chat by ANY means (back gesture, programmatic pop, app backgrounding then refocusing a different screen). Without it, the chat surface would stick after the user leaves.
    - DO NOT modify any other behavior of the screen — no changes to `useChatRoom`, `useChatTodos`, message rendering, send bar, image viewer, etc.
    - DO NOT use `useEffect` instead of `useFocusEffect`. `useEffect` runs once on mount and never on subsequent focus/blur transitions, so it would not restore `'tabs'` when the user navigates back to the same `ChatRoomScreen` from a child modal.
    - Place the new hook AFTER existing hooks like `useAuthStore`, `useChatRoom`, `useChatTodos`, `useChatMembers` but BEFORE any `if` branch — locate the first conditional in the function and insert the block just above it.
  </action>
  <verify>
    <automated>grep -cE "setSurface\\('chat'\\)|setSurface\\('tabs'\\)" src/screens/chat/ChatRoomScreen.tsx &amp;&amp; ! grep -qF "React.useCallback" src/screens/chat/ChatRoomScreen.tsx &amp;&amp; grep -qE "^\\s*useCallback\\(" src/screens/chat/ChatRoomScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "import { useFocusEffect, useNavigation, useRouter } from 'expo-router';" src/screens/chat/ChatRoomScreen.tsx` returns `1`
    - `grep -cF "import { useNavigationStore } from '@/stores/useNavigationStore';" src/screens/chat/ChatRoomScreen.tsx` returns `1`
    - `grep -cF "useNavigationStore((s) => s.setSurface)" src/screens/chat/ChatRoomScreen.tsx` returns `1`
    - `grep -cF "setSurface('chat');" src/screens/chat/ChatRoomScreen.tsx` returns `1`
    - `grep -cF "return () => setSurface('tabs');" src/screens/chat/ChatRoomScreen.tsx` returns `1`
    - `grep -cF "useFocusEffect(" src/screens/chat/ChatRoomScreen.tsx` returns `1`
    - `grep -cF "useCallback, useEffect, useMemo, useRef, useState" src/screens/chat/ChatRoomScreen.tsx` returns `1` (the named-import list now includes useCallback alphabetically)
    - `grep -cE "^\\s*useCallback\\(" src/screens/chat/ChatRoomScreen.tsx` returns at least `1` (bare `useCallback(` call exists at start of a line inside the function body)
    - `grep -cF "React.useCallback" src/screens/chat/ChatRoomScreen.tsx` returns `0` (the `React.useCallback` form is forbidden by this plan)
    - `npx tsc --noEmit 2>&1 | grep -c "ChatRoomScreen.tsx"` returns `0` (no new TS errors)
  </acceptance_criteria>
  <done>ChatRoomScreen writes `'chat'` to the navigation store on focus and `'tabs'` on blur. The bar will hide on entry and restore on exit from ANY entry path.</done>
</task>

</tasks>

<verification>
- `src/components/common/CustomTabBar.tsx` reads `currentSurface` via selector; visibility returns null when surface is not `'tabs'`.
- `src/screens/chat/ChatRoomScreen.tsx` uses `useFocusEffect` to push/pop the surface.
- `npx tsc --noEmit` introduces no new errors.
- Manual smoke test (executor will verify each of the 8 entry paths from CONTEXT.md verification anchor):
  1. Chat tab → chat row → chat → back ✓
  2. Squad Friends sub-tab → friend → DM → back ✓
  3. Plans tab → plan → "Open Chat" pill → back ✓
  4. Squad Memories → plan section → PlanDashboard → "Open Chat" pill → back (originating bug) ✓
  5. Home screen friend chip (4 components) → DM → back ✓
  6. Birthday page → wish chat → back ✓
  7. Friend detail page → DM → back ✓
  8. Push notification deep link → chat → back ✓
</verification>

<success_criteria>
1. `CustomTabBar.tsx:123-129` original block is gone — replaced by 2 lines reading from `useNavigationStore`.
2. `ChatRoomScreen.tsx` declares `useFocusEffect(() => { setSurface('chat'); return () => setSurface('tabs'); })`.
3. From any of the 8 verification-anchor entry paths, the bottom bar hides on chat entry and restores on chat exit.
4. The originating bug (Squad → Memories → PlanDashboard → Open Chat) is fixed.
</success_criteria>

<output>
After completion, create `.planning/phases/30-unify-navigation-source-of-truth-and-chat-entry-handlers/30-04-SUMMARY.md` documenting: CustomTabBar consumer wired, ChatRoomScreen writer wired, all 8 entry paths verified.
</output>
