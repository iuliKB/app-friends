---
phase: 30-unify-navigation-source-of-truth-and-chat-entry-handlers
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/friends/index.tsx
  - src/screens/friends/FriendsList.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "`src/app/friends/index.tsx` no longer exists — the legacy `/friends` index route is gone"
    - "`src/screens/friends/FriendsList.tsx` no longer exists — the legacy screen file is gone"
    - "No code in the codebase imports `FriendsList`"
    - "The `/friends/<id>`, `/friends/add`, `/friends/requests` sub-routes are PRESERVED — those files are NOT touched"
    - "No `router.push('/friends')` call (exact match for the index path) exists in the codebase"
  artifacts: []
  key_links: []
---

<objective>
Delete the legacy `/friends` index route. PATTERNS.md §"Dead-Code Verification" confirms:
- `src/app/friends/index.tsx` is reachable only by manual URL entry (no `router.push('/friends')` deep links anywhere).
- `src/screens/friends/FriendsList.tsx` is imported only by the route file above (grep returns exactly 2 hits: the import in `app/friends/index.tsx:6` and the export at `FriendsList.tsx:16`).
- The Squad Friends sub-tab at `src/app/(tabs)/squad.tsx:180-205` already duplicates the friends-action-sheet logic — no unique behavior in the legacy screen to merge.

Purpose: Remove duplicate Friends-list code surface so there is one source of truth (the Squad Friends sub-tab). This also removes one of the 13 chat-entry callsites (`FriendsList.tsx:68`) — fewer migration targets for Plan 05.

Output: Two files deleted. Sub-routes `/friends/[id]`, `/friends/add`, `/friends/requests` preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/30-unify-navigation-source-of-truth-and-chat-entry-handlers/CONTEXT.md
@.planning/phases/30-unify-navigation-source-of-truth-and-chat-entry-handlers/30-PATTERNS.md
@src/app/friends/index.tsx
@src/screens/friends/FriendsList.tsx
@src/app/friends/_layout.tsx

<interfaces>
<!-- Files PRESERVED (do not touch):
  - src/app/friends/_layout.tsx (declares the Stack for /friends/* sub-routes; the index route is one of multiple sub-routes — removing it leaves the layout valid)
  - src/app/friends/[id].tsx (active deep link target)
  - src/app/friends/add.tsx (active deep link target — Phase 29 Empty State CTA points here)
  - src/app/friends/requests.tsx (active deep link target)
  - src/screens/friends/AddFriend.tsx (consumed by friends/add.tsx)
  - src/screens/friends/FriendRequests.tsx (consumed by friends/requests.tsx)
-->

<!-- Files DELETED (this plan's scope):
  - src/app/friends/index.tsx (route file, ~70 lines)
  - src/screens/friends/FriendsList.tsx (screen file, ~80 lines)
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-verify zero deep links and zero non-self imports, then delete both files</name>
  <files>src/app/friends/index.tsx, src/screens/friends/FriendsList.tsx</files>
  <read_first>
    - src/app/friends/index.tsx (entire file — confirm what is being deleted: a thin route wrapping FriendsList)
    - src/screens/friends/FriendsList.tsx (entire file — confirm contents — includes one `/chat/room` push at line 68 which the deletion eliminates without migration)
    - src/app/friends/_layout.tsx (confirm it does NOT explicitly enumerate `index` as a Stack.Screen — expo-router file-based routing infers from filesystem)
    - .planning/phases/30-unify-navigation-source-of-truth-and-chat-entry-handlers/30-PATTERNS.md (lines 415-425 — dead-code verification table)
  </read_first>
  <action>
    Step 1: Verify no deep links target `/friends` (the bare index path, not the sub-routes):

    ```bash
    grep -rn "router\\.push.*['\"]/friends['\"]\\|router\\.push.*\\`/friends\\`" src/ 2>/dev/null
    ```

    Expected output: empty. All `/friends/<id>`, `/friends/add`, `/friends/requests` references match `/friends/...` (with a trailing path segment) and are preserved by this regex's `/friends`-with-no-suffix anchor.

    A simpler exhaustive check:

    ```bash
    grep -rn "/friends'" src/ 2>/dev/null
    grep -rn "/friends\\`" src/ 2>/dev/null
    grep -rn "/friends\"" src/ 2>/dev/null
    ```

    Expected: any matches must include a trailing path segment (e.g., `/friends/${id}`, `/friends/add`, `/friends/requests`). No bare `/friends` push should exist.

    Step 2: Verify `FriendsList` import inventory:

    ```bash
    grep -rn "FriendsList" src/ 2>/dev/null
    ```

    Expected output: EXACTLY TWO lines —
    - `src/app/friends/index.tsx:6:import { FriendsList } from '@/screens/friends/FriendsList';`
    - `src/screens/friends/FriendsList.tsx:16:export function FriendsList() {`

    If any OTHER line is returned, STOP and consult the user.

    Step 3: Delete both files:

    ```bash
    rm src/app/friends/index.tsx
    rm src/screens/friends/FriendsList.tsx
    ```

    Step 4: Re-run the grep to confirm zero matches:

    ```bash
    grep -rn "FriendsList" src/ 2>/dev/null
    ```

    Expected output: empty.

    Step 5: Verify the four preserved files still exist:

    ```bash
    test -f 'src/app/friends/[id].tsx' && \
    test -f src/app/friends/add.tsx && \
    test -f src/app/friends/requests.tsx && \
    test -f src/app/friends/_layout.tsx && \
    test -f src/screens/friends/AddFriend.tsx && \
    test -f src/screens/friends/FriendRequests.tsx && echo "PRESERVED_OK"
    ```

    Expected output: `PRESERVED_OK`.

    Step 6: Confirm the chat-entry callsite count drops by 1 (the `FriendsList.tsx:68` site is gone — Plan 05 does NOT need to migrate it):

    ```bash
    grep -rn "/chat/room" src/ 2>/dev/null | wc -l | tr -d ' '
    ```

    Expected: this count is now ONE less than before (the count before this plan ran was 12 across the relevant files; the executor should note the new count in the SUMMARY).

    CRITICAL constraints:
    - DO NOT modify `src/app/friends/_layout.tsx`. Expo-router does not require explicit Stack.Screen entries for file-based sub-routes; removing `index.tsx` simply makes the URL `/friends` 404 in dev (acceptable per CONTEXT.md §"If not, delete entirely").
    - DO NOT delete `src/screens/friends/AddFriend.tsx` or `src/screens/friends/FriendRequests.tsx` — they are consumed by the preserved sub-routes.
    - DO NOT delete `src/screens/friends/` directory — the two preserved screen files live in it.
    - If the verification grep finds an unexpected `FriendsList` reference (e.g., in a test or storybook), STOP. The dead-code premise must hold before deletion proceeds.
  </action>
  <verify>
    <automated>test ! -f src/app/friends/index.tsx && test ! -f src/screens/friends/FriendsList.tsx && grep -rn "FriendsList" src/ 2>/dev/null | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/app/friends/index.tsx` exits 0 (route file deleted)
    - `test ! -f src/screens/friends/FriendsList.tsx` exits 0 (screen file deleted)
    - `grep -rn "FriendsList" src/ 2>/dev/null | wc -l | tr -d ' '` returns `0` (no references anywhere)
    - `test -f 'src/app/friends/[id].tsx'` exits 0 (sub-route preserved)
    - `test -f src/app/friends/add.tsx` exits 0 (sub-route preserved)
    - `test -f src/app/friends/requests.tsx` exits 0 (sub-route preserved)
    - `test -f src/app/friends/_layout.tsx` exits 0 (layout preserved)
    - `test -f src/screens/friends/AddFriend.tsx` exits 0 (consumed by /friends/add)
    - `test -f src/screens/friends/FriendRequests.tsx` exits 0 (consumed by /friends/requests)
    - `npx tsc --noEmit 2>&1 | grep -cE "FriendsList|app/friends/index"` returns `0` (no dangling errors)
  </acceptance_criteria>
  <done>Both legacy files deleted. Sub-routes preserved. No code references the deleted symbols.</done>
</task>

</tasks>

<verification>
- `src/app/friends/index.tsx` no longer exists.
- `src/screens/friends/FriendsList.tsx` no longer exists.
- Sub-routes (`/friends/[id]`, `/friends/add`, `/friends/requests`) still resolve.
- `npx tsc --noEmit` introduces no new errors.
- The chat-entry callsite at the deleted `FriendsList.tsx:68` is gone — Plan 05's migration target list shrinks by 1.
</verification>

<success_criteria>
1. `app/friends/index.tsx` and `screens/friends/FriendsList.tsx` are deleted.
2. The `/friends` URL returns a 404 in dev (acceptable per CONTEXT.md §Scope item 5).
3. The Squad Friends sub-tab (`src/app/(tabs)/squad.tsx`) continues to function as the single source for the friends list.
4. The four preserved files (`[id].tsx`, `add.tsx`, `requests.tsx`, `_layout.tsx` plus the two consumed screen files) are intact.
</success_criteria>

<output>
After completion, create `.planning/phases/30-unify-navigation-source-of-truth-and-chat-entry-handlers/30-07-SUMMARY.md` documenting: two files deleted, sub-routes preserved, one chat-entry callsite eliminated (no migration needed).
</output>
