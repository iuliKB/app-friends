---
phase: 30-unify-navigation-source-of-truth-and-chat-entry-handlers
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/RecentMemoriesSection.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "`src/components/home/RecentMemoriesSection.tsx` no longer exists on disk"
    - "No import of `RecentMemoriesSection` remains anywhere in the codebase"
    - "The actively-used `src/components/home/MemoriesSection.tsx` (the non-dead sibling) is unchanged"
  artifacts: []
  key_links: []
---

<objective>
Delete `src/components/home/RecentMemoriesSection.tsx` — confirmed dead code (PATTERNS.md §"Dead-Code Verification" line 419: `grep -rn "RecentMemoriesSection" src/` returns exactly one hit, the file's own export declaration; zero importers).

Purpose: Reduce noise. Phase 30 is the appropriate consolidation phase to remove this older sibling of `MemoriesSection.tsx`. Keeping it risks future maintenance churn or accidental import.

Output: One file deleted. Zero behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/30-unify-navigation-source-of-truth-and-chat-entry-handlers/CONTEXT.md
@.planning/phases/30-unify-navigation-source-of-truth-and-chat-entry-handlers/30-PATTERNS.md
@src/components/home/RecentMemoriesSection.tsx
@src/components/home/MemoriesSection.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-verify zero imports, then delete RecentMemoriesSection.tsx</name>
  <files>src/components/home/RecentMemoriesSection.tsx</files>
  <read_first>
    - src/components/home/RecentMemoriesSection.tsx (entire file — to confirm what is being deleted)
    - src/components/home/MemoriesSection.tsx (entire file — to confirm the non-dead sibling that STAYS)
    - .planning/phases/30-unify-navigation-source-of-truth-and-chat-entry-handlers/30-PATTERNS.md (lines 415-425 — dead-code verification table)
  </read_first>
  <action>
    Step 1: Run the verification grep one more time as a safety check immediately before deletion:

    ```bash
    grep -rn "RecentMemoriesSection" src/ 2>/dev/null
    ```

    Expected output: EXACTLY ONE line — `src/components/home/RecentMemoriesSection.tsx:19:export function RecentMemoriesSection() {` (or similar — the file's own export declaration). If any OTHER line is returned (an import, a JSX usage), STOP and consult the user — the dead-code premise is invalidated.

    Step 2: Delete the file:

    ```bash
    rm src/components/home/RecentMemoriesSection.tsx
    ```

    Step 3: Re-run the grep to confirm zero matches in the codebase:

    ```bash
    grep -rn "RecentMemoriesSection" src/ 2>/dev/null
    ```

    Expected output: empty (no matches).

    Step 4: Verify the sibling file `src/components/home/MemoriesSection.tsx` still exists and is unmodified.

    CRITICAL constraints:
    - DO NOT modify any other file. This is a pure deletion.
    - DO NOT delete `MemoriesSection.tsx` (the non-"Recent"-prefixed file) — that one is actively imported.
    - DO NOT delete `MemoriesTabContent` or any other Memories component.
    - Use `rm`, not `git rm` (the user will commit the deletion at the end of the phase per their no-co-author convention).
  </action>
  <verify>
    <automated>test ! -f src/components/home/RecentMemoriesSection.tsx && grep -rn "RecentMemoriesSection" src/ 2>/dev/null | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/components/home/RecentMemoriesSection.tsx` exits 0 (file deleted)
    - `grep -rn "RecentMemoriesSection" src/ 2>/dev/null | wc -l | tr -d ' '` returns `0`
    - `test -f src/components/home/MemoriesSection.tsx` exits 0 (sibling preserved)
    - `npx tsc --noEmit 2>&1 | grep -c "RecentMemoriesSection"` returns `0` (no dangling import errors)
  </acceptance_criteria>
  <done>`RecentMemoriesSection.tsx` deleted. Zero imports remain. `MemoriesSection.tsx` untouched.</done>
</task>

</tasks>

<verification>
- File `src/components/home/RecentMemoriesSection.tsx` no longer exists.
- Global grep confirms zero references to the deleted symbol.
- `MemoriesSection.tsx` is intact.
- `npx tsc --noEmit` introduces no new errors.
- App build still succeeds.
</verification>

<success_criteria>
1. The dead `RecentMemoriesSection.tsx` file is deleted.
2. No code in the codebase references the deleted symbol.
3. The non-dead `MemoriesSection.tsx` continues to render in Squad → Memories tab.
</success_criteria>

<output>
After completion, create `.planning/phases/30-unify-navigation-source-of-truth-and-chat-entry-handlers/30-06-SUMMARY.md` documenting: one file deleted, zero imports remaining.
</output>
