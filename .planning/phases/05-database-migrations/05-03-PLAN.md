---
phase: 05-database-migrations
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - supabase/seed.sql
autonomous: true
requirements:
  - IOU-01
  - IOU-02
  - IOU-03
  - IOU-04
  - IOU-05
  - BDAY-01
  - BDAY-02
  - BDAY-03

must_haves:
  truths:
    - "supabase db push applies both 0015 and 0016 migrations without errors"
    - "iou_groups and iou_members tables exist in the remote schema after push"
    - "birthday_month and birthday_day columns exist on profiles after push"
    - "general_notes column exists on plans (iou_notes gone) after push"
    - "seed.sql uses general_notes instead of iou_notes"
    - "seed.sql contains IOU expense rows and birthday values for test users"
  artifacts:
    - path: "supabase/seed.sql"
      provides: "Test data for IOU and birthday features"
      contains: "general_notes"
  key_links:
    - from: "supabase db push"
      to: "Supabase remote project"
      via: "npx supabase db push"
      pattern: "Applied migration 0015, Applied migration 0016"
    - from: "supabase/seed.sql"
      to: "iou_groups / iou_members tables"
      via: "INSERT INTO iou_groups / iou_members"
      pattern: "iou_groups"
---

<objective>
Push both migrations to Supabase and extend seed.sql with IOU expense and birthday test data.

Purpose: Migration files written in Plans 01 and 02 are inert until pushed. This plan applies them to the remote Supabase project, verifies schema state, and extends seed.sql so developers get realistic test data immediately after seeding. Per D-13, seed.sql must include IOU expenses and birthday values for the existing test users (Alice, Bob, Charlie, etc.). The iou_notes references in seed.sql (Pitfall 5) must be fixed to general_notes in the same operation.

Output: Both migrations applied to remote Supabase project. seed.sql updated with general_notes, IOU INSERT rows, and birthday data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-database-migrations/05-CONTEXT.md
@.planning/phases/05-database-migrations/05-RESEARCH.md
@.planning/phases/05-database-migrations/05-01-SUMMARY.md
@.planning/phases/05-database-migrations/05-02-SUMMARY.md

<interfaces>
<!-- seed.sql current iou_notes occurrences (must become general_notes): -->
<!-- supabase/seed.sql lines 174 and 208 — INSERT INTO plans (..., iou_notes, ...) -->
<!--                                                                                 -->
<!-- iou_groups table shape (from 0015_iou_v1_4.sql):                               -->
<!--   INSERT INTO public.iou_groups (id, created_by, title, total_amount_cents, split_mode, created_at, updated_at) -->
<!--                                                                                 -->
<!-- iou_members table shape (from 0015_iou_v1_4.sql):                              -->
<!--   INSERT INTO public.iou_members (iou_group_id, user_id, share_amount_cents, settled_at, settled_by) -->
<!--   PRIMARY KEY is (iou_group_id, user_id) — no id column                        -->
<!--                                                                                 -->
<!-- profiles birthday columns (from 0016_birthdays_v1_4.sql):                      -->
<!--   UPDATE public.profiles SET birthday_month = N, birthday_day = N WHERE id = '...' -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Push migrations 0015 and 0016 to Supabase remote</name>
  <files>supabase/config.toml</files>

  <read_first>
    - supabase/migrations/0015_iou_v1_4.sql — Confirm file exists and is syntactically complete before pushing
    - supabase/migrations/0016_birthdays_v1_4.sql — Confirm file exists and is syntactically complete before pushing
    - .planning/phases/05-database-migrations/05-01-SUMMARY.md — Confirm Plan 01 completed successfully
    - .planning/phases/05-database-migrations/05-02-SUMMARY.md — Confirm Plan 02 completed successfully
  </read_first>

  <action>
Run the migration push command. The Supabase CLI is installed as a dev dependency (version 2.81.1) and is invoked via npx.

Command:
  cd /Users/iulian/Develop/campfire && npx supabase db push

Expected output contains lines like:
  Applying migration 0015_iou_v1_4.sql...
  Applying migration 0016_birthdays_v1_4.sql...

If the command fails with an interactive prompt asking to confirm the remote URL or database password, set SUPABASE_ACCESS_TOKEN env var first using the value from .env or .env.local (look for SUPABASE_ACCESS_TOKEN or SUPABASE_SERVICE_ROLE_KEY), then retry.

If supabase db push is not available or the project is not linked, run:
  npx supabase link --project-ref (read project ref from supabase/config.toml)
then retry db push.

Do NOT skip this task. Client phases 6-10 cannot proceed until schema objects exist in the remote project.
  </action>

  <verify>
    <automated>cd /Users/iulian/Develop/campfire && npx supabase migration list 2>&1 | grep -E '0015|0016'</automated>
  </verify>

  <acceptance_criteria>
    - npx supabase db push exits with code 0
    - npx supabase migration list output contains "0015_iou_v1_4" (migration applied)
    - npx supabase migration list output contains "0016_birthdays_v1_4" (migration applied)
    - No "ERROR" lines in push output
  </acceptance_criteria>

  <done>Both migrations applied to remote Supabase project. npx supabase db push exits 0 with no errors. Migration history includes 0015 and 0016.</done>
</task>

<task type="auto">
  <name>Task 2: Fix seed.sql — rename iou_notes, add IOU and birthday test data</name>
  <files>supabase/seed.sql</files>

  <read_first>
    - supabase/seed.sql — Full file. Identify: (1) the two INSERT INTO plans statements at lines 174 and 208 that use iou_notes, (2) the test user UUIDs (Alice, Bob, Charlie, etc.) used in existing INSERT statements — these same UUIDs are needed for IOU and birthday seed rows
    - supabase/migrations/0015_iou_v1_4.sql — iou_groups and iou_members column names and types to match INSERT shapes
    - supabase/migrations/0016_birthdays_v1_4.sql — profiles birthday column names to match UPDATE statements
  </read_first>

  <action>
Make the following changes to supabase/seed.sql:

CHANGE 1 — Fix iou_notes column name (Pitfall 5 from RESEARCH.md):
In both INSERT INTO plans statements (lines 174 and 208), rename the column in the column list:
  iou_notes  →  general_notes
The VALUES clause string content does not change — only the column name in the INSERT column list.

CHANGE 2 — Add IOU expense seed data (D-13):
After the existing plans INSERT statements, append a new section. Use the existing test user UUIDs already present in seed.sql (read the file to find Alice/Bob/Charlie UUIDs — do NOT hardcode fictional UUIDs, use the actual ones from the file).

Add 2 iou_groups rows and corresponding iou_members rows:

Example shape (substitute real UUIDs from seed.sql):
  -- IOU seed data (v1.4)
  INSERT INTO public.iou_groups (id, created_by, title, total_amount_cents, split_mode, created_at, updated_at)
  VALUES
    ('iou-group-uuid-1', 'alice-uuid', 'Dinner at Noma', 12000, 'even', now(), now()),
    ('iou-group-uuid-2', 'bob-uuid',   'Camping supplies', 7500, 'custom', now(), now());

  -- alice paid 120.00 for dinner, split evenly with bob and charlie (40.00 each)
  INSERT INTO public.iou_members (iou_group_id, user_id, share_amount_cents, settled_at, settled_by)
  VALUES
    ('iou-group-uuid-1', 'alice-uuid',   4000, now(), 'alice-uuid'),  -- alice: paid, marked settled
    ('iou-group-uuid-1', 'bob-uuid',     4000, NULL,  NULL),          -- bob: owes 40.00
    ('iou-group-uuid-1', 'charlie-uuid', 4000, NULL,  NULL);          -- charlie: owes 40.00

  -- bob paid 75.00 for camping, custom split
  INSERT INTO public.iou_members (iou_group_id, user_id, share_amount_cents, settled_at, settled_by)
  VALUES
    ('iou-group-uuid-2', 'bob-uuid',     3000, now(), 'bob-uuid'),    -- bob: paid, marked settled
    ('iou-group-uuid-2', 'alice-uuid',   2500, NULL,  NULL),          -- alice: owes 25.00
    ('iou-group-uuid-2', 'charlie-uuid', 2000, NULL,  NULL);          -- charlie: owes 20.00

Use gen_random_uuid() or stable literal UUIDs (e.g. '00000000-0000-0000-0000-000000000010') for the
iou_group IDs. Use the ACTUAL test user UUIDs from the existing seed.sql file — read them first.

CHANGE 3 — Add birthday seed data (D-13):
After the IOU section, append birthday UPDATE statements for the test users:

  -- Birthday seed data (v1.4)
  UPDATE public.profiles SET birthday_month = 3,  birthday_day = 15 WHERE id = 'alice-uuid';
  UPDATE public.profiles SET birthday_month = 7,  birthday_day = 4  WHERE id = 'bob-uuid';
  UPDATE public.profiles SET birthday_month = 12, birthday_day = 25 WHERE id = 'charlie-uuid';

Substitute the actual UUIDs from seed.sql. Give each test user a different birthday spread across the year so get_upcoming_birthdays() returns varied days_until values during testing.
  </action>

  <verify>
    <automated>cd /Users/iulian/Develop/campfire && grep -c "iou_notes" supabase/seed.sql && echo "FAIL: iou_notes still present" || echo "PASS: iou_notes gone" && grep -c "general_notes" supabase/seed.sql && grep -c "iou_groups" supabase/seed.sql && grep -c "birthday_month" supabase/seed.sql</automated>
  </verify>

  <acceptance_criteria>
    - grep "iou_notes" supabase/seed.sql returns no results (zero occurrences)
    - grep "general_notes" supabase/seed.sql returns 2 matches (the two plans INSERT column lists)
    - grep "iou_groups" supabase/seed.sql returns at least 1 match (INSERT INTO public.iou_groups)
    - grep "iou_members" supabase/seed.sql returns at least 2 matches (two INSERT INTO public.iou_members blocks)
    - grep "birthday_month" supabase/seed.sql returns at least 3 matches (3 UPDATE statements)
    - grep "birthday_day" supabase/seed.sql returns at least 3 matches
    - All share_amount_cents values in seed are integers (cents), not decimals — grep "share_amount_cents.*\." returns no results
  </acceptance_criteria>

  <done>seed.sql contains zero occurrences of iou_notes. Two IOU expenses exist with iou_members rows (one settled, one unsettled for each expense). Three test users have birthday_month and birthday_day set via UPDATE statements. All monetary values are integer cents.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → Supabase remote | npx supabase db push uses project credentials from supabase/config.toml and local auth; runs with service-role equivalent access |
| seed.sql INSERT statements | Seed data runs with service-role privileges during supabase db seed; can bypass RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | supabase db push (destructive migration) | mitigate | RENAME COLUMN is non-destructive; CREATE TABLE / ALTER TABLE ADD COLUMN do not touch existing rows. Review migration diff before push to confirm no DROP statements. |
| T-05-12 | Information Disclosure | seed.sql hardcoded test UUIDs | accept | seed.sql is dev/test data only; never applied to production. Test UUIDs are already present in seed.sql from prior phases — no new exposure. |
| T-05-13 | Denial of Service | iou_members settled_at set on creator row at seed time | accept | Seed data marks creator's own member row as settled (realistic: payer already paid). Not a security concern — seed-only data, deleted before production. |
</threat_model>

<verification>
After both tasks complete:

1. Confirm migration history: run `npx supabase migration list` and verify 0015 and 0016 appear as applied.

2. Confirm no iou_notes anywhere in the project (seed.sql was the last holdout):
   `grep -r "iou_notes" supabase/`
   Expected: no output

3. Confirm seed.sql has all new sections:
   `grep -c "iou_groups\|iou_members\|birthday_month" supabase/seed.sql`
   Expected: at least 8 matches

4. TypeScript check still passes (Plans 01 changes must not have regressed):
   `cd /Users/iulian/Develop/campfire && npx tsc --noEmit`
   Expected: exits 0
</verification>

<success_criteria>
- npx supabase migration list shows 0015 and 0016 applied
- grep -r "iou_notes" supabase/ returns zero results
- seed.sql contains IOU expense rows (integer cents), birthday UPDATE statements for test users
- npx tsc --noEmit exits 0 (no TypeScript regressions from Plans 01-02)
</success_criteria>

<output>
After completion, create `.planning/phases/05-database-migrations/05-03-SUMMARY.md`
</output>
