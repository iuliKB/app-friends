---
phase: 05-hardware-verification-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-hardware-verification-gate/05-02-SUMMARY.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "All 22 eas_build checks (PUSH-01..10, SMOKE-01..09, MORN-01..03) are marked PASS"
    - "v1.3 is declared fully shipped after this plan completes"
    - "05-02-SUMMARY.md records the final sign-off for all 33 v1.3 hardware checks"
  artifacts:
    - path: ".planning/phases/05-hardware-verification-gate/05-02-SUMMARY.md"
      provides: "Final v1.3 ship sign-off document"
      contains: "eas_build Gate"
  key_links:
    - from: "05-CHECKLIST.md eas_build section"
      to: "01-SMOKE-TEST.md"
      via: "PUSH-01..10 steps reference Phase 1 source"
      pattern: "PUSH-0[1-9]|PUSH-10"
    - from: "05-CHECKLIST.md eas_build section"
      to: "03-SMOKE-TEST.md"
      via: "SMOKE-01..09 steps reference Phase 3 source"
      pattern: "SMOKE-0[1-9]"
    - from: "05-CHECKLIST.md eas_build section"
      to: "04-VALIDATION.md"
      via: "MORN-01..03 steps reference Phase 4 Manual-Only table"
      pattern: "MORN-0[1-3]"
---

<objective>
Execute the eas_build track of the Phase 5 Hardware Verification Gate: run 22 manual checks requiring EAS dev builds on real iOS and Android hardware. This plan is the final ship gate for v1.3.

Purpose: Verify push delivery, iOS action button categories, Android notification channels, morning prompt delivery, expiry local notification, and Friend Went Free end-to-end flow on real devices with a native EAS build.

Output: 05-02-SUMMARY.md declaring all 22 checks passed and v1.3 fully shipped.

DEFERRED STATUS: This plan cannot execute until:
  1. Apple Developer Program account is active ($99/yr, acquired near publication)
  2. EAS dev build distributed to at least one real iPhone (iOS 15+)
  3. EAS dev build distributed to at least one real Android device or emulator with Play Services

Do NOT attempt to execute this plan in Expo Go. All 22 checks silently fail or produce incorrect results in Expo Go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hardware-verification-gate/05-CHECKLIST.md
@.planning/phases/05-hardware-verification-gate/05-CONTEXT.md
@.planning/phases/05-hardware-verification-gate/05-01-SUMMARY.md
@.planning/phases/01-push-infrastructure-dm-entry-point/SMOKE-TEST.md
@.planning/phases/03-friend-went-free-loop/03-SMOKE-TEST.md
@.planning/phases/04-morning-prompt-squad-goals-streak/04-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Confirm eas_build prerequisites before starting</name>
  <read_first>
    - .planning/phases/05-hardware-verification-gate/05-CHECKLIST.md (eas_build prerequisites section)
    - .planning/phases/05-hardware-verification-gate/05-01-SUMMARY.md (expo_go track must be PASSED first)
  </read_first>
  <action>
Before running any eas_build checks, confirm all prerequisites are satisfied:

PREREQUISITE CHECKLIST:
- [ ] Apple Developer Program account is active (can be verified at developer.apple.com)
- [ ] EAS dev build exists for iOS: run `eas build:list --platform ios --profile development` — shows at least one successful build
- [ ] EAS dev build installed on a real iPhone (iOS 15+): app icon visible on home screen
- [ ] EAS dev build exists for Android: run `eas build:list --platform android --profile development` — shows at least one successful build
- [ ] EAS dev build installed on a real Android device or emulator with Play Services
- [ ] Two test user accounts (User A + User B) exist, are friends, and are each signed in on a separate device
- [ ] `push_tokens` migration applied: `SELECT COUNT(*) FROM public.push_tokens;` returns without error
- [ ] `notify-plan-invite` Edge Function deployed: `supabase functions list` shows it as active
- [ ] `notify-friend-free` Edge Function deployed: `supabase functions list` shows it as active
- [ ] Supabase SQL editor accessible in browser

If any prerequisite is not met, do NOT proceed. Record the blocking prerequisite and return to this plan when it is met.

If expo_go track (Plan 01) has not been completed, complete that first.
  </action>
  <how-to-verify>
    - `eas build:list --platform ios --profile development` output shows status: finished
    - `eas build:list --platform android --profile development` output shows status: finished
    - `supabase functions list` output includes notify-plan-invite and notify-friend-free
    - Physical devices show Campfire app icon (not Expo Go icon)
  </how-to-verify>
  <acceptance_criteria>
    - `eas build:list --platform ios --profile development` contains the string "finished"
    - `eas build:list --platform android --profile development` contains the string "finished"
    - User confirms both devices have the EAS build installed (not Expo Go)
    - User confirms both test accounts are signed in on separate devices
  </acceptance_criteria>
  <resume-signal>Type "eas prerequisites met" to proceed to the 22-check execution, or "blocked: [missing prerequisite]" to defer.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute eas_build track — 22 checks</name>
  <read_first>
    - .planning/phases/05-hardware-verification-gate/05-CHECKLIST.md (eas_build Track: 22 Checks section — follow step by step)
    - .planning/phases/01-push-infrastructure-dm-entry-point/SMOKE-TEST.md (full steps for PUSH-01..10)
    - .planning/phases/03-friend-went-free-loop/03-SMOKE-TEST.md (full steps for SMOKE-01..09)
    - .planning/phases/04-morning-prompt-squad-goals-streak/04-VALIDATION.md (full steps for MORN-01..03, Manual-Only Verifications table)
  </read_first>
  <action>
Run all 22 eas_build checks in the order below. For each check, follow the full step-by-step instructions in the source file referenced in the 05-CHECKLIST.md eas_build section.

EXECUTION ORDER:

PHASE 1 CHECKS (PUSH-01..10) — run in this order:
  PUSH-10: Verify EAS build artifacts exist (run first so the session is gated on a working build)
  PUSH-03: Schema column verification via `\d public.push_tokens` (SQL, fast)
  PUSH-01: Token registers on session-ready (fresh install User B, SQL confirms row within 5s)
  PUSH-02: Foreground re-register on AppState 'active' (background 30s, foreground, last_seen_at advances)
  PUSH-04: Profile toggle OFF deletes row; toggle ON re-registers within 5s
  PUSH-05: Plan invite reaches fresh install (delete+reinstall User B, User A creates plan with invite)
  PUSH-06: iOS action buttons on morning_prompt category (lock screen long-press shows Free/Busy/Maybe)
  PUSH-07: Android notification channels (Settings → Apps → Campfire → Notifications shows 4 channels)
  PUSH-08: iOS pre-prompt timing (fresh install → no system modal → first status set → pre-prompt modal → "Sounds good" → native modal)
  PUSH-09: DeviceNotRegistered marks invalidated_at (corrupt token in SQL, trigger invite, re-query)

PHASE 3 CHECKS (SMOKE-01..09) — run in this order:
  SMOKE-01: Push arrives within ~5s (User B status ALIVE+maybe, User A flips to Free, stopwatch)
  SMOKE-02: Push body format (on SMOKE-01 success, verify "<name> is Free • <tag>" or "<name> is Free")
  SMOKE-03: Warm start tap opens DM (app backgrounded, tap SMOKE-01 notification, DM opens)
  SMOKE-04: Cold start tap opens DM (force-kill, wait 15min or use different sender, tap, DM opens)
  SMOKE-05: Rate limits (4 flips in 10 min → 1 push received; SQL shows suppression rows)
  SMOKE-06: Quiet hours suppression (set recipient timezone to local 22:00–08:00, sender flips Free, no push received)
  SMOKE-07: Expiry local notification fires (1h window, wait 30 min, local notification fires with Keep it/Heads down)
  SMOKE-08: [Keep it] extends window (on SMOKE-07 notification, tap Keep it, window extends to 3h, SQL confirms)
  SMOKE-09: [Heads down] flips to Busy (repeat SMOKE-07, tap Heads down, Busy 3h shown)

PHASE 4 CHECKS (MORN-01..03):
  MORN-01: Daily notification fires at configured local time (set time to 2 minutes from now in Profile, wait, notification fires on device)
  MORN-02: Action button taps from lock screen cold start (force-kill, wait for MORN-01 notification, tap Free/Busy/Maybe from lock screen, app opens and sets status correctly)
  MORN-03: 12h valid_until guard (advance device clock 13h past the fire time, tap action button, no status mutation — use `select current_status from public.statuses` to confirm no change)

INLINE FIX PROTOCOL (D-04): If any check fails:
  a. Note check ID and exact observed behavior.
  b. Apply the minimal fix (code change, SQL patch, Edge Function redeploy).
  c. If code changed: run `npx tsc --noEmit && npx expo lint` before retesting.
  d. If Edge Function changed: `supabase functions deploy <function-name>`.
  e. Re-run only the failing check.
  f. Record fix in SUMMARY.md with commit SHA.
  No shipping with any known eas_build failures.

TIMING NOTE: SMOKE-07/08/09 require a 30-minute wait. SMOKE-04 requires 15 minutes (or a second sender). MORN-01 can be set to fire 2 minutes from now (set Profile time accordingly). Schedule these at the start and run other checks during the wait.
  </action>
  <what-built>
    eas_build track execution of 22 checks across Phase 1 push infrastructure, Phase 3 Friend Went Free, and Phase 4 morning prompt
  </what-built>
  <how-to-verify>
    1. All 22 check IDs confirmed PASS by direct device observation and SQL verification where required.
    2. `npx tsc --noEmit` exits 0 (after any inline fixes).
    3. `npx expo lint` exits 0 (after any inline fixes).
    4. SMOKE-05 SQL query shows suppression rows with correct suppression_reason values.
    5. PUSH-09 SQL query shows non-null invalidated_at for the corrupted token.
  </how-to-verify>
  <acceptance_criteria>
    - All 22 check IDs (PUSH-01..10, SMOKE-01..09, MORN-01..03) confirmed PASS by device observation
    - SMOKE-05: SQL shows at least 1 row with suppressed=true and suppression_reason='pair_15min'
    - PUSH-09: SQL shows invalidated_at IS NOT NULL for the corrupted test token
    - SMOKE-06: SQL shows a friend_free_pushes row with suppression_reason='quiet_hours'
    - `npx tsc --noEmit` exit code 0
    - `npx expo lint` exit code 0
  </acceptance_criteria>
  <resume-signal>Type "eas_build complete" and list any check IDs that needed inline fixes (or "none"), or "blocked: [check ID, reason]" if a check fails without a known fix.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author 05-02-SUMMARY.md and declare v1.3 shipped</name>
  <read_first>
    - .planning/phases/05-hardware-verification-gate/05-01-SUMMARY.md (expo_go track results to reference)
    - .planning/phases/05-hardware-verification-gate/05-CHECKLIST.md (eas_build check IDs for the results table)
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY template format)
  </read_first>
  <action>
Create `.planning/phases/05-hardware-verification-gate/05-02-SUMMARY.md`.

The file must contain exactly these sections:

SECTION 1 — Frontmatter:
```
~~~
phase: 05-hardware-verification-gate
plan: 02
status: complete
completed: <ISO-8601 date>
~~~
```

SECTION 2 — eas_build Track Results:
A markdown table with columns: Check ID | Requirement | Description | Result | Notes

Rows (fill Result from Task 2 execution; add fix commit SHA in Notes if applicable):
- PUSH-10 | PUSH-10 | EAS dev build artifacts exist | PASS | —
- PUSH-03 | PUSH-03 | push_tokens schema columns + indexes | PASS | —
- PUSH-01 | PUSH-01 | Token registers on session-ready | PASS | —
- PUSH-02 | PUSH-02 | Foreground re-register on AppState active | PASS | —
- PUSH-04 | PUSH-04 | Profile toggle OFF/ON | PASS | —
- PUSH-05 | PUSH-05 | Plan invite reaches fresh install | PASS | —
- PUSH-06 | PUSH-06 | iOS action buttons on morning_prompt | PASS | —
- PUSH-07 | PUSH-07 | Android notification channels | PASS | —
- PUSH-08 | PUSH-08 | iOS pre-prompt timing | PASS | —
- PUSH-09 | PUSH-09 | DeviceNotRegistered marks invalidated_at | PASS | —
- SMOKE-01 | FREE-01 | Push within ~5s of Free transition | PASS | —
- SMOKE-02 | FREE-08 | Push body format | PASS | —
- SMOKE-03 | FREE-09 | Warm start tap opens DM | PASS | —
- SMOKE-04 | FREE-09 | Cold start tap opens DM | PASS | —
- SMOKE-05 | FREE-03/04/05 | Rate limits enforced | PASS | —
- SMOKE-06 | FREE-06 | Quiet hours suppression | PASS | —
- SMOKE-07 | EXPIRY-01 | Expiry local notification fires | PASS | —
- SMOKE-08 | EXPIRY-01 | Keep it extends window | PASS | —
- SMOKE-09 | EXPIRY-01 | Heads down flips to Busy | PASS | —
- MORN-01 | MORN-01/02 | Daily notification fires at configured time | PASS | —
- MORN-02 | MORN-03/04 | Action button cold-start routing | PASS | —
- MORN-03 | MORN-05 | 12h valid_until guard no-ops | PASS | —

SECTION 3 — Inline Fixes (omit if none):
For each fix: Check ID, symptom observed, change made, commit SHA.

SECTION 4 — Phase 5 Final Gate Outcome:
```
## Phase 5 Gate: COMPLETE
expo_go track (Plan 01): PASSED — 11/11 checks, 18/18 regression scenarios
eas_build track (Plan 02): PASSED — 22/22 checks

## v1.3 Milestone: SHIPPED
Milestone: v1.3 Liveness & Notifications
Phases: 1 (Push Infrastructure), 2 (Status Liveness), 3 (Friend Went Free), 4 (Morning Prompt + Streak), 5 (Hardware Gate)
Total requirements verified: 44/44
Hardware checks: 33/33 (11 expo_go + 22 eas_build)
Date: <ISO date>
```
  </action>
  <verify>
    <automated>test -f .planning/phases/05-hardware-verification-gate/05-02-SUMMARY.md && grep -q "eas_build Gate\|Phase 5 Gate" .planning/phases/05-hardware-verification-gate/05-02-SUMMARY.md && grep -q "v1.3 Milestone: SHIPPED" .planning/phases/05-hardware-verification-gate/05-02-SUMMARY.md && grep -q "status: complete" .planning/phases/05-hardware-verification-gate/05-02-SUMMARY.md</automated>
  </verify>
  <done>
    - 05-02-SUMMARY.md exists at the correct path
    - File contains eas_build results table with 22 rows
    - File contains "v1.3 Milestone: SHIPPED" with date
    - File contains "status: complete" in frontmatter
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test device -> APNs/FCM | Real push tokens flow through Apple/Google infrastructure — test tokens are real tokens, not sandboxed |
| SQL editor -> production push_tokens | PUSH-09 deliberately corrupts a token row; must use a test user account, not a real user's token |
| Edge Function -> Supabase DB | notify-friend-free reads live friend data; rate-limit SQL manipulation in SMOKE-05/06 affects real rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | PUSH-09 token corruption via SQL | accept | Intentional test step; performed on a known test user account only; invalidated_at column is designed to handle this state |
| T-05-05 | Tampering | SMOKE-06 timezone manipulation via SQL profiles.timezone update | accept | Test-only manipulation; test account's timezone is restored or left — does not affect real user data |
| T-05-06 | Spoofing | MORN-03 device clock advance to bypass 12h guard | accept | Device clock advance is a test technique; the 12h guard is being verified, not bypassed maliciously; test device is isolated |
| T-05-07 | Information Disclosure | Real APNs tokens visible in push_tokens table during testing | mitigate | Use test accounts only; do not screenshot or log real user token values; invalidate test tokens after session with `UPDATE public.push_tokens SET invalidated_at = now() WHERE user_id IN ('<test-a>', '<test-b>')` |
</threat_model>

<verification>
Session is complete when:
- `test -f .planning/phases/05-hardware-verification-gate/05-02-SUMMARY.md` exits 0
- `grep -q "v1.3 Milestone: SHIPPED" .planning/phases/05-hardware-verification-gate/05-02-SUMMARY.md` exits 0
- `npx tsc --noEmit` exits 0
- `npx expo lint` exits 0
</verification>

<success_criteria>
1. All 22 eas_build checks (PUSH-01..10, SMOKE-01..09, MORN-01..03) passed on real EAS dev build hardware.
2. 05-02-SUMMARY.md authored and committed declaring v1.3 shipped.
3. Combined with Plan 01: all 33 v1.3 hardware checks are accounted for (11 expo_go PASSED + 22 eas_build PASSED).
4. v1.3 milestone formally closed.
</success_criteria>

<output>
After completion, commit the SUMMARY.md and update ROADMAP.md to mark Phase 5 complete:

```bash
git add .planning/phases/05-hardware-verification-gate/05-02-SUMMARY.md .planning/ROADMAP.md
git commit -m "docs(05-02): eas_build track sign-off — v1.3 shipped"
```
</output>
