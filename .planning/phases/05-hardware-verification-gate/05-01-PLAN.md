---
phase: 05-hardware-verification-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-hardware-verification-gate/05-CHECKLIST.md
  - .planning/phases/05-hardware-verification-gate/05-01-SUMMARY.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "All 11 expo_go checks (UAT-01..10 + DM-01) are marked PASS"
    - "All 18 regression scenarios (6 features x 3 scenarios) are marked PASS"
    - "STREAK-08 human gate is cleared with an explicit written disposition"
    - "Any inline fixes applied during the session are documented with commit SHAs"
    - "TypeScript and lint pass after any inline code changes"
    - "05-01-SUMMARY.md exists and records the session outcome"
  artifacts:
    - path: ".planning/phases/05-hardware-verification-gate/05-CHECKLIST.md"
      provides: "Consolidated 33-check list (already authored)"
      contains: "expo_go Track: 11 Checks"
    - path: ".planning/phases/05-hardware-verification-gate/05-01-SUMMARY.md"
      provides: "Session sign-off document"
      contains: "expo_go Gate"
  key_links:
    - from: "05-CHECKLIST.md"
      to: "02-HUMAN-UAT.md"
      via: "UAT-01..10 steps reference the Phase 2 UAT source"
      pattern: "UAT-0[1-9]|UAT-10"
    - from: "05-01-SUMMARY.md"
      to: "04-COPY-REVIEW.md"
      via: "STREAK-08 disposition references the Phase 4 copy review"
      pattern: "STREAK-08"
---

<objective>
Execute the expo_go track of the Phase 5 Hardware Verification Gate: run 11 manual UI behavioral checks on Expo Go, complete the v1.0-v1.2 regression walkthrough, and clear the STREAK-08 human gate. This is the ship gate for the expo_go portion of v1.3.

Purpose: Confirm that all code-complete v1.3 features work correctly on a real device under Expo Go, and that no regressions exist in shipped v1.0-v1.2 functionality.

Output: A signed-off SUMMARY.md with pass/fail table, inline fix notes, STREAK-08 disposition, and formal deferral of the eas_build track.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hardware-verification-gate/05-CHECKLIST.md
@.planning/phases/05-hardware-verification-gate/05-CONTEXT.md
@.planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md
@.planning/phases/02-status-liveness-ttl/02-HUMAN-UAT.md
@.planning/phases/01-push-infrastructure-dm-entry-point/SMOKE-TEST.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Execute expo_go track — 11 checks + regression walkthrough</name>
  <read_first>
    - .planning/phases/05-hardware-verification-gate/05-CHECKLIST.md (the session guide — follow it exactly)
    - .planning/phases/02-status-liveness-ttl/02-HUMAN-UAT.md (source for UAT-01..10 full steps)
    - .planning/phases/01-push-infrastructure-dm-entry-point/SMOKE-TEST.md (source for DM-01 full steps)
    - .planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md (for STREAK-08 context)
  </read_first>
  <action>
Run the full expo_go track in the order defined in 05-CHECKLIST.md. Do not skip any check.

SESSION ORDER:

1. Complete all prerequisites (Expo Go on device, dev server running via `npx expo start`, User A + B friendship verified with SQL, Supabase SQL editor open in browser, `npx tsc --noEmit && npx expo lint` passes).

2. Group A — Home Screen Flow (8 checks in this order):
   - UAT-01: MoodPicker two-stage commit (tap mood row, tap preset chip, tap window chip; own-status updates instantly)
   - UAT-06: Cold launch with DEAD heartbeat (SQL: `UPDATE public.statuses SET last_active_at = now() - interval '9 hours' WHERE user_id = '<user-a-uuid>'`, force-kill Expo Go, reopen; heading visible)
   - UAT-07: Friend card FADING/DEAD states (SQL ages User B's last_active_at to 5h ago; observe reduced opacity or partition)
   - UAT-08: START timer (SQL: set User B's status_expires_at = now() + 90 seconds; leave Home foregrounded; come back after 150s)
   - [during UAT-08 wait]: Set up UAT-09 scenario and UAT-10 steps
   - UAT-02: ReEngagementBanner (SQL: `UPDATE public.statuses SET last_active_at = now() - interval '5 hours' WHERE user_id = '<user-a-uuid>'`; background and foreground app; wait up to 70s for banner)
   - UAT-03: "Keep it" tap (while banner is visible; SQL confirms last_active_at advances)
   - UAT-04: "Heads down" tap (reproduce FADING state; tap Heads down; MoodPicker shows Busy)
   - UAT-05: "Update" tap (reproduce FADING state; tap Update; MoodPicker scrolls into view)
   - UAT-08 OBSERVE: Check if User B's card flipped to DEAD within 150s

3. Group B — DM-01 (single tap friend card opens DM; long-press shows action sheet with View profile + Plan with; both routes work; Home list scroll unaffected)

4. Group C — Session Boundary:
   - UAT-09: Sign out as User A, sign in as User B; own-status chip shows User B's data (not User A's)
   - UAT-10: Note last_active_at; rapidly background+foreground 5 times in 30s; SQL shows exactly one timestamp change

INLINE FIX PROTOCOL (D-04): If any check fails:
  a. Note check ID and exact observed behavior.
  b. Apply the minimal fix (code change, SQL data patch, or config).
  c. Run `npx tsc --noEmit && npx expo lint` — must both pass.
  d. Hot-reload in Expo Go, re-run only the failing check.
  e. Note fix description and commit SHA for SUMMARY.md.
  No shipping with known expo_go failures (D-05).

REGRESSION WALKTHROUGH (D-06/D-07, ~30 min):
After all 11 expo_go checks pass, execute these 18 scenarios:

  Login:
    1. Email sign-in with valid credentials -> Home screen loads
    2. OAuth sign-in (Google or Apple) -> Home screen loads
    3. Sign-out then re-sign-in -> session fully reset, correct user shown

  Status set:
    4. Set Free + emoji tag + window chip -> chip updates on Home and Profile instantly
    5. Set Busy (no tag) -> Busy chip shown
    6. Set Maybe + no tag -> Maybe chip shown with no window label

  Plan create + RSVP:
    7. Create plan, invite User B, User B RSVPs Going -> plan shows Going count
    8. Create a second plan, RSVP Not Going -> plan shows Not Going state
    9. Navigate to plan list with no plans -> empty state renders without crash

  Chat send:
    10. Send DM message to friend -> message visible on both sides
    11. Send message in a group chat room -> message visible
    12. Open chat with no messages -> empty state renders without crash

  Friend add + accept:
    13. Send friend request from User A to a 3rd test account; accept on that account -> friendship appears
    14. Send friend request; decline on recipient device -> request disappears
    15. Check pending badge state after sending request -> badge visible before accept

  Squad view:
    16. Open Squad tab, Friends subtab -> friend list renders
    17. Open Goals subtab -> StreakCard component visible (not "Coming soon" stub)
    18. View Goals tab as a user with no streak history -> zero state renders with "Start your first week" copy

STREAK-08 HUMAN GATE:
After the regression walkthrough, open `.planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md` and read the "Reviewer" field. Record disposition as one of:
  - Option A: "STREAK-08 approved by [Name, Role] — non-engineer distinct from the developer."
  - Option B: "STREAK-08 approved by project owner (same person as solo developer). Copy reviewed with anti-anxiety lens per CONTEXT D-09. Non-engineer review not possible in solo-dev context."
Record this verbatim in SUMMARY.md under "STREAK-08 Gate Clearance".
  </action>
  <what-built>
    expo_go track execution of 11 checks + v1.0-v1.2 regression walkthrough across 18 scenarios + STREAK-08 gate clearance
  </what-built>
  <how-to-verify>
    1. All 11 check IDs (UAT-01..10 + DM-01) observed and confirmed by user on device.
    2. Regression table: 18 scenarios all passed (no crashes, no blank screens, no data loss).
    3. STREAK-08 disposition is explicitly written (Option A or B).
    4. `npx tsc --noEmit` exits 0.
    5. `npx expo lint` exits 0.
    6. If any inline fix was applied: `git log --oneline -5` shows a fix commit from this session.
  </how-to-verify>
  <acceptance_criteria>
    - UAT-01 through UAT-10 and DM-01 each confirmed PASS by direct device observation
    - 6 regression features x 3 scenarios each = 18 total scenarios all PASS
    - STREAK-08 disposition contains either literal text "non-engineer distinct" (Option A) or "same person as solo developer" (Option B)
    - `npx tsc --noEmit` exit code 0
    - `npx expo lint` exit code 0
    - If any fix commit exists: `git log --oneline -5` contains a commit with message referencing the fixed check ID
  </acceptance_criteria>
  <resume-signal>Type "expo_go complete" and list any check IDs that needed inline fixes (or "none"), or "blocked: [reason]" if a check cannot be executed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author 05-01-SUMMARY.md</name>
  <read_first>
    - .planning/phases/05-hardware-verification-gate/05-CHECKLIST.md (for pass/fail outcome data from Task 1)
    - .planning/phases/05-hardware-verification-gate/05-CONTEXT.md (for decisions D-01..D-07 to reference)
    - .planning/phases/04-morning-prompt-squad-goals-streak/04-COPY-REVIEW.md (for STREAK-08 reviewer name)
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY template format)
  </read_first>
  <action>
Create `.planning/phases/05-hardware-verification-gate/05-01-SUMMARY.md`.

The file must contain exactly the following sections:



SECTION 1 — Frontmatter:
```
~~~
phase: 05-hardware-verification-gate
plan: 01
status: complete
completed: <ISO-8601 date>
~~~
```

SECTION 2 — expo_go Track Results:
A markdown table with columns: Check ID | Requirement | Description | Result | Notes

Rows (fill Result from execution; add fix commit SHA in Notes if applicable):
- UAT-01 | TTL-01/02 | MoodPicker two-stage commit | PASS | —
- UAT-02 | HEART-03/05 | ReEngagementBanner appears FADING | PASS | —
- UAT-03 | HEART-05 | Keep it action | PASS | —
- UAT-04 | HEART-05 | Heads down action | PASS | —
- UAT-05 | HEART-05 | Update scroll-to-picker | PASS | —
- UAT-06 | HEART-04 | Cold launch DEAD heading | PASS | —
- UAT-07 | HEART-03 | Friend card FADING/DEAD states | PASS | —
- UAT-08 | TTL-02 | 60s setInterval silent expiry | PASS | —
- UAT-09 | (store hygiene) | Signout clears cache | PASS | —
- UAT-10 | HEART-02 | touch() 60s debounce | PASS | —
- DM-01 | DM-01 | HomeFriendCard tap + action sheet | PASS | —

SECTION 3 — Regression Walkthrough Results:
A markdown table with columns: Feature | Scenario | Result | Notes

18 rows (one per scenario from Task 1 action):
Login x3, Status set x3, Plan create x3, Chat send x3, Friend add x3, Squad view x3.

SECTION 4 — STREAK-08 Gate Clearance:
```
## STREAK-08 Human Gate
Status: CLEARED
Disposition: [exact Option A or Option B text recorded during Task 1]
Date: <ISO date>
```

SECTION 5 — Inline Fixes (omit this section entirely if no fixes were needed):
For each fix: check ID, symptom observed, change made, commit SHA.
```
## Inline Fixes
- [Check ID]: [symptom] → [change] (commit: [SHA])
```

SECTION 6 — eas_build Track Formal Deferral:
```
## eas_build Track: Formally Deferred
Status: DEFERRED
Reason: No Apple Developer Program account (pre-publication constraint per project memory)
Scope: 22 checks — PUSH-01..10 (Phase 1), SMOKE-01..09 (Phase 3), MORN-01..03 (Phase 4)
Source checklist: .planning/phases/05-hardware-verification-gate/05-CHECKLIST.md
Execution plan: 05-02-PLAN.md
Prerequisites:
  - Apple Developer Program account active
  - EAS dev build on real iPhone (iOS 15+)
  - EAS dev build on Android device with Play Services
  - notify-plan-invite and notify-friend-free Edge Functions deployed to production project
Execute: /gsd-execute-phase 05 when prerequisites met
```

SECTION 7 — Phase Gate Outcome:
```
## expo_go Gate
Status: PASSED
expo_go checks: 11/11
Regression scenarios: 18/18
STREAK-08: CLEARED
v1.3 expo_go-testable features: SHIP-READY
eas_build track: DEFERRED (see 05-02-PLAN.md)
```
  </action>
  <verify>
    <automated>test -f .planning/phases/05-hardware-verification-gate/05-01-SUMMARY.md && grep -q "expo_go Gate" .planning/phases/05-hardware-verification-gate/05-01-SUMMARY.md && grep -q "STREAK-08" .planning/phases/05-hardware-verification-gate/05-01-SUMMARY.md && grep -q "eas_build Track: Formally Deferred" .planning/phases/05-hardware-verification-gate/05-01-SUMMARY.md && grep -q "status: complete" .planning/phases/05-hardware-verification-gate/05-01-SUMMARY.md</automated>
  </verify>
  <done>
    - 05-01-SUMMARY.md exists at the correct path
    - File contains "expo_go Gate" section with Status: PASSED
    - File contains "STREAK-08 Human Gate" section with Status: CLEARED and a non-empty Disposition field
    - File contains "eas_build Track: Formally Deferred" section with Execution plan: 05-02-PLAN.md
    - File frontmatter contains status: complete with an ISO date
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL editor -> Supabase | Direct DB manipulation during testing bypasses RLS via service-role key — used only for test data setup, not a production pattern |
| Device -> Expo Go bundle | No auth, no push; Expo Go sandboxes native modules — no threat surface beyond UI behavioral testing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | SQL editor test queries | accept | Test UUIDs used in SQL during the session expose user IDs in browser history; acceptable because these are test accounts in a dev Supabase project, no PII of real users involved |
| T-05-02 | Tampering | Direct SQL UPDATE of last_active_at during UAT-02/06/07 setup | accept | Intentional test data manipulation in dev environment; no production data at risk; changes are observable and reversible |
| T-05-03 | Denial of Service | Inline fix deploys bad code | mitigate | `npx tsc --noEmit && npx expo lint` must pass before any hot-reload retest; prevents type errors from reaching Expo Go bundle |
</threat_model>

<verification>
Session is complete when:
- `test -f .planning/phases/05-hardware-verification-gate/05-01-SUMMARY.md` exits 0
- `grep -q "Status: PASSED" .planning/phases/05-hardware-verification-gate/05-01-SUMMARY.md` exits 0
- `grep -q "Status: CLEARED" .planning/phases/05-hardware-verification-gate/05-01-SUMMARY.md` exits 0
- `npx tsc --noEmit` exits 0
- `npx expo lint` exits 0
</verification>

<success_criteria>
1. All 11 expo_go checks (UAT-01..10 + DM-01) passed on a real device.
2. All 18 v1.0-v1.2 regression scenarios passed without crashes or data loss.
3. STREAK-08 human gate cleared with written Option A or Option B disposition.
4. 05-01-SUMMARY.md authored and committed with the complete session record.
5. eas_build track formally deferred in SUMMARY.md with 05-02-PLAN.md as the execution pointer.
</success_criteria>

<output>
After completion, the executor MUST commit `.planning/phases/05-hardware-verification-gate/05-01-SUMMARY.md` to git:

```bash
git add .planning/phases/05-hardware-verification-gate/05-01-SUMMARY.md
git commit -m "docs(05-01): expo_go track sign-off — all 11 checks passed, STREAK-08 cleared"
```

If inline fixes were applied, include the fix files in the same commit or as a prior commit with message:
`fix(05-01): inline repair for [check ID] during expo_go session`
</output>
