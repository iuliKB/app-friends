---
phase: 33-friend-profile-redesign
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - src/app/friends/[id].tsx
  - src/app/friends/[id]/photos.tsx
  - src/hooks/useChatDmPreferences.ts
  - src/hooks/__tests__/useChatDmPreferences.test.ts
  - src/app/friends/__tests__/[id].test.tsx
autonomous: false
requirements:
  - D-06
  - D-08
  - D-09
  - D-11
  - D-12
  - D-15
  - D-16
  - REQ-FP-06
  - REQ-FP-07
  - REQ-FP-09
  - REQ-FP-11
  - REQ-FP-12
  - GATE-mutationShape
tags: [screen, glue, integration, friend-profile, phase-33]

must_haves:
  truths:
    - "`src/app/friends/[id].tsx` is a full rewrite — no `useState + useEffect + supabase.from('profiles').select(...)` inline fetch remains (D-13); mounts `useFriendProfile` + `useFriendMutuals` + existing `useFriendWishList` + new `useChatDmPreferences`"
    - "Header renders `FriendProfileHeader` consuming a parent-owned `scrollY` SharedValue; `Stack.Screen.options.headerTitle` reads the same `scrollY` for the cross-fade nav title"
    - "Quick actions wired: Message → `openChat(router, { kind: 'dmFriend', friendId, friendName })`; Mute → lazy-create DM channel + upsert `chat_preferences` via canonical Pattern 5 mutation; Photos → `router.push('/friends/[id]/photos')`; More → `showActionSheet` with single 'Remove Friend' destructive item"
    - "Remove Friend follows canonical Pattern 5 (optimistic remove from `queryKeys.friends.list(myId)`, invalidate home keys on settle, router.back() on success) — mutationShape gate green without exemption"
    - "Tap big avatar → `ImageViewerModal` opens (`onAvatarPress` wired only when `profile.avatar_url !== null`)"
    - "Friend-not-found state — when `data.friendsSince === null` — renders custom ~15 LOC inline empty view with 'Back to friends' CTA (NOT `ErrorDisplay`, per RESEARCH §Recommendations §Friend-not-found fallback)"
    - "Loading state — when `isLoading && !data` — renders `SkeletonPulse` shells per UI-SPEC §Loading & Error Contract §Skeleton"
    - "Error state — when `error !== null` — renders `<ErrorDisplay mode=\"screen\" message=\"Couldn't load profile. Check your connection and try again.\" onRetry={refetch} />` (NOT the UI-SPEC's suggested 4-prop API — actual component takes `{ message, technicalDetails?, mode?, onRetry? }` per PATTERNS §Corrections row 5)"
    - "`/friends/[id]/photos` route mounts existing `useAllPlanPhotos` filtered to `sharedPlanIds` from `useFriendMutuals` (RESEARCH §Recommendations §Shared-photos viewer route)"
    - "`useChatDmPreferences(channelId)` reads `chat_preferences` for the DM channel; queryKey `queryKeys.chat.preferences(channelId)`; nullable channelId support (returns null when DM not yet created)"
  artifacts:
    - path: "src/app/friends/[id].tsx"
      provides: "Redesigned friend profile screen (D-13 + everything else this phase)"
      contains: "useFriendProfile"
    - path: "src/app/friends/[id]/photos.tsx"
      provides: "Shared-photos grid route (D-09 Photos quick-action target)"
      contains: "sharedPlanIds"
    - path: "src/hooks/useChatDmPreferences.ts"
      provides: "Mute initialization read for the DM channel"
      contains: "queryKeys.chat.preferences"
    - path: "src/app/friends/__tests__/[id].test.tsx"
      provides: "RTL coverage: REQ-FP-06 handler-level + REQ-FP-07 row visibility + REQ-FP-09 avatar viewer state + REQ-FP-11 remove + REQ-FP-12 friend-not-found"
      contains: "Friend"
  key_links:
    - from: "src/app/friends/[id].tsx"
      to: "useFriendProfile + useFriendMutuals + useFriendWishList + useChatDmPreferences"
      via: "Mounted at top of component; data hoisted to friends sections via props"
      pattern: "useFriendProfile|useFriendMutuals|useChatDmPreferences"
    - from: "src/app/friends/[id].tsx"
      to: "FriendProfileHeader + QuickActionsRow + GroupedInsetSection + ProfileInfoRow + BioRow"
      via: "Composed inside Animated.ScrollView; parent-owned scrollY shared to header + Stack headerTitle"
      pattern: "Animated\\.ScrollView"
    - from: "src/app/friends/[id].tsx"
      to: "supabase.from('friendships').delete()"
      via: "Pattern 5 remove mutation; optimistic splice of queryKeys.friends.list(myId)"
      pattern: "from\\('friendships'\\).*delete"
    - from: "src/app/friends/[id].tsx"
      to: "ImageViewerModal"
      via: "Tap big avatar → setState → modal renders with profile.avatar_url"
      pattern: "ImageViewerModal"
---

<objective>
The integration plan. Rewrites the friend profile screen using the components
from Plans 03-05, the hooks from Plan 02, and the migration + queryKeys from
Plan 01. Adds the new shared-photos route and the Mute-state read hook.

Purpose: every other plan in this phase produces inputs that this plan composes.
Everything ships together here. Includes the full screen test that pins the
six requirement IDs marked "manual-or-unit" in VALIDATION.md.

Output:
- `src/app/friends/[id].tsx` (rewrite)
- `src/app/friends/[id]/photos.tsx` (new route)
- `src/hooks/useChatDmPreferences.ts` (new — Mute init)
- `src/hooks/__tests__/useChatDmPreferences.test.ts`
- `src/app/friends/__tests__/[id].test.tsx` (or equivalent path; tests REQ-FP-06/07/09/11/12)
- Manual hardware smoke checkpoint at end (REQ-FP-04/05/06/09 visual)

This plan is NOT autonomous — it ends in a `checkpoint:human-verify` for the
hardware-only behaviors that jest cannot reliably cover (header collapse animation,
blurred wash render, haptics, ImageViewerModal swipe-down).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/33-friend-profile-redesign/33-CONTEXT.md
@.planning/phases/33-friend-profile-redesign/33-RESEARCH.md
@.planning/phases/33-friend-profile-redesign/33-PATTERNS.md
@.planning/phases/33-friend-profile-redesign/33-UI-SPEC.md
@.planning/phases/33-friend-profile-redesign/33-VALIDATION.md
@.planning/phases/33-friend-profile-redesign/33-01-SUMMARY.md
@.planning/phases/33-friend-profile-redesign/33-02-SUMMARY.md
@.planning/phases/33-friend-profile-redesign/33-03-SUMMARY.md
@.planning/phases/33-friend-profile-redesign/33-04-SUMMARY.md
@.planning/phases/33-friend-profile-redesign/33-05-SUMMARY.md
@src/app/friends/[id].tsx
@src/hooks/useFriendWishList.ts
@src/hooks/useExpensesWithFriend.ts
@src/hooks/useAllPlanPhotos.ts
@src/screens/chat/ChatListScreen.tsx
@src/components/chat/ImageViewerModal.tsx
@src/components/common/ErrorDisplay.tsx
@src/components/common/SkeletonPulse.tsx
@src/lib/openChat.ts
@src/lib/action-sheet.ts

<interfaces>
<!-- Hook contracts (post Plans 01+02) -->

From src/hooks/useFriendProfile.ts (Plan 02):
```typescript
export function useFriendProfile(friendId: string): {
  data: FriendProfileData | null;  // { profile, friendsSince, status, contextTag, statusExpiresAt, lastActiveAt } | null
  isLoading: boolean;
  error: string | null;
  refetch: () => Promise<unknown>;
};
```

From src/hooks/useFriendMutuals.ts (Plan 02):
```typescript
export function useFriendMutuals(friendId: string): {
  data: { mutualPlansCount, mutualFriendsCount, sharedPhotosCount, sharedPlanIds } | null;
  isLoading: boolean;
  error: string | null;
  refetch: () => Promise<unknown>;
};
```

From src/hooks/useFriendWishList.ts (existing):
```typescript
{ items: WishListItemWithClaim[]; loading; error; refetch; toggleClaim }
```

From src/hooks/useExpensesWithFriend.ts (existing — used for IOU balance derivation):
```typescript
{ expenses: ExpenseWithFriend[]; loading; error; refetch }
// totalCents, payerName, isFullySettled per row; aggregate on screen per RESEARCH §Code-level Confirmations #5 recommendation (a)
```

From src/hooks/useAllPlanPhotos.ts (existing — used in /friends/[id]/photos.tsx):
```typescript
{ groups: PlanPhotoGroup[]; recentPhotos; isLoading; error; refetch; deletePhoto }
```

<!-- Component contracts (post Plans 03+04+05) -->

From src/components/friends/FriendProfileHeader.tsx (Plan 03):
```typescript
interface FriendProfileHeaderProps {
  scrollY: SharedValue<number>;
  displayName: string;
  username: string;
  avatarUrl: string | null;
  status: 'free' | 'busy' | 'maybe' | null;
  contextTag: string | null;
  statusExpiresAt: string | null;
  lastActiveAt: string | null;
  onAvatarPress?: () => void;
}
```

From src/components/friends/QuickActionsRow.tsx (Plan 05):
```typescript
interface QuickActionsRowProps {
  onMessage: () => void;
  onToggleMute: () => void;
  onPhotos: () => void;
  onMore: () => void;
  isMuted: boolean;
  friendFirstName: string;
  muteDisabled?: boolean;
  messageDisabled?: boolean;
}
```

From src/components/friends/GroupedInsetSection.tsx (Plan 04): `{ title, children }`
From src/components/friends/ProfileInfoRow.tsx (Plan 04): `{ iconTint, label, value?, onPress?, chevron?, accessibilityLabel?, accessibilityHint?, isLast? }`
From src/components/friends/BioRow.tsx (Plan 04): `{ bio: string, isLast? }`

<!-- External helpers -->

From src/lib/openChat.ts (Phase 30):
```typescript
openChat(router, { kind: 'dmFriend', friendId: string, friendName: string }): Promise<void>
```

From src/lib/action-sheet.ts (existing):
```typescript
showActionSheet(title: string, items: Array<{ label: string; onPress: () => void; destructive?: boolean }>): void
// Helper auto-appends Cancel — DO NOT add it to items
```

From src/components/chat/ImageViewerModal.tsx (Phase 16 — VERIFIED via PATTERNS §Corrections row 7):
```typescript
interface ImageViewerModalProps {
  visible: boolean;
  imageUrl: string | null;
  onClose: () => void;
}
```

From src/components/common/ErrorDisplay.tsx (PATTERNS §Corrections row 5 — ACTUAL API):
```typescript
interface ErrorDisplayProps {
  message: string;
  technicalDetails?: string;
  mode?: 'inline' | 'screen';
  onRetry?: () => void;
}
// CTA is hard-coded as "Try Again" — no override prop
```

<!-- Mute upsert pattern from ChatListScreen.tsx:120-123 (verbatim) -->

```typescript
await supabase.from('chat_preferences').upsert(
  { user_id, chat_type: 'dm', chat_id, is_muted, updated_at: new Date().toISOString() },
  { onConflict: 'user_id,chat_type,chat_id' },
);
```

<!-- DM channel resolution RPC (used internally by openChat — for Mute we call it directly) -->

```typescript
const { data, error } = await supabase.rpc('get_or_create_dm_channel', { p_friend_id: friendId });
// returns { channel_id: string }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author useChatDmPreferences.ts (Mute initialization read for the DM channel) + test</name>
  <files>src/hooks/useChatDmPreferences.ts, src/hooks/__tests__/useChatDmPreferences.test.ts</files>
  <read_first>
    - src/hooks/useChatList.ts (lines around 347, 357, 381-383 — chat_preferences shape in the chat list query)
    - src/screens/chat/ChatListScreen.tsx (lines 99-103, 120-123 — chat_preferences upsert composite key)
    - src/lib/queryKeys.ts (post-Plan-01; confirms `chat.preferences(channelId)`)
    - .planning/phases/33-friend-profile-redesign/33-RESEARCH.md §Risks #5 (Mute initialization — recommendation (a): single row read)
    - .planning/phases/33-friend-profile-redesign/33-UI-SPEC.md §Quick-Action Buttons §Mute (line 388) — cache key shape
  </read_first>
  <behavior>
    - `useChatDmPreferences(channelId: string | null)` returns `{ data: { isMuted: boolean } | null, isLoading, error, refetch }`
    - When `channelId === null` (DM not yet created), `enabled: false` — query never fires; `data` stays null; `isLoading: false`
    - When `channelId` provided, query reads `chat_preferences` row for `(user_id = me, chat_type = 'dm', chat_id = channelId)`; returns `{ isMuted: data?.is_muted ?? false }`
    - `staleTime: 60_000` (matches Phase 31 default)
    - QueryKey `queryKeys.chat.preferences(channelId)` (Plan 01 added this factory)
    - Tests cover: enabled-false branch when channelId null, happy path when row exists, missing-row branch (default isMuted: false)
  </behavior>
  <action>
**1a. Create `src/hooks/useChatDmPreferences.ts`** (~60-90 LOC):

```typescript
// Phase 33 — Mute state read for the friend profile QuickActionsRow.
//
// Reads chat_preferences for (myId, 'dm', channelId). When channelId is null
// (no DM created yet) the query is disabled — data stays null and the Mute
// button defaults to "Mute" (not muted) per UI-SPEC §Quick-Action Buttons.
//
// The Mute mutation itself lives inline in the friend profile screen (Plan 06)
// — this hook only handles the read so the screen's Mute button label/icon
// reflects current state on mount.

import { useQuery } from '@tanstack/react-query';
import { supabase } from '@/lib/supabase';
import { useAuthStore } from '@/stores/useAuthStore';
import { queryKeys } from '@/lib/queryKeys';

export interface ChatDmPreferencesData {
  isMuted: boolean;
}

export interface UseChatDmPreferencesResult {
  data: ChatDmPreferencesData | null;
  isLoading: boolean;
  error: string | null;
  refetch: () => Promise<unknown>;
}

export function useChatDmPreferences(channelId: string | null): UseChatDmPreferencesResult {
  const session = useAuthStore((s) => s.session);
  const userId = session?.user?.id ?? null;

  const query = useQuery({
    queryKey: queryKeys.chat.preferences(channelId ?? ''),
    queryFn: async (): Promise<ChatDmPreferencesData> => {
      if (!userId || !channelId) return { isMuted: false };
      const { data, error } = await supabase
        .from('chat_preferences')
        .select('is_muted')
        .eq('user_id', userId)
        .eq('chat_type', 'dm')
        .eq('chat_id', channelId)
        .maybeSingle();
      if (error) throw error;
      return { isMuted: data?.is_muted ?? false };
    },
    enabled: !!userId && !!channelId,
    staleTime: 60_000,
  });

  return {
    data: query.data ?? null,
    isLoading: query.isLoading,
    error: query.error ? (query.error as Error).message : null,
    refetch: query.refetch,
  };
}
```

**1b. Create `src/hooks/__tests__/useChatDmPreferences.test.ts`** (~100-140 LOC) — 3 tests:

1. `channelId === null` → query disabled; `data === null`; `isLoading === false`; `mockFrom` never called
2. `channelId === 'ch-1'` + row returns `{ is_muted: true }` → `data.isMuted === true`
3. `channelId === 'ch-1'` + row returns `null` (no preference row yet) → `data.isMuted === false` (default)

Use the same `mockFrom` factory + `createTestQueryClient` + `useAuthStore` mock pattern as `useFriendProfile.test.ts` (Plan 02).
  </action>
  <verify>
    <automated>npx jest --testPathPatterns="useChatDmPreferences" --no-coverage --runInBand 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/hooks/useChatDmPreferences.ts` exits 0
    - `test -f src/hooks/__tests__/useChatDmPreferences.test.ts` exits 0
    - `grep -c "queryKeys.chat.preferences" src/hooks/useChatDmPreferences.ts` returns at least 1
    - `grep -c "enabled: !!userId && !!channelId" src/hooks/useChatDmPreferences.ts` returns 1
    - `grep -c "from('chat_preferences')" src/hooks/useChatDmPreferences.ts` returns 1
    - `grep -c "chat_type', 'dm'" src/hooks/useChatDmPreferences.ts` returns 1
    - `grep -c "useMutation" src/hooks/useChatDmPreferences.ts` returns 0 (read-only)
    - `npx jest src/hooks/__tests__/useChatDmPreferences.test.ts --runInBand --no-coverage` exits 0
    - The test file contains at least 3 `test(` / `it(` blocks
    - `npx tsc --noEmit -p .` reports zero errors in `src/hooks/useChatDmPreferences.ts`
  </acceptance_criteria>
  <done>Hook reads chat_preferences for the DM channel, disabled when channelId is null, defaults isMuted to false; 3 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite src/app/friends/[id].tsx (full screen integration + photos route + screen test)</name>
  <files>src/app/friends/[id].tsx, src/app/friends/[id]/photos.tsx, src/app/friends/__tests__/[id].test.tsx</files>
  <read_first>
    - src/app/friends/[id].tsx (CURRENT full file — understand what's being replaced)
    - src/hooks/useFriendProfile.ts (Plan 02 SUMMARY shape)
    - src/hooks/useFriendMutuals.ts (Plan 02 SUMMARY shape)
    - src/hooks/useChatDmPreferences.ts (just-written)
    - src/hooks/useFriendWishList.ts (existing — `{ items, loading, error, refetch, toggleClaim }` shape)
    - src/hooks/useExpensesWithFriend.ts (existing — used for IOU balance aggregation)
    - src/hooks/useAllPlanPhotos.ts (existing — used in /friends/[id]/photos.tsx)
    - src/components/friends/FriendProfileHeader.tsx (Plan 03 SUMMARY props)
    - src/components/friends/QuickActionsRow.tsx (Plan 05 SUMMARY props)
    - src/components/friends/GroupedInsetSection.tsx (Plan 04 SUMMARY props)
    - src/components/friends/ProfileInfoRow.tsx (Plan 04 SUMMARY props)
    - src/components/friends/BioRow.tsx (Plan 04 SUMMARY props)
    - src/components/chat/ImageViewerModal.tsx (existing — `{ visible, imageUrl, onClose }`)
    - src/components/common/ErrorDisplay.tsx (existing — `{ message, technicalDetails?, mode?, onRetry? }`)
    - src/components/common/SkeletonPulse.tsx (existing — Phase 24)
    - src/lib/openChat.ts (Phase 30)
    - src/lib/action-sheet.ts (existing)
    - src/screens/chat/ChatListScreen.tsx (lines 99-123 — Mute upsert pattern)
    - src/components/squad/MemoriesTabContent.tsx (analog for /friends/[id]/photos.tsx)
    - .planning/phases/33-friend-profile-redesign/33-PATTERNS.md §16, §17 (photos route + screen rewrite patterns)
    - .planning/phases/33-friend-profile-redesign/33-RESEARCH.md §Technical Approach (full screen wiring) + §Remove Friend (Pattern 5 + invalidation map) + §Recommendations §Friend-not-found fallback (custom 15-LOC inline empty)
    - .planning/phases/33-friend-profile-redesign/33-UI-SPEC.md §Layout Skeleton, §Interaction Contracts, §Loading & Error Contract, §Copywriting Contract, §Stack Screen Header
  </read_first>
  <behavior>
    Screen file (`src/app/friends/[id].tsx`):
    - Mounts `useFriendProfile(friendId)`, `useFriendMutuals(friendId)`, `useFriendWishList(friendId)`, `useExpensesWithFriend(friendId)`, and lazily `useChatDmPreferences(dmChannelId)` (dmChannelId starts null; resolved by Mute press)
    - Owns `scrollY = useSharedValue(0)` + `scrollHandler = useAnimatedScrollHandler({ onScroll: e => { scrollY.value = e.contentOffset.y; } })` at the screen level — passes scrollY to `FriendProfileHeader` AND to the `Stack.Screen.options.headerTitle` AnimatedStackTitle render-prop
    - Renders `<Animated.ScrollView onScroll={scrollHandler} scrollEventThrottle={16}>` containing: FriendProfileHeader → QuickActionsRow → GroupedInsetSection("INFO", [BioRow?, ProfileInfoRow×4]) → GroupedInsetSection("MUTUAL", [ProfileInfoRow×4]) → GroupedInsetSection("WISH LIST", [WishListItems])
    - Stack.Screen options: `{ title: data?.profile?.display_name ?? '', headerTransparent: true, headerTitle: () => <AnimatedStackTitle scrollY={scrollY} displayName={data?.profile?.display_name ?? ''} /> }`
    - `AnimatedStackTitle` is a top-level component in the same file (NOT inline arrow — per RESEARCH §Risks #1 + UI-SPEC §Reanimated Implementation Notes #2)
    - Quick action handlers:
      - `onMessage`: `await openChat(router, { kind: 'dmFriend', friendId, friendName: profile.display_name })`
      - `onToggleMute`: lazy-resolve dmChannelId via `supabase.rpc('get_or_create_dm_channel', { p_friend_id: friendId })` if null; then run Mute mutation (canonical Pattern 5 — optimistic flip on `queryKeys.chat.preferences(channelId)`; invalidate on settle + invalidate `queryKeys.chat.list(myId)` so chat list icon stays consistent)
      - `onPhotos`: `router.push(\`/friends/${friendId}/photos\`)`
      - `onMore`: `showActionSheet(displayName, [{ label: 'Remove Friend', destructive: true, onPress: handleConfirmRemove }])` — helper auto-appends Cancel
    - `handleConfirmRemove`: `Alert.alert("Remove Friend", "Remove {displayName} from your friends? You can add them again by searching their username.", [{ text: "Remove", style: "destructive", onPress: () => removeMutation.mutate() }, { text: "Cancel", style: "cancel" }])`
    - Remove mutation: canonical Pattern 5 — `mutationFn: supabase.from('friendships').delete().or(...)`; `onMutate`: optimistic splice of `queryKeys.friends.list(myId)`; `onError`: rollback + `Alert.alert('Error', "Couldn't remove friend. Try again.")`; `onSettled`: invalidate `friends.list(myId)` + `home.friends(myId)` + `home.pendingRequestCount(myId)`; on success: `router.back()`
    - Big avatar onPress: `setAvatarViewerOpen(true)` ONLY when `data.profile.avatar_url !== null`
    - `<ImageViewerModal visible={avatarViewerOpen} imageUrl={profile?.avatar_url ?? null} onClose={() => setAvatarViewerOpen(false)} />`
    - Loading state: `if (isLoading && !data) return <SkeletonShells />` (in-file shape per UI-SPEC §Skeleton)
    - Error state: `if (error) return <ErrorDisplay mode="screen" message="Couldn't load profile. Check your connection and try again." onRetry={refetch} />`
    - Friend-not-found state: `if (!isLoading && data && data.friendsSince === null) return <NotFriendsView />` — inline ~15 LOC component with `<Ionicons name="people-outline" size={48} />`, "No longer friends" heading, "This person isn't in your friends list anymore." body, `<PrimaryButton title="Back to friends" onPress={() => router.back()} />`
    - IOU balance row value: aggregate from `useExpensesWithFriend(friendId).expenses` per RESEARCH §Code-level Confirmations #5 recommendation (a) — compute `balanceCents` + direction, format per UI-SPEC §Copywriting Contract row IOU balance
    - INFO section rows (conditional render):
      - Bio: `{ profile.bio && <BioRow bio={profile.bio} /> }`
      - Friends since: always (when friendsSince !== null, format `Since {Mon} {YYYY}` via `Date(friendsSince).toLocaleDateString` or moment-equivalent)
      - Birthday: `{ profile.birthday_month && profile.birthday_day && <ProfileInfoRow iconTint="birthday" label="Birthday" value={formatBirthday(...)} /> }`
      - Timezone: `{ profile.timezone && <ProfileInfoRow iconTint="timezone" label="Timezone" value={profile.timezone} /> }`
    - MUTUAL section rows (always render all 4; value displays "None yet" + non-tappable when count === 0):
      - Mutual plans: `<ProfileInfoRow iconTint="mutualPlans" label="Mutual plans" value={mutualPlansCount > 0 ? String(mutualPlansCount) : "None yet"} onPress={mutualPlansCount > 0 ? () => router.push(...) : undefined} chevron={mutualPlansCount > 0} />` — destination route is researcher's call (no spec'd route exists yet; safe to defer — use a no-op `() => {}` and add a TODO comment if unsure)
      - Mutual friends: same pattern
      - Shared photos: same pattern → `router.push(\`/friends/${friendId}/photos\`)` when count > 0
      - IOU balance: tap navigates to existing per-friend expenses screen (researcher's call per RESEARCH §Open Q — preserve current navigation if it exists in the current `[id].tsx`)
    - WISH LIST section: wrap existing `useFriendWishList` rendering in `<GroupedInsetSection title="WISH LIST">` using `WishListItem` components as direct children — note WishListItem may not accept `isLast`; if not, render them inside an inner wrapper so GroupedInsetSection's React.Children injection still aligns with the visual separators (or skip the auto-suppression for this section). Document the trade-off in SUMMARY.

    Photos route (`src/app/friends/[id]/photos.tsx`):
    - Mounts `useFriendMutuals(friendId)` to get `sharedPlanIds` + `useAllPlanPhotos()` to get the full groups list; filter `groups.filter(g => sharedPlanIds.includes(g.planId))`
    - Renders Memories-style grid (analog: `MemoriesTabContent.tsx`); empty state when filtered groups is empty
    - Stack.Screen title: `Shared photos`
    - Loading + error states match MemoriesTabContent pattern

    Screen test (`src/app/friends/__tests__/[id].test.tsx`):
    - Mock `useFriendProfile`, `useFriendMutuals`, `useFriendWishList`, `useExpensesWithFriend`, `useChatDmPreferences` — return shapes
    - Mock `openChat`, `showActionSheet`, supabase, expo-router (router.back / router.push)
    - Test cases (cover REQ-FP-06 / 07 / 09 / 11 / 12):
      - **REQ-FP-07 happy path:** mocks return full data; renders Display name + Username + Bio + Friends since + Birthday + Timezone + all 4 MUTUAL rows; "Mutual plans" shows count when > 0
      - **REQ-FP-07 sparse profile:** mocks return profile without bio/birthday/timezone — INFO section renders ONLY Friends since row; MUTUAL renders all 4 rows with "None yet"
      - **REQ-FP-06 Message:** tap Message button → `openChat` called with `{ kind: 'dmFriend', friendId, friendName }`
      - **REQ-FP-06 More:** tap More button → `showActionSheet` called with single 'Remove Friend' destructive item (helper auto-appends Cancel — assert items array length 1)
      - **REQ-FP-09 Avatar viewer:** tap big avatar (when avatar_url present) → `ImageViewerModal` `visible` flips true; tap close → flips false
      - **REQ-FP-09 Avatar viewer absent:** when `avatar_url === null`, tap big avatar → no modal opens (onAvatarPress not wired)
      - **REQ-FP-11 Remove Friend optimistic + rollback:** mock `removeMutation` flow → assert `queryClient.setQueryData(queryKeys.friends.list(myId), ...)` called with filtered list; mock supabase delete error → assert rollback restores original list
      - **REQ-FP-12 Friend-not-found:** mock useFriendProfile to return `data: { friendsSince: null, profile: {...} }` → renders "No longer friends" heading; tap CTA → `router.back()` called
  </behavior>
  <action>
This is the longest task in the phase. Be methodical. Order:

1. Read the CURRENT `src/app/friends/[id].tsx` end-to-end before deleting anything — capture: the WishListItem render shape, the Stack.Screen header customization, any haptic uses, the existing Remove Friend confirmation copy (must be preserved VERBATIM per UI-SPEC §Copywriting Contract)
2. Write `src/hooks/useChatDmPreferences.ts` first if not already done by Task 1 (it is — confirm)
3. Author the rewritten `src/app/friends/[id].tsx`:
   - Add a single `AnimatedStackTitle` top-level component to the same file (NOT a named export — file-private)
   - Add a single `NotFriendsView` inline component (~15 LOC) — file-private
   - Add a single `SkeletonShells` inline component for the loading state — file-private
   - Compose the screen body with proper Animated.ScrollView wiring
   - Wire all 4 quick-action handlers + the Remove Friend Pattern 5 mutation
   - The remove mutation MUST satisfy mutationShape gate — include all four literals (`mutationFn`, `onMutate`, `onError`, `onSettled`) OR carry an `// @mutationShape: no-optimistic` comment with rationale (canonical Pattern 5 is preferred — RESEARCH §Remove Friend supplies the exact shape)
   - Wire `Stack.Screen options={{ title, headerTransparent: true, headerTitle: () => <AnimatedStackTitle ... /> }}`
4. Author `src/app/friends/[id]/photos.tsx`:
   - Stack.Screen title: 'Shared photos'
   - Loading skeleton: `<ActivityIndicator>` matching MemoriesTabContent
   - Empty state: `<View><Ionicons name="images-outline" size={48} /><Text>No shared photos yet</Text></View>` (UI-SPEC mandates "None yet" in MUTUAL rows; this is the dedicated screen — pick descriptive copy)
   - Filter logic: `const sharedSet = new Set(mutuals?.sharedPlanIds ?? []); const filteredGroups = groups.filter(g => sharedSet.has(g.planId));`
   - Reuse `GalleryViewerModal` for full-screen photo view (NOT `ImageViewerModal` — gallery has the swipe-between feature; check `src/components/plans/GalleryViewerModal.tsx` exists; if it doesn't, fall back to mounting `MemoriesTabContent`'s render path directly)
5. Author the screen test:
   - Use `@testing-library/react-native` `render` + `fireEvent` + `screen.getByText` / `getByRole`
   - Mock the 5 hooks at the module boundary (`jest.mock`)
   - Mock `expo-router` for `useLocalSearchParams` + `useRouter`
   - Mock `@/lib/openChat` + `@/lib/action-sheet` + `@/lib/supabase`
   - At least 7 `test(` blocks per the behavior list above
   - File size ~250-400 LOC

The TDD heuristic for this task: write the screen test BEFORE the screen — it's the only way to know your contract upfront for 7 testable behaviors.
  </action>
  <verify>
    <automated>npx jest --testPathPatterns="friends/__tests__/\[id\]" --no-coverage --runInBand 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/app/friends/[id].tsx` exits 0 (rewrite landed)
    - `test -f src/app/friends/[id]/photos.tsx` exits 0 (new route)
    - `test -f src/app/friends/__tests__/[id].test.tsx` exits 0 (screen test)
    - **Inline fetch eliminated:** `grep -c "useState\\|useEffect" src/app/friends/[id].tsx` may be > 0 but `grep -c "from('profiles')" src/app/friends/[id].tsx` returns 0 — the raw supabase profile select MUST be gone (replaced by useFriendProfile)
    - `grep -c "useFriendProfile" src/app/friends/[id].tsx` returns at least 2 (import + use)
    - `grep -c "useFriendMutuals" src/app/friends/[id].tsx` returns at least 2 (import + use)
    - `grep -c "useChatDmPreferences" src/app/friends/[id].tsx` returns at least 2 (import + use)
    - `grep -c "Animated.ScrollView" src/app/friends/[id].tsx` returns 1
    - `grep -c "useAnimatedScrollHandler" src/app/friends/[id].tsx` returns 1
    - `grep -c "useSharedValue(0)" src/app/friends/[id].tsx` returns 1 (parent owns scrollY)
    - `grep -c "FriendProfileHeader" src/app/friends/[id].tsx` returns at least 2 (import + JSX)
    - `grep -c "QuickActionsRow" src/app/friends/[id].tsx` returns at least 2 (import + JSX)
    - `grep -c "GroupedInsetSection" src/app/friends/[id].tsx` returns at least 4 (import + 3 sections: INFO, MUTUAL, WISH LIST)
    - `grep -c "ProfileInfoRow" src/app/friends/[id].tsx` returns at least 5 (import + 4 INFO/MUTUAL rows)
    - `grep -c "BioRow" src/app/friends/[id].tsx` returns at least 2 (import + conditional render)
    - `grep -c "ImageViewerModal" src/app/friends/[id].tsx` returns at least 2 (import + JSX)
    - `grep -c "openChat(router" src/app/friends/[id].tsx` returns at least 1 (Message action)
    - `grep -c "showActionSheet(" src/app/friends/[id].tsx` returns at least 1 (More action)
    - `grep -c "Remove Friend" src/app/friends/[id].tsx` returns at least 2 (showActionSheet item label + Alert title)
    - `grep -c "destructive: true" src/app/friends/[id].tsx` returns at least 1 (ActionSheet item)
    - `grep -c "router.back()" src/app/friends/[id].tsx` returns at least 2 (NotFriendsView CTA + Remove Friend on success)
    - `grep -c "ErrorDisplay" src/app/friends/[id].tsx` returns at least 2 (import + use in error state)
    - **Friend-not-found uses `friendsSince === null`** (NOT `profile === null` per PATTERNS §Corrections row 8): `grep -c "friendsSince === null\\|friendsSince == null\\|!data.friendsSince" src/app/friends/[id].tsx` returns at least 1
    - **Remove mutation passes mutationShape gate:** `grep -c "mutationFn\\|onMutate\\|onError\\|onSettled" src/app/friends/[id].tsx | head -1` is at least 4
    - `grep -c "from('friendships').*delete\\|from('friendships').delete\\|.from(\"friendships\")" src/app/friends/[id].tsx` returns at least 1 (delete present)
    - `grep -c "queryKeys.friends.list" src/app/friends/[id].tsx` returns at least 1 (Pattern 5 optimistic splice key)
    - `grep -c "queryKeys.home.friends" src/app/friends/[id].tsx` returns at least 1 (invalidation on settle)
    - `grep -c "queryKeys.chat.preferences" src/app/friends/[id].tsx` returns at least 1 (Mute mutation key OR consumed via useChatDmPreferences)
    - `grep -c "get_or_create_dm_channel" src/app/friends/[id].tsx` returns at least 1 (lazy DM resolution for Mute)
    - `grep -c "useFriendsOfFriend\\|useFriends" src/app/friends/[id].tsx` returns at least 1 (mounted somewhere on screen to warm friends.list / friends.ofFriend caches per Plan 02 SUMMARY — required for useFriendMutuals.mutualFriendsCount to be non-zero on first load)
    - `grep -c "AnimatedStackTitle" src/app/friends/[id].tsx` returns at least 2 (declaration + use)
    - `grep -c "NotFriendsView\\|No longer friends" src/app/friends/[id].tsx` returns at least 1
    - **Photos route:** `grep -c "useAllPlanPhotos\\|useFriendMutuals" src/app/friends/[id]/photos.tsx` returns at least 2 (both hooks mounted)
    - `grep -c "sharedPlanIds" src/app/friends/[id]/photos.tsx` returns at least 2 (read + use)
    - **Screen test:** `npx jest src/app/friends/__tests__/[id].test.tsx --runInBand --no-coverage` exits 0
    - The screen test contains at least 7 `test(` / `it(` blocks
    - `grep -c "friendsSince: null\\|friendsSince === null" src/app/friends/__tests__/[id].test.tsx` returns at least 1 (REQ-FP-12 test)
    - `grep -c "openChat" src/app/friends/__tests__/[id].test.tsx` returns at least 1 (REQ-FP-06 Message test)
    - `grep -c "showActionSheet" src/app/friends/__tests__/[id].test.tsx` returns at least 1 (REQ-FP-06 More test)
    - `grep -c "ImageViewerModal\\|visible.*true" src/app/friends/__tests__/[id].test.tsx` returns at least 1 (REQ-FP-09 viewer test)
    - **Full suite green:** `npm test` exits 0
    - **mutationShape gate green:** `npx jest src/hooks/__tests__/mutationShape.test.ts --no-coverage --runInBand` exits 0 (Remove Friend mutation in the screen file satisfies the gate's canonical-shape OR exemption-marker requirement — note: the gate scans `src/hooks/` only; a mutation INSIDE the screen file does NOT trigger the gate. If the planner moves the Remove Friend mutation into a hook (`src/hooks/useRemoveFriend.ts`), the gate WILL scan it — in that case Pattern 5 literal strings are required.)
    - **tsc green:** `npx tsc --noEmit -p .` reports zero new errors in any of the three new files
  </acceptance_criteria>
  <done>Screen rewrite complete; photos route shipped; screen test green covering REQ-FP-06/07/09/11/12; all data hooks composed; mutations follow Pattern 5; ErrorDisplay used with actual API; friend-not-found inline view with correct detection logic (friendsSince null); tsc green; full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [CHECKPOINT] Manual hardware smoke for animation + modal + haptic behaviors</name>
  <files>(none — human verification only; record outcomes in 33-06-SUMMARY.md)</files>
  <action>
HUMAN VERIFICATION GATE. This task does NOT produce code. Claude pauses here and the human operator runs through the 8-step hardware smoke described in <how-to-verify> below on a real iPhone (dev client or TestFlight build — simulator cannot exercise haptics). On approval, Claude records the outcomes in 33-06-SUMMARY.md and continues. On failure, Claude stops and routes a fix-up plan via /gsd-plan-phase 33 --gaps.
  </action>
  <what-built>
The friend profile redesign — header collapse animation, blurred-avatar wash render with cross-fade on load, four quick-action buttons with per-button haptic feedback, ImageViewerModal swipe-down dismiss. These are visual/native behaviors that jsdom + jest cannot reliably exercise per RESEARCH §Validation Architecture.
  </what-built>
  <how-to-verify>
On a real iPhone (Expo dev client or TestFlight build — NOT simulator; haptics are real-hardware-only):

1. **Cold-start to a friend with a populated profile (avatar + bio + birthday + timezone + mutual plans/friends/photos):**
   - Navigate Home → tap a friend → friend profile screen opens
   - Confirm large 140pt avatar visible at top; name + username + status pill below
   - Confirm blurred-avatar wash is visible behind avatar; should appear with a smooth cross-fade as the avatar decodes (~300ms fade-in)
   - Confirm INFO section shows: Bio (collapsed to 3 lines if long), Friends since, Birthday, Timezone — with colored leading icons (Bio=blue, FriendsSince=neon-green, Birthday=pink, Timezone=sky-blue)
   - Confirm MUTUAL section shows all 4 rows with counts and tappable chevrons

2. **Header collapse animation (REQ-FP-04):**
   - Slowly scroll the screen down
   - Avatar should smoothly shrink from 140pt to ~32pt as scrollY goes from 0 → 160
   - Display name + status pill fade out during the collapse
   - Stack header (top nav bar) title fades IN simultaneously
   - No jank, no flicker, no double-render of the title

3. **Blurred wash render (REQ-FP-05):**
   - Wash should be visible behind the avatar circle, NOT fully covering the avatar
   - Wash should fade out as you scroll past WASH_FADE_END (~220px scroll)
   - Test with a friend who has NO avatar — wash falls back to solid surface.card color (no broken image)

4. **Quick actions row + haptics (REQ-FP-06):**
   - Tap Message → light haptic (gentle tap) + opens DM
   - Tap Mute (when unmuted) → selection haptic (click) + button label flips to "Unmute"; verify by re-opening the chat list — DM should show muted indicator
   - Tap Unmute → selection haptic + label flips back to "Mute"
   - Tap Photos → light haptic + navigates to `/friends/[id]/photos`
   - Tap More → selection haptic + native ActionSheet opens with "Remove Friend" (red) + "Cancel"
   - Press scale: every button should scale slightly (0.96) on press-in, spring back on press-out

5. **Full-screen avatar viewer (REQ-FP-09):**
   - Tap the big 140pt avatar (NOT scrolled — at the top of the screen)
   - ImageViewerModal opens with the full avatar
   - Swipe down or tap close → modal dismisses
   - Pinch-to-zoom should work inside the modal

6. **Remove Friend flow (REQ-FP-11):**
   - Tap More → "Remove Friend" → second confirmation Alert appears
   - Tap "Remove" (destructive) → warning haptic + screen pops back to Friends list
   - Verify the friend is gone from the list (optimistic removal)
   - To test rollback: enable airplane mode, tap Remove Friend → confirm Alert → the screen should pop back BUT the friend should re-appear in the Friends list after a moment when the DELETE fails (rollback)

7. **Friend-not-found (REQ-FP-12) — can be skipped if no easy way to trigger:**
   - In practice this fires when the OTHER party removes you while you're viewing their profile, OR you deep-link to a stranger's id
   - If reproducible: confirm the "No longer friends" inline view with the "Back to friends" CTA

8. **Reduced Motion (UI-SPEC §Reduced Motion Path):**
   - iOS Settings → Accessibility → Motion → enable "Reduce Motion"
   - Re-open the friend profile screen
   - Avatar should be statically rendered at the COLLAPSED state (32pt invisible avatar; the Stack nav title shows the full name from the start)
   - Tap quick actions — no scale spring, no haptic
   - Disable Reduce Motion again; behavior returns to normal

**If ANY step fails:** record the exact behavior in the SUMMARY and stop. Do NOT mark the checkpoint approved.
  </how-to-verify>
  <resume-signal>Type "approved" once all 8 steps pass on hardware. If any step fails, describe the failure and stop — the planner will route a fix-up plan.</resume-signal>
  <verify>Human verification only — no automated check. Outcomes recorded in 33-06-SUMMARY.md "Manual Verification" section. /gsd-verify-work scans the SUMMARY for the explicit "Hardware smoke: APPROVED" marker.</verify>
  <done>All 8 hardware smoke steps pass on real iPhone hardware. SUMMARY.md "Manual Verification" section records the marker "Hardware smoke: APPROVED 2026-XX-XX by <operator>".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Supabase delete on `friendships` | Pattern 5 mutation; RLS gates by `friendships_delete_participant USING (auth.uid() = requester_id OR auth.uid() = addressee_id)` |
| Client → Supabase upsert on `chat_preferences` | Mute write; RLS gates by `auth.uid() = user_id` |
| Client → Supabase RPC `get_or_create_dm_channel` | Existing RPC; creates an empty DM channel row if none exists |
| Screen → ImageViewerModal | Full-screen modal; passes `avatar_url` as `imageUrl` prop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-21 | Tampering (XSS) | Bio renders user-controlled text in `<BioRow>` | mitigate | React Native `<Text>` auto-escapes — no `dangerouslySetInnerHTML` equivalent. Bio.length capped at 160 client-side (Plan 07). RESEARCH §Threats #1 + #16. |
| T-33-22 | Tampering | Remove Friend race — both parties delete simultaneously | mitigate | `friendships_delete_participant` policy permits either side; second DELETE matches zero rows (no-op). Optimistic local splice + `router.back()` mask the race per RESEARCH §Threats #3. |
| T-33-23 | Information Disclosure | `(supabase as any)` cast on `bio` SELECT could bypass typed RLS scrutiny | accept | RLS is the security boundary; the cast is purely a TypeScript ergonomics workaround. `profiles_select_authenticated USING (true)` is the SELECT policy — bio is no more sensitive than `display_name`/`avatar_url` which are already exposed by the same policy. RESEARCH §Code-level Confirmations #14. |
| T-33-24 | DoS | Rapid double-tap on Remove Friend triggers two DELETEs | mitigate | `removeMutation.isPending` disables the More button while in-flight (UI guard) AND server-side second DELETE is a no-op (DELETE matches zero rows after the first succeeds). |
| T-33-25 | DoS | Lazy DM channel creation on Mute press creates orphan DM rows | accept | One empty DM channel per friend pair max; harmless. Matches existing `openChat()` Phase-30 pattern. |
| T-33-26 | Information Disclosure | ImageViewerModal exposes full-resolution avatar to anyone tapping it | accept | The avatar is already SELECT-able by any auth'd user via the existing `profiles.avatar_url` column. The viewer just renders what's already accessible. |
| T-33-27 | Tampering | Remove Friend mutation skipped from mutationShape gate (mutation lives in screen file, not in `src/hooks/`) | mitigate | Acceptance criteria requires the four literal strings be present in the screen file (defensive). If the mutation is extracted to a hook later, the gate auto-catches drift. |
</threat_model>

<verification>
## Plan-level checks

1. Inline supabase fetch removed: `grep -c "from('profiles').*select" src/app/friends/[id].tsx` returns 0
2. All 3 new files exist with correct grep signals (see Task 2 acceptance criteria)
3. Screen test green: `npx jest src/app/friends/__tests__/[id].test.tsx --runInBand --no-coverage` exits 0
4. tsc green: `npx tsc --noEmit -p .` reports zero new errors
5. Full suite green: `npm test` exits 0
6. mutationShape gate green: `npx jest src/hooks/__tests__/mutationShape.test.ts --no-coverage --runInBand` exits 0
7. Manual hardware smoke approved by user (Task 3 checkpoint)

## Cross-plan verification

After Plans 01-06 land:
- Every D-XX from CONTEXT.md is covered: D-01..D-16 (D-10 deferred — no work; D-14 covered by Plan 07)
- Every REQ-FP-XX from VALIDATION.md is covered: REQ-FP-01..REQ-FP-12 + GATE-mutationShape + GATE-queryKeys
- VALIDATION.md `nyquist_compliant` can flip to `true` once Plans 01-07 ship green
</verification>

<success_criteria>
- [ ] `src/app/friends/[id].tsx` rewritten — no inline `supabase.from('profiles').select(...)` remains
- [ ] All 7 hooks composed (useFriendProfile, useFriendMutuals, useFriendWishList, useExpensesWithFriend, useChatDmPreferences, useFriends/useFriendsOfFriend for cache-warming, useStatus is read via the shared slice)
- [ ] Parent-owned `scrollY` shared by `FriendProfileHeader` AND `Stack.Screen.headerTitle`
- [ ] All 4 quick-action handlers wired correctly with correct copy + haptic
- [ ] Remove Friend Pattern 5 mutation lands (with the 4 literal strings present)
- [ ] ImageViewerModal opens on big-avatar tap when avatar_url present; non-interactive when null
- [ ] Loading / error / friend-not-found states render with the locked copy
- [ ] `/friends/[id]/photos.tsx` route mounts useAllPlanPhotos filtered to sharedPlanIds; renders Memories-style grid
- [ ] `useChatDmPreferences` hook lands with 3 tests green
- [ ] Screen test covers REQ-FP-06/07/09/11/12 with at least 7 test blocks
- [ ] tsc green; full suite green; mutationShape gate green
- [ ] Manual hardware smoke checkpoint approved (REQ-FP-04/05/06/09 + Reduced Motion verified)
</success_criteria>

<output>
After completion, create `.planning/phases/33-friend-profile-redesign/33-06-SUMMARY.md` capturing:
- File counts: 3 new files + 0 modified (the rewrite of `src/app/friends/[id].tsx` is treated as "rewrite" — call it out explicitly so reviewers know to git-diff for full context)
- The full Quick Action wiring: 4 onPress handlers + their data dependencies + their cache invalidations
- The Remove Friend mutation shape (verbatim — the 4 literal strings must be visible in the SUMMARY so future plans see the contract)
- The friend-not-found detection key: `data?.friendsSince === null` (NOT `profile === null` — locked correction to CONTEXT D-16)
- Which D-XX and REQ-FP-XX this plan closed (cross-reference for the VALIDATION.md sign-off)
- Manual smoke result for Task 3: which hardware behaviors passed, which (if any) needed adjustment
- Note any TODO for future polish: mutual-plans / mutual-friends destination routes (no list screens exist yet — placeholder no-op for now per RESEARCH discretion)
</output>
