---
phase: 13-profile-rework-friend-profile
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/profile/edit.tsx
  - src/app/profile/wish-list.tsx
  - src/app/profile/_layout.tsx
autonomous: true
requirements:
  - PROF-03

must_haves:
  truths:
    - "edit.tsx is stripped to display name field, read-only @username display, and BirthdayPicker only"
    - "Avatar section and wish list section are absent from edit.tsx"
    - "isDirty in edit.tsx compares only displayName, birthdayMonth, birthdayDay, birthdayYear (no avatarUrl)"
    - "handleSave in edit.tsx does NOT include avatar_url in the profiles UPDATE payload"
    - "wish-list.tsx exists as a new screen with add/delete wish list item functionality"
    - "profile/_layout.tsx registers the wish-list screen with headerShown: false"
  artifacts:
    - path: "src/app/profile/edit.tsx"
      provides: "Stripped detail-only editor"
      contains: "usernameValue"
    - path: "src/app/profile/wish-list.tsx"
      provides: "Wish list management screen"
      exports: ["default function WishListScreen"]
    - path: "src/app/profile/_layout.tsx"
      provides: "Profile group layout with wish-list registered"
      contains: "wish-list"
  key_links:
    - from: "src/app/profile/edit.tsx isDirty"
      to: "displayName/birthday fields only"
      via: "comparison expression"
      pattern: "isDirty.*displayName.*birthday"
    - from: "src/app/profile/wish-list.tsx"
      to: "useMyWishList hook"
      via: "hook call"
      pattern: "useMyWishList"
    - from: "src/app/profile/_layout.tsx"
      to: "wish-list screen"
      via: "Stack.Screen name"
      pattern: "name.*wish-list"
---

<objective>
Strip edit.tsx to a text-only details editor (display name, read-only username, birthday), create the new wish-list.tsx screen with the moved wish list CRUD, and register wish-list in the profile group layout.

Purpose: Delivers the edit-path side of PROF-03 (D-04, D-05, D-06, D-07, D-08). The wish list code moves verbatim from edit.tsx to wish-list.tsx — no logic changes, just relocation. edit.tsx becomes a focused detail editor without avatar or wish list concerns.

Output: Modified `src/app/profile/edit.tsx`, new `src/app/profile/wish-list.tsx`, modified `src/app/profile/_layout.tsx`.
</objective>

<execution_context>
@/Users/iulian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iulian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-profile-rework-friend-profile/13-CONTEXT.md
@.planning/phases/13-profile-rework-friend-profile/13-UI-SPEC.md
@.planning/phases/13-profile-rework-friend-profile/13-PATTERNS.md

<interfaces>
<!-- Hooks and components used in these screens. Extracted from codebase. -->

From src/hooks/useMyWishList.ts:
```typescript
export interface WishListItem {
  id: string;
  title: string;
  url: string | null;
  notes: string | null;
  created_at: string;
}

export function useMyWishList(): {
  items: WishListItem[];
  loading: boolean;
  error: string | null;
  refetch: () => Promise<void>;
  addItem: (title: string, url?: string, notes?: string) => Promise<{ error: PostgrestError | null }>;
  deleteItem: (itemId: string) => Promise<{ error: PostgrestError | null }>;
}
```

From src/components/common/BirthdayPicker.tsx (component remains in edit.tsx):
```typescript
// Props:
interface BirthdayPickerProps {
  month: number | null;
  day: number | null;
  year: number | null;
  onChange: (month: number | null, day: number | null, year: number | null) => void;
  disabled?: boolean;
}
```

From src/app/profile/edit.tsx — wish list JSX to move verbatim (lines 276-340):
```typescript
// Wish list items map
{wishListItems.map((item) => (
  <View key={item.id} style={styles.wishListRow}>
    <View style={styles.wishListItemContent}>
      <Text style={styles.wishListItemTitle} numberOfLines={2}>{item.title}</Text>
      {item.url ? (
        <Text style={styles.wishListItemUrl} numberOfLines={1}>{item.url}</Text>
      ) : null}
      {item.notes ? (
        <Text style={styles.wishListItemNotes} numberOfLines={2}>{item.notes}</Text>
      ) : null}
    </View>
    <TouchableOpacity
      onPress={() => void deleteItem(item.id)}
      style={styles.deleteWishItem}
      accessibilityLabel={`Delete ${item.title}`}
    >
      <Text style={styles.deleteWishItemText}>Remove</Text>
    </TouchableOpacity>
  </View>
))}

{wishListItems.length === 0 && (
  <Text style={styles.wishListEmpty}>No wish list items yet.</Text>
)}

// Add item form (lines 302-340)
<TextInput style={[styles.textInput, styles.wishItemInput]} value={newItemTitle} onChangeText={setNewItemTitle}
  placeholder="Item title (required)" placeholderTextColor={COLORS.text.secondary} maxLength={120} editable={!addingWishItem} />
<TextInput style={[styles.textInput, styles.wishItemInput]} value={newItemUrl} onChangeText={setNewItemUrl}
  placeholder="Link (optional)" placeholderTextColor={COLORS.text.secondary} maxLength={500}
  editable={!addingWishItem} keyboardType="url" autoCapitalize="none" />
<TextInput style={[styles.textInput, styles.wishItemInput]} value={newItemNotes} onChangeText={setNewItemNotes}
  placeholder="Notes (optional)" placeholderTextColor={COLORS.text.secondary} maxLength={200} editable={!addingWishItem} />
<TouchableOpacity
  style={[styles.addWishItemButton, (!newItemTitle.trim() || addingWishItem) && styles.addWishItemButtonDisabled]}
  onPress={handleAddWishItem}
  disabled={!newItemTitle.trim() || addingWishItem}
  accessibilityLabel="Add wish list item"
>
  <Text style={styles.addWishItemButtonText}>{addingWishItem ? 'Adding...' : 'Add Item'}</Text>
</TouchableOpacity>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Strip edit.tsx to details-only editor (D-03, D-05, D-06, D-08, PROF-03)</name>
  <read_first>
    - src/app/profile/edit.tsx (full current file — understand what to remove and keep)
    - .planning/phases/13-profile-rework-friend-profile/13-PATTERNS.md (edit.tsx strip instructions)
    - .planning/phases/13-profile-rework-friend-profile/13-RESEARCH.md (Pitfall 5 — isDirty fix; Pitfall 6 — username fetch)
  </read_first>
  <files>src/app/profile/edit.tsx</files>
  <action>
    Rewrite `src/app/profile/edit.tsx` to the stripped-down details editor. The result must contain ONLY:
    - Display name TextInput (unchanged)
    - Read-only @username display (new — D-06)
    - BirthdayPicker (unchanged)
    - Save Changes PrimaryButton (updated isDirty, updated handleSave payload)

    **Step 1 — Remove these imports entirely:**
    ```typescript
    import { decode } from 'base64-arraybuffer';
    import * as ImagePicker from 'expo-image-picker';
    import { AvatarCircle } from '@/components/common/AvatarCircle';
    import { useMyWishList } from '@/hooks/useMyWishList';
    ```

    **Step 2 — Remove these state declarations:**
    ```typescript
    const [avatarUrl, setAvatarUrl] = useState<string | null>(null);
    const [originalAvatarUrl, setOriginalAvatarUrl] = useState<string | null>(null);
    const [avatarLoading, setAvatarLoading] = useState(false);
    const { items: wishListItems, addItem, deleteItem } = useMyWishList();
    const [addingWishItem, setAddingWishItem] = useState(false);
    const [newItemTitle, setNewItemTitle] = useState('');
    const [newItemUrl, setNewItemUrl] = useState('');
    const [newItemNotes, setNewItemNotes] = useState('');
    ```

    **Step 3 — Add username state** (after existing birthday state declarations):
    ```typescript
    const [username, setUsername] = useState<string | null>(null);
    ```

    **Step 4 — Update the profiles SELECT** to add `username` (currently at line 57, currently selects `display_name, avatar_url, birthday_month, birthday_day, birthday_year`):
    ```typescript
    supabase
      .from('profiles')
      .select('display_name, avatar_url, birthday_month, birthday_day, birthday_year, username')
      .eq('id', session.user.id)
      .single()
    ```
    In the `.then()` callback, add:
    ```typescript
    setUsername(data.username ?? null);
    ```
    Also remove these lines from the callback (avatar state no longer exists):
    ```typescript
    setAvatarUrl(data.avatar_url ?? null);
    setOriginalAvatarUrl(data.avatar_url ?? null);
    ```

    **Step 5 — Remove the `uploadAvatar` and `handleChangeAvatar` functions entirely** (lines 77-141). These have already moved to profile.tsx in Plan 01.

    **Step 6 — Fix isDirty to remove avatarUrl comparison (Pitfall 5):**
    BEFORE:
    ```typescript
    const isDirty =
      displayName.trim() !== originalDisplayName ||
      avatarUrl !== originalAvatarUrl ||
      birthdayMonth !== originalBirthdayMonth ||
      birthdayDay !== originalBirthdayDay ||
      birthdayYear !== originalBirthdayYear;
    ```
    AFTER:
    ```typescript
    const isDirty =
      displayName.trim() !== originalDisplayName ||
      birthdayMonth !== originalBirthdayMonth ||
      birthdayDay !== originalBirthdayDay ||
      birthdayYear !== originalBirthdayYear;
    ```

    **Step 7 — Update handleSave to remove avatar_url from the update payload:**
    REMOVE the line `avatar_url: avatarUrl,` from the supabase profiles.update() call. The update payload becomes:
    ```typescript
    {
      display_name: displayName.trim(),
      birthday_month: finalMonth,
      birthday_day: finalDay,
      birthday_year: finalMonth !== null ? birthdayYear : null,
      updated_at: new Date().toISOString(),
    }
    ```

    **Step 8 — Remove the Avatar section JSX** (the `<View style={styles.avatarSection}>` block — lines 219-243). Delete the entire block.

    **Step 9 — Remove the Wish List section JSX** (from `<Text style={styles.birthdayLabel}>My Wish List</Text>` through the closing `</TouchableOpacity>` of "Add Item" — lines 273-340). Delete the entire block.

    **Step 10 — Add read-only username display (D-06):**
    Insert BETWEEN the char count text and the birthday label:
    ```typescript
    <Text style={styles.fieldLabel}>Username</Text>
    <Text style={styles.usernameValue}>@{username ?? ''}</Text>
    ```

    **Step 11 — Add new styles for the username display and remove unused styles:**
    Add to StyleSheet:
    ```typescript
    fieldLabel: {
      fontSize: FONT_SIZE.md,
      fontWeight: FONT_WEIGHT.regular,
      color: COLORS.text.secondary,
      marginTop: SPACING.xl,
      marginBottom: SPACING.sm,
    },
    usernameValue: {
      fontSize: FONT_SIZE.lg,
      fontWeight: FONT_WEIGHT.regular,
      color: COLORS.text.secondary,
      paddingHorizontal: SPACING.lg,
    },
    ```
    Remove these styles that are no longer needed (no JSX references them):
    `avatarSection`, `avatarWrapper`, `avatarOverlay`, `changePhotoButton`, `changePhotoText`,
    `wishListRow`, `wishListItemContent`, `wishListItemTitle`, `wishListItemUrl`, `wishListItemNotes`,
    `wishListEmpty`, `wishItemInput`, `deleteWishItem`, `deleteWishItemText`,
    `addWishItemButton`, `addWishItemButtonDisabled`, `addWishItemButtonText`

    Also remove `ActivityIndicator` from the react-native imports (no longer used after avatar section removed).

    Run `npx tsc --noEmit` and fix all errors.
  </action>
  <verify>
    <automated>cd /Users/iulian/Develop/campfire && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/profile/edit.tsx` does NOT contain `AvatarCircle` (import and usage removed)
    - `src/app/profile/edit.tsx` does NOT contain `useMyWishList` (import and usage removed)
    - `src/app/profile/edit.tsx` does NOT contain `avatarUrl` (state removed)
    - `src/app/profile/edit.tsx` does NOT contain `handleChangeAvatar` (function removed)
    - `src/app/profile/edit.tsx` does NOT contain `uploadAvatar` (function removed)
    - `src/app/profile/edit.tsx` DOES contain `usernameValue` (new style for read-only display)
    - `src/app/profile/edit.tsx` DOES contain `@{username ?? ''}` (read-only username render)
    - `src/app/profile/edit.tsx` isDirty does NOT contain `avatarUrl` (Pitfall 5 fixed)
    - `src/app/profile/edit.tsx` handleSave update payload does NOT contain `avatar_url` (Pitfall 7 fixed)
    - `src/app/profile/edit.tsx` profiles SELECT contains `username` (Pitfall 6 fixed — username fetched)
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>edit.tsx contains only display name TextInput, read-only @username display, BirthdayPicker, and Save Changes button. Avatar and wish list sections are absent. isDirty and handleSave are cleaned up. TypeScript compiles clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create wish-list.tsx screen and register in _layout.tsx (D-07, D-08, PROF-03)</name>
  <read_first>
    - src/app/profile/edit.tsx (read AFTER Task 1 strip — to see exact code to move verbatim)
    - src/app/profile/_layout.tsx (current layout — to add wish-list Screen entry)
    - .planning/phases/13-profile-rework-friend-profile/13-PATTERNS.md (wish-list.tsx screen shell pattern)
    - .planning/phases/13-profile-rework-friend-profile/13-UI-SPEC.md (Screen C layout spec)
    - .planning/phases/13-profile-rework-friend-profile/13-RESEARCH.md (Pitfall 4 — layout registration)
  </read_first>
  <files>src/app/profile/wish-list.tsx, src/app/profile/_layout.tsx</files>
  <action>
    **Part A — Create `src/app/profile/wish-list.tsx`:**

    This is a new screen that wraps the wish list CRUD code that was in edit.tsx. Create the file with this exact structure:

    ```typescript
    import { router } from 'expo-router';
    import React, { useState } from 'react';
    import {
      KeyboardAvoidingView,
      Platform,
      ScrollView,
      StyleSheet,
      Text,
      TextInput,
      TouchableOpacity,
      View,
    } from 'react-native';
    import { LoadingIndicator } from '@/components/common/LoadingIndicator';
    import { ScreenHeader } from '@/components/common/ScreenHeader';
    import { useMyWishList } from '@/hooks/useMyWishList';
    import { COLORS, SPACING, FONT_SIZE, FONT_WEIGHT, RADII } from '@/theme';

    export default function WishListScreen() {
      const { items: wishListItems, addItem, deleteItem, loading } = useMyWishList();
      const [addingWishItem, setAddingWishItem] = useState(false);
      const [newItemTitle, setNewItemTitle] = useState('');
      const [newItemUrl, setNewItemUrl] = useState('');
      const [newItemNotes, setNewItemNotes] = useState('');

      async function handleAddWishItem() {
        const trimmedTitle = newItemTitle.trim();
        if (!trimmedTitle) return;
        setAddingWishItem(true);
        await addItem(trimmedTitle, newItemUrl.trim() || undefined, newItemNotes.trim() || undefined);
        setNewItemTitle('');
        setNewItemUrl('');
        setNewItemNotes('');
        setAddingWishItem(false);
      }

      if (loading) return <LoadingIndicator />;

      return (
        <KeyboardAvoidingView
          style={styles.flex}
          behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
        >
          <ScrollView
            style={styles.scroll}
            contentContainerStyle={styles.scrollContent}
            keyboardShouldPersistTaps="handled"
          >
            <ScreenHeader title="My Wish List" onBack={() => router.back()} />

            {/* Existing items */}
            {wishListItems.map((item) => (
              <View key={item.id} style={styles.wishListRow}>
                <View style={styles.wishListItemContent}>
                  <Text style={styles.wishListItemTitle} numberOfLines={2}>{item.title}</Text>
                  {item.url ? (
                    <Text style={styles.wishListItemUrl} numberOfLines={1}>{item.url}</Text>
                  ) : null}
                  {item.notes ? (
                    <Text style={styles.wishListItemNotes} numberOfLines={2}>{item.notes}</Text>
                  ) : null}
                </View>
                <TouchableOpacity
                  onPress={() => void deleteItem(item.id)}
                  style={styles.deleteWishItem}
                  accessibilityLabel={`Delete ${item.title}`}
                >
                  <Text style={styles.deleteWishItemText}>Remove</Text>
                </TouchableOpacity>
              </View>
            ))}

            {wishListItems.length === 0 && (
              <Text style={styles.wishListEmpty}>No wish list items yet.</Text>
            )}

            {/* Add item form */}
            <Text style={styles.addSectionLabel}>ADD ITEM</Text>
            <TextInput
              style={[styles.textInput, styles.wishItemInput]}
              value={newItemTitle}
              onChangeText={setNewItemTitle}
              placeholder="Item title (required)"
              placeholderTextColor={COLORS.text.secondary}
              maxLength={120}
              editable={!addingWishItem}
            />
            <TextInput
              style={[styles.textInput, styles.wishItemInput]}
              value={newItemUrl}
              onChangeText={setNewItemUrl}
              placeholder="Link (optional)"
              placeholderTextColor={COLORS.text.secondary}
              maxLength={500}
              editable={!addingWishItem}
              keyboardType="url"
              autoCapitalize="none"
            />
            <TextInput
              style={[styles.textInput, styles.wishItemInput]}
              value={newItemNotes}
              onChangeText={setNewItemNotes}
              placeholder="Notes (optional)"
              placeholderTextColor={COLORS.text.secondary}
              maxLength={200}
              editable={!addingWishItem}
            />
            <TouchableOpacity
              style={[
                styles.addWishItemButton,
                (!newItemTitle.trim() || addingWishItem) && styles.addWishItemButtonDisabled,
              ]}
              onPress={handleAddWishItem}
              disabled={!newItemTitle.trim() || addingWishItem}
              accessibilityLabel="Add wish list item"
            >
              <Text style={styles.addWishItemButtonText}>
                {addingWishItem ? 'Adding...' : 'Add Item'}
              </Text>
            </TouchableOpacity>
          </ScrollView>
        </KeyboardAvoidingView>
      );
    }

    const styles = StyleSheet.create({
      flex: {
        flex: 1,
        backgroundColor: COLORS.surface.base,
      },
      scroll: {
        flex: 1,
        backgroundColor: COLORS.surface.base,
      },
      scrollContent: {
        paddingHorizontal: SPACING.lg,
        paddingTop: SPACING.xxl,
        paddingBottom: SPACING.xxl,
      },
      addSectionLabel: {
        fontSize: FONT_SIZE.md,
        fontWeight: FONT_WEIGHT.regular,
        color: COLORS.text.secondary,
        marginTop: SPACING.xl,
        marginBottom: SPACING.sm,
      },
      textInput: {
        backgroundColor: COLORS.surface.card,
        borderRadius: RADII.lg,
        height: 52,
        paddingHorizontal: SPACING.lg,
        fontSize: FONT_SIZE.lg,
        color: COLORS.text.primary,
      },
      wishItemInput: {
        marginTop: SPACING.sm,
      },
      wishListRow: {
        flexDirection: 'row',
        alignItems: 'flex-start',
        paddingVertical: SPACING.md,
        gap: SPACING.sm,
      },
      wishListItemContent: {
        flex: 1,
      },
      wishListItemTitle: {
        fontSize: FONT_SIZE.md,
        fontWeight: FONT_WEIGHT.regular,
        color: COLORS.text.primary,
      },
      wishListItemUrl: {
        fontSize: FONT_SIZE.sm,
        fontWeight: FONT_WEIGHT.regular,
        color: COLORS.interactive.accent,
        marginTop: SPACING.xs,
      },
      wishListItemNotes: {
        fontSize: FONT_SIZE.sm,
        fontWeight: FONT_WEIGHT.regular,
        color: COLORS.text.secondary,
        marginTop: SPACING.xs,
      },
      wishListEmpty: {
        fontSize: FONT_SIZE.md,
        fontWeight: FONT_WEIGHT.regular,
        color: COLORS.text.secondary,
        marginTop: SPACING.sm,
        marginBottom: SPACING.md,
      },
      deleteWishItem: {
        paddingHorizontal: SPACING.md,
        paddingVertical: SPACING.sm,
        alignSelf: 'flex-start',
        marginTop: SPACING.xs,
      },
      deleteWishItemText: {
        fontSize: FONT_SIZE.sm,
        fontWeight: FONT_WEIGHT.regular,
        color: COLORS.interactive.destructive,
      },
      addWishItemButton: {
        marginTop: SPACING.md,
        backgroundColor: COLORS.surface.card,
        borderRadius: RADII.lg,
        paddingVertical: SPACING.md,
        alignItems: 'center',
      },
      addWishItemButtonDisabled: {
        opacity: 0.5,
      },
      addWishItemButtonText: {
        fontSize: FONT_SIZE.lg,
        fontWeight: FONT_WEIGHT.semibold,
        color: COLORS.text.primary,
      },
    });
    ```

    NOTE on `ScreenHeader`: Check if `ScreenHeader` accepts an `onBack` prop by looking at its component API. If it does not accept `onBack`, use `router.back()` in a standard back-button row pattern consistent with how other screens handle this, or wrap the ScreenHeader without onBack and rely on the Stack navigation. Do NOT add a custom prop to ScreenHeader — use what the component already accepts.

    **Part B — Update `src/app/profile/_layout.tsx`:**

    Add the wish-list Stack.Screen entry (Pitfall 4 — missing registration causes double header):

    BEFORE:
    ```typescript
    <Stack.Screen name="edit" options={{ headerShown: false }} />
    ```

    AFTER:
    ```typescript
    <Stack.Screen name="edit" options={{ headerShown: false }} />
    <Stack.Screen name="wish-list" options={{ headerShown: false }} />
    ```

    Run `npx tsc --noEmit` after both files are written.
  </action>
  <verify>
    <automated>cd /Users/iulian/Develop/campfire && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/profile/wish-list.tsx` exists
    - `src/app/profile/wish-list.tsx` contains `export default function WishListScreen`
    - `src/app/profile/wish-list.tsx` contains `useMyWishList` (hook import and usage)
    - `src/app/profile/wish-list.tsx` contains `ScreenHeader` with title "My Wish List"
    - `src/app/profile/wish-list.tsx` contains `maxLength={120}` (item title input validation)
    - `src/app/profile/wish-list.tsx` contains `No wish list items yet.` (empty state copy per UI-SPEC)
    - `src/app/profile/_layout.tsx` contains `name="wish-list"` (screen registered)
    - `src/app/profile/_layout.tsx` contains `headerShown: false` for wish-list entry
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>wish-list.tsx exists as a complete screen with add/delete functionality using useMyWishList. The profile/_layout.tsx registers it with headerShown: false preventing double headers. TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User device → Supabase profiles table | display_name update in edit.tsx handleSave |
| User device → Supabase wish_list_items table | addItem and deleteItem operations in wish-list.tsx |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Tampering | edit.tsx handleSave display_name | mitigate | `maxLength={APP_CONFIG.displayNameMaxLength}` on TextInput caps input client-side; Supabase DB column has VARCHAR constraint as server-side guard |
| T-13-02-02 | Tampering | wish-list.tsx addItem title | mitigate | `maxLength={120}` on TextInput input; RLS on wish_list_items enforces `user_id = auth.uid()` so user can only insert their own items |
| T-13-02-03 | Tampering | wish-list.tsx deleteItem | mitigate | `deleteItem` in useMyWishList passes `.eq('user_id', userId)` as belt-and-suspenders; RLS on wish_list_items enforces ownership |
| T-13-02-04 | Information Disclosure | edit.tsx read-only username display | accept | Username is displayed as non-interactive Text to the authenticated owner only — no cross-user disclosure; session enforces identity |
| T-13-02-05 | Elevation of Privilege | wish_list_items RLS bypass | accept | RLS was verified as correct in v1.4 (migration 0017); no new RLS changes in this phase; client-side userId guard in deleteItem is redundant layer |
</threat_model>

<verification>
TypeScript compile:
```bash
cd /Users/iulian/Develop/campfire && npx tsc --noEmit
```

Structure checks:
```bash
# Verify edit.tsx is stripped
grep -c "AvatarCircle\|useMyWishList\|avatarUrl\|handleChangeAvatar" src/app/profile/edit.tsx  # must be 0

# Verify username fetch and display
grep -c "username" src/app/profile/edit.tsx  # must be >= 2 (SELECT and render)
grep -c "usernameValue" src/app/profile/edit.tsx  # must be >= 1

# Verify isDirty fix
grep "isDirty" src/app/profile/edit.tsx  # must NOT contain "avatarUrl"

# Verify wish-list.tsx exists and is complete
test -f src/app/profile/wish-list.tsx && echo "EXISTS"
grep -c "useMyWishList" src/app/profile/wish-list.tsx  # must be >= 1

# Verify layout registration
grep "wish-list" src/app/profile/_layout.tsx  # must output a matching line
```

ESLint:
```bash
cd /Users/iulian/Develop/campfire && npx eslint src/app/profile/edit.tsx src/app/profile/wish-list.tsx src/app/profile/_layout.tsx --max-warnings 0
```
</verification>

<success_criteria>
- edit.tsx compiles with TypeScript strict
- edit.tsx contains no references to AvatarCircle, useMyWishList, avatarUrl, handleChangeAvatar, uploadAvatar
- edit.tsx isDirty compares only displayName and birthday fields
- edit.tsx handleSave update payload has no avatar_url field
- edit.tsx fetches and displays username as read-only text
- wish-list.tsx exists with WishListScreen default export
- wish-list.tsx uses useMyWishList for item CRUD with empty state copy "No wish list items yet."
- _layout.tsx has Stack.Screen name="wish-list" with headerShown: false
- All three files pass ESLint with 0 warnings
</success_criteria>

<output>
After completion, create `.planning/phases/13-profile-rework-friend-profile/13-02-SUMMARY.md` with:
- What was removed from edit.tsx (list of deleted sections/functions)
- What was added to edit.tsx (username display)
- Description of wish-list.tsx (structure, hook used, patterns followed)
- _layout.tsx change (one-line addition)
- Any TypeScript or ESLint issues encountered and how they were resolved
</output>
