---
phase: 09-screen-consistency-sweep
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/screens/home/HomeScreen.tsx
  - src/components/home/HomeFriendCard.tsx
  - src/components/status/EmojiTagPicker.tsx
  - src/components/status/SegmentedControl.tsx
autonomous: true
requirements: [SCRN-01, SCRN-02, SCRN-03, SCRN-05, SCRN-07]

must_haves:
  truths:
    - "HomeScreen uses FAB component instead of inline FAB JSX"
    - "HomeScreen uses ScreenHeader for its title"
    - "All home and status component files import from @/theme, not @/constants/colors"
    - "Zero no-hardcoded-styles violations in home and status files"
  artifacts:
    - path: "src/screens/home/HomeScreen.tsx"
      provides: "Token-migrated home screen with FAB and ScreenHeader"
    - path: "src/components/home/HomeFriendCard.tsx"
      provides: "Token-migrated friend card component"
    - path: "src/components/status/EmojiTagPicker.tsx"
      provides: "Token-migrated emoji tag picker"
    - path: "src/components/status/SegmentedControl.tsx"
      provides: "Token-migrated segmented control"
  key_links:
    - from: "src/screens/home/HomeScreen.tsx"
      to: "@/components/common/FAB"
      via: "import statement"
      pattern: "import.*FAB.*from '@/components/common/FAB'"
    - from: "src/screens/home/HomeScreen.tsx"
      to: "@/components/common/ScreenHeader"
      via: "import statement"
      pattern: "import.*ScreenHeader.*from '@/components/common/ScreenHeader'"
---

<objective>
Migrate the Home domain (HomeScreen + HomeFriendCard + EmojiTagPicker + SegmentedControl) to design tokens, replace the inline FAB with the shared FAB component, and adopt ScreenHeader for the screen title.

Purpose: Home is the primary screen users see daily. Token migration + shared component adoption here establishes visual consistency with the Plans reference view.
Output: 4 files migrated with zero lint violations, FAB and ScreenHeader adopted.
</objective>

<execution_context>
@/Users/iulian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iulian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-screen-consistency-sweep/09-CONTEXT.md

<interfaces>
From src/components/common/FAB.tsx:
```typescript
export interface FABProps {
  icon: React.ReactNode;
  label?: string;
  onPress: () => void;
  accessibilityLabel: string;
  size?: number;
}
export function FAB({ icon, label, onPress, accessibilityLabel, size = 56 }: FABProps);
```

From src/components/common/ScreenHeader.tsx:
```typescript
export interface ScreenHeaderProps {
  title: string;
  subtitle?: string;
  rightAction?: React.ReactNode;
}
export function ScreenHeader({ title, subtitle, rightAction }: ScreenHeaderProps);
```

Token mapping (old -> new):
- COLORS.accent -> COLORS.interactive.accent
- COLORS.dominant -> COLORS.surface.base
- COLORS.secondary -> COLORS.surface.card
- COLORS.destructive -> COLORS.interactive.destructive
- COLORS.textPrimary -> COLORS.text.primary
- COLORS.textSecondary -> COLORS.text.secondary
- COLORS.border -> COLORS.border
- COLORS.surfaceOverlay -> COLORS.surface.overlay
- Raw spacing: 4->SPACING.xs, 8->SPACING.sm, 12->SPACING.md, 16->SPACING.lg, 24->SPACING.xl, 32->SPACING.xxl
- Raw fontSize: 11->xs, 13->sm, 14->md, 16->lg, 20->xl, 24->xxl
- Raw fontWeight: '400'->FONT_WEIGHT.regular, '600'->FONT_WEIGHT.semibold
- Raw borderRadius: 4->RADII.xs, 6->RADII.sm, 8->RADII.md, 12->RADII.lg, 16->RADII.xl
- Raw shadow blocks -> ...SHADOWS.card or ...SHADOWS.fab spread

Note: HomeScreen has `import { COLORS as THEME } from '@/theme'` from Phase 8 — replace alias with direct `COLORS` import.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate HomeScreen with FAB and ScreenHeader adoption</name>
  <files>src/screens/home/HomeScreen.tsx</files>
  <read_first>src/screens/home/HomeScreen.tsx, src/components/common/FAB.tsx, src/components/common/ScreenHeader.tsx, src/theme/colors.ts, src/theme/spacing.ts, src/theme/typography.ts, src/theme/radii.ts, src/theme/shadows.ts</read_first>
  <action>
1. Replace `import { COLORS } from '@/constants/colors'` with `import { COLORS, SPACING, FONT_SIZE, FONT_WEIGHT, RADII, SHADOWS } from '@/theme'` (only what's used).
2. If `import { COLORS as THEME } from '@/theme'` exists, remove it and replace all `THEME.xxx` references with `COLORS.xxx` semantic paths.
3. Add `import { FAB } from '@/components/common/FAB'` and `import { ScreenHeader } from '@/components/common/ScreenHeader'`.
4. Replace the inline FAB JSX (the TouchableOpacity with "Start Plan" label and icon) with `<FAB icon={...same icon...} label="Start Plan" onPress={...same handler...} accessibilityLabel="Create a new plan" />`. Remove the inline FAB styles from StyleSheet.
5. Replace the inline screen title (the Text element with the large title like "Campfire" or similar) with `<ScreenHeader title="Campfire" />`. Remove the inline title styles from StyleSheet.
6. Replace ALL old flat COLORS keys: accent->interactive.accent, dominant->surface.base, secondary->surface.card, textPrimary->text.primary, textSecondary->text.secondary, etc.
7. Replace ALL hardcoded hex colors with COLORS.xxx tokens.
8. Replace ALL raw fontSize/fontWeight/padding/margin/gap/borderRadius with tokens per the mapping.
9. Replace raw shadow property blocks with ...SHADOWS.card or ...SHADOWS.fab spread where appropriate.
10. Remove any now-unused styles from the StyleSheet.
  </action>
  <verify>
    <automated>npx eslint src/screens/home/HomeScreen.tsx 2>&1 | grep -c "no-hardcoded-styles" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "@/constants/colors" src/screens/home/HomeScreen.tsx` returns empty
    - `grep "COLORS as THEME" src/screens/home/HomeScreen.tsx` returns empty
    - `grep "import.*FAB.*from '@/components/common/FAB'" src/screens/home/HomeScreen.tsx` returns a match
    - `grep "import.*ScreenHeader.*from '@/components/common/ScreenHeader'" src/screens/home/HomeScreen.tsx` returns a match
    - `grep "<FAB" src/screens/home/HomeScreen.tsx` returns a match
    - `grep "<ScreenHeader" src/screens/home/HomeScreen.tsx` returns a match
    - `npx eslint src/screens/home/HomeScreen.tsx 2>&1 | grep "no-hardcoded-styles"` returns empty
  </acceptance_criteria>
  <done>HomeScreen migrated to @/theme tokens, inline FAB replaced with FAB component, title replaced with ScreenHeader. Zero lint violations.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate HomeFriendCard, EmojiTagPicker, SegmentedControl</name>
  <files>src/components/home/HomeFriendCard.tsx, src/components/status/EmojiTagPicker.tsx, src/components/status/SegmentedControl.tsx</files>
  <read_first>src/components/home/HomeFriendCard.tsx, src/components/status/EmojiTagPicker.tsx, src/components/status/SegmentedControl.tsx, src/theme/colors.ts, src/theme/spacing.ts, src/theme/typography.ts, src/theme/radii.ts, src/theme/shadows.ts</read_first>
  <action>
For each of the 3 component files:
1. Replace `import { COLORS } from '@/constants/colors'` with `import { COLORS, SPACING, FONT_SIZE, FONT_WEIGHT, RADII, SHADOWS } from '@/theme'` (only what's used).
2. Replace old flat COLORS keys with semantic paths per mapping.
3. Replace ALL hardcoded hex colors with COLORS.xxx tokens.
4. Replace ALL raw fontSize: 11->FONT_SIZE.xs, 13->FONT_SIZE.sm, 14->FONT_SIZE.md, 16->FONT_SIZE.lg, 20->FONT_SIZE.xl, 24->FONT_SIZE.xxl.
5. Replace ALL raw fontWeight: '400'->FONT_WEIGHT.regular, '600'->FONT_WEIGHT.semibold.
6. Replace ALL raw padding/margin/gap: 4->SPACING.xs, 8->SPACING.sm, 12->SPACING.md, 16->SPACING.lg, 24->SPACING.xl, 32->SPACING.xxl.
7. Replace raw borderRadius: 4->RADII.xs, 6->RADII.sm, 8->RADII.md, 12->RADII.lg.
8. Replace raw shadow blocks with ...SHADOWS.card or ...SHADOWS.fab spread.
9. Keep any raw numeric value that has no exact token match, with a `// no exact token` comment.

Violation counts: HomeFriendCard (7), EmojiTagPicker (7), SegmentedControl (3) = 17 total.
  </action>
  <verify>
    <automated>npx eslint src/components/home/HomeFriendCard.tsx src/components/status/EmojiTagPicker.tsx src/components/status/SegmentedControl.tsx 2>&1 | grep -c "no-hardcoded-styles" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "@/constants/colors" src/components/home/ src/components/status/` returns empty
    - `npx eslint src/components/home/HomeFriendCard.tsx src/components/status/EmojiTagPicker.tsx src/components/status/SegmentedControl.tsx 2>&1 | grep "no-hardcoded-styles"` returns empty
  </acceptance_criteria>
  <done>All 3 home/status component files migrated to @/theme tokens. Zero lint violations.</done>
</task>

</tasks>

<verification>
- `grep -r "@/constants/colors" src/screens/home/ src/components/home/ src/components/status/` returns empty
- `npx eslint src/screens/home/HomeScreen.tsx src/components/home/HomeFriendCard.tsx src/components/status/EmojiTagPicker.tsx src/components/status/SegmentedControl.tsx 2>&1 | grep "no-hardcoded-styles"` returns empty
- `grep "<FAB" src/screens/home/HomeScreen.tsx` returns a match
- `grep "<ScreenHeader" src/screens/home/HomeScreen.tsx` returns a match
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- 4 files migrated with zero no-hardcoded-styles violations
- HomeScreen uses FAB component and ScreenHeader
- No file imports from @/constants/colors
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/09-screen-consistency-sweep/09-02-SUMMARY.md`
</output>
