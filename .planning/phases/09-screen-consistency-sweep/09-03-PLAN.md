---
phase: 09-screen-consistency-sweep
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/screens/plans/PlansListScreen.tsx
  - src/screens/plans/PlanDashboardScreen.tsx
  - src/screens/plans/PlanCreateModal.tsx
  - src/components/plans/PlanCard.tsx
  - src/components/plans/AvatarStack.tsx
  - src/components/plans/IOUNotesField.tsx
  - src/components/plans/LinkDumpField.tsx
  - src/components/plans/MemberList.tsx
  - src/components/plans/RSVPButtons.tsx
autonomous: true
requirements: [SCRN-01, SCRN-02, SCRN-03, SCRN-05, SCRN-07]

must_haves:
  truths:
    - "PlansListScreen uses FAB component instead of inline FAB JSX"
    - "PlansListScreen uses ScreenHeader for its title"
    - "All plans domain files import from @/theme, not @/constants/colors"
    - "Zero no-hardcoded-styles violations in all 9 plans domain files"
  artifacts:
    - path: "src/screens/plans/PlansListScreen.tsx"
      provides: "Token-migrated plans list with FAB and ScreenHeader"
    - path: "src/screens/plans/PlanDashboardScreen.tsx"
      provides: "Token-migrated plan dashboard"
    - path: "src/screens/plans/PlanCreateModal.tsx"
      provides: "Token-migrated plan create modal"
  key_links:
    - from: "src/screens/plans/PlansListScreen.tsx"
      to: "@/components/common/FAB"
      via: "import statement"
      pattern: "import.*FAB.*from '@/components/common/FAB'"
    - from: "src/screens/plans/PlansListScreen.tsx"
      to: "@/components/common/ScreenHeader"
      via: "import statement"
      pattern: "import.*ScreenHeader.*from '@/components/common/ScreenHeader'"
---

<objective>
Migrate the Plans domain (PlansListScreen, PlanDashboardScreen, PlanCreateModal + PlanCard, AvatarStack, IOUNotesField, LinkDumpField, MemberList, RSVPButtons) to design tokens, replace the inline FAB on PlansListScreen with the shared FAB component, and adopt ScreenHeader.

Purpose: Plans is the largest domain by violation count (140 violations across 9 files). It's also the reference view that other screens should match.
Output: 9 files migrated with zero lint violations, FAB and ScreenHeader adopted on PlansListScreen.
</objective>

<execution_context>
@/Users/iulian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iulian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-screen-consistency-sweep/09-CONTEXT.md

<interfaces>
From src/components/common/FAB.tsx:
```typescript
export interface FABProps {
  icon: React.ReactNode;
  label?: string;
  onPress: () => void;
  accessibilityLabel: string;
  size?: number;
}
export function FAB({ icon, label, onPress, accessibilityLabel, size = 56 }: FABProps);
```

From src/components/common/ScreenHeader.tsx:
```typescript
export interface ScreenHeaderProps {
  title: string;
  subtitle?: string;
  rightAction?: React.ReactNode;
}
export function ScreenHeader({ title, subtitle, rightAction }: ScreenHeaderProps);
```

From src/components/common/SectionHeader.tsx:
```typescript
export interface SectionHeaderProps {
  title: string;
  rightAction?: React.ReactNode;
}
export function SectionHeader({ title, rightAction }: SectionHeaderProps);
```

Token mapping (old -> new):
- COLORS.accent -> COLORS.interactive.accent
- COLORS.dominant -> COLORS.surface.base
- COLORS.secondary -> COLORS.surface.card
- COLORS.destructive -> COLORS.interactive.destructive
- COLORS.textPrimary -> COLORS.text.primary
- COLORS.textSecondary -> COLORS.text.secondary
- COLORS.border -> COLORS.border
- COLORS.surfaceOverlay -> COLORS.surface.overlay
- Raw spacing: 4->SPACING.xs, 8->SPACING.sm, 12->SPACING.md, 16->SPACING.lg, 24->SPACING.xl, 32->SPACING.xxl
- Raw fontSize: 11->xs, 13->sm, 14->md, 16->lg, 20->xl, 24->xxl
- Raw fontWeight: '400'->FONT_WEIGHT.regular, '600'->FONT_WEIGHT.semibold
- Raw borderRadius: 4->RADII.xs, 6->RADII.sm, 8->RADII.md, 12->RADII.lg, 16->RADII.xl
- Raw shadow blocks -> ...SHADOWS.card or ...SHADOWS.fab spread

Note: PlansListScreen has `import { COLORS as THEME } from '@/theme'` from Phase 8 — replace alias with direct `COLORS` import.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate PlansListScreen, PlanDashboardScreen, PlanCreateModal</name>
  <files>src/screens/plans/PlansListScreen.tsx, src/screens/plans/PlanDashboardScreen.tsx, src/screens/plans/PlanCreateModal.tsx</files>
  <read_first>src/screens/plans/PlansListScreen.tsx, src/screens/plans/PlanDashboardScreen.tsx, src/screens/plans/PlanCreateModal.tsx, src/components/common/FAB.tsx, src/components/common/ScreenHeader.tsx, src/theme/colors.ts, src/theme/spacing.ts, src/theme/typography.ts, src/theme/radii.ts, src/theme/shadows.ts</read_first>
  <action>
**PlansListScreen.tsx (39 violations):**
1. Replace `import { COLORS } from '@/constants/colors'` with `import { COLORS, SPACING, FONT_SIZE, FONT_WEIGHT, RADII, SHADOWS } from '@/theme'` (only what's used).
2. If `import { COLORS as THEME } from '@/theme'` exists, remove and replace all `THEME.xxx` with `COLORS.xxx` semantic paths.
3. Add `import { FAB } from '@/components/common/FAB'` and `import { ScreenHeader } from '@/components/common/ScreenHeader'`.
4. Replace the inline FAB JSX (the "+" icon-only TouchableOpacity) with `<FAB icon={...same plus icon...} onPress={...same handler...} accessibilityLabel="Create a new plan" />`. Remove inline FAB styles.
5. Replace the inline screen title with `<ScreenHeader title="Plans" />` (check actual title text in file). Remove inline title styles.
6. Replace ALL old flat COLORS keys, hardcoded hex, raw fontSize/fontWeight/padding/margin/gap/borderRadius with tokens per mapping.
7. Replace raw shadow blocks with ...SHADOWS.card spread.

**PlanDashboardScreen.tsx (40 violations):**
1. Same import replacement pattern.
2. Replace ALL old COLORS keys and hardcoded values with tokens.
3. This is the highest-violation file — be thorough with every StyleSheet property.
4. Replace raw shadow blocks with ...SHADOWS.card spread.

**PlanCreateModal.tsx (24 violations):**
1. Same import replacement pattern.
2. Replace ALL old COLORS keys and hardcoded values with tokens.
3. This is a modal — check for any raw borderRadius on the modal container and replace with RADII.lg (12).
  </action>
  <verify>
    <automated>npx eslint src/screens/plans/PlansListScreen.tsx src/screens/plans/PlanDashboardScreen.tsx src/screens/plans/PlanCreateModal.tsx 2>&1 | grep -c "no-hardcoded-styles" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "@/constants/colors" src/screens/plans/` returns empty
    - `grep "COLORS as THEME" src/screens/plans/PlansListScreen.tsx` returns empty
    - `grep "import.*FAB.*from '@/components/common/FAB'" src/screens/plans/PlansListScreen.tsx` returns a match
    - `grep "import.*ScreenHeader.*from '@/components/common/ScreenHeader'" src/screens/plans/PlansListScreen.tsx` returns a match
    - `grep "<FAB" src/screens/plans/PlansListScreen.tsx` returns a match
    - `npx eslint src/screens/plans/PlansListScreen.tsx src/screens/plans/PlanDashboardScreen.tsx src/screens/plans/PlanCreateModal.tsx 2>&1 | grep "no-hardcoded-styles"` returns empty
  </acceptance_criteria>
  <done>3 plans screen files migrated with zero lint violations. PlansListScreen uses FAB and ScreenHeader.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate plan components (PlanCard, AvatarStack, IOUNotesField, LinkDumpField, MemberList, RSVPButtons)</name>
  <files>src/components/plans/PlanCard.tsx, src/components/plans/AvatarStack.tsx, src/components/plans/IOUNotesField.tsx, src/components/plans/LinkDumpField.tsx, src/components/plans/MemberList.tsx, src/components/plans/RSVPButtons.tsx</files>
  <read_first>src/components/plans/PlanCard.tsx, src/components/plans/AvatarStack.tsx, src/components/plans/IOUNotesField.tsx, src/components/plans/LinkDumpField.tsx, src/components/plans/MemberList.tsx, src/components/plans/RSVPButtons.tsx, src/theme/colors.ts, src/theme/spacing.ts, src/theme/typography.ts, src/theme/radii.ts, src/theme/shadows.ts</read_first>
  <action>
For each of the 6 plan component files:
1. Replace `import { COLORS } from '@/constants/colors'` with `import { COLORS, SPACING, FONT_SIZE, FONT_WEIGHT, RADII, SHADOWS } from '@/theme'` (only what's used).
2. Replace old flat COLORS keys with semantic paths per mapping.
3. Replace ALL hardcoded hex colors with COLORS.xxx tokens.
4. Replace ALL raw fontSize/fontWeight/padding/margin/gap/borderRadius with tokens.
5. Replace raw shadow blocks with ...SHADOWS.card spread.
6. Keep any raw value with no exact token match, adding `// no exact token` comment.

Violation counts: PlanCard (11), AvatarStack (2), IOUNotesField (5), LinkDumpField (8), MemberList (9), RSVPButtons (2) = 37 total.
  </action>
  <verify>
    <automated>npx eslint src/components/plans/PlanCard.tsx src/components/plans/AvatarStack.tsx src/components/plans/IOUNotesField.tsx src/components/plans/LinkDumpField.tsx src/components/plans/MemberList.tsx src/components/plans/RSVPButtons.tsx 2>&1 | grep -c "no-hardcoded-styles" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "@/constants/colors" src/components/plans/` returns empty
    - `npx eslint src/components/plans/*.tsx 2>&1 | grep "no-hardcoded-styles"` returns empty
  </acceptance_criteria>
  <done>All 6 plan component files migrated to @/theme tokens. Zero lint violations.</done>
</task>

</tasks>

<verification>
- `grep -r "@/constants/colors" src/screens/plans/ src/components/plans/` returns empty
- `npx eslint src/screens/plans/*.tsx src/components/plans/*.tsx 2>&1 | grep "no-hardcoded-styles"` returns empty
- `grep "<FAB" src/screens/plans/PlansListScreen.tsx` returns a match
- `grep "<ScreenHeader" src/screens/plans/PlansListScreen.tsx` returns a match
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- 9 files migrated with zero no-hardcoded-styles violations
- PlansListScreen uses FAB component and ScreenHeader
- No file imports from @/constants/colors
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/09-screen-consistency-sweep/09-03-SUMMARY.md`
</output>
