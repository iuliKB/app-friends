---
phase: 20-map-feature
plan: "06"
type: execute
wave: 3
depends_on: ["20-03", "20-04", "20-05"]
files_modified: []
autonomous: false
requirements: [MAP-01, MAP-02, MAP-03, MAP-04, MAP-05]

must_haves:
  truths:
    - "User can attach a location to a plan and see the address label in PlanCreateModal"
    - "Plan Dashboard shows a static map tile with a pin when a plan has coordinates"
    - "Explore tab shows friend plan pins on a map by default; toggling shows list view"
    - "Tapping Directions on Plan Dashboard opens the native maps app"
    - "EAS development build runs the full map feature without native module errors"
  artifacts:
    - path: "src/components/maps/LocationPicker.tsx"
      provides: "LocationPicker component verified working on device"
    - path: "src/components/maps/ExploreMapView.tsx"
      provides: "ExploreMapView component verified working on device"
    - path: "src/screens/plans/PlanDashboardScreen.tsx"
      provides: "Map tile + directions verified working on device"
    - path: "src/screens/plans/PlansListScreen.tsx"
      provides: "List/map toggle verified working on device"
  key_links:
    - from: "EAS dev build"
      to: "react-native-maps native module"
      via: "npx eas build --profile development"
      pattern: "eas build"
---

<objective>
Trigger the EAS development build (required after adding react-native-maps native plugin), then verify all five MAP requirements on a real device: location picker, plan dashboard map tile, Explore map view, directions deep link, and null-safe plans.

Purpose: react-native-maps contains native code — it will not run in Expo Go or an old dev build. A new EAS dev build is mandatory before any map UI can be tested. This checkpoint combines the EAS build step (human-action) with device verification (human-verify).
Output: Verified MAP-01 through MAP-05 on real device.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-map-feature/20-CONTEXT.md
@.planning/phases/20-map-feature/20-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Trigger EAS development build</name>
  <what-built>All five plans (20-01 through 20-05) have added react-native-maps and expo-location native plugins. The current dev build does not include these native modules — running in Expo Go or the old dev build will crash with "Invariant Violation: Native module MapView not found". A new EAS development build is required.</what-built>
  <how-to-verify>
    Run the following from the project root:
    ```bash
    npx eas build --profile development --platform all
    ```
    Or build per platform if you prefer:
    ```bash
    npx eas build --profile development --platform ios
    npx eas build --profile development --platform android
    ```

    Wait for the build to complete (typically 10–15 minutes). Install the new build on your device:
    - iOS: Download the .ipa / scan the QR code from the EAS build page
    - Android: Download the .apk from the EAS build page and install

    Confirm the app launches without a "native module not found" crash.
  </how-to-verify>
  <resume-signal>Type "build done" when the new dev build is installed on your device and the app opens without crashing</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify MAP-01 — Location picker attaches location to plan</name>
  <what-built>PlanCreateModal now shows "Add location" button instead of a text field. Tapping opens a full-screen map modal where the user can drag the map to position the center pin, then confirm to save coordinates and a reverse-geocoded address label.</what-built>
  <how-to-verify>
    1. Open the app → tap any tab that shows Plans → tap "+" or "Create Plan"
    2. In PlanCreateModal, find the "Where" row — it should show an "Add location" button (not a text input)
    3. Tap "Add location" — a full-screen map should appear
    4. Verify the map centers on your current GPS location (if you allowed location permission when prompted)
    5. Drag the map in any direction — the pin at screen center should stay fixed while the map moves
    6. Tap "Confirm Location"
    7. Verify: the address row now shows a human-readable label (e.g., "123 Main St, Toronto") — NOT raw coordinates like "43.65, -79.38"
    8. Verify: a clear [×] appears next to the address label; tapping it removes the location
    9. Complete plan creation — verify the plan saves without errors

    Expected: MAP-01 (attach location), MAP-03 (human-readable address label) verified.
  </how-to-verify>
  <resume-signal>Type "map-01 ok" if location picker works, or describe any issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify MAP-02 + MAP-05 — Plan Dashboard map tile + Directions</name>
  <what-built>PlanDashboardScreen now shows a 160pt static map tile with a pin at the plan's location when lat/lng are set. Below it, an address row with a "Directions" button opens the native maps app. Plans without coordinates show no tile.</what-built>
  <how-to-verify>
    **Plan WITH location:**
    1. Navigate to the plan you just created (with a location)
    2. Scroll to the location section — below the address text you should see a static map tile (160pt height) with an accent-colored pin
    3. Try scrolling the screen over the map tile — the scroll should work normally (not get stuck on the map)
    4. Below the tile, verify the address row shows the address label + a "Directions" button
    5. Tap "Directions" — your native maps app (Apple Maps on iOS, Google Maps on Android) should open with the location pre-filled

    **Plan WITHOUT location:**
    6. Navigate to any existing plan (created before this phase, or a new plan without a location)
    7. Verify: NO map tile and NO directions row appear — not even an empty placeholder

    Expected: MAP-02 (plan shows map with pin), MAP-05 (directions opens native maps) verified.
  </how-to-verify>
  <resume-signal>Type "map-02 ok" if both plans (with and without location) display correctly and Directions works</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify MAP-04 — Explore tab map view with friend plan pins</name>
  <what-built>PlansListScreen (Explore tab) now opens in map mode by default, showing a list/map icon toggle in the header. The map shows pins for friend plans within 25km. Tapping a pin navigates directly to that plan's dashboard.</what-built>
  <how-to-verify>
    1. Open the app → navigate to the tab showing friend plans (likely "Plans" or "Explore")
    2. Verify: the header shows two icons in the top-right (list icon + map icon); the map icon is highlighted/active by default
    3. Verify: the map fills the screen below the header
    4. If any friend plans have locations, they should appear as accent-colored pins on the map
    5. If you are within 25km of a friend plan's location, it should be visible; plans farther than 25km should not appear
    6. Tap a plan pin — verify the app navigates directly to that plan's dashboard (no callout bubble in between)
    7. Tap the list icon in the header — verify the map switches to the existing plan list view
    8. Tap the map icon — verify the map view returns

    If no friend plans have locations yet: the map should show an "No nearby plans yet" empty state.

    Expected: MAP-04 (Explore map with friend plan pins) verified.
  </how-to-verify>
  <resume-signal>Type "map-04 ok" if the Explore map view works, or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device→EAS | EAS build includes EXPO_PUBLIC_GOOGLE_MAPS_KEY in Android binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-17 | Information Disclosure | EAS build log may display env vars | accept | EXPO_PUBLIC_GOOGLE_MAPS_KEY is intentionally public (EXPO_PUBLIC_ prefix); key is restricted to Android Maps SDK at Google Console — no sensitive secrets in build |
</threat_model>

<verification>
All MAP requirements verified on device:
- MAP-01: Location picker opens, user drags map, confirm saves coordinates
- MAP-02: Plan dashboard shows 160pt static map tile when lat/lng present; absent when null
- MAP-03: Address label is human-readable (street + city format), not raw coordinates
- MAP-04: Explore tab opens in map view; friend plan pins visible; tapping pin navigates to dashboard
- MAP-05: Directions button opens native maps app with location pre-filled
</verification>

<success_criteria>
- EAS dev build installs and launches without "native module not found" crash
- All five MAP requirements manually verified on device
- No regression on existing plans (plans without coordinates show no map tile)
- Theme consistency: map chrome (pin color, confirm bar, address row) matches active light/dark theme
</success_criteria>

<output>
After completion, create `.planning/phases/20-map-feature/20-06-SUMMARY.md`
</output>
