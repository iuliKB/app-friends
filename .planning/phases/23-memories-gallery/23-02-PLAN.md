---
phase: 23-memories-gallery
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - src/components/home/RecentMemoriesSection.tsx
  - src/screens/home/HomeScreen.tsx
autonomous: true
requirements:
  - MEMO-01

must_haves:
  truths:
    - "RecentMemoriesSection renders a horizontal thumbnail strip with the 6 most recent photos"
    - "Each widget thumbnail shows a 72×72px photo and the plan name caption below it"
    - "Widget is hidden entirely when recentPhotos.length === 0 (D-03)"
    - "Tapping a thumbnail navigates to /memories via router.push('/memories') (D-12)"
    - "Tapping See all navigates to /memories (D-11)"
    - "FlatList has style={{ height: 104 }} to prevent height collapse inside outer ScrollView"
    - "RecentMemoriesSection is rendered in HomeScreen after UpcomingEventsSection"
    - "All StyleSheet.create calls are inside useMemo([colors])"
  artifacts:
    - path: "src/components/home/RecentMemoriesSection.tsx"
      provides: "Home widget — horizontal FlatList of recent photos with plan captions"
      exports: ["RecentMemoriesSection"]
    - path: "src/screens/home/HomeScreen.tsx"
      provides: "HomeScreen modified to render RecentMemoriesSection after UpcomingEventsSection"
  key_links:
    - from: "RecentMemoriesSection.tsx"
      to: "useAllPlanPhotos hook"
      via: "import and call useAllPlanPhotos()"
      pattern: "useAllPlanPhotos"
    - from: "HomeScreen.tsx"
      to: "RecentMemoriesSection.tsx"
      via: "JSX import + render after UpcomingEventsSection"
      pattern: "RecentMemoriesSection"
---

<objective>
Create the `RecentMemoriesSection` home widget component and wire it into the Home screen.

Purpose: Surface the 6 most recent cross-plan photos on the Home screen as a horizontal scrollable thumbnail strip, matching the visual rhythm of `UpcomingEventsSection`. Hidden when no photos exist.

Output: `src/components/home/RecentMemoriesSection.tsx` (new) + one-line JSX addition in `src/screens/home/HomeScreen.tsx`.
</objective>

<execution_context>
@/Users/iulian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iulian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-memories-gallery/23-CONTEXT.md
@.planning/phases/23-memories-gallery/23-PATTERNS.md
@.planning/phases/23-memories-gallery/23-UI-SPEC.md
@.planning/phases/23-memories-gallery/23-01-SUMMARY.md
</context>

<interfaces>
From src/hooks/useAllPlanPhotos.ts (created in Plan 01):
```typescript
export type PlanPhotoGroup = {
  planId: string;
  planTitle: string;
  photos: PlanPhotoWithUploader[];
};

export type UseAllPlanPhotosResult = {
  groups: PlanPhotoGroup[];
  recentPhotos: PlanPhotoWithUploader[]; // flat, most recent 6 (for widget)
  isLoading: boolean;
  error: string | null;
  refetch: () => Promise<void>;
  deletePhoto: (photoId: string, planId: string) => Promise<{ error: Error | null }>;
};

export function useAllPlanPhotos(): UseAllPlanPhotosResult;
```

Note: Each photo in `recentPhotos` carries an extended `planTitle` field (set during assembly in the hook). Access it as `(photo as PlanPhotoWithUploader & { planTitle: string }).planTitle` or declare the local type in the component.

From src/components/common/SectionHeader.tsx (existing):
```typescript
interface SectionHeaderProps {
  title: string;
  rightAction?: React.ReactNode;
}
export function SectionHeader({ title, rightAction }: SectionHeaderProps): JSX.Element;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create RecentMemoriesSection widget component</name>
  <files>src/components/home/RecentMemoriesSection.tsx</files>

  <read_first>
    - src/components/home/UpcomingEventsSection.tsx — exact structural analog; replicate imports, SectionHeader usage, FlatList pattern, height constraint, ItemSeparatorComponent, useMemo styles
    - src/components/common/SectionHeader.tsx — confirm SectionHeader props signature before use
    - src/hooks/useAllPlanPhotos.ts — confirm exported function name and recentPhotos shape
    - src/theme/index.ts (or src/theme/spacing.ts + typography.ts) — verify SPACING, FONT_SIZE, FONT_FAMILY, RADII token names
  </read_first>

  <action>
Create `src/components/home/RecentMemoriesSection.tsx` using `UpcomingEventsSection.tsx` as the structural template.

**Imports (adapt from UpcomingEventsSection.tsx — replicate its exact import paths):**
```typescript
import React, { useMemo } from 'react';
import { ActivityIndicator, FlatList, StyleSheet, Text, TouchableOpacity, View } from 'react-native';
import { useRouter } from 'expo-router';
import { Image } from 'expo-image';
import { useTheme, SPACING, FONT_SIZE, FONT_FAMILY, RADII } from '@/theme';
import { SectionHeader } from '@/components/common/SectionHeader';
import { useAllPlanPhotos } from '@/hooks/useAllPlanPhotos';
import type { PlanPhotoWithUploader } from '@/types/database';
```

**Module-level constant (ESLint-exempt):**
```typescript
// eslint-disable-next-line campfire/no-hardcoded-styles
const THUMB_SIZE = 72;
```

**Component structure:**
```typescript
export function RecentMemoriesSection() {
  const { colors } = useTheme();
  const router = useRouter();
  const { recentPhotos, isLoading } = useAllPlanPhotos();

  // D-03: Widget hidden entirely when no photos exist
  if (!isLoading && recentPhotos.length === 0) return null;

  const styles = useMemo(
    () =>
      StyleSheet.create({
        container: {
          marginBottom: SPACING.lg,
        },
        headerWrapper: {
          paddingHorizontal: SPACING.lg,
        },
        seeAllText: {
          fontSize: FONT_SIZE.md,
          fontFamily: FONT_FAMILY.body.regular,
          color: colors.interactive.accent,
        },
        flatList: {
          // REQUIRED — prevents height collapse inside outer ScrollView (RESEARCH.md Pitfall 1)
          // eslint-disable-next-line campfire/no-hardcoded-styles
          height: 104,
        },
        listContent: {
          paddingLeft: SPACING.lg,
          paddingRight: SPACING.sm,
        },
        thumbContainer: {
          alignItems: 'center',
          gap: SPACING.xs,
        },
        thumb: {
          // eslint-disable-next-line campfire/no-hardcoded-styles
          width: THUMB_SIZE,
          // eslint-disable-next-line campfire/no-hardcoded-styles
          height: THUMB_SIZE,
          borderRadius: RADII.sm,
          backgroundColor: colors.surface.card,
        },
        caption: {
          fontSize: FONT_SIZE.xs,
          fontFamily: FONT_FAMILY.body.regular,
          color: colors.text.secondary,
          // eslint-disable-next-line campfire/no-hardcoded-styles
          maxWidth: 80,
        },
        loadingContainer: {
          // eslint-disable-next-line campfire/no-hardcoded-styles
          height: 104,
          justifyContent: 'center',
          alignItems: 'center',
        },
      }),
    [colors]
  );

  // D-11: "See all" navigates to /memories
  const seeAllAction = (
    <TouchableOpacity
      onPress={() => router.push('/memories')}
      // eslint-disable-next-line campfire/no-hardcoded-styles
      hitSlop={8}
      accessibilityLabel="See all memories"
    >
      <Text style={styles.seeAllText}>See all</Text>
    </TouchableOpacity>
  );

  if (isLoading) {
    return (
      <View style={styles.container}>
        <View style={styles.headerWrapper}>
          <SectionHeader title="Recent Memories" rightAction={seeAllAction} />
        </View>
        <View style={styles.loadingContainer}>
          <ActivityIndicator color={colors.interactive.accent} />
        </View>
      </View>
    );
  }

  return (
    <View style={styles.container}>
      <View style={styles.headerWrapper}>
        <SectionHeader title="Recent Memories" rightAction={seeAllAction} />
      </View>
      <FlatList<PlanPhotoWithUploader & { planTitle?: string }>
        data={recentPhotos as (PlanPhotoWithUploader & { planTitle?: string })[]}
        keyExtractor={(item) => item.id}
        horizontal
        showsHorizontalScrollIndicator={false}
        contentContainerStyle={styles.listContent}
        ItemSeparatorComponent={() => <View style={{ width: SPACING.sm }} />}
        style={styles.flatList}
        renderItem={({ item }) => (
          // D-12: Tapping thumbnail navigates to /memories (not inline viewer)
          <TouchableOpacity
            style={styles.thumbContainer}
            onPress={() => router.push('/memories')}
            activeOpacity={0.85}
            accessibilityLabel={`Photo from ${item.planTitle ?? ''}`}
          >
            <Image
              source={{ uri: item.signedUrl ?? undefined }}
              style={styles.thumb}
              contentFit="cover"
            />
            {/* D-02: Plan name caption below thumbnail */}
            <Text style={styles.caption} numberOfLines={1}>
              {item.planTitle ?? ''}
            </Text>
          </TouchableOpacity>
        )}
      />
    </View>
  );
}
```

After writing, run `npx expo lint` and fix any errors. If FONT_FAMILY token path differs from what UpcomingEventsSection uses, replicate the exact token names from UpcomingEventsSection.tsx.
  </action>

  <verify>
    <automated>cd /Users/iulian/Develop/campfire && npx expo lint 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - src/components/home/RecentMemoriesSection.tsx exists
    - File contains `export function RecentMemoriesSection`
    - File contains `useAllPlanPhotos`
    - File contains `height: 104` (FlatList height constraint — prevents collapse)
    - File contains `router.push('/memories')` (navigation destination)
    - File contains `if (!isLoading && recentPhotos.length === 0) return null` (D-03 hidden when empty)
    - File contains `StyleSheet.create` inside `useMemo` (not at module level)
    - File does NOT contain `StyleSheet.create` outside of `useMemo`
    - `npx expo lint` exits 0
  </acceptance_criteria>

  <done>RecentMemoriesSection component renders a horizontal thumbnail strip of the 6 most recent photos, displays plan name captions, is hidden when no photos exist, navigates to /memories on any tap, and passes lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire RecentMemoriesSection into HomeScreen</name>
  <files>src/screens/home/HomeScreen.tsx</files>

  <read_first>
    - src/screens/home/HomeScreen.tsx — read entire file to find the exact insertion point (UpcomingEventsSection reference at ~lines 202–205) and the existing import block
  </read_first>

  <action>
Make two targeted edits to `src/screens/home/HomeScreen.tsx`:

**Edit 1 — Add import** (after the existing UpcomingEventsSection import line):
Find the line:
```typescript
import { UpcomingEventsSection } from '@/components/home/UpcomingEventsSection';
```
Add immediately after it:
```typescript
import { RecentMemoriesSection } from '@/components/home/RecentMemoriesSection';
```

**Edit 2 — Render component** (in JSX, after UpcomingEventsSection):
Find the JSX block containing:
```tsx
<UpcomingEventsSection />
```
Add immediately after it:
```tsx
<RecentMemoriesSection />
```

Ensure the placement is between `<UpcomingEventsSection />` and `<HomeWidgetRow .../>` (or whatever follows). Do not move or modify any other code. Do not change the outer ScrollView, FlatList, or any other component.

After editing, run `npx expo lint` and fix any reported errors.
  </action>

  <verify>
    <automated>cd /Users/iulian/Develop/campfire && npx expo lint 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - src/screens/home/HomeScreen.tsx contains `import { RecentMemoriesSection } from '@/components/home/RecentMemoriesSection'`
    - src/screens/home/HomeScreen.tsx contains `<RecentMemoriesSection />`
    - The `<RecentMemoriesSection />` line appears AFTER `<UpcomingEventsSection />` in the file (grep line numbers confirm order)
    - `npx expo lint` exits 0
  </acceptance_criteria>

  <done>HomeScreen imports RecentMemoriesSection and renders it after UpcomingEventsSection. Lint passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Widget → /memories route | Navigation only — no data crossing this boundary |
| RecentMemoriesSection → useAllPlanPhotos | Hook enforces session guard; widget renders data returned from hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | Information Disclosure | RecentMemoriesSection — renders recentPhotos | accept | Data filtered by RLS at hook layer (T-23-01); widget renders only what hook returns; no additional client-side risk |
| T-23-06 | Denial of Service | FlatList with many photos | accept | recentPhotos is sliced to 6 items in the hook; FlatList renders at most 6 thumbnails — no performance concern |
</threat_model>

<verification>
After plan execution:
1. `npx expo lint` exits 0
2. `grep -n "RecentMemoriesSection" src/screens/home/HomeScreen.tsx` shows both import and JSX usage
3. `grep "height: 104" src/components/home/RecentMemoriesSection.tsx` returns a match
4. `grep "router.push.*memories" src/components/home/RecentMemoriesSection.tsx` returns a match
5. Manual: launch simulator, open Home tab — widget renders with thumbnails and plan captions
</verification>

<success_criteria>
- RecentMemoriesSection renders a horizontal FlatList with `style={{ height: 104 }}`
- Thumbnails show plan name captions (D-02)
- Widget is absent from DOM when recentPhotos is empty (D-03)
- Any thumbnail tap and "See all" tap route to /memories (D-11, D-12)
- HomeScreen renders RecentMemoriesSection after UpcomingEventsSection
- `npx expo lint` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/23-memories-gallery/23-02-SUMMARY.md`
</output>
