---
phase: 04-upcoming-events-section
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - src/screens/home/HomeScreen.tsx
  - app.config.ts
autonomous: false
requirements: [EVT-01, EVT-02, EVT-03, EVT-04, EVT-05, EVT-06]

must_haves:
  truths:
    - "UpcomingEventsSection appears below the Radar/Cards viewSwitcher in HomeScreen ScrollView (D-09)"
    - "app.config.ts ios section contains NSPhotoLibraryUsageDescription key (required for Plan 04 image picker)"
    - "usePlans hook is called in HomeScreen so the store is populated when UpcomingEventsSection mounts"
    - "HomeScreen compiles and runs without TypeScript errors"
  artifacts:
    - path: src/screens/home/HomeScreen.tsx
      provides: "HomeScreen with UpcomingEventsSection wired into ScrollView"
      contains: "UpcomingEventsSection"
    - path: app.config.ts
      provides: "iOS photo library permission"
      contains: "NSPhotoLibraryUsageDescription"
  key_links:
    - from: src/screens/home/HomeScreen.tsx
      to: src/components/home/UpcomingEventsSection.tsx
      via: "import { UpcomingEventsSection }"
      pattern: "UpcomingEventsSection"
    - from: src/screens/home/HomeScreen.tsx
      to: src/hooks/usePlans.ts
      via: "usePlans() called to populate store"
      pattern: "usePlans"
---

<objective>
Wire UpcomingEventsSection into HomeScreen's ScrollView below the Radar/Cards view
switcher, and add the iOS photo library permission to app.config.ts (prerequisite for
Plan 04's image picker).

Purpose: Makes the Upcoming Events section visible in the running app. Also unblocks
Plan 04 — expo-image-picker silently fails on iOS without NSPhotoLibraryUsageDescription
(RESEARCH.md Pitfall 2).
Output: Updated HomeScreen.tsx with UpcomingEventsSection; updated app.config.ts with
iOS permission string.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-upcoming-events-section/04-CONTEXT.md
@.planning/phases/04-upcoming-events-section/04-RESEARCH.md
@.planning/phases/04-upcoming-events-section/04-02-SUMMARY.md
</context>

<interfaces>
<!-- Current HomeScreen structure (read from source before editing) -->

From src/screens/home/HomeScreen.tsx (CURRENT state — before this plan's changes):
```
ScrollView (style=scrollView, contentContainerStyle=[scrollContent, paddingTop])
  ├── View (headerContainer) → ScreenHeader title="Campfire"
  ├── View (statusCardContainer) → OwnStatusCard
  ├── error Text (conditional)
  ├── View (toggleContainer, conditional on !prefLoading) → RadarViewToggle
  └── View (viewSwitcher)
        ├── Animated.View (radarOpacity) → RadarView
        └── Animated.View (cardsOpacity, absoluteFill) → CardStackView
// [INSERT UpcomingEventsSection HERE — after </View> closing viewSwitcher, before </ScrollView>]

scrollContent style: { paddingBottom: 100 }  ← existing, no change needed
```

From app.config.ts (CURRENT — no NSPhotoLibraryUsageDescription):
```typescript
ios: {
  supportsTablet: false,
  bundleIdentifier: 'com.campfire.app',
  // ADD infoPlist block here
},
```

usePlans hook note (RESEARCH.md Pattern 5 + Pitfall 4):
The UpcomingEventsSection uses useUpcomingEvents which reads from usePlansStore.
The store is populated by the usePlans hook via useFocusEffect. HomeScreen currently
does NOT call usePlans directly — but useHomeScreen may. Verify this before editing.
If usePlans is not called from HomeScreen or useHomeScreen, add it so the store
is populated when UpcomingEventsSection mounts.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire UpcomingEventsSection into HomeScreen + add iOS permission</name>
  <files>
    src/screens/home/HomeScreen.tsx,
    app.config.ts
  </files>

  <read_first>
    - src/screens/home/HomeScreen.tsx (full file — understand current ScrollView structure and imports)
    - src/hooks/useHomeScreen.ts (check if usePlans is called here — needed for store population)
    - app.config.ts (full file — see current ios block before adding infoPlist)
  </read_first>

  <action>
**1. Update src/screens/home/HomeScreen.tsx:**

a) Add import at the top with other home component imports:
```typescript
import { UpcomingEventsSection } from '@/components/home/UpcomingEventsSection';
```

b) If usePlans is NOT already called in HomeScreen or useHomeScreen, add:
```typescript
import { usePlans } from '@/hooks/usePlans';
// Inside HomeScreen component body, before return:
usePlans(); // Populates usePlansStore so UpcomingEventsSection can filter client-side
```
If usePlans IS already called (via useHomeScreen or directly), skip this step.

c) Insert `<UpcomingEventsSection />` inside the ScrollView, AFTER the closing
`</View>` of the `viewSwitcher` View, BEFORE the `</ScrollView>` closing tag.
The exact insertion point in the JSX (D-09: below Radar/Cards view area):

```tsx
        </View>  {/* closes viewSwitcher */}

        {/* D-09: Upcoming events section — below Radar/Cards view */}
        <UpcomingEventsSection />

      </ScrollView>
```

No other changes to HomeScreen. The existing `paddingBottom: 100` on scrollContent
already provides FAB clearance — no style change needed.

**2. Update app.config.ts — add NSPhotoLibraryUsageDescription to ios block:**

In the `ios` object, add `infoPlist` after `bundleIdentifier`:
```typescript
ios: {
  supportsTablet: false,
  bundleIdentifier: 'com.campfire.app',
  infoPlist: {
    NSPhotoLibraryUsageDescription:
      'Campfire uses your photo library to set a cover image for your plans.',
  },
},
```
This is required by iOS for expo-image-picker (RESEARCH.md Pitfall 2). Without it,
launchImageLibraryAsync silently returns canceled: true on real devices.
  </action>

  <verify>
    <automated>grep -n "UpcomingEventsSection\|NSPhotoLibraryUsageDescription\|usePlans" /Users/iulian/Develop/campfire/src/screens/home/HomeScreen.tsx /Users/iulian/Develop/campfire/app.config.ts</automated>
  </verify>

  <acceptance_criteria>
    - src/screens/home/HomeScreen.tsx contains `import { UpcomingEventsSection }` from the home components path
    - src/screens/home/HomeScreen.tsx contains `<UpcomingEventsSection />` in JSX (inside ScrollView, after viewSwitcher)
    - app.config.ts contains `NSPhotoLibraryUsageDescription` key in the ios.infoPlist block
    - app.config.ts infoPlist value is a non-empty string describing photo library use
    - usePlans is called somewhere reachable from HomeScreen (either directly or via useHomeScreen) so usePlansStore is populated
    - `cd /Users/iulian/Develop/campfire && npx tsc --noEmit 2>&1 | head -10` exits 0
  </acceptance_criteria>

  <done>UpcomingEventsSection renders below the Radar/Cards switcher in HomeScreen; iOS photo library permission declared in app.config.ts.</done>
</task>

<task type="auto" tdd="false">
  <name>[BLOCKING] Task 2: Apply Supabase schema migration</name>
  <files></files>

  <read_first>
    - supabase/migrations/0013_cover_image_url.sql (verify migration file exists from Plan 01)
  </read_first>

  <action>
Run the Supabase schema push to apply the 0013_cover_image_url.sql migration to the
live database. This is BLOCKING — build and type checks pass without it (types come
from the local database.ts file), but the actual column will be missing until pushed.

```bash
cd /Users/iulian/Develop/campfire && supabase db push
```

If the command requires interactive confirmation, use SUPABASE_ACCESS_TOKEN env var:
```bash
SUPABASE_ACCESS_TOKEN=$(cat .env.local | grep SUPABASE_ACCESS_TOKEN | cut -d= -f2) supabase db push
```

If SUPABASE_ACCESS_TOKEN is not in .env.local, check:
- .env
- .env.local
- Ask the user to run `supabase login` first

After push completes successfully, verify the column exists:
```bash
supabase db diff --schema public 2>/dev/null | grep cover_image_url || echo "Column confirmed in remote DB"
```
  </action>

  <verify>
    <automated>cd /Users/iulian/Develop/campfire && supabase db push 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `supabase db push` exits 0 with no errors
    - Output does not contain "error" or "failed" (case-insensitive)
    - Migration 0013_cover_image_url.sql is reported as applied (or "No schema changes" if already applied)
  </acceptance_criteria>

  <done>cover_image_url column exists in the live plans table; all subsequent cover image uploads and reads will work correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    UpcomingEventsSection integrated into HomeScreen below the Radar/Cards view.
    DB migration applied (cover_image_url column exists in plans table).
    iOS photo library permission added to app.config.ts.
  </what-built>

  <how-to-verify>
    1. Start the app: `npx expo start` then open in Expo Go
    2. Navigate to the Home tab
    3. Scroll down past the Radar/Cards view
    4. Verify: "Upcoming events ✨" section header is visible with "See all" on the right
    5. If you have no future plans you're creator/going: verify the placeholder card shows
       with a calendar icon, "No plans yet", and "Start one and invite your crew"
    6. Tap the placeholder card — verify it navigates to the plan creation screen
    7. Tap "See all" — verify it navigates to the Explore tab
    8. If you have a future plan you created: verify an EventCard appears with title,
       date in "Mon 15, Aug · in N days" format, and avatar stack
    9. Tap an EventCard — verify it navigates to the plan detail screen
  </how-to-verify>

  <resume-signal>Type "approved" or describe any issues seen</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usePlansStore → UpcomingEventsSection | Store data is populated from authenticated Supabase queries in usePlans; no untrusted data enters here |
| iOS infoPlist | NSPhotoLibraryUsageDescription is a static string compiled into the app bundle — no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Denial of Service | HomeScreen ScrollView | accept | Adding UpcomingEventsSection is additive; section renders at most 5 cards (useUpcomingEvents cap) — no performance risk |
| T-04-07 | Elevation | supabase db push | mitigate | Run only with valid SUPABASE_ACCESS_TOKEN from authenticated session; migration uses IF NOT EXISTS to be idempotent |
</threat_model>

<verification>
After all tasks:
1. `npx tsc --noEmit` exits 0
2. HomeScreen JSX contains `<UpcomingEventsSection />` inside ScrollView after viewSwitcher
3. app.config.ts ios block contains infoPlist.NSPhotoLibraryUsageDescription
4. `supabase db push` applied 0013_cover_image_url.sql successfully
5. Human verification checkpoint approved
</verification>

<success_criteria>
- UpcomingEventsSection visible in running app below Radar/Cards area
- Empty state or event cards render correctly based on user's plan data
- cover_image_url column live in Supabase plans table
- iOS permission declared for image picker (Plan 04 unblocked)
- TypeScript clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-upcoming-events-section/04-03-SUMMARY.md`
</output>
