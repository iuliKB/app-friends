---
phase: 31-adopt-tanstack-query-for-server-state-caching-and-cross-scre
plan: 07
type: execute
wave: 7
depends_on: [06]
files_modified:
  - src/hooks/useSpotlight.ts
  - src/hooks/useStreakData.ts
  - src/hooks/__tests__/useSpotlight.test.ts
autonomous: true
requirements: [TSQ-01, TSQ-02, TSQ-07, TSQ-08]
user_setup: []

must_haves:
  truths:
    - "useSpotlight (extended by Phase 29.1) migrates to useQuery and keeps Phase 29.1's extensions intact"
    - "useStreakData migrates to useQuery keyed by queryKeys.habits.streak(userId) and shares NO cache with useHabits (different RPC, different concept)"
    - "Streak display on home Bento tile reflects habit check-ins via invalidation cascade (useHabits.toggleToday already invalidates home.all, which is the parent of streak)"
    - "Any supabase.functions.invoke usage from Wave 1 audit is modeled as a useMutation in its respective wave (or recorded if none exist)"
  artifacts:
    - path: "src/hooks/useSpotlight.ts"
      provides: "useQuery wrapper preserving Phase 29.1 extensions"
      contains: "useQuery"
    - path: "src/hooks/useStreakData.ts"
      provides: "useQuery keyed by queryKeys.habits.streak"
      contains: "useQuery"
  key_links:
    - from: "src/hooks/useStreakData.ts"
      to: "src/lib/queryKeys.ts"
      via: "queryKeys.habits.streak(userId)"
      pattern: "queryKeys\\.habits\\.streak"
    - from: "src/hooks/useSpotlight.ts"
      to: "src/lib/queryKeys.ts"
      via: "queryKeys.home.spotlight(userId)"
      pattern: "queryKeys\\.home\\.spotlight"
---

<objective>
Migrate the two remaining non-chat read-only hooks deferred from Wave 3:

- `useSpotlight` — Phase 29.1 extended this hook (STATE.md line 91 — "useSpotlight extension"). Migration preserves the extension contract.
- `useStreakData` — research §Pilot Vertical Deep-Dive deferred this from Wave 2 (different RPC, different table, different concept from habit check-ins). Migrate it as a vanilla read-only useQuery keyed by `queryKeys.habits.streak(userId)`.

Also process any Edge Function findings carried forward from Wave 1's audit (Plan 01 Task 9). If the count was zero, this plan records that. If non-zero, the corresponding files are migrated here (read the Wave 1 SUMMARY to determine scope).

Purpose: Close out the non-chat surface. After this plan ships, only chat (Wave 8) plus the persistence/boundary-doc plan remain.

Output:
- 2 hook migrations.
- Edge Function audit follow-through recorded in SUMMARY.
- mutationShape gate green (no new mutations expected — both hooks are read-only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/31-adopt-tanstack-query-for-server-state-caching-and-cross-scre/CONTEXT.md
@.planning/phases/31-adopt-tanstack-query-for-server-state-caching-and-cross-scre/31-RESEARCH.md
@.planning/phases/31-adopt-tanstack-query-for-server-state-caching-and-cross-scre/31-PATTERNS.md
@.planning/phases/31-adopt-tanstack-query-for-server-state-caching-and-cross-scre/31-01-SUMMARY.md
@.planning/phases/31-adopt-tanstack-query-for-server-state-caching-and-cross-scre/31-06-SUMMARY.md
@src/hooks/useSpotlight.ts
@src/hooks/useStreakData.ts
@src/hooks/__tests__/useSpotlight.test.ts
@src/hooks/useHabits.ts
@src/lib/queryKeys.ts
</context>

<assumptions>
- Wave 6 shipped clean.
- Phase 29.1's useSpotlight extension shape is documented in the pre-migration file — read it fully before migrating.
- Edge Function audit from Wave 1 SUMMARY is the source of truth for any additional migrations in this plan.
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate useSpotlight.ts preserving Phase 29.1 extension contract</name>
  <files>src/hooks/useSpotlight.ts, src/hooks/__tests__/useSpotlight.test.ts</files>
  <read_first>
    - src/hooks/useSpotlight.ts (full file — note Phase 29.1 extension)
    - src/hooks/__tests__/useSpotlight.test.ts (existing test)
    - src/hooks/useHomeScreen.ts (Wave 3 — composition analog)
    - src/lib/queryKeys.ts (queryKeys.home.spotlight)
    - .planning/phases/31-adopt-tanstack-query-for-server-state-caching-and-cross-scre/31-PATTERNS.md (line 49 — composition analog)
  </read_first>
  <action>
    Read pre-migration `useSpotlight.ts`. It likely composes multiple sources (Phase 29.1 added habits/todos to the spotlight aggregation per STATE.md line 91). Rewrite using either a single `useQuery` if the aggregation happens server-side, or `useQueries` if multiple RPCs are involved client-side.

    Key: `queryKeys.home.spotlight(userId)`.

    Preserve the public return shape exactly. Update `useSpotlight.test.ts` to wrap renderHook with `createTestQueryClient().wrapper`.

    CRITICAL constraints:
    - Phase 29.1's extension shape is intact — read the file carefully and do NOT drop any of the new fields/branches the extension added.
    - No mutations expected (this is a read-only aggregate).
  </action>
  <verify>
    <automated>npx jest --testPathPatterns="useSpotlight" --no-coverage 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "useQuery\\(|useQueries\\(" src/hooks/useSpotlight.ts` returns at least `1`
    - `grep -c "queryKeys.home.spotlight" src/hooks/useSpotlight.ts` returns at least `1`
    - `grep -cE "useState<" src/hooks/useSpotlight.ts` returns `0`
    - `grep -c "createTestQueryClient" src/hooks/__tests__/useSpotlight.test.ts` returns at least `1`
    - `npx jest --testPathPatterns="useSpotlight" --no-coverage 2>&1 | grep -c "passed"` is at least `1`
  </acceptance_criteria>
  <done>useSpotlight migrated; Phase 29.1 extensions preserved; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate useStreakData.ts to useQuery keyed by queryKeys.habits.streak(userId)</name>
  <files>src/hooks/useStreakData.ts</files>
  <read_first>
    - src/hooks/useStreakData.ts (full file — note: different RPC + table from useHabits per research §Pilot Vertical Deep-Dive A5/Q3)
    - src/hooks/useHabits.ts (analog for query-only — strip the mutation)
    - src/lib/queryKeys.ts (queryKeys.habits.streak)
  </read_first>
  <action>
    Replace useState+useEffect+useCallback fetcher with single `useQuery({ queryKey: queryKeys.habits.streak(userId), queryFn })`. Preserve public return shape.

    Note: this key is in the `habits` namespace (`queryKeys.habits.streak`) — invalidating `queryKeys.habits.all()` will invalidate BOTH overview AND streak. The Wave 2 `useHabits.toggleToday.onSettled` currently invalidates only `queryKeys.habits.overview(today)`. **Do not change Wave 2's invalidation set** — streak is computed separately on the server; toggling a habit check-in does NOT necessarily change the streak (only end-of-day batches do).

    If a manual smoke reveals the streak tile is stale after a habit toggle, broaden Wave 2's invalidation to `queryKeys.habits.all()` in a future cleanup. Record the observation in SUMMARY.
  </action>
  <verify>
    <automated>npx jest --testPathPatterns="useStreakData" --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useQuery({" src/hooks/useStreakData.ts` returns at least `1`
    - `grep -c "queryKeys.habits.streak" src/hooks/useStreakData.ts` returns at least `1`
    - `grep -cE "useState<" src/hooks/useStreakData.ts` returns `0`
    - `grep -c "supabase.channel" src/hooks/useStreakData.ts` returns `0`
  </acceptance_criteria>
  <done>useStreakData migrated to single useQuery; cache namespace shared with habits.</done>
</task>

<task type="auto">
  <name>Task 3: Process Edge Function findings from Wave 1 audit</name>
  <files>.planning/phases/31-adopt-tanstack-query-for-server-state-caching-and-cross-scre/31-07-SUMMARY.md</files>
  <read_first>
    - .planning/phases/31-adopt-tanstack-query-for-server-state-caching-and-cross-scre/31-01-SUMMARY.md (`## Audit Findings` section)
  </read_first>
  <action>
    Read the Wave 1 SUMMARY's `## Audit Findings` section. Based on the findings:

    **Case A — count is 0:** Record in 31-07-SUMMARY.md: "No Edge Function invocations found in src/ at Wave 1 audit. No additional mutations to migrate." Move on.

    **Case B — count is >0:** For each invocation:
    1. Read the surrounding hook code.
    2. If the invocation lives in a hook NOT yet migrated (i.e., not chat — those wait for Wave 8), wrap it in a `useMutation` here. The `mutationFn` is the `supabase.functions.invoke(...)` call (it returns `{data, error}` like other Supabase calls — `throw error` on rejection).
    3. Follow canonical Pattern 5 OR `@mutationShape: no-optimistic` per the function's semantics.
    4. Invalidate any relevant query keys.

    **Case C — count is >0 AND the invocations are all in chat hooks:** Record in 31-07-SUMMARY.md that Wave 8 (chat) owns them. Do NOT migrate here.

    Record the final result in 31-07-SUMMARY.md under `## Edge Function Migration Status`.
  </action>
  <verify>
    <automated>grep -q "## Edge Function Migration Status" .planning/phases/31-adopt-tanstack-query-for-server-state-caching-and-cross-scre/31-07-SUMMARY.md && echo SECTION_PRESENT || echo "SUMMARY pending — task 3 records this at plan completion"</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY contains a `## Edge Function Migration Status` section with one of three explicit statuses: "None", "Migrated here", or "Deferred to Wave 8 (chat)".
    - If migrated: the corresponding hook files contain a useMutation block whose mutationFn calls `supabase.functions.invoke`.
    - If deferred to chat: a TODO note appears in the 31-07-SUMMARY.md `## Carry-Forward to Wave 8` section.
  </acceptance_criteria>
  <done>Edge Function audit fully processed; status recorded; any non-chat invocations migrated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| streak data cache → home tile | No new boundaries; streak is read-only and per-user. |
| edge function response payload → cache | If any edge function returns data that lands in the cache, it inherits the same Realtime-payload trust treatment (validate, prefer invalidate over setQueryData). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-22 | Information Disclosure | streak cache survives sign-out | mitigate | Inherited Wave 1 authBridge. Per-user key. |
| T-31-23 | Tampering | Edge function response with unexpected shape corrupts cache | mitigate | useMutation's `onSuccess: invalidateQueries` (no-optimistic exemption for any edge-function mutation) forces a re-read from canonical sources. Direct setQueryData on edge function output is forbidden unless the function returns a fully-typed row guarded by a runtime type check. |
</threat_model>

<verification>
- 2 hooks migrated.
- Edge Function audit closed out (Case A/B/C recorded).
- mutationShape gate green.
- jest full suite green; tsc clean.
</verification>

<success_criteria>
1. useSpotlight migrated; Phase 29.1 extension preserved; test wrapped with createTestQueryClient.
2. useStreakData migrated; cache lives under queryKeys.habits.streak namespace.
3. Edge Function migration status definitive (none / migrated / deferred).
4. No callsite changes for either hook.
</success_criteria>

<output>
After completion, create `.planning/phases/31-adopt-tanstack-query-for-server-state-caching-and-cross-scre/31-07-SUMMARY.md` documenting:
- 2 hook migrations
- `## Edge Function Migration Status` with explicit Case A/B/C
- `## Carry-Forward to Wave 8` listing: chat hooks (3) + persistence + boundary doc + useChatStore.chatList strip
</output>
