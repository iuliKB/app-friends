---
phase: 01-push-infrastructure-dm-entry-point
plan: 10
type: execute
wave: 5
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
  - "01-04"
  - "01-05"
  - "01-06"
  - "01-07"
  - "01-08"
  - "01-09"
files_modified:
  - .planning/phases/01-push-infrastructure-dm-entry-point/SMOKE-TEST.md
autonomous: false
requirements:
  - PUSH-01
  - PUSH-02
  - PUSH-03
  - PUSH-04
  - PUSH-05
  - PUSH-06
  - PUSH-07
  - PUSH-08
  - PUSH-09
  - PUSH-10
  - DM-01
must_haves:
  truths:
    - "SMOKE-TEST.md exists in the phase folder with one explicit checklist item per requirement"
    - "User has run the checklist on the EAS dev build and ticked every item"
    - "Any failures discovered are documented as gap-closure candidates for /gsd:plan-phase 1 --gaps"
  artifacts:
    - path: ".planning/phases/01-push-infrastructure-dm-entry-point/SMOKE-TEST.md"
      provides: "Manual checklist mapped 1:1 to PUSH-01..10 + DM-01"
      contains: "PUSH-01"
  key_links:
    - from: ".planning/phases/01-push-infrastructure-dm-entry-point/SMOKE-TEST.md"
      to: "Real EAS dev build on real devices"
      via: "Manual sign-off"
      pattern: "EAS dev build"
---

<objective>
Author the manual smoke-test checklist for Phase 1 and have the user execute it on the EAS dev build from Plan 01. This is the phase gate — without manual sign-off, push delivery, action sheets, and channel correctness cannot be verified (no JS test framework, no E2E runtime in v1.3 per the validation strategy).

Purpose: Per CONTEXT.md D-10, the smoke-test checklist lives inside the phase folder. Per the VALIDATION.md sampling strategy, manual sign-off on the EAS dev build is the phase gate before `/gsd:verify-work`.

Output: SMOKE-TEST.md with one checklist item per requirement, plus instructions for what to do if a check fails (open a gap-closure plan via `/gsd:plan-phase 1 --gaps`).
</objective>

<execution_context>
@/Users/iulian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iulian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-push-infrastructure-dm-entry-point/01-CONTEXT.md
@.planning/phases/01-push-infrastructure-dm-entry-point/01-VALIDATION.md
@.planning/phases/01-push-infrastructure-dm-entry-point/01-RESEARCH.md
</context>

<threat_model>
- Manual checklist is the verification gate for runtime behaviors that have no automated coverage.
- Out-of-scope: any threat that requires a test framework — deferred to v1.4 if test infra is added.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author SMOKE-TEST.md checklist</name>
  <files>.planning/phases/01-push-infrastructure-dm-entry-point/SMOKE-TEST.md</files>
  <read_first>
    - .planning/phases/01-push-infrastructure-dm-entry-point/01-VALIDATION.md (Manual-Only Verifications table)
    - .planning/phases/01-push-infrastructure-dm-entry-point/01-CONTEXT.md (D-10, D-11)
  </read_first>
  <action>
Create `.planning/phases/01-push-infrastructure-dm-entry-point/SMOKE-TEST.md` with the following content. Use markdown checkboxes; copy the requirement IDs and behavior verbatim from VALIDATION.md "Manual-Only Verifications":

```markdown
# Phase 1 Smoke-Test Checklist

**Phase:** 01 — Push Infrastructure & DM Entry Point
**Validation strategy:** Manual sign-off on EAS dev build (no JS test framework in v1.3 per zero-new-deps rule).

## Prerequisites
- [ ] EAS dev build installed on a real iPhone (Plan 01 sign-off)
- [ ] EAS dev build installed on a real Android device or emulator (Plan 01 sign-off)
- [ ] `supabase/migrations/0004_push_tokens_v1_3.sql` applied (Plan 02 sign-off)
- [ ] `notify-plan-invite` Edge Function deployed (Plan 07 sign-off)
- [ ] Two distinct test users (User A and User B), already friends
- [ ] Supabase SQL editor access for spot-checks

## Per-requirement checklist

### PUSH-01 — Token registers on session-ready
- [ ] Fresh install on iPhone, log in as User B
- [ ] Within 5 seconds, run in Supabase SQL editor:
  `SELECT * FROM public.push_tokens WHERE user_id = '<user-b-id>';`
- [ ] Expected: at least one row with non-null device_id, last_seen_at within the last minute, invalidated_at IS NULL

### PUSH-02 — Foreground re-register on AppState 'active'
- [ ] Note current `last_seen_at` for User B's row
- [ ] Background the app for 30 seconds (home button / app switcher)
- [ ] Foreground the app
- [ ] Re-query `last_seen_at` — expected: advanced to within the last 10 seconds

### PUSH-03 — Schema columns and composite unique
- [ ] In Supabase SQL editor: `\d public.push_tokens`
- [ ] Expected columns present: device_id text NOT NULL, last_seen_at timestamptz NOT NULL DEFAULT now(), invalidated_at timestamptz
- [ ] Expected indexes: idx_push_tokens_user_device (UNIQUE), idx_push_tokens_active (partial WHERE invalidated_at IS NULL)

### PUSH-04 — Toggle OFF deletes server row, ON re-registers
- [ ] On the device: Profile → Notifications toggle → OFF
- [ ] Re-query: row should be GONE for that (user_id, device_id)
- [ ] Toggle → ON
- [ ] Re-query: row reappears within 5 seconds, no UI prompt shown

### PUSH-05 — Plan invite reaches a fresh install
- [ ] Fresh install (delete + reinstall) on User B's device
- [ ] Log in as User B (DO NOT toggle the Profile notifications switch)
- [ ] On User A's device, create a plan and invite User B
- [ ] Expected: User B receives a "Plan invite" push within ~5 seconds without ever opening the Profile screen

### PUSH-06 — iOS notification action buttons (morning_prompt category)
- [ ] On iOS only. Send a local notification with categoryIdentifier 'morning_prompt' from the Expo Push Tool, OR temporarily add a "Test Push" button in the dev build that calls `Notifications.scheduleNotificationAsync({ content: { title: 'test', categoryIdentifier: 'morning_prompt' }, trigger: null })`
- [ ] On the lock screen, swipe down on the notification
- [ ] Expected: three action buttons visible — Free, Busy, Maybe

### PUSH-07 — Android channels exist
- [ ] On Android only. Settings → Apps → Campfire → Notifications
- [ ] Expected channels visible: plan_invites, friend_free, morning_prompt, system
- [ ] (Legacy `default` channel may also be present on upgraded installs — acceptable per D-18)

### PUSH-08 — iOS pre-prompt timing
- [ ] Fresh install on iPhone, log in
- [ ] Expected: NO iOS system permission prompt yet
- [ ] Set your status (Free / Busy / Maybe) for the first time
- [ ] Expected: pre-prompt modal "Stay in the loop" appears (or surfaces on next foreground)
- [ ] Tap "Sounds good"
- [ ] Expected: native iOS permission modal appears

### PUSH-09 — DeviceNotRegistered marks invalidated_at
- [ ] In Supabase SQL editor, manually corrupt a known token:
  `UPDATE public.push_tokens SET token = 'ExponentPushToken[INVALID_TEST]' WHERE user_id = '<test-user>';`
- [ ] Trigger a plan invite to that user (any flow that calls notify-plan-invite)
- [ ] Re-query: `SELECT invalidated_at FROM public.push_tokens WHERE user_id = '<test-user>';`
- [ ] Expected: invalidated_at is non-null

### PUSH-10 — EAS dev build exists
- [ ] `eas build:list --platform ios --profile development` shows a successful build
- [ ] `eas build:list --platform android --profile development` shows a successful build
- [ ] Both artifacts are installable on test devices

### DM-01 — HomeFriendCard tappable
- [ ] Single tap on any friend card on the Home screen
- [ ] Expected: chat room opens at `/chat/room?dm_channel_id=...&friend_name=...`
- [ ] Long-press on a friend card
- [ ] Expected: action sheet appears with "View profile" and "Plan with {firstName}..."
- [ ] Tap "View profile" — routes to `/friends/[id]`
- [ ] Tap "Plan with..." — routes to `/plan-create` with that friend pre-selected (verify only the long-pressed friend is selected in the picker)
- [ ] List scrolling on Home still works smoothly (Pitfall #6 — long-press should not block scroll)

## Sign-off

- [ ] All requirement checks above passed
- [ ] No regressions in existing v1.0–v1.2 features (login, status set, plan create, chat send)
- [ ] Phase ready for `/gsd:verify-work`

## If a check fails

1. Note which requirement failed and what was observed.
2. Run `/gsd:plan-phase 1 --gaps` and pass the failing item as the gap source.
3. The gap-closure plan will be added as Plan 11+ and executed before re-running this checklist.
```

  </action>
  <verify>
    <automated>test -f .planning/phases/01-push-infrastructure-dm-entry-point/SMOKE-TEST.md && for req in PUSH-01 PUSH-02 PUSH-03 PUSH-04 PUSH-05 PUSH-06 PUSH-07 PUSH-08 PUSH-09 PUSH-10 DM-01; do grep -q "$req" .planning/phases/01-push-infrastructure-dm-entry-point/SMOKE-TEST.md || { echo "MISSING: $req"; exit 1; }; done && grep -q "Sign-off" .planning/phases/01-push-infrastructure-dm-entry-point/SMOKE-TEST.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/01-push-infrastructure-dm-entry-point/SMOKE-TEST.md
    - Contains a checklist item for every requirement: PUSH-01..10 and DM-01 (11 total)
    - Contains "Sign-off" section
    - Contains instructions for handling a failed check (gap-closure mention)
    - Each requirement section has at least one actionable bullet starting with `- [ ]`
  </acceptance_criteria>
  <done>SMOKE-TEST.md exists with all 11 requirement checks and a sign-off section.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User runs the smoke-test checklist on the EAS dev build</name>
  <what-built>SMOKE-TEST.md with one check per requirement (Task 1 output) + the cumulative output of Plans 01-09</what-built>
  <how-to-verify>
    1. Open `.planning/phases/01-push-infrastructure-dm-entry-point/SMOKE-TEST.md` in your editor.
    2. Confirm all Plan-01..09 prerequisites are signed off (EAS dev build installed, migration applied, Edge Function deployed).
    3. Walk through each requirement section in order (PUSH-01 → DM-01).
    4. Tick each checkbox as you verify the behavior on the real device(s).
    5. If anything fails: note exactly what went wrong, then run `/gsd:plan-phase 1 --gaps` to author a gap-closure plan.
    6. When all 11 requirement checks pass and the Sign-off section is fully ticked, you are done.
  </how-to-verify>
  <resume-signal>Type "smoke green" when all 11 checks pass and the sign-off section is ticked. If any check failed, paste the failing requirement ID and observed behavior so Claude can plan a gap closure.</resume-signal>
  <action>Manual checkpoint — see <how-to-verify> instructions above. Claude does not execute this task; the user performs the steps and signals completion via <resume-signal>.</action>
  <verify>
    <automated>MANUAL — user signals completion per <resume-signal></automated>
  </verify>
  <done>User has executed the steps in <how-to-verify> and replied with the <resume-signal>.</done>
</task>

</tasks>

<verification>
- SMOKE-TEST.md exists with all 11 requirement checks
- User has signed off the entire checklist on the EAS dev build
- Phase 1 is verifiably complete from the user's perspective
</verification>

<success_criteria>
1. Manual smoke-test checklist exists in the phase folder.
2. User has run the checklist on real devices and signed off every item.
3. Phase 1 is ready for `/gsd:verify-work`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-push-infrastructure-dm-entry-point/01-10-SUMMARY.md`
</output>
