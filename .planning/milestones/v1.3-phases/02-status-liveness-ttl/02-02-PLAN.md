---
phase: 02-status-liveness-ttl
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - supabase/migrations/0009_status_liveness_v1_3.sql
autonomous: false
gap_closure: false
requirements:
  - TTL-08
must_haves:
  truths:
    - "Migration 0009 has been applied to the linked Supabase project's live database"
    - "public.statuses has columns status_expires_at (NOT NULL) and last_active_at (NOT NULL DEFAULT now())"
    - "public.status_history table exists with RLS enabled and is queryable for own + friend rows"
    - "public.effective_status view exists with security_invoker=true and returns NULL when status_expires_at < now() OR last_active_at < now() - interval '8 hours'"
    - "TTL-08 retention rollup/GC is documented as DEFERRED to v1.4 (per OVR-05) — no rollup function shipped"
  artifacts:
    - path: "supabase/migrations/0009_status_liveness_v1_3.sql"
      provides: "Already authored in Plan 01 — this plan APPLIES it via supabase db push"
      contains: "CREATE TRIGGER on_status_transition"
  key_links:
    - from: "supabase/migrations/0009_status_liveness_v1_3.sql"
      to: "live Supabase project schema"
      via: "npx supabase db push"
      pattern: "supabase db push"
---

<objective>
[BLOCKING] Apply migration `0009_status_liveness_v1_3.sql` (authored in Plan 01) to the live Supabase project so all downstream consumer plans (02-04, 02-05, 02-06) can read from `public.effective_status`, write `last_active_at`, and rely on the `status_history` trigger.

Purpose: Unblocks every Phase 2 plan that touches the database. Without this push, build + tsc + eslint pass (TypeScript types come from `src/types/database.ts`, not the live schema), but every runtime read against `effective_status` and every write to `last_active_at` will fail at first foreground.

Also formally documents TTL-08 (retention rollup/GC) as DEFERRED to v1.4 per OVR-05. No rollup/GC code is shipped in this plan.

Output: Live Supabase database has migration 0009 applied; tables/columns/view/trigger/RLS all present.
</objective>

<execution_context>
@/Users/iulian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iulian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-status-liveness-ttl/02-CONTEXT.md
@.planning/phases/02-status-liveness-ttl/02-01-SUMMARY.md
@supabase/migrations/0009_status_liveness_v1_3.sql

<interfaces>
The Supabase CLI is installed as a devDependency (`supabase` ^2.81.1 in package.json:62). There is NO `db:push` npm script — the canonical command is:

```
npx supabase db push
```

This applies all unapplied migrations from `supabase/migrations/` to the project linked via `supabase link`. The project must already be linked (Plan 01 era — verify via `npx supabase projects list` or by inspecting `supabase/.temp/project-ref` if it exists).

Per OVR-05 (02-CONTEXT.md): TTL-08 retention rollup + GC are explicitly DEFERRED to v1.4. pg_cron is NOT enabled on the project. No scheduled-job code ships in v1.3. The trigger + table + RLS land via 0009; manual cleanup is acceptable for the milestone.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → Supabase project | Authenticated via Supabase access token / linked project; pushes DDL |
| migration DDL → live schema | New tables/views/policies become enforceable immediately |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | applied schema differs from authored migration | mitigate | Run `npx supabase db diff --linked` before push and confirm only the 0009 file is unapplied; after push, run a verification SELECT against `pg_policies`, `information_schema.columns`, and `information_schema.views` to confirm the schema landed exactly as 0009 specifies |
| T-02-08 | Information Disclosure | RLS misconfiguration on status_history | mitigate | Post-push verification SELECT against `pg_policies` confirms `status_history_select_own_or_friend` exists and references `is_friend_of(user_id)`; no INSERT/UPDATE/DELETE policy is present |
| T-02-09 | Elevation of Privilege | trigger fails to set search_path | mitigate | Post-push verification queries `pg_proc` to confirm `on_status_transition` has `prosecdef = true` and `proconfig` includes `search_path=` |
| T-02-10 | Denial of Service | migration fails partway and leaves schema half-applied | accept | Supabase migrations run in a transaction by default; failure rolls back. Operator (user) re-runs after fixing. Low risk for v1.3 scale. |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Apply migration 0009 to live Supabase project</name>
  <files>supabase/migrations/0009_status_liveness_v1_3.sql</files>
  <read_first>
    - .planning/phases/02-status-liveness-ttl/02-01-SUMMARY.md (what was authored)
    - supabase/migrations/0009_status_liveness_v1_3.sql (the file being applied)
    - package.json (confirm `supabase` devDep ^2.81.1; confirm there is NO `db:push` script)
  </read_first>
  <action>
Executor: this is a [BLOCKING] checkpoint task. Perform all steps in `<what-built>` below (1. verify file → 2. `npx supabase db diff --linked` → 3. `npx supabase db push` → 4. run 6 verification SELECTs → 5. append outputs to SUMMARY), then present `<how-to-verify>` to the user and pause for their `approved` signal before proceeding. If ANY step fails or the linked-project check errors, stop and surface the error — do NOT bypass the checkpoint.
  </action>
  <what-built>
Plan 01 authored `supabase/migrations/0009_status_liveness_v1_3.sql` but did not apply it. This task applies it to the linked Supabase project so the new columns, table, view, trigger, and RLS policies become live.

The Claude executor will:
1. Verify the migration file exists and matches Plan 01's canonical shape (grep assertions from 02-01-PLAN.md `<verify>` block).
2. Run `npx supabase db diff --linked --schema public 2>&1 | tee /tmp/supabase-diff-pre.txt` to confirm 0009 is in fact pending and no other unrelated drift exists. If the linked-project check fails or asks for `supabase login`, the executor stops and surfaces the error.
3. Run `npx supabase db push` (no `--include-all` flag — only unapplied migrations should land).
4. After push: run verification queries via `npx supabase db execute` (or, if unavailable, instruct user to paste them into the Supabase Studio SQL editor):
   - `SELECT column_name, is_nullable, column_default FROM information_schema.columns WHERE table_schema='public' AND table_name='statuses' AND column_name IN ('status_expires_at','last_active_at') ORDER BY column_name;` — must return 2 rows, both `is_nullable='NO'`, `last_active_at` default `now()`.
   - `SELECT to_regclass('public.status_history') IS NOT NULL AS exists;` — must return `true`.
   - `SELECT to_regclass('public.effective_status') IS NOT NULL AS exists;` — must return `true`.
   - `SELECT polname FROM pg_policy WHERE polrelid = 'public.status_history'::regclass;` — must return exactly `status_history_select_own_or_friend`.
   - `SELECT prosecdef, proconfig FROM pg_proc WHERE proname='on_status_transition';` — `prosecdef` must be `true` and `proconfig` must contain `search_path=`.
   - `SELECT viewname FROM pg_views WHERE schemaname='public' AND viewname='effective_status';` — must return one row.
   - `SELECT (reloptions::text) LIKE '%security_invoker=true%' AS is_invoker FROM pg_class WHERE oid='public.effective_status'::regclass;` — must return `true`.
5. Append all six SELECT outputs to the SUMMARY as the verification trace.
  </what-built>
  <how-to-verify>
After Claude reports completion, the user MUST manually confirm one of these in the Supabase Studio (https://supabase.com/dashboard → project → Table Editor / SQL Editor):

1. Open **Table Editor → statuses** → confirm `status_expires_at` and `last_active_at` columns are visible and marked NOT NULL.
2. Open **Table Editor → status_history** → confirm the table exists with columns `id, user_id, status, context_tag, status_expires_at, occurred_at`. Click the **Auth** padlock icon → confirm RLS is **enabled** and the policy `status_history_select_own_or_friend` is listed.
3. Open **Database → Functions** → confirm `on_status_transition` exists, language `plpgsql`, **Security Definer** badge present.
4. Open **Database → Views** → confirm `effective_status` exists. Click it → confirm the SQL definition contains `security_invoker = true` (or run `\d+ public.effective_status` from psql).
5. Run in **SQL Editor**: `SELECT * FROM public.effective_status LIMIT 1;` — should return at least one row with `effective_status` column populated (or NULL for expired/dead users); no permission error.

If any of the five checks fail, type `failed: <which check>` and Claude will diagnose. If all pass, type `approved`.

**TTL-08 deferral confirmation:** The user also confirms they understand that `status_history` retention rollup and GC are NOT shipped in v1.3 — this is explicitly per CONTEXT.md OVR-05. At v1.3 scale (3-15 person friend graphs, ~120 transition rows/user/month worst case) the table will not need active management before v1.4 ships the rollup function.
  </how-to-verify>
  <acceptance_criteria>
    - `npx supabase db push` exited with code 0 and reported migration 0009 as applied
    - `information_schema.columns` confirms `status_expires_at` and `last_active_at` exist on `public.statuses`, both `is_nullable = 'NO'`, `last_active_at` `column_default` matches `now()`
    - `to_regclass('public.status_history')` returns non-null
    - `to_regclass('public.effective_status')` returns non-null
    - `pg_policy` for `public.status_history` lists exactly one policy named `status_history_select_own_or_friend`
    - `pg_proc.prosecdef = true` for `on_status_transition` and `proconfig` contains `search_path=`
    - `pg_class.reloptions` for `public.effective_status` contains `security_invoker=true`
    - User typed `approved` after the 5 manual Studio checks
    - SUMMARY.md captures all six verification SELECT outputs verbatim
    - SUMMARY.md notes TTL-08 deferred per OVR-05 and links the v1.4 follow-up
  </acceptance_criteria>
  <done>Migration 0009 is live on the linked Supabase project; all 6 verification SELECTs return the expected shape; user has typed `approved` after the 5 Studio checks; SUMMARY.md contains the verification trace and the TTL-08 deferral note linking OVR-05.</done>
  <resume-signal>Type "approved" after the 5 Studio checks pass, or "failed: &lt;which check&gt;" with details</resume-signal>
</task>

</tasks>

<verification>
- `npx supabase db push` succeeds and applies exactly migration 0009
- Six verification SELECTs all return the expected shape (captured in SUMMARY)
- Five manual Supabase Studio checks pass
- TTL-08 deferral note is present in SUMMARY.md and references CONTEXT.md OVR-05
</verification>

<success_criteria>
1. Migration 0009 is applied to the live Supabase project linked to this repo.
2. All schema artifacts from Plan 01 (columns, table, view, trigger, RLS policy) are queryable via Supabase Studio.
3. `effective_status` view is accessible to authenticated users and returns NULL for rows with expired status or stale (>8h) last_active_at.
4. Downstream Phase 2 plans (02-04, 02-05, 02-06) can run without database errors at first foreground.
5. TTL-08 is documented as deferred per OVR-05; no rollup/GC code or pg_cron schedule shipped.
</success_criteria>

<output>
After completion, create `.planning/phases/02-status-liveness-ttl/02-02-SUMMARY.md`
</output>
</content>
