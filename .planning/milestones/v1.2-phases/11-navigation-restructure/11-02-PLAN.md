---
phase: 11-navigation-restructure
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - tests/visual/design-system.spec.ts
autonomous: false
requirements:
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "Playwright test navigates to Explore tab (not Plans)"
    - "Playwright test navigates to Chats tab (not Chat)"
    - "All 7 visual regression snapshots regenerated with new tab bar"
    - "npx playwright test passes with zero failures"
  artifacts:
    - path: "tests/visual/design-system.spec.ts"
      provides: "Updated locators and test names for renamed tabs"
      contains: "getByText(\"Explore\")"
  key_links:
    - from: "tests/visual/design-system.spec.ts"
      to: "src/app/(tabs)/_layout.tsx"
      via: "getByText locators matching tab titles"
      pattern: "getByText.*Explore|getByText.*Chats"
---

<objective>
Update Playwright visual regression tests to match the renamed tabs (Explore, Chats) and regenerate all snapshot baselines.

Purpose: Tests must pass after the tab renames from Plan 01. Locators referencing old names ("Plans", "Chat") will fail. All 7 snapshots are stale because the tab bar layout changed (reorder + new icons).
Output: Updated test file with new locators, regenerated snapshot baselines, green test suite.
</objective>

<execution_context>
@/Users/iulian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iulian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-navigation-restructure/11-01-SUMMARY.md

<interfaces>
<!-- Current Playwright test file — locators that need updating -->

From tests/visual/design-system.spec.ts:
```typescript
// Line 52: await page.getByText("Plans").click();  -> change to "Explore"
// Line 60: await page.getByText("Chat").click();   -> change to "Chats"
// Line 68: await page.getByText("Squad").click();  -> unchanged
// Line 76: await page.getByText("Profile").click(); -> unchanged

// Test names to update:
// "plans screen" -> "explore screen"  (changes snapshot filename)
// "chat screen"  -> "chats screen"   (changes snapshot filename)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Playwright locators, test names, and regenerate snapshots</name>
  <files>tests/visual/design-system.spec.ts</files>
  <read_first>
    - tests/visual/design-system.spec.ts
    - playwright.config.ts
    - .planning/phases/11-navigation-restructure/11-RESEARCH.md (section: "Updated Playwright locators")
  </read_first>
  <action>
**Step 1: Update locators in `tests/visual/design-system.spec.ts`:**

- Line 52: `await page.getByText("Plans").click()` -> `await page.getByText("Explore").click()`
- Line 60: `await page.getByText("Chat").click()` -> `await page.getByText("Chats").click()`

**Step 2: Update test names to match new labels:**

- `test("plans screen", ...)` -> `test("explore screen", ...)`
- `test("chat screen", ...)` -> `test("chats screen", ...)`

**Step 3: Update screenshot filenames in `toHaveScreenshot()` calls:**

- `"plans-screen.png"` -> `"explore-screen.png"`
- `"chat-screen.png"` -> `"chats-screen.png"`

**Step 4: Delete old snapshot files that will be orphaned:**

```bash
rm -f tests/visual/design-system.spec.ts-snapshots/plans-screen*.png
rm -f tests/visual/design-system.spec.ts-snapshots/chat-screen*.png
```

**Step 5: Regenerate ALL snapshots** (all 7 are stale because tab bar changed):

```bash
npx playwright test --update-snapshots
```

This creates new baseline files for all 7 tests including the renamed `explore-screen.png` and `chats-screen.png`.

**Step 6: Run tests without --update-snapshots to confirm green:**

```bash
npx playwright test
```

**Do NOT:**
- Change test logic, login helper, or timeouts
- Change other locators (Home, Squad, Profile are unchanged)
- Skip the snapshot regeneration step
  </action>
  <verify>
    <automated>cd /Users/iulian/Develop/campfire && npx playwright test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep 'getByText("Explore")' tests/visual/design-system.spec.ts returns a match
    - grep 'getByText("Chats")' tests/visual/design-system.spec.ts returns a match
    - grep 'getByText("Plans")' tests/visual/design-system.spec.ts returns NO match
    - grep 'getByText("Chat")' tests/visual/design-system.spec.ts returns NO match (note: "Chats" contains "Chat" so use exact grep pattern 'getByText("Chat")')
    - File tests/visual/design-system.spec.ts-snapshots/explore-screen*.png exists
    - File tests/visual/design-system.spec.ts-snapshots/chats-screen*.png exists
    - File tests/visual/design-system.spec.ts-snapshots/plans-screen*.png does NOT exist
    - File tests/visual/design-system.spec.ts-snapshots/chat-screen*.png does NOT exist (note: match exactly, not chatS)
    - npx playwright test passes with 7/7 green
  </acceptance_criteria>
  <done>
    All Playwright locators reference new tab names, all 7 snapshots regenerated with new tab bar layout, full test suite passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify navigation on device</name>
  <files>n/a</files>
  <action>
Human visual verification of the complete navigation restructure on a real device or simulator. No code changes -- this is a verification-only checkpoint.
  </action>
  <what-built>
    Complete navigation restructure: tabs reordered to Home|Squad|Explore|Chats|Profile, Plans renamed to Explore with compass icon, Chat renamed to Chats with chatbubbles icon, all Playwright tests green.
  </what-built>
  <how-to-verify>
    1. Open the app on your device/simulator
    2. Verify bottom tab bar shows (left to right): Home | Squad | Explore | Chats | Profile
    3. Verify Explore tab has compass icon and shows the plans list
    4. Verify Chats tab has chatbubbles (plural) icon and shows chat list
    5. Tap Explore -> tap a plan -> verify plan detail opens correctly
    6. Tap Chats -> tap a chat room -> verify chat room opens correctly
    7. Verify Squad tab still has pending request badge (if any pending)
    8. Verify Explore tab still has invitation badge (if any invitations)
  </how-to-verify>
  <verify>User confirms all 8 verification steps pass</verify>
  <done>User has approved the navigation restructure visually on device.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx playwright test` passes 7/7
- All snapshot baselines regenerated
- Visual confirmation of tab order and icons on device
</verification>

<success_criteria>
Playwright test suite passes with updated locators and fresh snapshots. User has visually confirmed the navigation restructure on device.
</success_criteria>

<output>
After completion, create `.planning/phases/11-navigation-restructure/11-02-SUMMARY.md`
</output>
