---
phase: 11-navigation-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(tabs)/_layout.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04

must_haves:
  truths:
    - "Bottom nav displays tabs in order: Home | Squad | Explore | Chats | Profile"
    - "Explore tab shows compass icon and label 'Explore'"
    - "Chats tab shows chatbubbles icon and label 'Chats'"
    - "All existing router.push calls to /plans/ and /chat/ routes still resolve"
    - "invitationCount badge remains on Explore tab"
    - "pendingCount badge remains on Squad tab"
  artifacts:
    - path: "src/app/(tabs)/_layout.tsx"
      provides: "Reordered tab declarations with renamed titles and icons"
      contains: "title: 'Explore'"
  key_links:
    - from: "src/app/(tabs)/_layout.tsx"
      to: "src/app/(tabs)/plans.tsx"
      via: "Tabs.Screen name='plans' (unchanged route segment)"
      pattern: "name=\"plans\""
    - from: "src/app/(tabs)/_layout.tsx"
      to: "src/app/(tabs)/chat/"
      via: "Tabs.Screen name='chat' (unchanged route segment)"
      pattern: "name=\"chat\""
---

<objective>
Reorder bottom navigation tabs and rename Plans to Explore (with compass icon) and Chat to Chats (with chatbubbles icon) in the tab layout file.

Purpose: Deliver the new information architecture — Squad moves to position 2 (prominent placement), Plans becomes Explore (future discovery direction), Chat becomes Chats (plural consistency).
Output: Updated `_layout.tsx` with correct tab order, labels, and icons.
</objective>

<execution_context>
@/Users/iulian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iulian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-navigation-restructure/11-RESEARCH.md

<interfaces>
<!-- Current tab layout — the only file being modified -->

From src/app/(tabs)/_layout.tsx:
```typescript
import { Tabs } from 'expo-router';
import { Ionicons } from '@expo/vector-icons';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { COLORS, FONT_SIZE } from '@/theme';
import { usePendingRequestsCount } from '@/hooks/usePendingRequestsCount';
import { useInvitationCount } from '@/hooks/useInvitationCount';

// Current tab order: index → plans → chat → squad → profile
// Target tab order:  index → squad → plans → chat → profile

// Current titles: Home, Plans, Chat, Squad, Profile
// Target titles:  Home, Squad, Explore, Chats, Profile

// Current icons: home, calendar, chatbubble, people, person
// Target icons:  home, people, compass, chatbubbles, person
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder tabs and update titles and icons</name>
  <files>src/app/(tabs)/_layout.tsx</files>
  <read_first>
    - src/app/(tabs)/_layout.tsx
    - .planning/phases/11-navigation-restructure/11-RESEARCH.md (section: "Final _layout.tsx tab declarations")
  </read_first>
  <action>
In `src/app/(tabs)/_layout.tsx`, reorder the five `Tabs.Screen` declarations and update titles/icons. Move entire `Tabs.Screen` blocks (do NOT reconstruct options piecemeal — badges must travel with their screens).

**Target declaration order inside `<Tabs>`:**

1. `name="index"` — title: "Home", icon: `home` / `home-outline` (unchanged)
2. `name="squad"` — title: "Squad", icon: `people` / `people-outline`, tabBarBadge: pendingCount (moved from position 4 to position 2, no changes to options)
3. `name="plans"` — title: **"Explore"** (was "Plans"), icon: **`compass` / `compass-outline`** (was calendar/calendar-outline), tabBarBadge: invitationCount (unchanged)
4. `name="chat"` — title: **"Chats"** (was "Chat"), icon: **`chatbubbles` / `chatbubbles-outline`** (was chatbubble/chatbubble-outline), no badge
5. `name="profile"` — title: "Profile", icon: `person` / `person-outline` (unchanged)

**Do NOT:**
- Rename any files (`plans.tsx` stays as `plans.tsx`, `chat/` directory stays as `chat/`)
- Change `name` props (these are route identifiers that must match filenames)
- Remove or alter badge logic
- Change `screenOptions`, `tabBarStyle`, imports, or hooks

**Verification that NAV-04 is satisfied:** The `name` props remain `"plans"` and `"chat"`, so all `router.push('/plans/...')` and `router.push('/chat/...')` deep routes continue to resolve. No changes needed to any other file.
  </action>
  <verify>
    <automated>cd /Users/iulian/Develop/campfire && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "title: 'Explore'" src/app/\(tabs\)/_layout.tsx returns a match
    - grep "title: 'Chats'" src/app/\(tabs\)/_layout.tsx returns a match
    - grep "compass" src/app/\(tabs\)/_layout.tsx returns matches for compass/compass-outline
    - grep "chatbubbles" src/app/\(tabs\)/_layout.tsx returns matches for chatbubbles/chatbubbles-outline
    - Tab declaration order in file is: index, squad, plans, chat, profile (grep -n 'name="' shows this order)
    - grep "name=\"plans\"" confirms route segment unchanged
    - grep "name=\"chat\"" confirms route segment unchanged
    - grep "invitationCount" confirms badge still on plans/Explore screen
    - grep "pendingCount" confirms badge still on squad screen
    - npx tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <done>
    _layout.tsx has tabs in order Home|Squad|Explore|Chats|Profile with correct icons, correct badges, and unchanged route segments. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify route references are unaffected</name>
  <files>src/app/(tabs)/_layout.tsx</files>
  <read_first>
    - src/app/(tabs)/_layout.tsx (after Task 1 changes)
    - src/app/_layout.tsx
    - src/components/chat/PinnedPlanBanner.tsx
    - src/screens/plans/PlanCreateModal.tsx
    - src/screens/plans/PlanDashboardScreen.tsx
    - src/screens/plans/PlansListScreen.tsx
    - src/screens/chat/ChatListScreen.tsx
  </read_first>
  <action>
Audit all seven `router.push` call sites documented in the research to confirm they still reference valid route segments after the tab reorder:

1. `src/app/_layout.tsx` lines ~86, ~95 — `/plans/${planId}` (deep Stack route, unaffected)
2. `src/components/chat/PinnedPlanBanner.tsx` line ~34 — `/plans/` + planId (deep Stack route, unaffected)
3. `src/screens/plans/PlanCreateModal.tsx` line ~109 — `/plans/${planId}` (deep Stack route, unaffected)
4. `src/screens/plans/PlanDashboardScreen.tsx` line ~297 — `/chat/room?plan_id=${planId}` (chat directory unchanged, unaffected)
5. `src/screens/plans/PlansListScreen.tsx` line ~134 — `/plans/${item.id}` (deep Stack route, unaffected)
6. `src/screens/chat/ChatListScreen.tsx` line ~20 — `/chat/room?plan_id=` + item.id (chat directory unchanged, unaffected)

This is a read-and-verify task. No code changes should be needed. If any route reference is broken (should not happen with title-only rename), fix it.

Also confirm that `src/app/(tabs)/plans.tsx` and `src/app/(tabs)/chat/` directory still exist with their original filenames (no accidental renames).
  </action>
  <verify>
    <automated>cd /Users/iulian/Develop/campfire && grep -rn "router\.push.*plans\|router\.push.*chat" src/ | head -20</automated>
  </verify>
  <acceptance_criteria>
    - All 7 router.push call sites reference valid route segments (/plans/ and /chat/)
    - File src/app/(tabs)/plans.tsx exists
    - Directory src/app/(tabs)/chat/ exists with _layout.tsx, index.tsx, room.tsx
    - No router.push references to nonexistent routes (no /explore/ or /chats/ segments)
  </acceptance_criteria>
  <done>
    All existing navigation routes confirmed working after tab rename. NAV-04 is satisfied.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Tab declaration order: index, squad, plans, chat, profile
- Titles: Home, Squad, Explore, Chats, Profile
- Icons: home, people, compass, chatbubbles, person (with -outline variants)
- Badges: pendingCount on squad, invitationCount on plans
- All 7 router.push call sites verified unchanged
</verification>

<success_criteria>
Bottom navigation shows Home | Squad | Explore | Chats | Profile with correct icons and badges. All deep routes work. TypeScript compiles.
</success_criteria>

<output>
After completion, create `.planning/phases/11-navigation-restructure/11-01-SUMMARY.md`
</output>
