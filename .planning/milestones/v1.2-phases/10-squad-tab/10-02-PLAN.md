---
phase: 10-squad-tab
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/app/(tabs)/squad.tsx
  - src/app/(tabs)/_layout.tsx
  - src/components/squad/SquadTabSwitcher.tsx
autonomous: false
requirements:
  - SQAD-01
  - SQAD-02
  - SQAD-03
  - SQAD-04
  - SQAD-05
  - SQAD-06
  - SQAD-07
  - SQAD-08
  - SQAD-09

must_haves:
  truths:
    - "User visually confirms Friends tab is default, friend list loads, FAB is present"
    - "User visually confirms Goals tab shows coming soon, FAB is hidden"
    - "User visually confirms badge is on Squad tab, not on Profile tab"
    - "User confirms Friend Requests row appears when requests exist and navigates correctly"
  artifacts: []
  key_links: []
---

<objective>
Verify the Squad tab implementation visually and functionally on a real device or simulator.

Purpose: Catch visual regressions, layout issues, and interaction bugs that automated checks miss.
Output: User approval or issue list for fixes.
</objective>

<execution_context>
@/Users/iulian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iulian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-squad-tab/10-CONTEXT.md
@.planning/phases/10-squad-tab/10-UI-SPEC.md
@.planning/phases/10-squad-tab/10-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual and functional verification of Squad tab</name>
  <files>src/app/(tabs)/squad.tsx, src/components/squad/SquadTabSwitcher.tsx, src/app/(tabs)/_layout.tsx</files>
  <read_first>
    src/app/(tabs)/squad.tsx
    src/components/squad/SquadTabSwitcher.tsx
    src/app/(tabs)/_layout.tsx
  </read_first>
  <action>
Present the verification checklist to the user. Before presenting, run `npx tsc --noEmit` to confirm no TypeScript errors exist. Then ask the user to open the app and walk through the verification steps below.
  </action>
  <verify>
    <automated>cd /Users/iulian/Develop/campfire && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <what-built>
Complete Squad tab with Friends/Goals sub-tabs, friend list embedding, Friend Requests row, badge migration from Profile to Squad.
  </what-built>
  <how-to-verify>
Open the app on device/simulator and check:

**Step 1 — Squad tab default state:**
1. Tap the Squad tab in bottom nav
2. Verify: "Friends" and "Goals" text tabs visible at top, "Friends" has orange underline (#f97316), "Goals" has no underline
3. Verify: Friend list is visible below the tab switcher with status indicators
4. Verify: FAB (+ person icon) is visible at bottom-right

**Step 2 — Tab switching:**
5. Tap "Goals" tab
6. Verify: Underline moves to Goals, haptic fires, content changes to lock icon + "Group challenges and streaks — coming soon."
7. Verify: FAB is NOT visible on Goals tab
8. Tap "Friends" tab to switch back
9. Verify: Friend list reappears, FAB visible again

**Step 3 — Friend Requests row (if you have pending requests):**
10. If pending requests exist: verify "Friend Requests (N)" row appears above the friend list with person-add icon and chevron
11. Tap the row — verify it navigates to the Friend Requests screen
12. If NO pending requests: verify the row is NOT visible

**Step 4 — Badge migration:**
13. Look at bottom tab bar — Squad tab icon should show a numeric badge if pending requests exist
14. Tap Profile tab — verify NO badge on the Profile tab icon

**Step 5 — Safe area:**
15. Verify the tab labels do not overlap with the status bar / notch area
  </how-to-verify>
  <acceptance_criteria>
    - Friends tab is default with orange underline indicator
    - Goals tab shows coming soon placeholder with no FAB
    - Tab switching works with haptic feedback
    - Friend Requests row only shows when pendingCount > 0
    - Badge is on Squad tab icon, not Profile tab icon
    - No layout overlap with status bar
  </acceptance_criteria>
  <done>User approves all 5 verification steps or provides specific issues for gap closure.</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
User confirms all 5 verification steps pass or provides specific issues.
</verification>

<success_criteria>
- User approves all visual and functional checks
- No layout issues or interaction bugs identified
- If issues found: they are logged and a gap closure plan is created
</success_criteria>

<output>
After completion, create `.planning/phases/10-squad-tab/10-02-SUMMARY.md`
</output>
