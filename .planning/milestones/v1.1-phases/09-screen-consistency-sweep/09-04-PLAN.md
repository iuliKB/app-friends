---
phase: 09-screen-consistency-sweep
plan: 04
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/screens/chat/ChatListScreen.tsx
  - src/screens/chat/ChatRoomScreen.tsx
  - src/components/chat/ChatListRow.tsx
  - src/components/chat/MessageBubble.tsx
  - src/components/chat/PinnedPlanBanner.tsx
  - src/components/chat/SendBar.tsx
autonomous: true
requirements: [SCRN-01, SCRN-02, SCRN-03, SCRN-04]

must_haves:
  truths:
    - "All chat domain files import from @/theme, not @/constants/colors"
    - "The undeclared #3b82f6 in ChatListRow.tsx is replaced with COLORS.feedback.info"
    - "Zero no-hardcoded-styles violations in all 6 chat domain files"
  artifacts:
    - path: "src/screens/chat/ChatListScreen.tsx"
      provides: "Token-migrated chat list screen"
    - path: "src/components/chat/ChatListRow.tsx"
      provides: "Token-migrated chat list row with COLORS.feedback.info"
    - path: "src/components/chat/MessageBubble.tsx"
      provides: "Token-migrated message bubble (highest violation component)"
  key_links:
    - from: "src/components/chat/ChatListRow.tsx"
      to: "COLORS.feedback.info"
      via: "unread dot color"
      pattern: "COLORS\\.feedback\\.info"
---

<objective>
Migrate the Chat domain (ChatListScreen, ChatRoomScreen + ChatListRow, MessageBubble, PinnedPlanBanner, SendBar) to design tokens. Specifically resolve the undeclared #3b82f6 in ChatListRow.tsx with COLORS.feedback.info.

Purpose: Chat screens handle the unread indicator blue (#3b82f6) that was flagged in the original audit. This plan resolves SCRN-04 specifically.
Output: 6 files migrated with zero lint violations, #3b82f6 replaced with semantic token.
</objective>

<execution_context>
@/Users/iulian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iulian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-screen-consistency-sweep/09-CONTEXT.md

<interfaces>
Token mapping (old -> new):
- COLORS.accent -> COLORS.interactive.accent
- COLORS.dominant -> COLORS.surface.base
- COLORS.secondary -> COLORS.surface.card
- COLORS.destructive -> COLORS.interactive.destructive
- COLORS.textPrimary -> COLORS.text.primary
- COLORS.textSecondary -> COLORS.text.secondary
- COLORS.border -> COLORS.border
- COLORS.surfaceOverlay -> COLORS.surface.overlay
- '#3b82f6' (hardcoded unread dot blue) -> COLORS.feedback.info
- Raw spacing: 4->SPACING.xs, 8->SPACING.sm, 12->SPACING.md, 16->SPACING.lg, 24->SPACING.xl, 32->SPACING.xxl
- Raw fontSize: 11->xs, 13->sm, 14->md, 16->lg, 20->xl, 24->xxl
- Raw fontWeight: '400'->FONT_WEIGHT.regular, '600'->FONT_WEIGHT.semibold
- Raw borderRadius: 4->RADII.xs, 6->RADII.sm, 8->RADII.md, 12->RADII.lg, 16->RADII.xl, 18->RADII.pill
- Raw shadow blocks -> ...SHADOWS.card spread

Note: ChatListScreen has `import { COLORS as THEME } from '@/theme'` from Phase 8 — replace alias with direct `COLORS` import.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate chat screens (ChatListScreen, ChatRoomScreen)</name>
  <files>src/screens/chat/ChatListScreen.tsx, src/screens/chat/ChatRoomScreen.tsx</files>
  <read_first>src/screens/chat/ChatListScreen.tsx, src/screens/chat/ChatRoomScreen.tsx, src/theme/colors.ts, src/theme/spacing.ts, src/theme/typography.ts, src/theme/radii.ts, src/theme/shadows.ts</read_first>
  <action>
**ChatListScreen.tsx (4 violations):**
1. Replace `import { COLORS } from '@/constants/colors'` with `import { COLORS, SPACING, FONT_SIZE, FONT_WEIGHT, RADII } from '@/theme'` (only what's used).
2. If `import { COLORS as THEME } from '@/theme'` exists, remove and replace all `THEME.xxx` with `COLORS.xxx` semantic paths.
3. Replace old flat COLORS keys with semantic paths per mapping.
4. Replace ALL hardcoded values with tokens.

**ChatRoomScreen.tsx (5 violations):**
1. Same import replacement pattern.
2. Replace old flat COLORS keys with semantic paths.
3. Replace ALL hardcoded values with tokens.
  </action>
  <verify>
    <automated>npx eslint src/screens/chat/ChatListScreen.tsx src/screens/chat/ChatRoomScreen.tsx 2>&1 | grep -c "no-hardcoded-styles" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "@/constants/colors" src/screens/chat/` returns empty
    - `grep "COLORS as THEME" src/screens/chat/ChatListScreen.tsx` returns empty
    - `npx eslint src/screens/chat/ChatListScreen.tsx src/screens/chat/ChatRoomScreen.tsx 2>&1 | grep "no-hardcoded-styles"` returns empty
  </acceptance_criteria>
  <done>Both chat screen files migrated with zero lint violations. COLORS as THEME alias removed.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate chat components (ChatListRow, MessageBubble, PinnedPlanBanner, SendBar)</name>
  <files>src/components/chat/ChatListRow.tsx, src/components/chat/MessageBubble.tsx, src/components/chat/PinnedPlanBanner.tsx, src/components/chat/SendBar.tsx</files>
  <read_first>src/components/chat/ChatListRow.tsx, src/components/chat/MessageBubble.tsx, src/components/chat/PinnedPlanBanner.tsx, src/components/chat/SendBar.tsx, src/theme/colors.ts, src/theme/spacing.ts, src/theme/typography.ts, src/theme/radii.ts, src/theme/shadows.ts</read_first>
  <action>
**ChatListRow.tsx (9 violations):**
1. Replace `import { COLORS } from '@/constants/colors'` with `import { COLORS, SPACING, FONT_SIZE, FONT_WEIGHT, RADII } from '@/theme'` (only what's used).
2. CRITICAL: Find the hardcoded `'#3b82f6'` (the unread dot blue) and replace with `COLORS.feedback.info`. This resolves SCRN-04.
3. Replace ALL other old COLORS keys and hardcoded values with tokens.

**MessageBubble.tsx (21 violations — highest violation component):**
1. Same import replacement pattern.
2. Replace ALL old COLORS keys. Message bubbles typically use accent for "mine" and secondary for "theirs" — map these to COLORS.interactive.accent and COLORS.surface.card respectively.
3. Replace ALL raw fontSize/fontWeight/padding/margin/borderRadius with tokens.
4. borderRadius: 18 -> RADII.pill (exact match for message bubbles).
5. Be thorough — 21 violations means many style properties need updating.

**PinnedPlanBanner.tsx (3 violations):**
1. Same pattern. 3 violations — small file.

**SendBar.tsx (3 violations):**
1. Same pattern. 3 violations — small file.
  </action>
  <verify>
    <automated>npx eslint src/components/chat/ChatListRow.tsx src/components/chat/MessageBubble.tsx src/components/chat/PinnedPlanBanner.tsx src/components/chat/SendBar.tsx 2>&1 | grep -c "no-hardcoded-styles" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "@/constants/colors" src/components/chat/` returns empty
    - `grep "#3b82f6" src/components/chat/ChatListRow.tsx` returns empty
    - `grep "COLORS.feedback.info" src/components/chat/ChatListRow.tsx` returns a match
    - `npx eslint src/components/chat/*.tsx 2>&1 | grep "no-hardcoded-styles"` returns empty
  </acceptance_criteria>
  <done>All 4 chat component files migrated. #3b82f6 replaced with COLORS.feedback.info. Zero lint violations.</done>
</task>

</tasks>

<verification>
- `grep -r "@/constants/colors" src/screens/chat/ src/components/chat/` returns empty
- `grep "#3b82f6" src/components/chat/ChatListRow.tsx` returns empty (SCRN-04 resolved)
- `npx eslint src/screens/chat/*.tsx src/components/chat/*.tsx 2>&1 | grep "no-hardcoded-styles"` returns empty
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- 6 files migrated with zero no-hardcoded-styles violations
- #3b82f6 replaced with COLORS.feedback.info in ChatListRow.tsx
- No file imports from @/constants/colors
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/09-screen-consistency-sweep/09-04-SUMMARY.md`
</output>
