---
phase: 09-screen-consistency-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eslint.config.js
  - src/screens/auth/AuthScreen.tsx
  - src/screens/auth/ProfileSetup.tsx
  - src/components/auth/AuthTabSwitcher.tsx
  - src/components/auth/OAuthButton.tsx
  - src/components/auth/UsernameField.tsx
  - src/components/common/AvatarCircle.tsx
  - src/components/common/EmptyState.tsx
  - src/components/common/FAB.tsx
  - src/components/common/FormField.tsx
  - src/components/common/OfflineBanner.tsx
  - src/components/common/PrimaryButton.tsx
  - src/components/common/LoadingIndicator.tsx
autonomous: true
requirements: [SCRN-01, SCRN-02, SCRN-04, SCRN-06]

must_haves:
  truths:
    - "ESLint no-hardcoded-styles rule is severity 'error' (not 'warn')"
    - "All auth screen and auth component files import from @/theme, not @/constants/colors"
    - "All common component files import from @/theme, not @/constants/colors"
    - "Auth screens use FormField from @/components/common/FormField"
    - "Zero no-hardcoded-styles violations in auth and common component files"
  artifacts:
    - path: "eslint.config.js"
      provides: "Severity upgrade from warn to error"
      contains: "'campfire/no-hardcoded-styles': 'error'"
    - path: "src/screens/auth/AuthScreen.tsx"
      provides: "Token-migrated auth screen"
    - path: "src/screens/auth/ProfileSetup.tsx"
      provides: "Token-migrated profile setup screen"
  key_links:
    - from: "src/screens/auth/AuthScreen.tsx"
      to: "@/theme"
      via: "import statement"
      pattern: "from '@/theme'"
    - from: "src/components/common/EmptyState.tsx"
      to: "@/theme"
      via: "import statement"
      pattern: "from '@/theme'"
---

<objective>
Flip the ESLint no-hardcoded-styles rule from 'warn' to 'error', then migrate all auth domain files (AuthScreen, ProfileSetup, AuthTabSwitcher, OAuthButton, UsernameField) and all common component files (AvatarCircle, EmptyState, FAB, FormField, OfflineBanner, PrimaryButton, LoadingIndicator) to use @/theme tokens instead of @/constants/colors and hardcoded values.

Purpose: The severity flip makes violations blocking — every subsequent domain plan must leave zero errors. Auth + common components are low-violation-count files that establish the migration pattern.
Output: eslint.config.js updated, 12 files migrated to @/theme tokens with zero lint violations.
</objective>

<execution_context>
@/Users/iulian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iulian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-screen-consistency-sweep/09-CONTEXT.md

<interfaces>
<!-- Design tokens (source of truth for ALL replacement values) -->
From src/theme/colors.ts:
```typescript
export const COLORS = {
  text: { primary: '#f5f5f5', secondary: '#9ca3af' },
  surface: { base: '#1a1a1a', card: '#2a2a2a', overlay: '#ffffff14' },
  interactive: { accent: '#f97316', destructive: '#ef4444' },
  feedback: { info: '#3b82f6', error: '#ef4444' },
  status: { free: '#22c55e', busy: '#ef4444', maybe: '#eab308' },
  border: '#3f3f46',
  shadow: '#000000',
  offline: { bg: '#92400e', text: '#fef3c7' },
  splash: { gradientStart: '#ff6b35', gradientEnd: '#dc2626', text: '#fef3c7' },
} as const;
```

From src/theme/spacing.ts:
```typescript
export const SPACING = { xs: 4, sm: 8, md: 12, lg: 16, xl: 24, xxl: 32 } as const;
```

From src/theme/typography.ts:
```typescript
export const FONT_SIZE = { xs: 11, sm: 13, md: 14, lg: 16, xl: 20, xxl: 24 } as const;
export const FONT_WEIGHT = { regular: '400', semibold: '600' } as const;
```

From src/theme/radii.ts:
```typescript
export const RADII = { xs: 4, sm: 6, md: 8, lg: 12, xl: 16, pill: 18, full: 9999 } as const;
```

From src/theme/shadows.ts:
```typescript
export const SHADOWS = {
  fab: { shadowColor: '#000', shadowOpacity: 0.3, shadowRadius: 4, shadowOffset: { width: 0, height: 2 }, elevation: 4 },
  card: { shadowColor: '#000', shadowOpacity: 0.15, shadowRadius: 3, shadowOffset: { width: 0, height: 1 }, elevation: 2 },
  none: { shadowColor: 'transparent', shadowOpacity: 0, shadowRadius: 0, shadowOffset: { width: 0, height: 0 }, elevation: 0 },
} as const;
```

Old colors mapping (src/constants/colors.ts to @/theme):
- COLORS.accent -> COLORS.interactive.accent
- COLORS.dominant -> COLORS.surface.base
- COLORS.secondary -> COLORS.surface.card
- COLORS.destructive -> COLORS.interactive.destructive
- COLORS.textPrimary -> COLORS.text.primary
- COLORS.textSecondary -> COLORS.text.secondary
- COLORS.border -> COLORS.border
- COLORS.surfaceOverlay -> COLORS.surface.overlay
- COLORS.offlineBg -> COLORS.offline.bg
- COLORS.offlineText -> COLORS.offline.text
- COLORS.splashGradientStart -> COLORS.splash.gradientStart
- COLORS.splashGradientEnd -> COLORS.splash.gradientEnd
- COLORS.splashText -> COLORS.splash.text
- COLORS.status.* -> COLORS.status.* (unchanged)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip ESLint severity and migrate common components</name>
  <files>eslint.config.js, src/components/common/AvatarCircle.tsx, src/components/common/EmptyState.tsx, src/components/common/FAB.tsx, src/components/common/FormField.tsx, src/components/common/OfflineBanner.tsx, src/components/common/PrimaryButton.tsx, src/components/common/LoadingIndicator.tsx</files>
  <read_first>eslint.config.js, src/components/common/AvatarCircle.tsx, src/components/common/EmptyState.tsx, src/components/common/FAB.tsx, src/components/common/FormField.tsx, src/components/common/OfflineBanner.tsx, src/components/common/PrimaryButton.tsx, src/components/common/LoadingIndicator.tsx, src/theme/colors.ts, src/theme/spacing.ts, src/theme/typography.ts, src/theme/radii.ts, src/theme/shadows.ts, src/constants/colors.ts</read_first>
  <action>
1. In eslint.config.js line 22, change `'campfire/no-hardcoded-styles': 'warn'` to `'campfire/no-hardcoded-styles': 'error'`.

2. For each common component file:
   - Replace `import { COLORS } from '@/constants/colors'` with `import { COLORS, SPACING, FONT_SIZE, FONT_WEIGHT, RADII, SHADOWS } from '@/theme'` (import only what the file actually uses)
   - If file has `import { COLORS as THEME } from '@/theme'`, change to `import { COLORS } from '@/theme'` and replace all `THEME.xxx` with `COLORS.xxx`
   - Replace old COLORS.xxx flat keys with new semantic paths per the mapping above
   - Replace hardcoded hex color strings with COLORS.xxx tokens
   - Replace raw numeric fontSize with FONT_SIZE.xxx
   - Replace raw fontWeight strings with FONT_WEIGHT.xxx
   - Replace raw padding/margin/gap numbers with SPACING.xxx (4->xs, 8->sm, 12->md, 16->lg, 24->xl, 32->xxl)
   - Replace raw borderRadius with RADII.xxx where exact match exists
   - Replace raw shadow properties with ...SHADOWS.xxx spread
   - If a raw numeric value has no exact token match (e.g., paddingVertical: 14), keep it as-is with a comment `// no exact token`

3. Special notes:
   - FAB.tsx has 1 violation (fontSize: 15 in label style) — replace with FONT_SIZE.lg (16) which is the closest semantic match for a button label, or keep as raw with comment if the 1px difference matters visually
   - FormField.tsx has 1 violation (paddingVertical: 14) — keep as raw per Phase 8 decision
   - AvatarCircle, OfflineBanner, PrimaryButton each have 1 violation — identify and fix each
  </action>
  <verify>
    <automated>npx eslint src/components/common/AvatarCircle.tsx src/components/common/EmptyState.tsx src/components/common/FAB.tsx src/components/common/FormField.tsx src/components/common/OfflineBanner.tsx src/components/common/PrimaryButton.tsx src/components/common/LoadingIndicator.tsx 2>&1 | grep -c "no-hardcoded-styles" | xargs -I{} test {} -eq 0 && echo "PASS: zero violations" || echo "FAIL: violations remain"</automated>
  </verify>
  <acceptance_criteria>
    - eslint.config.js line 22 contains `'campfire/no-hardcoded-styles': 'error'`
    - No file in src/components/common/ contains `@/constants/colors` import
    - `npx eslint src/components/common/*.tsx 2>&1 | grep "no-hardcoded-styles"` returns empty
  </acceptance_criteria>
  <done>ESLint rule is error severity. All 7 common component files have zero no-hardcoded-styles violations and import exclusively from @/theme.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate auth domain (AuthScreen, ProfileSetup, auth components)</name>
  <files>src/screens/auth/AuthScreen.tsx, src/screens/auth/ProfileSetup.tsx, src/components/auth/AuthTabSwitcher.tsx, src/components/auth/OAuthButton.tsx, src/components/auth/UsernameField.tsx</files>
  <read_first>src/screens/auth/AuthScreen.tsx, src/screens/auth/ProfileSetup.tsx, src/components/auth/AuthTabSwitcher.tsx, src/components/auth/OAuthButton.tsx, src/components/auth/UsernameField.tsx, src/theme/colors.ts, src/theme/spacing.ts, src/theme/typography.ts, src/theme/radii.ts</read_first>
  <action>
For each auth file:
1. Replace `import { COLORS } from '@/constants/colors'` with `import { COLORS, SPACING, FONT_SIZE, FONT_WEIGHT, RADII } from '@/theme'` (only what's used)
2. Replace old flat COLORS keys with semantic paths:
   - COLORS.accent -> COLORS.interactive.accent
   - COLORS.dominant -> COLORS.surface.base
   - COLORS.secondary -> COLORS.surface.card
   - COLORS.destructive -> COLORS.interactive.destructive
   - COLORS.textPrimary -> COLORS.text.primary
   - COLORS.textSecondary -> COLORS.text.secondary
   - COLORS.border -> COLORS.border (unchanged)
3. Replace ALL hardcoded hex colors with COLORS.xxx tokens
4. Replace raw fontSize numbers: 11->FONT_SIZE.xs, 13->FONT_SIZE.sm, 14->FONT_SIZE.md, 16->FONT_SIZE.lg, 20->FONT_SIZE.xl, 24->FONT_SIZE.xxl
5. Replace raw fontWeight: '400'->FONT_WEIGHT.regular, '600'->FONT_WEIGHT.semibold
6. Replace raw padding/margin/gap: 4->SPACING.xs, 8->SPACING.sm, 12->SPACING.md, 16->SPACING.lg, 24->SPACING.xl, 32->SPACING.xxl
7. Replace raw borderRadius where token match exists: 4->RADII.xs, 6->RADII.sm, 8->RADII.md, 12->RADII.lg, 16->RADII.xl

AuthScreen.tsx has 19 violations, ProfileSetup.tsx has 11. Fix every one.

Confirm auth screens still import FormField from '@/components/common/FormField' (not from '@/components/auth/FormField').
  </action>
  <verify>
    <automated>npx eslint src/screens/auth/AuthScreen.tsx src/screens/auth/ProfileSetup.tsx src/components/auth/AuthTabSwitcher.tsx src/components/auth/OAuthButton.tsx src/components/auth/UsernameField.tsx 2>&1 | grep -c "no-hardcoded-styles" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - No file in src/screens/auth/ or src/components/auth/ contains `@/constants/colors` import
    - `grep -r "@/constants/colors" src/screens/auth/ src/components/auth/` returns empty (excluding FormField.tsx re-export stub which only re-exports, doesn't import colors)
    - `npx eslint src/screens/auth/*.tsx src/components/auth/AuthTabSwitcher.tsx src/components/auth/OAuthButton.tsx src/components/auth/UsernameField.tsx 2>&1 | grep "no-hardcoded-styles"` returns empty
    - `grep "from '@/components/common/FormField'" src/screens/auth/AuthScreen.tsx` returns a match
  </acceptance_criteria>
  <done>All 5 auth domain files migrated to @/theme tokens. Zero no-hardcoded-styles violations. Auth screens import FormField from common/.</done>
</task>

</tasks>

<verification>
- `grep "'campfire/no-hardcoded-styles': 'error'" eslint.config.js` returns a match
- `grep -r "@/constants/colors" src/components/common/ src/screens/auth/ src/components/auth/AuthTabSwitcher.tsx src/components/auth/OAuthButton.tsx src/components/auth/UsernameField.tsx` returns empty
- `npx eslint src/components/common/*.tsx src/screens/auth/*.tsx src/components/auth/AuthTabSwitcher.tsx src/components/auth/OAuthButton.tsx src/components/auth/UsernameField.tsx 2>&1 | grep "no-hardcoded-styles"` returns empty
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- ESLint severity is 'error' for no-hardcoded-styles
- 12 files migrated: 7 common components + 5 auth domain files
- Zero lint violations in all migrated files
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/09-screen-consistency-sweep/09-01-SUMMARY.md`
</output>
