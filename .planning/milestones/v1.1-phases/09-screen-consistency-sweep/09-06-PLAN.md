---
phase: 09-screen-consistency-sweep
plan: 06
type: execute
wave: 3
depends_on: [09-02, 09-03, 09-04, 09-05]
files_modified:
  - src/app/(tabs)/profile.tsx
  - src/app/profile/edit.tsx
  - src/app/(tabs)/squad.tsx
  - src/app/(tabs)/_layout.tsx
  - src/app/(tabs)/chat/_layout.tsx
  - src/app/_layout.tsx
  - src/app/friends/_layout.tsx
  - src/app/plans/_layout.tsx
  - src/app/profile/_layout.tsx
  - src/app/qr-code.tsx
  - src/constants/colors.ts
  - src/components/auth/FormField.tsx
autonomous: true
requirements: [SCRN-01, SCRN-02, SCRN-07]

must_haves:
  truths:
    - "Profile tab and edit screen use ScreenHeader for titles"
    - "All navigation/layout files import from @/theme, not @/constants/colors"
    - "src/constants/colors.ts is deleted"
    - "src/components/auth/FormField.tsx re-export stub is deleted"
    - "Zero no-hardcoded-styles violations across entire src/ directory"
    - "Zero imports of @/constants/colors across entire src/ directory"
    - "npx tsc --noEmit passes"
  artifacts:
    - path: "src/app/(tabs)/profile.tsx"
      provides: "Token-migrated profile tab with ScreenHeader"
    - path: "src/app/profile/edit.tsx"
      provides: "Token-migrated profile edit screen"
  key_links:
    - from: "all src/**/*.tsx"
      to: "@/theme"
      via: "import statement"
      pattern: "from '@/theme'"
---

<objective>
Migrate the Profile/Navigation domain (profile tab, profile edit, squad tab, all layout files, qr-code route) to design tokens, adopt ScreenHeader on profile screens, then delete src/constants/colors.ts and src/components/auth/FormField.tsx re-export stub. Verify the entire codebase has zero lint violations and zero old imports.

Purpose: This is the final cleanup plan. After it completes, the entire codebase is on design tokens with zero violations — the milestone's capstone deliverable.
Output: All remaining files migrated, old files deleted, full codebase verification passes.
</objective>

<execution_context>
@/Users/iulian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iulian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-screen-consistency-sweep/09-CONTEXT.md

<interfaces>
From src/components/common/ScreenHeader.tsx:
```typescript
export interface ScreenHeaderProps {
  title: string;
  subtitle?: string;
  rightAction?: React.ReactNode;
}
export function ScreenHeader({ title, subtitle, rightAction }: ScreenHeaderProps);
```

Token mapping (old -> new):
- COLORS.accent -> COLORS.interactive.accent
- COLORS.dominant -> COLORS.surface.base
- COLORS.secondary -> COLORS.surface.card
- COLORS.destructive -> COLORS.interactive.destructive
- COLORS.textPrimary -> COLORS.text.primary
- COLORS.textSecondary -> COLORS.text.secondary
- COLORS.border -> COLORS.border
- COLORS.surfaceOverlay -> COLORS.surface.overlay
- COLORS.splashGradientStart -> COLORS.splash.gradientStart
- COLORS.splashGradientEnd -> COLORS.splash.gradientEnd
- COLORS.splashText -> COLORS.splash.text
- Raw spacing: 4->SPACING.xs, 8->SPACING.sm, 12->SPACING.md, 16->SPACING.lg, 24->SPACING.xl, 32->SPACING.xxl
- Raw fontSize: 11->xs, 13->sm, 14->md, 16->lg, 20->xl, 24->xxl
- Raw fontWeight: '400'->FONT_WEIGHT.regular, '600'->FONT_WEIGHT.semibold
- Raw borderRadius: 4->RADII.xs, 6->RADII.sm, 8->RADII.md, 12->RADII.lg, 16->RADII.xl
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate profile screens and navigation layout files</name>
  <files>src/app/(tabs)/profile.tsx, src/app/profile/edit.tsx, src/app/(tabs)/squad.tsx, src/app/(tabs)/_layout.tsx, src/app/(tabs)/chat/_layout.tsx, src/app/_layout.tsx, src/app/friends/_layout.tsx, src/app/plans/_layout.tsx, src/app/profile/_layout.tsx, src/app/qr-code.tsx</files>
  <read_first>src/app/(tabs)/profile.tsx, src/app/profile/edit.tsx, src/app/(tabs)/squad.tsx, src/app/(tabs)/_layout.tsx, src/app/(tabs)/chat/_layout.tsx, src/app/_layout.tsx, src/app/friends/_layout.tsx, src/app/plans/_layout.tsx, src/app/profile/_layout.tsx, src/app/qr-code.tsx, src/components/common/ScreenHeader.tsx, src/theme/colors.ts, src/theme/spacing.ts, src/theme/typography.ts, src/theme/radii.ts</read_first>
  <action>
**Profile tab — src/app/(tabs)/profile.tsx (20 violations):**
1. Replace `import { COLORS } from '@/constants/colors'` with `import { COLORS, SPACING, FONT_SIZE, FONT_WEIGHT, RADII } from '@/theme'` (only what's used).
2. Add `import { ScreenHeader } from '@/components/common/ScreenHeader'`.
3. Replace the inline profile title with `<ScreenHeader title="Profile" />` (check actual title text).
4. Replace ALL old flat COLORS keys, hardcoded hex, raw values with tokens. 20 violations.

**Profile edit — src/app/profile/edit.tsx (12 violations):**
1. Same import pattern.
2. Adopt ScreenHeader for edit screen title if it has one.
3. Replace ALL hardcoded values with tokens.

**Squad tab — src/app/(tabs)/squad.tsx (5 violations):**
1. Same import pattern.
2. Replace ALL hardcoded values with tokens.

**Layout files (6 files — most have only import migration, few violations):**
For each layout file (src/app/_layout.tsx, src/app/(tabs)/_layout.tsx, src/app/(tabs)/chat/_layout.tsx, src/app/friends/_layout.tsx, src/app/plans/_layout.tsx, src/app/profile/_layout.tsx):
1. Replace `import { COLORS } from '@/constants/colors'` with `import { COLORS } from '@/theme'`.
2. Replace old flat COLORS keys with semantic paths.
3. Layout files typically reference COLORS.dominant for background and COLORS.textPrimary for header text — map to COLORS.surface.base and COLORS.text.primary.
4. src/app/_layout.tsx has 4 violations — fix hardcoded values in splash screen or status bar config.

**QR code route — src/app/qr-code.tsx:**
1. Replace import and any hardcoded values.
  </action>
  <verify>
    <automated>npx eslint "src/app/(tabs)/profile.tsx" src/app/profile/edit.tsx "src/app/(tabs)/squad.tsx" "src/app/(tabs)/_layout.tsx" "src/app/(tabs)/chat/_layout.tsx" src/app/_layout.tsx src/app/friends/_layout.tsx src/app/plans/_layout.tsx src/app/profile/_layout.tsx src/app/qr-code.tsx 2>&1 | grep -c "no-hardcoded-styles" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "@/constants/colors" src/app/` returns empty
    - `grep "import.*ScreenHeader" src/app/\(tabs\)/profile.tsx` returns a match
    - `npx eslint "src/app/(tabs)/profile.tsx" src/app/profile/edit.tsx "src/app/(tabs)/squad.tsx" "src/app/(tabs)/_layout.tsx" "src/app/(tabs)/chat/_layout.tsx" src/app/_layout.tsx src/app/friends/_layout.tsx src/app/plans/_layout.tsx src/app/profile/_layout.tsx src/app/qr-code.tsx 2>&1 | grep "no-hardcoded-styles"` returns empty
  </acceptance_criteria>
  <done>All 10 profile/navigation files migrated. Profile screens use ScreenHeader. Zero lint violations.</done>
</task>

<task type="auto">
  <name>Task 2: Delete old files and verify full codebase</name>
  <files>src/constants/colors.ts, src/components/auth/FormField.tsx</files>
  <read_first>src/constants/colors.ts, src/components/auth/FormField.tsx</read_first>
  <action>
1. Delete `src/constants/colors.ts` entirely. This file is now replaced by `src/theme/colors.ts`.
2. Delete `src/components/auth/FormField.tsx` re-export stub. All auth screens now import from `@/components/common/FormField`.
3. Verify NO file in the entire `src/` directory still imports from `@/constants/colors`:
   - Run `grep -r "@/constants/colors" src/` — must return empty.
   - If any file still imports it, fix that file's import to use `@/theme` instead.
4. Verify NO file imports from `@/components/auth/FormField`:
   - Run `grep -r "@/components/auth/FormField" src/` — must return empty.
   - If any file still imports it, update to `@/components/common/FormField`.
5. Run full lint check: `npx eslint src/ 2>&1 | grep "no-hardcoded-styles"` — must return zero matches.
6. Run TypeScript check: `npx tsc --noEmit` — must pass with zero errors.
7. If any violations or type errors remain, fix them. This task is the final gate — nothing passes until everything is clean.
  </action>
  <verify>
    <automated>grep -r "@/constants/colors" src/ && echo "FAIL: old imports remain" || (npx eslint src/ 2>&1 | grep -c "no-hardcoded-styles" | xargs -I{} test {} -eq 0 && npx tsc --noEmit 2>&1 | tail -1 | grep -q "error" && echo "FAIL: tsc errors" || echo "PASS: zero violations, tsc clean")</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/constants/colors.ts` returns false (file deleted)
    - `test -f src/components/auth/FormField.tsx` returns false (file deleted)
    - `grep -r "@/constants/colors" src/` returns empty (zero old imports)
    - `grep -r "@/components/auth/FormField" src/` returns empty (zero old FormField imports)
    - `npx eslint src/ 2>&1 | grep "no-hardcoded-styles"` returns empty (zero violations)
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Old files deleted. Zero imports of @/constants/colors. Zero no-hardcoded-styles violations across entire codebase. TypeScript compiles clean. Phase 9 complete.</done>
</task>

</tasks>

<verification>
- `test -f src/constants/colors.ts && echo "EXISTS" || echo "DELETED"` returns "DELETED"
- `test -f src/components/auth/FormField.tsx && echo "EXISTS" || echo "DELETED"` returns "DELETED"
- `grep -r "@/constants/colors" src/` returns empty
- `npx eslint src/ 2>&1 | grep "no-hardcoded-styles"` returns empty
- `npx tsc --noEmit` passes
- `npx eslint src/ 2>&1 | grep -c "no-hardcoded-styles"` returns 0
</verification>

<success_criteria>
- 10 profile/navigation files migrated with zero violations
- src/constants/colors.ts deleted
- src/components/auth/FormField.tsx re-export stub deleted
- Zero no-hardcoded-styles violations in entire src/ directory
- Zero @/constants/colors imports in entire src/ directory
- npx tsc --noEmit passes with zero errors
- v1.1 milestone complete
</success_criteria>

<output>
After completion, create `.planning/phases/09-screen-consistency-sweep/09-06-SUMMARY.md`
</output>
