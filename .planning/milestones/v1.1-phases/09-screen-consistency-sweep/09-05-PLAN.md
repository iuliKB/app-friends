---
phase: 09-screen-consistency-sweep
plan: 05
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/screens/friends/FriendsList.tsx
  - src/screens/friends/AddFriend.tsx
  - src/screens/friends/FriendRequests.tsx
  - src/app/friends/[id].tsx
  - src/app/friends/index.tsx
  - src/components/friends/FriendActionSheet.tsx
  - src/components/friends/FriendCard.tsx
  - src/components/friends/QRCodeDisplay.tsx
  - src/components/friends/QRScanView.tsx
  - src/components/friends/RequestCard.tsx
  - src/components/friends/SearchResultCard.tsx
  - src/components/friends/StatusPill.tsx
autonomous: true
requirements: [SCRN-01, SCRN-02, SCRN-03, SCRN-05]

must_haves:
  truths:
    - "FriendsList uses FAB component instead of inline FAB JSX"
    - "All friends domain files import from @/theme, not @/constants/colors"
    - "Zero no-hardcoded-styles violations in all 12 friends domain files"
  artifacts:
    - path: "src/screens/friends/FriendsList.tsx"
      provides: "Token-migrated friends list with FAB"
    - path: "src/screens/friends/AddFriend.tsx"
      provides: "Token-migrated add friend screen (highest violation in domain)"
    - path: "src/components/friends/FriendActionSheet.tsx"
      provides: "Token-migrated action sheet (22 violations)"
  key_links:
    - from: "src/screens/friends/FriendsList.tsx"
      to: "@/components/common/FAB"
      via: "import statement"
      pattern: "import.*FAB.*from '@/components/common/FAB'"
---

<objective>
Migrate the Friends domain (FriendsList, AddFriend, FriendRequests screens + FriendActionSheet, FriendCard, QRCodeDisplay, QRScanView, RequestCard, SearchResultCard, StatusPill components + route files) to design tokens and replace the inline FAB on FriendsList with the shared FAB component.

Purpose: Friends domain is the second-largest by violation count (128 violations across 12 files). FAB replacement on FriendsList completes the last inline FAB in the app.
Output: 12 files migrated with zero lint violations, FAB adopted on FriendsList.
</objective>

<execution_context>
@/Users/iulian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iulian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-screen-consistency-sweep/09-CONTEXT.md

<interfaces>
From src/components/common/FAB.tsx:
```typescript
export interface FABProps {
  icon: React.ReactNode;
  label?: string;
  onPress: () => void;
  accessibilityLabel: string;
  size?: number;
}
export function FAB({ icon, label, onPress, accessibilityLabel, size = 56 }: FABProps);
```

Token mapping (old -> new):
- COLORS.accent -> COLORS.interactive.accent
- COLORS.dominant -> COLORS.surface.base
- COLORS.secondary -> COLORS.surface.card
- COLORS.destructive -> COLORS.interactive.destructive
- COLORS.textPrimary -> COLORS.text.primary
- COLORS.textSecondary -> COLORS.text.secondary
- COLORS.border -> COLORS.border
- COLORS.surfaceOverlay -> COLORS.surface.overlay
- Raw spacing: 4->SPACING.xs, 8->SPACING.sm, 12->SPACING.md, 16->SPACING.lg, 24->SPACING.xl, 32->SPACING.xxl
- Raw fontSize: 11->xs, 13->sm, 14->md, 16->lg, 20->xl, 24->xxl
- Raw fontWeight: '400'->FONT_WEIGHT.regular, '600'->FONT_WEIGHT.semibold
- Raw borderRadius: 4->RADII.xs, 6->RADII.sm, 8->RADII.md, 12->RADII.lg, 16->RADII.xl, 18->RADII.pill
- Raw shadow blocks -> ...SHADOWS.card spread

Note: FriendsList and FriendRequests have `import { COLORS as THEME } from '@/theme'` from Phase 8 — replace alias with direct `COLORS` import.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate friends screens and route files</name>
  <files>src/screens/friends/FriendsList.tsx, src/screens/friends/AddFriend.tsx, src/screens/friends/FriendRequests.tsx, src/app/friends/[id].tsx, src/app/friends/index.tsx</files>
  <read_first>src/screens/friends/FriendsList.tsx, src/screens/friends/AddFriend.tsx, src/screens/friends/FriendRequests.tsx, src/app/friends/[id].tsx, src/app/friends/index.tsx, src/components/common/FAB.tsx, src/theme/colors.ts, src/theme/spacing.ts, src/theme/typography.ts, src/theme/radii.ts, src/theme/shadows.ts</read_first>
  <action>
**FriendsList.tsx (1 violation + FAB replacement):**
1. Replace `import { COLORS } from '@/constants/colors'` with `import { COLORS, SPACING, FONT_SIZE, FONT_WEIGHT, RADII } from '@/theme'` (only what's used).
2. If `import { COLORS as THEME } from '@/theme'` exists, remove and replace all `THEME.xxx` with `COLORS.xxx` semantic paths.
3. Add `import { FAB } from '@/components/common/FAB'`.
4. Replace the inline FAB JSX (the person+ icon-only TouchableOpacity) with `<FAB icon={...same icon...} onPress={...same handler...} accessibilityLabel="Add a friend" />`. Remove inline FAB styles.
5. Replace old flat COLORS keys with semantic paths and fix remaining violations.

**AddFriend.tsx (31 violations — highest in domain):**
1. Same import replacement pattern.
2. Replace ALL old COLORS keys, hardcoded hex, raw values with tokens.
3. 31 violations means many style properties — be thorough.

**FriendRequests.tsx (0 violations but imports @/constants/colors):**
1. Replace `import { COLORS } from '@/constants/colors'` with `import { COLORS } from '@/theme'`.
2. If `import { COLORS as THEME } from '@/theme'` exists, replace alias with direct `COLORS` import.
3. Replace old flat COLORS keys with semantic paths.

**src/app/friends/[id].tsx (14 violations):**
1. Replace import and all hardcoded values with tokens.

**src/app/friends/index.tsx (3 violations):**
1. Replace import and all hardcoded values with tokens.
  </action>
  <verify>
    <automated>npx eslint src/screens/friends/FriendsList.tsx src/screens/friends/AddFriend.tsx src/screens/friends/FriendRequests.tsx "src/app/friends/[id].tsx" src/app/friends/index.tsx 2>&1 | grep -c "no-hardcoded-styles" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "@/constants/colors" src/screens/friends/ src/app/friends/` returns empty
    - `grep "COLORS as THEME" src/screens/friends/FriendsList.tsx` returns empty
    - `grep "import.*FAB.*from '@/components/common/FAB'" src/screens/friends/FriendsList.tsx` returns a match
    - `grep "<FAB" src/screens/friends/FriendsList.tsx` returns a match
    - `npx eslint src/screens/friends/*.tsx "src/app/friends/[id].tsx" src/app/friends/index.tsx 2>&1 | grep "no-hardcoded-styles"` returns empty
  </acceptance_criteria>
  <done>5 friends screen/route files migrated. FriendsList uses FAB component. Zero lint violations.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate friends components (FriendActionSheet, FriendCard, QRCodeDisplay, QRScanView, RequestCard, SearchResultCard, StatusPill)</name>
  <files>src/components/friends/FriendActionSheet.tsx, src/components/friends/FriendCard.tsx, src/components/friends/QRCodeDisplay.tsx, src/components/friends/QRScanView.tsx, src/components/friends/RequestCard.tsx, src/components/friends/SearchResultCard.tsx, src/components/friends/StatusPill.tsx</files>
  <read_first>src/components/friends/FriendActionSheet.tsx, src/components/friends/FriendCard.tsx, src/components/friends/QRCodeDisplay.tsx, src/components/friends/QRScanView.tsx, src/components/friends/RequestCard.tsx, src/components/friends/SearchResultCard.tsx, src/components/friends/StatusPill.tsx, src/theme/colors.ts, src/theme/spacing.ts, src/theme/typography.ts, src/theme/radii.ts, src/theme/shadows.ts</read_first>
  <action>
For each of the 7 friend component files:
1. Replace `import { COLORS } from '@/constants/colors'` with `import { COLORS, SPACING, FONT_SIZE, FONT_WEIGHT, RADII, SHADOWS } from '@/theme'` (only what's used).
2. Replace old flat COLORS keys with semantic paths per mapping.
3. Replace ALL hardcoded hex colors with COLORS.xxx tokens.
4. Replace ALL raw fontSize/fontWeight/padding/margin/gap/borderRadius with tokens.
5. Replace raw shadow blocks with ...SHADOWS.card spread.
6. Keep any raw value with no exact token match, adding `// no exact token` comment.

Violation counts: FriendActionSheet (22), FriendCard (6), QRCodeDisplay (9), QRScanView (17), RequestCard (13), SearchResultCard (10), StatusPill (2) = 79 total.

Pay special attention to:
- FriendActionSheet (22 violations) — many style properties in this bottom sheet
- QRScanView (17 violations) — camera overlay styling
- RequestCard (13 violations) — card layout styling
  </action>
  <verify>
    <automated>npx eslint src/components/friends/FriendActionSheet.tsx src/components/friends/FriendCard.tsx src/components/friends/QRCodeDisplay.tsx src/components/friends/QRScanView.tsx src/components/friends/RequestCard.tsx src/components/friends/SearchResultCard.tsx src/components/friends/StatusPill.tsx 2>&1 | grep -c "no-hardcoded-styles" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "@/constants/colors" src/components/friends/` returns empty
    - `npx eslint src/components/friends/*.tsx 2>&1 | grep "no-hardcoded-styles"` returns empty
  </acceptance_criteria>
  <done>All 7 friend component files migrated to @/theme tokens. Zero lint violations.</done>
</task>

</tasks>

<verification>
- `grep -r "@/constants/colors" src/screens/friends/ src/app/friends/ src/components/friends/` returns empty
- `npx eslint src/screens/friends/*.tsx "src/app/friends/[id].tsx" src/app/friends/index.tsx src/components/friends/*.tsx 2>&1 | grep "no-hardcoded-styles"` returns empty
- `grep "<FAB" src/screens/friends/FriendsList.tsx` returns a match
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- 12 files migrated with zero no-hardcoded-styles violations
- FriendsList uses FAB component
- No file imports from @/constants/colors
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/09-screen-consistency-sweep/09-05-SUMMARY.md`
</output>
