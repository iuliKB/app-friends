---
phase: 08-shared-components
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/home/HomeScreen.tsx
  - src/screens/plans/PlansListScreen.tsx
  - src/screens/friends/FriendsList.tsx
  - src/screens/friends/FriendRequests.tsx
  - src/screens/chat/ChatListScreen.tsx
autonomous: true
requirements: [COMP-05]

must_haves:
  truths:
    - "Home screen RefreshControl uses COLORS.interactive.accent as tintColor"
    - "Plans screen RefreshControl uses COLORS.interactive.accent as tintColor"
    - "FriendsList RefreshControl uses COLORS.interactive.accent as tintColor"
    - "FriendRequests RefreshControl uses COLORS.interactive.accent as tintColor"
    - "ChatListScreen RefreshControl uses COLORS.interactive.accent as tintColor"
    - "All five screens pull-to-refresh works (triggers data reload)"
  artifacts:
    - path: "src/screens/home/HomeScreen.tsx"
      provides: "Standardized RefreshControl tintColor"
      contains: "COLORS.interactive.accent"
    - path: "src/screens/plans/PlansListScreen.tsx"
      provides: "Standardized RefreshControl tintColor"
      contains: "COLORS.interactive.accent"
    - path: "src/screens/friends/FriendsList.tsx"
      provides: "Standardized RefreshControl tintColor"
      contains: "COLORS.interactive.accent"
    - path: "src/screens/friends/FriendRequests.tsx"
      provides: "Standardized RefreshControl tintColor"
      contains: "COLORS.interactive.accent"
    - path: "src/screens/chat/ChatListScreen.tsx"
      provides: "Standardized RefreshControl tintColor"
      contains: "COLORS.interactive.accent"
  key_links:
    - from: "all 5 screen files"
      to: "@/theme"
      via: "COLORS.interactive.accent imported for tintColor"
      pattern: "tintColor.*COLORS.interactive.accent"
---

<objective>
Standardize pull-to-refresh tint color across all five list screens using the accent color token.

Purpose: Currently Home and Plans use `COLORS.textSecondary` for tintColor, while Friends and Chat screens have no explicit tintColor (defaults to system color). This plan makes all five screens use `COLORS.interactive.accent` (campfire orange) for a consistent pull-to-refresh indicator.
Output: Five updated screen files with standardized RefreshControl tintColor
</objective>

<execution_context>
@/Users/iulian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iulian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-shared-components/08-CONTEXT.md

<interfaces>
<!-- Design tokens -->
From src/theme/colors.ts:
```typescript
export const COLORS = {
  interactive: { accent: '#f97316' },  // campfire orange — used for tintColor
  text: { secondary: '#9ca3af' },      // current tintColor on Home/Plans (being replaced)
} as const;
```

<!-- Current RefreshControl patterns across screens -->

Home (has explicit RefreshControl with tintColor):
```typescript
<RefreshControl refreshing={refreshing} onRefresh={handleRefresh} tintColor={COLORS.textSecondary} />
```

Plans (has explicit RefreshControl with tintColor):
```typescript
<RefreshControl refreshing={refreshing} onRefresh={handleRefresh} tintColor={COLORS.textSecondary} />
```

FriendsList (uses FlatList props, no explicit tintColor):
```typescript
<FlatList onRefresh={fetchFriends} refreshing={loadingFriends} ... />
```

FriendRequests (uses FlatList props, no explicit tintColor):
```typescript
<FlatList onRefresh={fetchPendingRequests} refreshing={loadingPending} ... />
```

ChatListScreen (uses FlatList props, no explicit tintColor):
```typescript
<FlatList onRefresh={handleRefresh} refreshing={refreshing} ... />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize RefreshControl tintColor on Home and Plans screens</name>
  <files>src/screens/home/HomeScreen.tsx, src/screens/plans/PlansListScreen.tsx</files>
  <read_first>
    - src/screens/home/HomeScreen.tsx (full file — find RefreshControl and existing COLORS import)
    - src/screens/plans/PlansListScreen.tsx (full file — find RefreshControl and existing COLORS import)
    - src/theme/colors.ts (COLORS.interactive.accent value)
  </read_first>
  <action>
**HomeScreen.tsx:**
1. Add import: `import { COLORS as THEME_COLORS } from '@/theme';` (keep existing `COLORS` from `@/constants/colors` — Phase 9 will do the full migration). Alternatively, if the file already imports from `@/theme`, just use that import. If importing both would be confusing, just add the token value directly: the tintColor line should become `tintColor={COLORS.interactive?.accent ?? '#f97316'}` — but the cleanest approach is:
   - Add `import { COLORS as TOKEN_COLORS } from '@/theme';` at top
   - Change line 69: `tintColor={COLORS.textSecondary}` to `tintColor={TOKEN_COLORS.interactive.accent}`

   Actually, simplest approach: since we only need one token value, just import COLORS from @/theme with an alias to avoid collision with the existing @/constants/colors import:
   - Add: `import { COLORS as THEME } from '@/theme';`
   - Change: `tintColor={COLORS.textSecondary}` to `tintColor={THEME.interactive.accent}`

**PlansListScreen.tsx:**
1. Same pattern: add `import { COLORS as THEME } from '@/theme';`
2. Change: `tintColor={COLORS.textSecondary}` to `tintColor={THEME.interactive.accent}`
  </action>
  <verify>
    <automated>cd /Users/iulian/Develop/campfire && npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - grep "THEME.interactive.accent" src/screens/home/HomeScreen.tsx returns a match (or equivalent token reference)
    - grep "textSecondary" src/screens/home/HomeScreen.tsx does NOT appear in a tintColor context
    - grep "THEME.interactive.accent" src/screens/plans/PlansListScreen.tsx returns a match (or equivalent token reference)
    - grep "textSecondary" src/screens/plans/PlansListScreen.tsx does NOT appear in a tintColor context
    - grep "from '@/theme'" src/screens/home/HomeScreen.tsx returns a match
    - grep "from '@/theme'" src/screens/plans/PlansListScreen.tsx returns a match
    - npx tsc --noEmit reports 0 new errors
  </acceptance_criteria>
  <done>Home and Plans screens use COLORS.interactive.accent (campfire orange) for pull-to-refresh tint color instead of textSecondary gray</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit RefreshControl with tintColor on Friends and Chat screens</name>
  <files>src/screens/friends/FriendsList.tsx, src/screens/friends/FriendRequests.tsx, src/screens/chat/ChatListScreen.tsx</files>
  <read_first>
    - src/screens/friends/FriendsList.tsx (full file — find FlatList onRefresh/refreshing props, check if RefreshControl is imported)
    - src/screens/friends/FriendRequests.tsx (full file — find FlatList onRefresh/refreshing props)
    - src/screens/chat/ChatListScreen.tsx (full file — find FlatList onRefresh/refreshing props)
    - src/theme/colors.ts (COLORS.interactive.accent)
  </read_first>
  <action>
For each of the three files, convert from FlatList's shorthand `onRefresh`/`refreshing` props to an explicit `refreshControl` prop with tintColor:

**FriendsList.tsx:**
1. Add `import { RefreshControl } from 'react-native';` to the existing RN import (if not already imported)
2. Add `import { COLORS as THEME } from '@/theme';`
3. Remove the `onRefresh={fetchFriends}` and `refreshing={loadingFriends}` props from FlatList
4. Add `refreshControl` prop to FlatList:
```tsx
refreshControl={
  <RefreshControl
    refreshing={loadingFriends}
    onRefresh={fetchFriends}
    tintColor={THEME.interactive.accent}
  />
}
```

**FriendRequests.tsx:**
1. Add `import { RefreshControl } from 'react-native';` to existing RN import (if not already imported)
2. Add `import { COLORS as THEME } from '@/theme';`
3. Remove `onRefresh={fetchPendingRequests}` and `refreshing={loadingPending}` props from FlatList
4. Add `refreshControl` prop:
```tsx
refreshControl={
  <RefreshControl
    refreshing={loadingPending}
    onRefresh={fetchPendingRequests}
    tintColor={THEME.interactive.accent}
  />
}
```

**ChatListScreen.tsx:**
1. Add `import { RefreshControl } from 'react-native';` to existing RN import (if not already imported)
2. Add `import { COLORS as THEME } from '@/theme';`
3. Remove `onRefresh={handleRefresh}` and `refreshing={refreshing}` props from FlatList
4. Add `refreshControl` prop:
```tsx
refreshControl={
  <RefreshControl
    refreshing={refreshing}
    onRefresh={handleRefresh}
    tintColor={THEME.interactive.accent}
  />
}
```

Note: Using `COLORS as THEME` alias to avoid collision with existing `COLORS` import from `@/constants/colors`. Phase 9 will do the full migration to remove the old import.
  </action>
  <verify>
    <automated>cd /Users/iulian/Develop/campfire && npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - grep "THEME.interactive.accent" src/screens/friends/FriendsList.tsx returns a match
    - grep "RefreshControl" src/screens/friends/FriendsList.tsx returns a match
    - grep "tintColor" src/screens/friends/FriendsList.tsx returns a match
    - grep "THEME.interactive.accent" src/screens/friends/FriendRequests.tsx returns a match
    - grep "RefreshControl" src/screens/friends/FriendRequests.tsx returns a match
    - grep "tintColor" src/screens/friends/FriendRequests.tsx returns a match
    - grep "THEME.interactive.accent" src/screens/chat/ChatListScreen.tsx returns a match
    - grep "RefreshControl" src/screens/chat/ChatListScreen.tsx returns a match
    - grep "tintColor" src/screens/chat/ChatListScreen.tsx returns a match
    - grep "from '@/theme'" src/screens/friends/FriendsList.tsx returns a match
    - grep "from '@/theme'" src/screens/friends/FriendRequests.tsx returns a match
    - grep "from '@/theme'" src/screens/chat/ChatListScreen.tsx returns a match
    - npx tsc --noEmit reports 0 new errors
  </acceptance_criteria>
  <done>FriendsList, FriendRequests, and ChatListScreen all use explicit RefreshControl with COLORS.interactive.accent tintColor (campfire orange)</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `grep -r "tintColor" src/screens/home/HomeScreen.tsx src/screens/plans/PlansListScreen.tsx src/screens/friends/FriendsList.tsx src/screens/friends/FriendRequests.tsx src/screens/chat/ChatListScreen.tsx` shows all five files have tintColor
- `grep -r "interactive.accent" src/screens/home/HomeScreen.tsx src/screens/plans/PlansListScreen.tsx src/screens/friends/FriendsList.tsx src/screens/friends/FriendRequests.tsx src/screens/chat/ChatListScreen.tsx` shows all five files use the accent token
</verification>

<success_criteria>
- All five list screens (Home, Plans, FriendsList, FriendRequests, ChatListScreen) use COLORS.interactive.accent for RefreshControl tintColor
- Pull-to-refresh spinner is campfire orange on all screens
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-shared-components/08-03-SUMMARY.md`
</output>
