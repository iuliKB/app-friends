---
phase: 260513-5as
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useTabBarSpacing.ts
  - src/hooks/__tests__/useTabBarSpacing.test.ts
  - src/screens/home/HomeScreen.tsx
  - src/screens/plans/PlansListScreen.tsx
  - src/app/(tabs)/profile.tsx
  - src/app/(tabs)/squad.tsx
  - src/components/squad/MemoriesTabContent.tsx
autonomous: true
requirements:
  - QUICK-260513-5as
must_haves:
  truths:
    - "A single `useTabBarSpacing()` hook in src/hooks/ returns the correct paddingBottom for tab screens"
    - "When currentSurface === 'tabs', the hook returns insets.bottom + TAB_BAR_HEIGHT + TAB_BAR_BOTTOM_GAP + 24 (numerically identical to HomeScreen's previous `insets.bottom + 100`)"
    - "When currentSurface !== 'tabs', the hook returns insets.bottom only"
    - "All 6 callsites (Home ScrollView, PlansListScreen FlatList, Profile ScrollView, Squad Friends SectionList, Squad Activity ScrollView, Memories SectionList) consume the hook"
    - "Pre-existing magic numbers (+100 in HomeScreen, +100 in PlansListScreen, SPACING.xxl in Squad Activity, SPACING.lg in Memories) are eliminated"
    - "The dangling `eslint-disable-next-line campfire/no-hardcoded-styles` comment in PlansListScreen.tsx:81 is removed"
    - "Hook test verifies both surface branches and uses real useNavigationStore (driven via getState().setSurface())"
    - "Home screen produces identical pixel output (zero visual regression — user emphasized 'homescreen is the ideal amount')"
  artifacts:
    - path: "src/hooks/useTabBarSpacing.ts"
      provides: "useTabBarSpacing hook returning number"
      exports: ["useTabBarSpacing"]
      min_lines: 15
    - path: "src/hooks/__tests__/useTabBarSpacing.test.ts"
      provides: "Unit tests for both surface branches"
      contains: "useTabBarSpacing"
      min_lines: 20
    - path: "src/screens/home/HomeScreen.tsx"
      provides: "Updated ScrollView contentContainerStyle uses hook"
      contains: "useTabBarSpacing"
    - path: "src/screens/plans/PlansListScreen.tsx"
      provides: "Updated FlatList contentContainerStyle uses hook"
      contains: "useTabBarSpacing"
    - path: "src/app/(tabs)/profile.tsx"
      provides: "ScrollView gets bottom padding from hook"
      contains: "useTabBarSpacing"
    - path: "src/app/(tabs)/squad.tsx"
      provides: "Friends SectionList + Activity ScrollView use hook"
      contains: "useTabBarSpacing"
    - path: "src/components/squad/MemoriesTabContent.tsx"
      provides: "Memories SectionList uses hook"
      contains: "useTabBarSpacing"
  key_links:
    - from: "src/hooks/useTabBarSpacing.ts"
      to: "src/components/common/CustomTabBar.tsx"
      via: "imports TAB_BAR_HEIGHT, TAB_BAR_BOTTOM_GAP constants"
      pattern: "from '@/components/common/CustomTabBar'"
    - from: "src/hooks/useTabBarSpacing.ts"
      to: "src/stores/useNavigationStore.ts"
      via: "selector reads currentSurface"
      pattern: "useNavigationStore\\(\\(s\\) => s\\.currentSurface\\)"
    - from: "src/hooks/useTabBarSpacing.ts"
      to: "react-native-safe-area-context"
      via: "useSafeAreaInsets()"
      pattern: "useSafeAreaInsets"
    - from: "src/screens/home/HomeScreen.tsx"
      to: "src/hooks/useTabBarSpacing.ts"
      via: "consumer of paddingBottom value"
      pattern: "useTabBarSpacing\\(\\)"
    - from: "src/screens/plans/PlansListScreen.tsx"
      to: "src/hooks/useTabBarSpacing.ts"
      via: "consumer in FlatList contentContainerStyle"
      pattern: "useTabBarSpacing\\(\\)"
    - from: "src/app/(tabs)/profile.tsx"
      to: "src/hooks/useTabBarSpacing.ts"
      via: "consumer in ScrollView contentContainerStyle"
      pattern: "useTabBarSpacing\\(\\)"
    - from: "src/app/(tabs)/squad.tsx"
      to: "src/hooks/useTabBarSpacing.ts"
      via: "consumer at Friends + Activity scroll containers"
      pattern: "useTabBarSpacing\\(\\)"
    - from: "src/components/squad/MemoriesTabContent.tsx"
      to: "src/hooks/useTabBarSpacing.ts"
      via: "consumer in Memories SectionList contentContainerStyle"
      pattern: "useTabBarSpacing\\(\\)"
---

<objective>
Centralize bottom-nav clearance for tab screens behind a single `useTabBarSpacing()` hook. The home screen's existing `insets.bottom + 100` is the visual baseline (user confirmed "homescreen is the ideal amount"). After this plan, all six in-tabs scroll containers consume the hook, eliminating four different magic-number formulas in the codebase and removing one dangling eslint-disable comment.

Purpose: Single source of truth for tab-screen bottom padding, surface-aware (returns plain `insets.bottom` when the floating bar is hidden), zero visual regression on Home.

Output:
- New hook file + unit tests
- 5 updated screen/component files consuming the hook
- Four magic-number paddings collapsed into one formula
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260513-5as-add-usetabbarspacing-hook-for-consistent/260513-5as-CONTEXT.md
@.planning/quick/260513-5as-add-usetabbarspacing-hook-for-consistent/260513-5as-RESEARCH.md

<interfaces>
<!-- Extracted from codebase. Executor should use these directly — no codebase exploration needed. -->

From src/components/common/CustomTabBar.tsx (lines 9-10, EXPORTED):
```ts
export const TAB_BAR_HEIGHT = 64;
export const TAB_BAR_BOTTOM_GAP = 12;
```

From src/stores/useNavigationStore.ts (lines 19, 30):
```ts
export type NavigationSurface = 'tabs' | 'chat' | 'plan' | 'modal' | 'auth';

export const useNavigationStore = create<NavigationState>((set) => ({
  currentSurface: 'tabs',
  setSurface: (next) => set({ currentSurface: next }),
  reset: () => set({ currentSurface: 'tabs' }),
}));
```

From react-native-safe-area-context (used everywhere):
```ts
import { useSafeAreaInsets } from 'react-native-safe-area-context';
// returns { top: number; right: number; bottom: number; left: number }
```

Project hook export convention (see src/hooks/usePendingRequestsCount.ts):
```ts
export function useXxx(...): ReturnType { ... }
// single named export, no default, explicit return type annotation
```

Idiomatic Zustand selector for currentSurface (see src/components/common/CustomTabBar.tsx:124 — already in production):
```ts
const surface = useNavigationStore((s) => s.currentSurface);
```

Test mock pattern for safe-area (see src/components/common/__tests__/CustomTabBar.test.tsx:27-29):
```ts
jest.mock('react-native-safe-area-context', () => ({
  useSafeAreaInsets: () => ({ top: 0, right: 0, bottom: 34, left: 0 }),
}));
```

Test pattern for driving Zustand store (see same file, line 60):
```ts
beforeEach(() => {
  useNavigationStore.getState().reset();
});

useNavigationStore.getState().setSurface('tabs'); // or 'chat'
```

Test renderHook pattern (see src/hooks/__tests__/useViewPreference.test.ts:10):
```ts
import { renderHook, act } from '@testing-library/react-native';
const { result, rerender } = renderHook(() => useXxx());
```
</interfaces>

<callsite_map>
<!-- Every callsite the executor must edit in Task 2, with current state and target state. -->

1. **HomeScreen** — `src/screens/home/HomeScreen.tsx:203`
   - Current: `contentContainerStyle={[styles.scrollContent, { paddingBottom: insets.bottom + 100 }]}`
   - Target: `contentContainerStyle={[styles.scrollContent, { paddingBottom: bottomSpacing }]}` (where `const bottomSpacing = useTabBarSpacing();`)
   - Numeric equivalence: `34 + 64 + 12 + 24 = 134 === 34 + 100 = 134` (iPhone X bottom inset). Zero pixel change on tabs surface.

2. **PlansListScreen** — `src/screens/plans/PlansListScreen.tsx:79-83, 437`
   - Current line 79-83 (inside StyleSheet):
     ```ts
     listContent: {
       paddingHorizontal: SPACING.lg,
       // eslint-disable-next-line campfire/no-hardcoded-styles
       paddingBottom: 100, // no exact token — intentional large FAB clearance
     },
     ```
   - Target: drop both the `paddingBottom: 100` line AND the `eslint-disable-next-line` comment immediately above it. Style block stays as:
     ```ts
     listContent: {
       paddingHorizontal: SPACING.lg,
     },
     ```
   - Current line 437: `contentContainerStyle={styles.listContent}`
   - Target line 437: `contentContainerStyle={[styles.listContent, { paddingBottom: bottomSpacing }]}` (after declaring `const bottomSpacing = useTabBarSpacing();` near the existing hook calls inside the component)

3. **Profile** — `src/app/(tabs)/profile.tsx:455`
   - Current: `contentContainerStyle={[styles.scrollContent, { paddingTop: SPACING.lg }]}`
   - Target: `contentContainerStyle={[styles.scrollContent, { paddingTop: SPACING.lg, paddingBottom: bottomSpacing }]}`

4. **Squad — Friends tab SectionList** — `src/app/(tabs)/squad.tsx:454`
   - Current: `contentContainerStyle={styles.listContent}` (where `listContent` is `{ flexGrow: 1 }`, line 272-274 — NO bottom padding at all)
   - Target: `contentContainerStyle={[styles.listContent, { paddingBottom: bottomSpacing }]}`. KEEP `flexGrow: 1` in `styles.listContent` so `ListEmptyComponent` still vertically centers.

5. **Squad — Activity tab ScrollView** — `src/app/(tabs)/squad.tsx:473-476`
   - Current:
     ```tsx
     contentContainerStyle={[
       styles.activityContent,
       { paddingBottom: insets.bottom + SPACING.xxl },
     ]}
     ```
   - Target:
     ```tsx
     contentContainerStyle={[
       styles.activityContent,
       { paddingBottom: bottomSpacing },
     ]}
     ```

6. **Memories** — `src/components/squad/MemoriesTabContent.tsx:286-289`
   - Current:
     ```tsx
     contentContainerStyle={{
       paddingTop: SPACING.sm,
       paddingBottom: insets.bottom + SPACING.lg,
     }}
     ```
   - Target:
     ```tsx
     contentContainerStyle={{
       paddingTop: SPACING.sm,
       paddingBottom: bottomSpacing,
     }}
     ```
   - Add `const bottomSpacing = useTabBarSpacing();` near the existing `useSafeAreaInsets()` call. The `insets.bottom` reference in `paddingBottom` is gone; verify whether `insets` is still used elsewhere in the component — if not, drop the `useSafeAreaInsets()` call too. If still used, leave it.

**Out of scope (do NOT touch):**
- `src/screens/plans/PlansListScreen.tsx:142-145` — `modalList` style (invite-modal full-screen FlatList, lives above tab bar)
- Any other ScrollView/FlatList/SectionList not listed above
</callsite_map>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useTabBarSpacing hook + unit tests</name>
  <files>src/hooks/useTabBarSpacing.ts, src/hooks/__tests__/useTabBarSpacing.test.ts</files>
  <behavior>
    - With `currentSurface === 'tabs'` and `insets.bottom === 34`, hook returns `134` (i.e. `insets.bottom + TAB_BAR_HEIGHT + TAB_BAR_BOTTOM_GAP + BREATHING_ROOM = 34 + 64 + 12 + 24`).
    - With `currentSurface === 'chat'` and `insets.bottom === 34`, hook returns `34` (i.e. only `insets.bottom`).
    - The same `'chat'` behavior applies to `'plan'`, `'modal'`, `'auth'` (any surface that is not `'tabs'`). At least one non-tabs assertion is sufficient.
    - Numeric coincidence: when `surface === 'tabs'`, the formula yields `insets.bottom + 100` — identical to HomeScreen's previous magic number. This is the zero-visual-regression guarantee for the home screen.
  </behavior>
  <action>
    Create the hook file `src/hooks/useTabBarSpacing.ts`:
    - Single named export: `export function useTabBarSpacing(): number { ... }`. Match the explicit-return-type style of `usePendingRequestsCount`.
    - Imports (use `@/` path alias — verified working):
      - `import { useSafeAreaInsets } from 'react-native-safe-area-context';`
      - `import { TAB_BAR_HEIGHT, TAB_BAR_BOTTOM_GAP } from '@/components/common/CustomTabBar';` (these are already exported at lines 9-10 — do NOT duplicate the numbers)
      - `import { useNavigationStore } from '@/stores/useNavigationStore';`
    - Module-level constant: `const BREATHING_ROOM = 24;` (NOT exported — internal visual tuning knob per CONTEXT.md decision).
    - Body:
      ```ts
      const insets = useSafeAreaInsets();
      const surface = useNavigationStore((s) => s.currentSurface);
      if (surface !== 'tabs') {
        return insets.bottom;
      }
      return insets.bottom + TAB_BAR_HEIGHT + TAB_BAR_BOTTOM_GAP + BREATHING_ROOM;
      ```
    - Add a brief 1–2 line header comment (mirrors useNavigationStore.ts style) explaining: "Returns `paddingBottom` for tab-screen scroll containers. Composes safe-area inset + tab-bar geometry + breathing room when the bar is visible; returns inset only when a full-screen surface (chat/plan/modal/auth) has hidden the bar."
    - No `useMemo` — returning a `number` primitive, downstream array-style `contentContainerStyle` allocates each render regardless. CONTEXT.md decision locked.
    - No `shallow` equality fn — single-primitive selector, Zustand v5 returns referentially stable primitives.

    Create the test file `src/hooks/__tests__/useTabBarSpacing.test.ts`:
    - Header comment matching the project convention (see `useViewPreference.test.ts:1-8`). Title: "useTabBarSpacing test — quick 260513-5as". Note the run command: `npx jest --testPathPatterns="useTabBarSpacing" --no-coverage`.
    - Mock `react-native-safe-area-context` at module scope, returning `{ top: 0, right: 0, bottom: 34, left: 0 }` (matches `CustomTabBar.test.tsx:27-29` pattern):
      ```ts
      jest.mock('react-native-safe-area-context', () => ({
        useSafeAreaInsets: () => ({ top: 0, right: 0, bottom: 34, left: 0 }),
      }));
      ```
    - Do NOT mock `useNavigationStore` — drive the real store via `useNavigationStore.getState().setSurface(...)` between assertions (matches `CustomTabBar.test.tsx:60` pattern).
    - `beforeEach`: call `useNavigationStore.getState().reset();` to restore default `'tabs'` surface.
    - Tests (minimum 3):
      1. `'returns insets.bottom + 100 (== insets.bottom + TAB_BAR_HEIGHT + TAB_BAR_BOTTOM_GAP + 24) when surface === tabs'` → `useNavigationStore.getState().setSurface('tabs')`, then `renderHook(() => useTabBarSpacing())`, assert `result.current === 134`.
      2. `'returns only insets.bottom when surface === chat'` → `useNavigationStore.getState().setSurface('chat')`, then `renderHook(...)`, assert `result.current === 34`.
      3. `'returns only insets.bottom for plan/modal/auth surfaces'` → loop over `(['plan', 'modal', 'auth'] as const)`, for each: `setSurface(s)`, `renderHook(...)`, assert `result.current === 34`. (One `renderHook` per iteration; no rerender chain — simpler.)
    - No imports of `useTabBarSpacing` from a relative path stronger than `../useTabBarSpacing`. Other hook tests use `from '../useViewPreference'` — match that.
  </action>
  <verify>
    <automated>npx jest --testPathPatterns="useTabBarSpacing" --no-coverage</automated>
  </verify>
  <done>
    - `src/hooks/useTabBarSpacing.ts` exists with single named export `useTabBarSpacing` returning `number`.
    - File imports `TAB_BAR_HEIGHT` and `TAB_BAR_BOTTOM_GAP` from `@/components/common/CustomTabBar` (NOT redefined locally — grep for `TAB_BAR_HEIGHT\s*=\s*64` in the new file must return zero matches).
    - File uses `useNavigationStore((s) => s.currentSurface)` selector form (verbatim).
    - `BREATHING_ROOM = 24` constant is in the file and NOT exported.
    - Test file exists at `src/hooks/__tests__/useTabBarSpacing.test.ts` with 3 passing tests covering tabs surface + at least one non-tabs surface.
    - `npx jest --testPathPatterns="useTabBarSpacing" --no-coverage` exits 0 with 3 passed.
    - `npx tsc --noEmit` shows no new TypeScript errors (run from repo root; if the project's tsc baseline already has errors, the count must not increase from the new files).
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply useTabBarSpacing at all 6 callsites + remove dangling eslint-disable</name>
  <files>src/screens/home/HomeScreen.tsx, src/screens/plans/PlansListScreen.tsx, src/app/(tabs)/profile.tsx, src/app/(tabs)/squad.tsx, src/components/squad/MemoriesTabContent.tsx</files>
  <action>
    Apply the hook at every callsite enumerated in the `<callsite_map>` block above. For each file:

    1. Add `import { useTabBarSpacing } from '@/hooks/useTabBarSpacing';` near the other `@/hooks/...` imports (or alphabetized into the existing hook-import group — match the file's existing style).
    2. Inside the component body, near the existing `useSafeAreaInsets()` call (every target file already has one), add: `const bottomSpacing = useTabBarSpacing();`
    3. Replace the per-file padding expression with `bottomSpacing` as detailed in `<callsite_map>`.

    Per-file specifics:

    **A. `src/screens/home/HomeScreen.tsx`** — single edit at line 203. Replace `paddingBottom: insets.bottom + 100` with `paddingBottom: bottomSpacing`. Do NOT remove the existing `useSafeAreaInsets()` call (it's still used for `paddingTop: insets.top` on line ~185).

    **B. `src/screens/plans/PlansListScreen.tsx`**:
    - Inside the `useMemo` styles block at lines 79-83: delete BOTH the `// eslint-disable-next-line campfire/no-hardcoded-styles` comment AND the `paddingBottom: 100, // no exact token — intentional large FAB clearance` line. The resulting `listContent` style becomes `{ paddingHorizontal: SPACING.lg }` only.
    - At line 437 JSX site: change `contentContainerStyle={styles.listContent}` to `contentContainerStyle={[styles.listContent, { paddingBottom: bottomSpacing }]}`.
    - Do NOT touch `modalList` (line 142-145) — research item 9 explicitly excludes it.

    **C. `src/app/(tabs)/profile.tsx`** — single edit at line 455. Change `contentContainerStyle={[styles.scrollContent, { paddingTop: SPACING.lg }]}` to `contentContainerStyle={[styles.scrollContent, { paddingTop: SPACING.lg, paddingBottom: bottomSpacing }]}`. Existing `useSafeAreaInsets()` call (used for `paddingTop: insets.top` on line 452) stays.

    **D. `src/app/(tabs)/squad.tsx`** — TWO edits in this file:
    - Line 454 (Friends SectionList): change `contentContainerStyle={styles.listContent}` to `contentContainerStyle={[styles.listContent, { paddingBottom: bottomSpacing }]}`. KEEP `styles.listContent` as `{ flexGrow: 1 }` (Pitfall 6 — empty-state vertical centering depends on `flexGrow: 1`).
    - Lines 473-476 (Activity ScrollView): replace `paddingBottom: insets.bottom + SPACING.xxl` with `paddingBottom: bottomSpacing` inside the inline style object.
    - Existing `useSafeAreaInsets()` call stays — it may be used elsewhere; even if not, dead-import cleanup is out of scope for this task to keep the diff focused. If a lint rule fails on unused imports, drop the import; otherwise leave it.

    **E. `src/components/squad/MemoriesTabContent.tsx`** — single edit at lines 286-289. Replace `paddingBottom: insets.bottom + SPACING.lg` with `paddingBottom: bottomSpacing`. After the change, check whether `insets` is referenced anywhere else in the file. If NOT, remove the local `const insets = useSafeAreaInsets();` line and the matching `import { useSafeAreaInsets } from 'react-native-safe-area-context';` import. If YES, leave both intact.

    Do not modify any other paddingBottom in any of the listed files. Do not modify `styles.activityContent` or `styles.listContent` definitions other than the PlansListScreen `paddingBottom: 100` removal already specified.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/screens/home/HomeScreen.tsx src/screens/plans/PlansListScreen.tsx 'src/app/(tabs)/profile.tsx' 'src/app/(tabs)/squad.tsx' src/components/squad/MemoriesTabContent.tsx src/hooks/useTabBarSpacing.ts --max-warnings=0 && grep -rn 'useTabBarSpacing' src/screens/home/HomeScreen.tsx src/screens/plans/PlansListScreen.tsx 'src/app/(tabs)/profile.tsx' 'src/app/(tabs)/squad.tsx' src/components/squad/MemoriesTabContent.tsx | wc -l | awk '{ if ($1 < 10) { print "FAIL: expected 10 import+call lines (2 per file × 5 files), got " $1; exit 1 } else print "OK" }' && ! grep -n 'paddingBottom: 100' src/screens/plans/PlansListScreen.tsx && ! grep -n 'eslint-disable-next-line campfire/no-hardcoded-styles' src/screens/plans/PlansListScreen.tsx && ! grep -n 'insets.bottom + 100' src/screens/home/HomeScreen.tsx</automated>
  </verify>
  <done>
    - All 5 files import `useTabBarSpacing` from `@/hooks/useTabBarSpacing`.
    - Each of the 5 files calls `useTabBarSpacing()` exactly once inside its component body and assigns to `bottomSpacing`.
    - Every callsite in the `<callsite_map>` block above is updated to consume `bottomSpacing` in its `contentContainerStyle`.
    - HomeScreen no longer contains `insets.bottom + 100` (replaced by `bottomSpacing`).
    - PlansListScreen no longer contains `paddingBottom: 100` or the `eslint-disable-next-line campfire/no-hardcoded-styles` comment.
    - Squad's Friends-tab SectionList now has bottom padding (previously had none).
    - Squad's Activity-tab ScrollView no longer contains `insets.bottom + SPACING.xxl` as `paddingBottom`.
    - MemoriesTabContent no longer contains `insets.bottom + SPACING.lg` as `paddingBottom`.
    - `npx tsc --noEmit` passes (no new errors introduced).
    - ESLint passes on all touched files with `--max-warnings=0`.
    - Pre-existing tests still pass: `npx jest --no-coverage` (full suite) exits 0, OR — if the project's full suite has unrelated pre-existing failures — at minimum `npx jest --testPathPatterns="useTabBarSpacing|CustomTabBar|HomeScreen|PlansListScreen|squad|profile|MemoriesTabContent" --no-coverage` exits 0.
  </done>
</task>

</tasks>

<verification>
## Phase-level checks (post-execution)

1. **Hook exists and tests pass:**
   `npx jest --testPathPatterns="useTabBarSpacing" --no-coverage` → 3 tests pass.

2. **All callsites consume the hook:**
   `grep -rln 'useTabBarSpacing()' src/screens/home/HomeScreen.tsx src/screens/plans/PlansListScreen.tsx 'src/app/(tabs)/profile.tsx' 'src/app/(tabs)/squad.tsx' src/components/squad/MemoriesTabContent.tsx | wc -l` → outputs `5`.

3. **Magic numbers eliminated:**
   - `grep -n 'insets.bottom + 100' src/screens/home/HomeScreen.tsx` → no matches.
   - `grep -n 'paddingBottom: 100' src/screens/plans/PlansListScreen.tsx` → no matches.
   - `grep -n 'eslint-disable-next-line campfire/no-hardcoded-styles' src/screens/plans/PlansListScreen.tsx` → no matches.
   - `grep -n 'insets.bottom + SPACING.xxl' src/app/(tabs)/squad.tsx` → no matches in the file's JSX (the constant `SPACING.xxl` may still be imported/used elsewhere — only the squad Activity `paddingBottom` site must be gone).
   - `grep -n 'insets.bottom + SPACING.lg' src/components/squad/MemoriesTabContent.tsx` → no matches.

4. **Hook does not duplicate constants:**
   `grep -n 'TAB_BAR_HEIGHT\s*=\s*64' src/hooks/useTabBarSpacing.ts` → no matches (must import, not redefine).

5. **Zero visual regression target (home):**
   Hook formula yields `insets.bottom + 64 + 12 + 24 = insets.bottom + 100` when surface === 'tabs' — algebraic equivalence with HomeScreen's previous magic number. Verified via Task 1 unit test (`result.current === 134` for `insets.bottom === 34`).

6. **TypeScript + ESLint:**
   `npx tsc --noEmit` → no new errors.
   `npx eslint <touched files> --max-warnings=0` → exits 0.
</verification>

<success_criteria>
- [ ] `src/hooks/useTabBarSpacing.ts` exists; exports `useTabBarSpacing(): number`; imports `TAB_BAR_HEIGHT`/`TAB_BAR_BOTTOM_GAP` from `@/components/common/CustomTabBar` (does not redefine them); uses `useNavigationStore((s) => s.currentSurface)` selector; defines `BREATHING_ROOM = 24` locally.
- [ ] `src/hooks/__tests__/useTabBarSpacing.test.ts` covers both surface branches; 3 tests pass.
- [ ] All 6 callsites consume the hook (Home, PlansListScreen, Profile, Squad Friends, Squad Activity, Memories).
- [ ] Four pre-existing magic-number paddings (HomeScreen +100, PlansListScreen +100, Squad Activity SPACING.xxl, Memories SPACING.lg) are removed and now sourced from the hook.
- [ ] Dangling `eslint-disable-next-line campfire/no-hardcoded-styles` comment in PlansListScreen.tsx is removed.
- [ ] Squad Friends tab now has bottom padding (previously had none — last friend row used to hide under the bar).
- [ ] HomeScreen produces identical pixel output (user-emphasized zero-regression requirement) — guaranteed by formula equivalence.
- [ ] `npx tsc --noEmit` passes with no new errors; ESLint on touched files passes with `--max-warnings=0`.
- [ ] Existing CustomTabBar tests still pass (touching `useNavigationStore` is read-only — no behavior change for the bar's own visibility logic).
</success_criteria>

<output>
After completion, create `.planning/quick/260513-5as-add-usetabbarspacing-hook-for-consistent/260513-5as-SUMMARY.md` covering:
- What was built (hook + 6 callsite migrations)
- Files touched (with line counts where relevant)
- Test output (jest run summary)
- Any deviations from this plan (e.g., if MemoriesTabContent's `insets` was found to be referenced elsewhere and `useSafeAreaInsets()` was therefore kept)
- Verification grep evidence (zero matches for the 5 magic-number patterns listed in the verification section)
</output>
