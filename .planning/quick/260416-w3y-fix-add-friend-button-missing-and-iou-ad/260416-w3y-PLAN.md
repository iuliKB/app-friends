---
phase: quick-260416-w3y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(tabs)/squad.tsx
  - src/app/squad/expenses/index.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Squad tab header shows a person-add icon in the top-right corner"
    - "Tapping the person-add icon navigates to /friends/add"
    - "IOU Balances screen shows a FAB (+) in the bottom-right corner"
    - "Tapping the FAB navigates to /squad/expenses/create"
  artifacts:
    - path: "src/app/(tabs)/squad.tsx"
      provides: "ScreenHeader with rightAction person-add button"
    - path: "src/app/squad/expenses/index.tsx"
      provides: "FAB overlaid in bottom-right via absolute positioning"
  key_links:
    - from: "src/app/(tabs)/squad.tsx"
      to: "/friends/add"
      via: "router.push in rightAction TouchableOpacity"
    - from: "src/app/squad/expenses/index.tsx"
      to: "/squad/expenses/create"
      via: "router.push in FAB TouchableOpacity"
---

<objective>
Fix two missing navigation entry points:
1. Squad tab is missing an add-friend button in the header
2. IOU Balances screen has no way to create a new expense

Purpose: Both flows exist (routes are live) but are unreachable from the UI — these are blocking usability gaps.
Output: `squad.tsx` gains a `rightAction` on its `ScreenHeader`; `expenses/index.tsx` gains an absolute-positioned FAB.
</objective>

<execution_context>
@/Users/iulian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iulian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add person-add rightAction to Squad ScreenHeader</name>
  <files>src/app/(tabs)/squad.tsx</files>
  <action>
    In `src/app/(tabs)/squad.tsx`, update the `<ScreenHeader title="Squad" />` at line 181 to add a `rightAction` prop.

    The rightAction JSX:
    ```tsx
    rightAction={
      <TouchableOpacity
        onPress={() => router.push('/friends/add')}
        accessibilityLabel="Add friend"
        accessibilityRole="button"
      >
        <Ionicons name="person-add-outline" size={24} color={COLORS.text.primary} />
      </TouchableOpacity>
    }
    ```

    Both `TouchableOpacity` and `Ionicons` are already imported at the top of the file (lines 13 and 15 respectively). `router` is already available via `useRouter()`. `COLORS` is already imported.

    No new imports needed.
  </action>
  <verify>
    Run: `npx tsc --noEmit`
    Visually: Squad tab header shows person-add icon top-right; tapping opens /friends/add screen.
  </verify>
  <done>ScreenHeader in squad.tsx has rightAction with person-add Ionicons icon that routes to /friends/add.</done>
</task>

<task type="auto">
  <name>Task 2: Add FAB to IOU Balances index screen</name>
  <files>src/app/squad/expenses/index.tsx</files>
  <action>
    In `src/app/squad/expenses/index.tsx`, the root `SafeAreaView` in the success state (line 78) uses `styles.container` which has `flex: 1`. Add an absolute-positioned FAB inside it, after the `FlatList`.

    1. Wrap the existing `SafeAreaView` content in a `View` with `style={{ flex: 1 }}` if needed — actually, since `SafeAreaView` already has `flex: 1`, just add the FAB as a sibling of `FlatList` inside the same `SafeAreaView`.

    Insert after the closing `</FlatList>` tag (before `</SafeAreaView>`) in the success-state return:
    ```tsx
    <TouchableOpacity
      style={styles.fab}
      onPress={() => router.push('/squad/expenses/create')}
      accessibilityLabel="Add expense"
      accessibilityRole="button"
    >
      <Ionicons name="add" size={28} color={COLORS.surface.base} />
    </TouchableOpacity>
    ```

    Add to `StyleSheet.create`:
    ```ts
    fab: {
      position: 'absolute',
      bottom: SPACING.xl,
      right: SPACING.xl,
      width: 56,
      height: 56,
      borderRadius: 28,
      backgroundColor: COLORS.interactive.accent,
      alignItems: 'center',
      justifyContent: 'center',
    },
    ```

    Add imports at the top: `TouchableOpacity` (add to existing RN import list), and `{ Ionicons } from '@expo/vector-icons'`.

    The `eslint-disable campfire/no-hardcoded-styles` comment is needed for `width: 56`, `height: 56`, `borderRadius: 28` since these are pixel values. Add `// eslint-disable-next-line campfire/no-hardcoded-styles` above each of those three lines in the StyleSheet.
  </action>
  <verify>
    Run: `npx tsc --noEmit`
    Visually: IOU Balances screen shows circular accent-colored FAB with "+" icon in bottom-right; tapping opens /squad/expenses/create.
  </verify>
  <done>expenses/index.tsx has a FAB at bottom-right that navigates to /squad/expenses/create. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` passes with no new errors.
Both routes are reachable via the new UI entry points.
</verification>

<success_criteria>
- Squad tab header displays person-add icon; tap navigates to /friends/add
- IOU Balances screen displays FAB (+); tap navigates to /squad/expenses/create
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/quick/260416-w3y-fix-add-friend-button-missing-and-iou-ad/260416-w3y-SUMMARY.md`
</output>
